# H4475 — Sanskrityatina `04_Reverse`: format table + census + `sanskrityatina-reverse-index` landing

_Created: 10-09-2026 · Last updated: 10-09-2026_

Executor: OxAlpha (`zai-coding-plan/glm-5.3-flash`) · Handoff: [H4475](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4475-OxAlpha_SanskritLexicography_sanskrityatina-reverse-index_09.09.26.md) · Source: `yadisk:Sanskrityatina/04_Reverse/` (177 entries · 1.786 GiB, scanned read-only via `rclone yadisk:`, H1333 landing discipline)

## 1. Verdict vs estate reverse-index assets

**REUSE_INDEX §2 "none known" CONFIRMED.** No kosha manifest row, no REUSE_INDEX asset, and no repo dataset derives from this source. The estate's canonical reverse-dictionary master ([`ReverseDictionary/.doc.pdf/266820-reverse-Gasuns.txt`](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/README.md), 266,820 headwords, ~30-source manual editorial merge, not rebuildable — [DATA_LAYERS_CENSUS](https://github.com/gasyoun/Uprava/blob/main/DATA_LAYERS_CENSUS.md)) is a **different object**: the Sanskrityatina stock (187,992 headwords, Schwarz-based, coded against `IEG/PD/BHS/MW/SCH`) is its earlier 2013–2015 workshop ancestor with a coding taxonomy the master never carried. Complementary, not duplicated; no estate asset is superseded.

## 2. Format table — `187992 headwords.txt`

| Property | Value |
|---|---|
| Size / encoding | 1,706 bytes · **ASCII, CRLF line terminators** |
| What it is | **The CODING LEGEND of the 187,992-headword stock — NOT the headword list** (the mint-time assumption "187992 headwords.txt = list of 187,992" is false on contact; see §6) |
| Headword script | **SLP1** (Cologne scheme) — ⚠ counter-intuitive mappings: **`R`=ṇ, `N`=ṅ, `f`=ṛ** (NOT R=ṛ); authority: sanskrit-util `SLP1_ALPHABET`, transcodes below |
| Structure | 1 stock-total line (`187992 headwords coded as 0`) + 23 subclass lines (`N headwords coded as CODE: description`) + one ASCII fence with **3 attested example headwords** tagged with source dict codes (`aMSagaRa:IEG,PD` · `paryantIkfta:BHS,MW` · `paryavadApayitar:BHS,SCH`) |
| Page refs | **Absent by construction** — no page/column refs exist in the legend (they exist in the sibling SCH XML cases, §4) |
| Codes | `0` stock (in ≥2 dicts) · `1,2,3a,3b,3a1,3acpd,3bcpd,4a,4b,5a,5b,0a` morphological classes · `cpd1,cpd1a,cpd1b,cpd2,cpd2a,cpdsrs1,cpdsrs1a,cpdsrs1b,cpdsandhi1,cpdsandhi2` compound/sandhi classes · `gray` 21,818 not-yet-whitelisted |
| Arithmetic | stock **187,992**; the 23 subclass counts sum to **197,019** → classes **overlap** (delta 9,027) — the legend's own numbers, machine-parsed, in the derived TSV |

## 3. Derived TSVs + verification (this landing)

Builder: [`tools/h4475_build_reverse_index.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/tools/h4475_build_reverse_index.py) (derive-don't-store; raw stays **gitignored** under `raw/`, refetch command in its `--help`).

- [`sanskrityatina_reverse_index_coding_classes.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/sanskrityatina_reverse_index_coding_classes.tsv) — 24 rows (`class_code · headwords · description · is_stock_total`).
- [`sanskrityatina_reverse_index_examples.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/sanskrityatina_reverse_index_examples.tsv) — 3 rows, columns `headword_slp1 · headword_iast · form_key · cdsl_key · exact_key · sources · rev_class · source_page_ref` (page ref column **empty by construction**, stated in [provenance.json](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/provenance.json)).
- Keys via sanskrit-util: `from_slp1`, `slp1_form_key` (= `form_key ∘ from_slp1`), `slp1_norm`, `norm` — the mission's mandated pipeline.
- **Idempotence:** two consecutive runs → byte-identical outputs (sha-checked).

**csl-orig cross-validation of the 3 legend examples** (against `~/Documents/GitHub/csl-orig/v02/`, `<k1>…<` prefix match):

| headword (SLP1) | legend tags | csl-orig result | verdict |
|---|---|---|---|
| `paryantIkfta` → paryantīkṛta | BHS,MW | in **MW** ✓ · in **BHS** ✓ | 2/2 tags confirmed |
| `aMSagaRa` → aṃśagaṇa | IEG,PD | in **IEG** ✓ (PD unprobeable — no `pd` dict in v02) | 1/2 confirmed |
| `paryavadApayitar` → paryavadāpayitar | BHS,SCH | in **BHS** ✓ · correctly **absent from MW** ✓ (negative control) | confirmed incl. absence |

`SCH` is Schwarz's *Rückläufiges Wörterbuch* — deliberately not a Cologne dict; SCH-tagged rows are the stock's reason to exist.

## 4. Folder census (separate lines, per mission)

| Asset | Files / size | Format | Disposition |
|---|---|---|---|
| **SCH-experimental XML research cases** | 6 XML · 6,294,178 B · **16,243 cases** (fiction-3147 · gedruckt-86 · grade-12427 · pw-comparison-319 · zu betonen-83 · zu-lesen-181) | **UTF-16LE**, one csl-orig-style entry per line: `<H1><h><key1>SLP1</key1><key2>accented</key2></h><body>German</body><tail><L>n</L><pc>page-col</pc></tail></H1>` — **carries the page refs the legend lacks**; plus `SCH meta research.txt` (UTF-16LE German notes on PWK-vs-SCH Nachträge; "not 12000 but 12427 actually new articles") | Census line only (mission). Landable later as a keyed TSV with `L/pc` refs — natural follow-up |
| **NCC** | `NCC 127980 headwords non-duplicate.txt` 3.06 MB + `NCC 152576 headwords.txt` 3.46 MB + JPG + publication-email note | UTF-8 BOM, **IAST** (`Aṃśatrayīvicāra`), one headword per line; 127,979 newlines + unterminated last line = 127,980 ✓ | Census only (distinct dataset, not reverse-index; note the raw/dedup pair 152,576 → 127,980) |
| **384k-word index** | docx+pdf ×2 (plain + devanagari), 3.86–6.33 MB each | Binary Word/PDF | Census only (binary; larger superset of the stock — see §6) |
| **васту суд** | 4 files ~40 MB (`vastu-expertise_Gasuns-Neivirt_2021` doc/pdf ×2) | Binary | Non-lexical (forensic expertise); census only |
| **Литература по обратным словарям** | Schwarz 1974 *Rückläufiges Wortbuch* PDFs (58.2 MB + intro-dhātu 5.1 MB + page extracts), Ruck-Deutsch ×2 (54.4/68.8 MB) | Scanned PDF | Source literature of the project; census only |
| Misc singles | Buck.xlsx · Bucknell endings · 500-frequent-words (Sūryasiddhānta/Ṛgvedajyotiṣa/Ṭikanikāyātrā) · MW roots · `IAST-hyphenation_b7.xlsm` 13.6 MB · `Freq2011.zip` · Wehr Arabic roots · `EndrAbArhaspatya.txt` · Whitney prefix-roots PDF | mixed | Census only |

## 5. Honest gap + leads

**The 187,992-row per-headword coded list itself is NOT in `04_Reverse`** — the legend is all that survived of the coding stage in this folder. Plausible carriers seen in the census: `IAST-hyphenation_b7.xlsm` (13.6 MB), `Freq2011.zip`, the 384k-word index docx. Chasing those is **out of H4475 scope** (binary forensics, likely its own unit); recorded as leads in the [metadoc backlog](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/sanskrityatina-reverse-index.meta.md).

## Verification (class:data — H4358)

This dataset flips to ✅ only after a **different session's** PASS recorded in the handoff file's `## Verifier` section ([template](https://github.com/gasyoun/Uprava/blob/main/docs/VERIFIER_PASS_TEMPLATE.md)). Cheapest reproduce: `python3 ReverseDictionary/sanskrityatina/tools/h4475_build_reverse_index.py` twice (needs the raw legend in `raw/` or the rclone refetch) + diff the committed TSVs + re-run the §3 grep table.

_Dr. Mārcis Gasūns_
