# H2267 dual-run compare — H2239 article-site parity (B10)

_Created: 06-08-2026 · Last updated: 06-08-2026_

**Lanes:** Sonnet 5 (`claude-sonnet-5`) override of Grok-tagged H2239 vs Grok 4.5
(`grok-4.5`) independent residual (this handoff).

**Source handoff:** [H2239](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H2239-Grok_SanskritLexicography_progress-kitchen-article-parity_03.08.26.md)
(override dual-run closed with residual H2267).

**Override lane outcome:** no PR — B10 already fully shipped by
[H2218](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H2218-Grok_SanskritLexicography_progress-kitchen-h2212-residual-b1-b9-b10-backfill_02.08.26.md)
(02-08-2026); H2239 was registry drift (minted one day after ship).

**Protocol:** [dual-run-override.md](https://github.com/gasyoun/claude-config/blob/main/references/dual-run-override.md)
· [dual-run-salvage](https://github.com/gasyoun/claude-config/blob/main/commands/dual-run-salvage.md)
classes: identical / equivalent / conflicting / net-new.

**Verified against:** SanskritLexicography `origin/master` @ `73abb20f9`
(2026-08-06 session).

---

## Goal restated (both lanes)

Independently confirm (or refute) that B10 **article-site parity** is fully
shipped: store-root set vs `article_site/` root set in the kitchen, with honest
`measured: false` when the site is absent — no silent false-pass.

**Fail =:** `article_site_parity` missing, not wired into the kitchen build, or
UI card dead; or no honest `measured=false` + why when site absent.

---

## Independent Grok verification (file:line, not copy of override summary)

| Acceptance item | Independent finding | Evidence @ `73abb20f9` |
|---|---|---|
| Parity computation | **Exists and matches H2218 design** | [`kitchen_slices.py:1347–1388`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard/kitchen_slices.py#L1347-L1388) — `article_site_parity(store, article_site, missing_cap=20)`: `store_root_set` vs `article_site_root_set`; returns `store_roots`, `article_roots`, `in_store_not_article`, `in_article_not_store`, `parity_pct`, capped `missing_from_article_sample` / `extra_in_article_sample`; when site missing → `measured: False` + note pointing at `build_article_site.py` |
| Build wiring | **Wired into kitchen JSON** | [`build_kitchen_data.py:714`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard/build_kitchen_data.py#L714) `parity = ks.article_site_parity(STORE, ARTICLE_SITE)`; [`:750`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard/build_kitchen_data.py#L750) `"article_parity": parity` in published dict; schema_note names H2218 additive key |
| UI card | **Live panel + render path** | Panel shell [`index.html:197`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard/index.html#L197) (`#parityBody`); render [`:712–731`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard/index.html#L712-L731) — measured branch shows counts/parity_pct/missing sample; unmeasured branch shows badge + note + store_roots |
| ROADMAP truth | **B10 already marked shipped** | [`ROADMAP_…:5`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard/ROADMAP_PROGRESS_KITCHEN_IMPROVEMENTS_2026.md#L5) H2218 residual line; backlog row B10 line 89: *H2218 shipped… measured=false if site absent* |
| Live JSON baseline | **Honest-false path as documented** | [`kitchen_data.json` `article_parity`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard/kitchen_data.json): `measured: false`, `store_roots: 67`, note = run `build_article_site.py` then rebuild kitchen. Local `RussianTranslation/article_site/` **absent** this machine — matches Fail-bar inverse (why present, not a bug) |

Optional `measured: true` sample (build_article_site once): **not run** — handoff marks nice-to-have only; honest-false path already proves the non-silent gate.

---

## Line-number drift note (not a behavioural conflict)

| Surface | Override lane (Sonnet, @ `32c77889266e`) | Grok residual (this pass) |
|---|---|---|
| `article_site_parity` def | `kitchen_slices.py:1125` | `kitchen_slices.py:1347` |
| build call | `build_kitchen_data.py:710` | `build_kitchen_data.py:714` |
| UI `const par = …` | `index.html:655` | `index.html:713` |

Drift is from intervening kitchen work (H2229/H2231/H2237/…) inserted above these
symbols. Behaviour and keys are unchanged — class **equivalent** for line anchors,
**identical** for semantics.

---

## Per-decision adjudication

| # | Decision / artifact | Sonnet override | Grok residual | Class | Keep |
|---|---|---|---|---|---|
| 1 | B10 code already shipped in H2218 | Confirmed three surfaces + live JSON | Independent re-read of same three surfaces + live JSON + ROADMAP B10 row | **identical** | Keep master as-is |
| 2 | H2239 was duplicate mint (registry drift) | Stated; no new PR | Confirmed: roadmap line 5 + B10 row already claim H2218 ship *before* H2239 mint date | **identical** | No build |
| 3 | Honest `measured: false` when site absent | Live JSON quote | Same JSON fields + confirmed `article_site/` missing locally + code path `art is None` → measured false | **identical** | Keep |
| 4 | No residual code gap on Fail bar | No-op close of H2239 | Fail bar still fails to fire: function exists, wired, UI live | **identical** | Close H2267 no-op |
| 5 | Optional measured=true sample | Not required | Not run (same choice) | **identical** | Optional remains optional |

**Class counts:** identical 5 · equivalent 0 (line drift only; not a decision) · conflicting 0 · net-new 0.

No conflicting decisions — dual-run is pure confirmation of already-shipped B10.

---

## What this residual ships

- This comparison doc only (no code delta).
- ROADMAP Grok residual queue row 10 (H2239) marked ✅ done via dual-run.
- Changelog note under the cut release for this PR.

---

## Close criteria map

| Evidence required (H2267) | Where |
|---|---|
| Independent confirm of `article_site_parity` | table row 1 + `kitchen_slices.py:1347–1388` |
| Independent confirm build writes `article_parity` | table row 2 + `build_kitchen_data.py:714,750` |
| Independent confirm UI renders card | table row 3 + `index.html:197,712–731` |
| Adjudication line (dual-run-salvage classes) | Per-decision table above — all **identical** |

_Dr. Mārcis Gasūns_
