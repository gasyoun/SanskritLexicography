- **H4527 (Opus 5 `claude-opus-5`) — the cohort path accepted and promoted its first card: work item 1's live evidence is complete. 4 paid calls (1 canary, 1 card, 2 readiness-probe legs = the day's one `c1` ration attempt).** The 17-09 planner gate ([#2261](https://github.com/gasyoun/SanskritLexicography/pull/2261)) merged after its one red check was diagnosed as **FINDINGS index drift on a 22-commit-stale branch, not the PR's code** — branch updated from `master`, 10/10 green, merged as `99b21e67e`. The gate then prepared the window it was built for: `--require-senses 1` walked past five sense-poor heads (`darv_i~~h0_zz_nws00`, `asa_mskfta~~h0_zz_nws00` — the exact card the 16-09 window burned a call on — `asvatantra` ×2, `avy_ahata`, all 0 senses) and stopped at `h4527sen08`: **one** sub-card, `darv_i~~h0_zz_pw`, 3 source senses, so a real repair lane. Run `h4527-acc-200920` went through the full gate chain unbypassed — zero-call dry run (`live_admission.admitted: true`), `probe-ration --account c1` exit 0 (`legal_now: true`), `dq_canary_puregloss` **CANARY GO** — and came back **clean**: `accepted_order ["h4527sen08"]` non-empty for the first time, **9/9 audit gates PASS**, 1 clean, 0 requeue, `calls_spent 1 == calls_reserved 1`, `peak_concurrency 1`, `wave.promoted`+`wave.tm_done` true (one promote, one TM, single-promoter rule held), store **11 521 → 11 524** rows. Both probe legs passed with `cli_safe_mode_effective: true` — the fourth independent clean reading since the 15-09 injection-shape repair. No guardrail was touched: no `--skip-canary-gate`, no `ALLOW_*`, no retry after an audit verdict; `--allow-unbounded` only because the cohort path refuses `--cost-ceiling` by design, with `--max-calls 3` binding (2 unspent). Serial-vs-cohort receipt [`h4527_acceptance_compare.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/h4527_acceptance_compare.py) (read-only, spawns nothing) **exit 0**: 19 shared sealed artifacts, `route_driven_differences []`, `schema_mismatches []`, `window_status.json` key sets identical (31), `window_ledger.jsonl` row key sets identical — with both limits stated rather than hidden (`same_outcome: false`; no clean accepted serial-route window exists on disk, and no same-card A/B is possible since a sub-card is translated and promoted once). **Defect this window exposed, FINDINGS §642:** `bounded_staged_run --execute --cohort-path` gates its exit code on four **supervisor** stop reasons the cohort path never produces, so this perfect wave exited **1** — invisible for nine days because no cohort wave had ever completed cleanly. Not fixed here (money-path code, distinct concern); routed to its own handoff. `COHORT_LIVE_ACCEPTANCE.json` stays **unwritten** and width 2 stays fail-closed: `validate_record` needs a `reviewer_sign_off` that is work item 2, a different session by construction — the `serial_acceptance` half is recorded paste-ready instead. Packet: [H4527_LIVE_COHORT_ACCEPTANCE_FIRST_CLEAN_20-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_LIVE_COHORT_ACCEPTANCE_FIRST_CLEAN_20-09-2026.md).
