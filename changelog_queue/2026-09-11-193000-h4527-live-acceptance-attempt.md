- **H4527 live half, third pass — the spend gate is OPEN and the acceptance window is blocked on one thing nobody had named: there is no card to run it on.** A `dq_canary_puregloss` window ran the production headless route on `c1` (15:58Z, `classification: success`, 82 862 ms, exit 0) and `canary_gate.py judge` returned **GO** — the first fresh GO receipt in weeks, valid until ~22:00Z for either lane, so `--execute` no longer needs another probe attempt today. The window itself never launched: `bounded_staged_run --execute --cohort-path` needs a *prepared* coordinator lease, and preparing one was refused with `nominal lease keys already active: arvant~~h0_zz_nws00`. Three probed facts behind that: the verb lane is `runnable: 0` (all 701 remaining roots missing rootmaps, so the handoff's "a real small verb root from `verb_worklist.py --top`" is unsatisfiable on this box); the no-PWG queue's 122 remaining headwords start with `arvant` in the **queue order itself**, not only in the `still_null` tail, so a plan-scoped `--residual-file` exclusion (the tracked `no_pwg_residuals.jsonl` untouched) moved nothing; and `arvant~~h0_zz_nws00` is held by H4213's open `needs_requeue` lease `h4213can091108282902`, which the coordinator correctly refuses to double-lease — the same wedge class that already cost that wave one attempt. Nothing was overridden, no `ALLOW_*` was set, no paid call was spent on the window. Packet: [pwg_ru/h4527/H4527_LIVE_ACCEPTANCE_ATTEMPT_11-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_LIVE_ACCEPTANCE_ATTEMPT_11-09-2026.md).
- **H4213: the F12 half is fixed on the evidence, and the remaining defect is named.** Re-judging H4213's existing `wf_output` against the merged [#2179](https://github.com/gasyoun/SanskritLexicography/pull/2179) `nws_split` owner-regex fix — offline, zero calls — moves the `nws` gate from exit 1 / 1 requeue to **exit 0 / 0 requeue**. `ru_style` still hard-flags `R1_yo` (the ё rule), so the card needs a real re-generation rather than a re-judge: one paid call, and the canary receipt above already authorises it.
- **H4527 correction — "one canary GO receipt satisfies both lanes" was true in principle and false in fact.** The handoff's *Armed launch* section deferred the day's remaining probe attempt to H4213 on the grounds that H4213's re-judge would produce the receipt H4527 needs. It cannot: H4213's 05:29–05:36Z window was a **real card window** (`pwg.no_pwg_scale_plan.v1`, key `arvant~~h0_zz_nws00`, `provenance_classes: real`), and that key does not match `promote_final_cards.SYNTHETIC_KEY_RE`, so `canary_gate.judge` refuses it as "NOT a synthetic-control key — refusing to judge a real window as a canary". A `dq_canary_puregloss` run is owed by whichever lane goes first; this pass made it.
- **Data-integrity hazard recorded: `no_pwg_scale_plan.py` regenerates four TRACKED dashboard JSONs as a planning side effect.** `progress_dashboard/{kitchen_data,progress_data,progress_timeseries,quality_timeseries}.json` are rewritten by a plain planning run. From a checkout behind `origin/master` (the shared main tree was behind 5) the rebuild is a net **−2191 lines** on `kitchen_data.json` and walks `store.senses` 11519 → 11516 — a session that planned a window and then committed everything it touched would erase curated data with no gate objecting. Reverted in place after confirming all four files carried that run's own mtime.
