- H1333: `DHĀTUP. x,y` → Palsule wired — H1307's third `<ls>` arm goes from acquisition spec to working enrichment. MG's supplied XLS (Palsule's *artha* index, 8,170 rows, rights cleared 07-09-2026) carries **no** Böhtlingk coordinate, so the crosswalk is built the other way round: a `<ls>DHĀTUP. x,y</ls>` sits inside the article of the root it numbers and that article's `<k1>` key is the dhātu, so the coordinate→root half is read off Böhtlingk's own text (the H328 ablaut-join risk never binds it) and only root→Palsule is a join. Two honest filters: a head-line citation outranks a body one, and a nominal claimant (`<lex>` article quoting the coordinate as a gloss, `{#loqana#}¦ … als Erkl. von {#bAD#}`) loses to the verbal one; what stays ambiguous — Böhtlingk's own double spellings `skand`/`skund` — is **dropped, never resolved by citation count**. Result **1,226 / 1,751 distinct coordinates (70.0%)**, 1,441 / 2,760 citations (52.2%); the 525 misses are 271 conflicts + 254 roots absent from Palsule's artha index. Verified against PWG's own glosses: `snih` 26,91 → *snehane*, `sthā` 22,30 → *sthāne*, `aṅg` 5,38 → *gatau* («gehen»). Palsule has no online edition, so the datum ships as **tooltip text with the printed page siglum, never a fabricated href**, and the existing gaṇa-level Westergaard link (96.3%) is untouched. Builder `src/build_dhatup_palsule.py` → committed table `src/data/dhatup_palsule.json` → runtime `src/dhatup_palsule.py`, wired into the shared `build_article_site._ls_tooltip` beside the `Spr. (II)` case so the H1301 review sheets inherit it — no second resolver. `ls_coverage.py` gains the DHĀTUP.→Palsule line, three fixtures added to `ls_enrichment_selftest.py` (10/10 green), LANG_PARITY entry `dhatup_palsule_enrichment_h1333` SHARED + five re-derived re-stamps, ledger clean (109 entries, no drift).
