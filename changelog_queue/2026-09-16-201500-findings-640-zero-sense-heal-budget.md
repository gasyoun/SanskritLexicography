- **FINDINGS §640 (Opus 5 `claude-opus-5`, H4527) — a zero-sense card gets a heal budget of exactly zero, and `budget_stops` counts refused spawns, never a truncated output.** Harvested from the H4527 null-card diagnosis so it survives the handoff's archival: `HeadlessEngine.call()` increments `budget_stops` only on a `_budget_ok()` spawn refusal or `CallLimitReached`, so reading it beside a null card as "output truncated" inverts its meaning; and `agent_budget.plan()` sums the heal pool per card over **sense groups**, so a card with `senses: []` plans `max_heal_agents: 0` and the single translate call is its only attempt. Measured on run `h4527acc160916` (subject `asa_mskfta~~h0_zz_nws00`). Recorded gap: the parsed `cards[]` is discarded after `normalize_batch`, so a `missing-or-mismatched-key` null leaves no retained evidence of what the model actually echoed. Apply: never plan an acceptance/canary/gate-proving window on a zero-sense card.
