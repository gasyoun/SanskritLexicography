- **H4436 — the readiness-probe latency/refusal class of 08/09-09 is closed by the lane's `--safe-mode` flag, proven by a live gate-0 reading; the `profile:init` validation spawn takes the same posture.** The handoff asked for a second `CLAUDE_CONFIG_DIR` holding c1's credentials without hooks; H2189 (03-08-2026) had already measured that option against the CLI flag and ruled for the flag (no second on-disk OAuth token, no second `ActiveCallClaim` fingerprint), and H4527 (15-09-2026, #2229/#2238) landed the flag on `_probe_call`. This pass supplies the paid reading no offline test substitutes for: `h963_c4_gate0_probe.py --account c1` on 16-09-2026 02:46Z — warm-up **8 712 ms** and measured **9 274 ms** wall, both `classification: success`, `cli_safe_mode_effective: true`, `probe_prompt_sha 90e2f1f6698b`, host commit 83.7 % — against the unflagged baseline rows of 142 799 / 176 814 / 151 327 ms (08/09-09, the last one a `refusal` citing the operator's Stop hook). Two consecutive successes meet the handoff's own stop condition. Residual code: [`max_account_orchestrator.profile_status`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/max_account_orchestrator.py) — the `profile:init` validation call was the last paid spawn in the module still carrying the full interactive profile; it now appends the flag through `headless_worker.resolve_safe_mode({}, claude)` (never a literal), pinned in the orchestrator selftest as equality with the lane's resolver both ways. No new profile directory, no credential copy, no `max_orchestrator.sqlite` change — c1 stays the only validated account. The parked branch `h4436-probe-safe-mode` is superseded by H4527's landing and this entry.
