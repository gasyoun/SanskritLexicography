- **H4530: the H4342 SAN-LOSS freeze breaker and the window-id collision fix land as committed code, and the documented store reversal now restores every row.** [`h4342_requeue_sanloss.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/h4342_fixes/h4342_requeue_sanloss.py) resolves the canonical store (the H4342 draft pointed at the mirror), and it quarantined the three July rows that re-froze the pc lane every morning (`audit_store_gates` hard-flagged 3 → 0). [`no_pwg_scale_plan.used_window_indices`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/no_pwg_scale_plan.py) now counts prepared-but-unfinished headless roots, so a regenerated launcher with a static prefix cannot re-pick a dead window id. A verifier pass (15-09-2026) found that `--restore` keyed rows on `(subcard, sense_tag)` (not unique, FINDINGS §551) and would have put back 2 of 3 rows. It now matches on whole-row content. The two selftests (5 + 4 checks) run in CI.
