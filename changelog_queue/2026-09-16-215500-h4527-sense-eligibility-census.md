- **H4527 (Opus 5 `claude-opus-5`) — the «prepare an acceptance card with `senses >= 1`» step is UNSATISFIABLE as written, and the census says why. 0 paid calls.** The 16-09 null card's zero heal pool was not this card's bad luck: `sense_count.count_source_senses()` over all 34 portrait sidecars on disk (`src/pilot/input/*~~h0_zz_*.raw.txt`, `~000d` duplicates excluded, cross-checked against each stamped `portrait.source_senses`) shows **0 of 10 `~~h0_zz_nws00` sub-cards declare a single source sense** — the whole class is zero-marker, so none of them can ever receive a repair lane (`agent_budget` derives the heal pool from sense groups); 15 of 17 `pw`, 1 of 3 `pwkvn` and 0 of 4 `sch` carry senses. And every sense-bearing card the planner can currently *reach* is a blocked residual: of 23 eligible (unpromoted, unblocked) sub-cards, 9 carry `senses >= 1`, but the 10 blocked residuals include **every `pw` sibling of the queue head** (`arvant`, `asa_mskfta`, `avy_ahata`, `avyagra`, `b_ahlika`). `no_pwg_scale_plan.py` carves windows strictly in queue order and omits a head only when *all* its unpromoted sub-cards are blocked, so on the live 122-headword plan order it would re-prepare the parked `asa_mskfta~~h0_zz_nws00`, and the first reachable sense-bearing head, `darvI`, would come as a **two** sub-card window (`nws00` 0 + `pw` 3). No existing flag targets a sense-bearing card (`--start-index` only labels, `--headwords` truncates from the front, `--include-residuals` unblocks all at once). Recommended fix, offline and selftest-backed: a `--require-senses N` planner flag that skips heads whose eligible sub-cards all declare fewer than N source senses — no residual-registry write, no lane-backlog semantics touched. Live ration re-probed at 21:51:57Z: `legal_now false`, `next_legal_utc 2026-09-17T00:00:00Z`, so no window was runnable; lease `h4527acc05` stays parked and the MSI shared checkout was left byte-identical (`--plan-only` with a `%TEMP%` manifest never reaches `_pilot_gen_merged.py`, so the tracked `progress_dashboard/*.json` rewrite hazard was not triggered). Packet: [H4527_SENSE_ELIGIBILITY_CENSUS_17-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_SENSE_ELIGIBILITY_CENSUS_17-09-2026.md).
