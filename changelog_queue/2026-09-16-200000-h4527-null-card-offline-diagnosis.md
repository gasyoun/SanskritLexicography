- **H4527 (Opus 5 `claude-opus-5`) — the 16-09 null card is diagnosed offline, 0 paid calls, and the earlier hypothesis was wrong.** `budget_stops: 1` is not an output-budget truncation: `headless_worker.HeadlessEngine.call()` increments it only when `_budget_ok()` refuses another spawn or the reservation ledger raises `CallLimitReached`. The refused spawn was the per-card **self-heal** — `heal_agents_spent: 0` against `max_heal_agents: 0` — and that zero is structural: `agent_budget.plan()` derives the heal pool from per-card **sense groups**, and the acceptance window's only subject `asa_mskfta~~h0_zz_nws00` is a zero-marker `no_pwg_supplement_chain` stub with `senses: []`, `ls 0`, `sk 0`. Zero sense groups ⇒ empty `groups` ⇒ `heal_default 0` ⇒ the window had exactly one shot and no repair lane, which a re-run reproduces exactly. The H220 nominal key-echo recovery in `normalize_batch` was present and satisfiable (`nominal_keymap: {asa_mskfta~~h0_zz_nws00: asaMskfta}`, reverse map unambiguous), so the model echoed neither form; **what** it echoed is unknowable from disk — the parsed `cards[]` is discarded once `normalize_batch` has run, a retention gap now recorded for this failure class. The window's own audit had filed one semantic risk, score 100: `missing_senses`. Consequence for the next paid attempt: the previous pass's «resume lease `h4527acc05`, a cheap re-run» is withdrawn — prepare a fresh window-size-1 lease on a card with `senses >= 1` (zero paid calls) so a repair lane exists, and keep `h4527acc05` parked. `LAUNCH_FUCKUPS.md` entry `H4527_COHORT_WINDOW_NULL_CARD_2026-09-16` updated in place (`root_cause`, `guardrail`, `residual_risk`; `check_launch_ledger.py` → 23 entries complete, PASS). Ration live-probed at 20:00:36Z: `legal_now false`, `next_legal_utc 2026-09-17T00:00:00Z`, so no window was runnable this pass. Packet: [H4527_NULL_CARD_OFFLINE_DIAGNOSIS_16-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_NULL_CARD_OFFLINE_DIAGNOSIS_16-09-2026.md).
