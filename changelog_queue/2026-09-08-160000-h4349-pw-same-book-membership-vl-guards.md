- H4349 (verifier correction): **both rows the first cut shipped were wrong, both are withdrawn, and H4349 is now the two-part negative result it always was.** An independent adversarial verifier confirmed the baseline-drift proof, the dotted-id measurement and the gaṇa-1 adjudication, and refuted the coverage gain. The two `pw` rows fell to the same underlying mistake — **treating a same-author source as an independent witness**. (1) `32,56 → cukk`: PWG splits that coordinate between `{#cakk#}` (pwg:120515) and `√{#cikk#}` (pwg:126200, itself `<ab>v. l.</ab>`), so H1333's filter drops it, MW gives it to `cakk`, and PWG puts `cukk` at **`34,21`** (pwg:128203) — the first cut passed `same_book_conflicted` to the `pwg-dotted` call and *not* to the `pw` call, contradicting `read_pw_coords`'s own docstring calling pw "the same author's numbering rather than a second opinion". (2) `33,67 → tras`: PWG **never cites it**; Böhtlingk renumbered between editions, so pw's `2. *√{#tras#}¦, {#trAsayati#} ({#DAraRe, grahaRe, varaRe#})` at `33,67` (pw:193921) is pwg's `2. {#tras#}¦, {#trAsa/yati#} {%halten%}` at `33,88` (pwg:155012) — one article, three matching glosses, two serials. The ceiling screen bounds a serial against its gaṇa's maximum and `33,67` cleared gaṇa 33's ceiling of 130, so nothing caught it; because `match_rate` divides by the set PWG cites, shipping it **counted a row its own denominator excluded**, and the advertised 83.8% was arithmetic on mismatched sets. Three guards close it: the same-book conflict screen now applies to **both** Böhtlingk siblings (MW keeps its exemption — it is a different author, which is the whole coverage argument of H4339); a **membership screen** requires a candidate coordinate to be one PWG actually cites, and runs *after* the ceiling screen so `1,840`/`1,960` keep the ceiling verdict that explains them while renumbering shows up as its own finding; and a **Böhtlingk-family variant-reading guard** — pw does use the construction at a `DHĀTUP.` citation (`*√{#plI#}¦ … <ls>DHĀTUP. 31,32</ls>, <ab>v. l.</ab>`, pw:309560) and the pass had no equivalent of H4339's MW guard. The note follows the citation here and marks the *article's own* headword as rejected, the mirror image of MW's leading `v.l. for √X`, so it is a separate pattern rather than a reuse; 364 PWG claimants carry it. **Coverage returns to 1465 of 1751 (83.7%), and the artifact is byte-for-byte identical to the pre-H4349 one** — H4349 moves no data at all. pw's 40 citations now classify as 12 nominal-head / 4 body-only / 11 same-book-conflicted / 3 not-cited-by-PWG / 2 out-of-space / 8 already present / **0 shipped**. The standalone verifier gained the invariant it was blind to by construction (`set(table) ⊆ cited`; it derived ceilings from `cited` and then tested only against the ceilings, which is why it reported 13/13 on a defective build) and now pins both withdrawn rows as ABSENT with the PWG lines that condemn them — 17 checks. Selftest 18/18, pytest 401/401, ruff clean, LANG_PARITY re-derived and re-stamped with the superseded numbers recorded rather than overwritten. **Declared residue:** `32,56` is "conflicted" only because PWG's explicitly-`v. l.` `cikk` counts as a full claimant — applying the new guard to H1333's own pass would resolve it cleanly to `cakk`, but that would move the shipped H1333 baseline, so it is logged for a future handoff instead of smuggled in.
