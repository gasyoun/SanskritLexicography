- **H4527 (Opus 5 `claude-opus-5`) — `no_pwg_scale_plan.py --require-senses N`: the planner will no longer hand a paid window a card with no repair lane. 0 paid calls.** The 17-09 census's own recommended fix, landed. `agent_budget` derives a card's self-heal pool from its sense groups, so a zero-sense sub-card runs with `max_heal_agents: 0` — one shot, no repair — which is exactly how the 16-09 live acceptance window spent a call on `asa_mskfta~~h0_zz_nws00` (`senses: []`) and got a null card back. `--require-senses N` (default 0 = off, the historical path byte-for-byte) admits only sub-cards whose input sidecars prove N+ top-level source senses — the portrait sidecar's stamped `source_senses` first, else a deterministic recount through `sense_count.count_source_senses` over the raw source blob — and omits a head whose eligible sub-cards are all sense-poor, exactly as it already omits a fully-blocked head, without consuming the preparation quota. The gate runs **after** local generation, because only the sidecars `_pilot_gen_merged.py` writes say how many senses a sub-card declares; generation is unpaid, the call after it is not. Strictly narrowing in both directions that matter: an **unprovable** count (no sidecars) is skipped rather than admitted, and a skipped key gets **no residual-registry row** — it stays eligible for the next planning run, so no lane-backlog semantics move. The plan manifest records the gate it ran under (`require_senses`) and every sub-card it skipped with the measured count (`sense_skipped`), so a window can be audited for why it is the size it is. Pinned by `test_no_pwg_require_senses_gate` (portrait-stamp-wins, raw recount, unprovable-is-None-never-zero, minimum refusal, head omission without residual rows, flag-off path untouched, manifest record, negative-value refusal). Checks: `window_selftest` 226 defined / failure set identical to pristine `origin/master` on the same box, `windows100_selftest` PASS, `cohort_engine_selftest` 10 pins, `cohort_live_admission_selftest` 6 pins, `cohort_live_dispatch_selftest` 6 pins, `bounded_staged_run_selftest` PASS, `lang_parity_check` 117 entries no drift (42 entries re-derived + new SHARED entry `no_pwg_sense_gate_h4527` — the count reads the German source, never a target field; receipt `src/pilot/h4527c_parity_restamp.py`). Packet: [H4527_REQUIRE_SENSES_PLANNER_GATE_17-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_REQUIRE_SENSES_PLANNER_GATE_17-09-2026.md).
