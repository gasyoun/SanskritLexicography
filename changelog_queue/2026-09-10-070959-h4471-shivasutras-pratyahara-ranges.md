- H4471: **ShivaSutras.xlsx schema inspected, prior art checked, pratyāhāra-range dataset derived.** The Yandex Disk source (`yadisk:Panini/ShivaSutras.xlsx`/`.docx`) is not a sūtra-text-with-numbering list as the mission expected — it is a 42-pratyāhāra range table (`aK`, `aC`, ..., `haṤ`), with range membership encoded by cell fill color, not cell text (every row repeats the same 57-symbol alphabet). Prior-art check found no overlap: kosha's `paninian-sutra-coverage-map` covers the 3,983 Aṣṭādhyāyī sūtras via vidyut's `Source.Ashtadhyayi`, and vidyut 0.4.0's `Source` enum has no Śivasūtra/Māheśvara member at all — the 14 Śiva Sūtras / pratyāhāras are a genuine gap, confirmed against VisualDCS and Sangram's `grammar-lab-g1` (verb morphology, unrelated) too. Derived `data/shiva_sutras_pratyahara_ranges.tsv`/`.json` (42 rows) committed; raw xlsx/docx stay gitignored under `data/_raw_local/` (rights unconfirmed). kosha registration (`shiva-sutras-machine`) is an **explicit skip**, not a completion — see `data/SHIVA_SUTRAS_PRATYAHARA_REPORT_2026-09-10.md`.
