- **H4915: the readiness-probe ration is now enforced in code, on one machine-wide ledger.** The standing ration allows at most 2 probe attempts per UTC day per profile, at least 6 h apart. On 15-09 `c1` was probed three times in one UTC day anyway (01:35Z, 14:23Z, 14:26:55Z). Two things let the third attempt through. First, nothing in code refused it: `ActiveCallClaim` only serialises calls that overlap. Second, the 14:23Z row sat in another evidence root's `health_probe_log.jsonl`, so the session checking its own root saw a clear ration. `execution_contract.ProbeRation` keeps one JSONL attempt ledger per profile, keyed by `config_dir_fingerprint`, in `%TEMP%\pwg-probe-ration\` beside the active-call lock dir. It has no env override, because a per-session root would split the count again. `_probe_call` is the one spawn behind `live_probe`, `probe_fleet`, `bounded_staged_run`, `h963_c4_gate0_probe` and `latency_payload_sweep`. It checks and records the attempt under the profile's claim, before the call reservation and the spawn. One held claim counts as one attempt, so a warm-up plus measured pair, or a whole sweep, is one attempt. `probe_fleet` pre-checks every profile first, so a fleet never spends account 1 and then stops on account 2. The refusal is a `ProbeRationRefused` (not a `SystemExit`, so it is never read as a health NO-GO). It names the next legal UTC time. An unreadable ledger row fails closed. New pin: `_test_h4915_probe_ration_is_code_enforced_across_evidence_roots`. `call_reservation_selftest` now runs on a scratch ledger, and 7 LANG_PARITY entries were re-derived (every verdict stands). The ledger starts empty: the three 15-09 `c1` attempts predate it and are not in it.
