- H4539 (OxAlpha `deepseek/deepseek-v4-flash`): **the `nws_split` OWNER regex is anchored to the LAST `Name : page` token, so a condensed cross-ref entry no longer swallows its grammar + bracketed x-ref into the owner and the F12 gate stops failing CORRECT cards.** The H4213 canary (`h4213can091108282902`, `arvant~~h0_zz_nws00`) was quarantined with `card owner=[Hillebrandt] expected=[Adj f [f zu árvant ] Hillebrandt]` — the old name class `[^>:.;]` began at the first capital («A» of `Adj`) and matched through `Adj f [f zu árvant ] Hillebrandt 1885 : 72`. A real owner name never contains `[`/`]`, so excluding the brackets stops the name at the x-ref and the match starts at the real owner; the grammar stays in the gloss. `card_rows`/`row_owner` are untouched — the card was linguistically right, the audit was wrong. Corpus sweep: of 215 real NWS fragments only the two `arvant*` files change, both to the correct `Hillebrandt 1885 : 72 (s.v. árvant)`. The selftest is now self-contained — an inline arvant fixture asserts 5 entries / owners {MW, Graßmann, Hillebrandt, Geldner} and `check_result` **CLEAN** on the canary card (was MISATTRIBUTION); the aMSa ground-truth half self-skips without the gitignored `pilot/input` fixture, and the run is wired into CI after `window_selftest.py`. Verified: `python src/nws_split.py selftest` PASS · window_selftest NWS subset (nws_fp_suppressed, guard3 nws_de_locked, threaded-gate, quarantine-replace, tag-routing) 5/5 green · ruff E9/F63/F7/F82 clean. Handoff: [H4539](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4539-OxAlpha_SanskritLexicography_nws-split-owner-regex-crossref_11.09.26.md).
