- H4349: **the last two untried PWG-family coordinate sources are now measured — one is empty, and saying so is the result.** H4339 left the `DHĀTUP.`→Palsule concordance at 1465 of 1751 (83.7%) with two sources unopened: PWG's 636 dotted-id articles, invisible to H1333's all-digit `<L>` pattern, and pw (the *Sanskrit-Wörterbuch in kürzerer Fassung*, Böhtlingk's own abridgement). Both are now read, each by a pass that runs **beside** the H1333 pass rather than widening `_L` in place, so `pwg_entries` and every shipped baseline are untouched. **The dotted-id class is one coordinate, not a hidden corpus:** those 636 articles cite `15,89` and nothing else, and PWG already contests it — `<L>18794` heads it `kfv` and Böhtlingk's prose there says the root is `kṛv`, placed under `1. kar`, its final `-v` "without the slightest justification", while `<L>69734` claims it for `kar` again. A dotted-id citation is the **same book**, so filling that coordinate would break Böhtlingk's own tie by adding a vote — exactly what H1333's multi-claimant filter exists not to do — and an explicit same-book guard refuses it rather than leaving the refusal to the accident that its head line also lacks a `√`. The emptiness is pinned by a regression, so a corpus update that puts real coordinates into that id space fails a test instead of passing silently. **pw cites 40 coordinates and yields 2** (`32,56` cukk, `33,67` tras): 19 sit on the head line of an *artha* noun rather than a root (pw abridges and moves its citations — `{#uttrAsana#}¦ <lex>n.</lex> … <ls>DHĀTUP. 9,15</ls>`), 5 are body quotations, 12 are coordinates the table already had, 2 have no Palsule row. **The `<lex>` test alone was not enough, and that is the load-bearing correction:** pw often omits the tag entirely — `{#DAnya#}¦ (von {#Dana#}) {%das Reichsein%} <ls>DHĀTUP. 20,3</ls>` is a noun meaning "wealth" with no `<lex>` anywhere, and a negative test admits it as a root, for a coordinate PWG gives to `jal`. Böhtlingk marks verbal articles **positively** with a head-line `√`, which separates all 37 head-line claimants cleanly, so the pass now requires the marker instead of merely not forbidding it. **`1,840` and `1,960` are adjudicated and refused, not silently dropped:** pwg and mw cite gaṇa 1 exactly three times and every time as `1,1` (bhū), so its attested ceiling is 1 and a gaṇa-1 serial of 840 is a citation-split artifact — some other numbering of Westergaard that pw's Nachträge reach for twice. The ceiling is **derived from the corpus at build time** (`coordinate_ceilings`), never typed, and derived from PWG alone, since the source being screened must not widen the space it is screened against; both refusals ship in `_out_of_coordinate_space` with the ceiling they failed. Cross-validation is reported as the weak thing it is: 8 comparable coordinates, 1 agreeing strictly (12.5%), 3 modulo citation form (37.5%), the other 5 published in `_sibling_disagreements` and never resolved — **too few to be a quality verdict on either edition**, and said so in the doc. **Coverage 1465 → 1467 of 1751 (83.7% → 83.8%)**, citations 1933 → 1935; `pwg` 1226 / `mw` 140 / `mw-respell` 99 / `pw` 2 / `pwg-dotted` 0. Baselines proven unmoved as zero diffs rather than matching row counts: `--mw "" --pw "" --no-pwg-dotted` rebuilds H1333 byte-for-byte (1226 / 70.0% / 139-of-232 = 59.9%), and all 1465 H4339 rows are present and byte-identical. Selftest 15 → 18 checks, pytest 401/401, ruff clean on CI's gate, LANG_PARITY re-derived and re-stamped (SHARED holds — every input is markup or Sanskrit, and the new sigla `[pw]` / `[pwg°]` are language-neutral by the same argument as `[MW]`). New standalone verifier `src/pilot/dhatup_h4349_verify.py` re-derives all 13 published numbers from the artifact and the raw corpora **without importing the builder**, so a bug in the builder's own bookkeeping cannot make it pass.
