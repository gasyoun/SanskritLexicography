- **H4837-support (Opus 5 `claude-opus-5`) — `mw_fuzzy` in the MW-vs-PWK Nachträge candidates no longer depends on `PYTHONHASHSEED`.** [`HeadwordLists/mw_pwk_nachtraege_missing_entries.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pwk_nachtraege_missing_entries.py) picked the near-form among equal difflib scores by set iteration order (~396 rows differed between seeds 0 and 1). `fuzzy_best()` now uses an explicit, documented tie-break: highest case-folded ratio, then highest case-sensitive ratio (SLP1 case is phonemic, so `Akalita`/`akalita` tie only after folding), then the lexicographically smallest candidate. The bucket build also iterates `sorted(mw_hw)`. Re-run against local `csl-orig/v02` with seeds 0, 1 and 12345: byte-identical output, ~14 s. The regenerated [`mw_pwk_nachtraege_candidates_14-09-2026.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pwk_nachtraege_candidates_14-09-2026.tsv) keeps its name, no-BOM state and 4,152 lines. 186 rows change in `mw_fuzzy` only; every other column is identical. The motivating `kAritra` row now reads `kArita`, matching the trial note's §4. No offline contract pin covers this file.
