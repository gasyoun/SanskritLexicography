- **H4213: the PWG-RU lane produced and promoted a card — the first since 14-07-2026.** `arvant~~h0_zz_nws00` was re-generated on `c1` (`classification: success`, 210 912 ms, no null keys), audited **1/1 clean with all nine gates at exit 0** — `ru_style`/`R1_yo`, the last standing defect, included — and promoted through the sanctioned coordinator route after a raw `--merge` was correctly refused by the H2089 route guard: `prepare-requeue --defect` → `begin-run` → `record-output` → `promote-ready`. The coordinator **re-audited the result independently** before moving the lease to `ready`, so the promotion rests on two separate audits and cost no extra call. Store **11516 → 11521 rows** (1 subcard, 5 sense rows), TM denylist cleared, pre-merge backup written. Two mechanical traps recorded: `requeue_from_audit.py` needs `--nominal` for a no-PWG supplement window and otherwise dies on `no rootmap for <root>` — which reads exactly like the known missing-rootmap data gap but is unrelated; and `headless_worker.py` aborts `classification: configuration — paid v2 execution requires --preflight`, a provisioning abort that spends nothing and consumes no probe attempt.
- **H4527 work item 1: the card blocker cleared, and a different gate stopped the window — with the lane provably healthy.** With the queue head free, `no_pwg_scale_plan --headless` prepared `h4527acc05` (arvant's own window correctly omitted: its `~~h0_zz_pw` sibling is a July residual), dry-run scope verified at 1 lease / 1 window / importable, host at 4079 MB free, canary receipt re-validated `GO receipt valid` seconds before launch. `bounded_staged_run --execute --cohort-path --cohort-width 1` then stopped before dispatching anything: `fleet probe STOP on account c1: warm-up probe content`. **Latency is not implicated** — 113 531 ms wall / 93 090 ms api against a 240 000 ms `production_v4` ceiling at 80.38% host commit. The envelope shows the mechanism: `stop_reason: "tool_use"`, 8038 output tokens of which **7312 thinking**, `is_error: false`, `result: {"ok":false}` — the model reaches for a tool instead of answering the readiness question. **The decisive evidence is a same-day counterexample on the same profile:** three paid calls succeeded through `headless_worker.py` in the preceding two hours (canary GO at 15:58Z and 16:19Z, and the real translation window at 17:58Z that promoted). `headless_worker.py` does not call `probe_fleet`; `bounded_staged_run --execute` does. Route, credentials, profile and host are therefore all exonerated, and the only component refusing is **the readiness warm-up prompt itself** — which picks H4213 §6.3 option (b), *"retune `_probe_prompt` task-shape (selftest-backed)"*, out of the five unblock options and retires (a), (c) and (e) as host/hook theories. RED=STOP honoured, no retry; `c1`'s 2-attempt ration for the day is spent (05:32Z + 18:08Z). Ledger row `H4527_ACCEPTANCE_PROBE_REFUSAL_2026-09-11` in [LAUNCH_FUCKUPS.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/LAUNCH_FUCKUPS.md), `check_launch_ledger.py` → 20 entries complete.
- **Spend stated honestly: four paid calls, ledger cost UNKNOWN rather than zero.** Two canaries (one re-bought after a forced worktree removal destroyed the first receipt), one translation window, one readiness probe. Every reservation records `cost_evaluable: false` — Max-route credit billing is dormant — so a `$0` in those ledgers is "not evaluable", never a measured total. The one envelope read directly carried `total_cost_usd: 0.28122` at `costBasis: "list"`, a list-equivalent price rather than a charge.
