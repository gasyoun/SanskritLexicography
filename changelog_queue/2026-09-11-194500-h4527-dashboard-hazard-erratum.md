- **Erratum, same day — the `progress_dashboard/*.json` churn is a running daemon, not `no_pwg_scale_plan.py`.** The earlier H4527 entry blamed the planner; probing the box found `progress_dashboard/live_refresh.py --idle-stop 0 --interval 60 --active-within 900` (PID 10616, running since 04-09-2026) rebuilding all four tracked JSONs every 60 s whenever `src/pilot/` artifacts are touched. That is the script's documented design — the numbers derive from **gitignored** RussianTranslation artifacts CI cannot see, so it rebuilds locally and publishes to `origin/gh-pages`, deliberately never committing minute-level churn to `master`. Reverting the files is futile while it runs (this pass reverted them once; they were back inside two minutes). **The real hazard is narrower and still live:** the daemon rebuilds from the **shared main checkout**, which was behind `origin/master` by 5 commits all day, so the working-tree diff it leaves is a net **−2191 lines** on `kitchen_data.json` with `store.senses` 11519 → 11516 — and those four files are tracked, so any `git add -A` in that tree (the sweep the shared-tree guard already warns about) commits a large stale-checkout deletion nobody read. Stage explicit paths there; treat dirty `progress_dashboard/*.json` as the daemon's normal state, not your own edit.
