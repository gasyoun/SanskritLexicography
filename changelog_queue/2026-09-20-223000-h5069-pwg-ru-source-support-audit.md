- **H5069 (Opus 5 `claude-opus-5`) — thirty published PWG-RU sense records traced back to their German source spans; the corpus-wide markup finding is a census, not an extrapolation.** Frozen sample of 30 from [`release/pwg_tm_canonical/canonical.v1.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/release/pwg_tm_canonical/canonical.v1.jsonl) (sha256 `b9ad8e9f…`, 2392 rows, 2096 eligible), six per stratum across `short_gloss` / `uncertainty` / `polysemy` / `compound` / `citation_dense`. **Selection is seedless**: strata are computed from the record text and the six members of each are the six smallest `sha256(record_id)`, so identical store bytes rebuild the identical thirty on any machine with no seed to lose — re-proved by `h5069_source_support_audit.py verify`, which re-hashes the store, re-hashes every frozen record and re-runs the selection. Verdicts: 21 `faithful`, 4 `addition`, 3 `conflation`, 2 `omission`, each with the deciding German and Russian words quoted and **a refutation criterion for every substantive criticism**. **Headline defect — `adhi-vid`:** German `durch eine zweite Frau (instr.) hintansetzen` shipped as «ставить первую жену позади второй жены (instr.)» — an instrumental rendered spatially while the `(instr.)` label is retained, so the record now asserts that this Sanskrit instrumental carries a locative sense; the same record renders `hintansetzen` correctly twice further down, which is what makes it an error rather than a judgment call. Also confirmed: «союзить» is not a Russian verb in the sense "to ally" (`yat`); an unresolved translator alternative «влекомый/тянущийся» shipped to readers (`vah`); Russian prose emitted inside the `{#…#}` **Sanskrit** mask (`vid`); an untranslated German `(consistent)` surviving in two incompatible renderings inside one record (`saṃhan`). **Corpus-wide, counted not estimated** (`poolcheck`, deterministic predicates over all 2392): **898 = 37.5%** of records have fewer `{%…%}` spans in Russian than in German, **718 = 30.0%** have none at all, 133 of those substitute guillemets «…» that appear nowhere in the German, and 25 carry Cyrillic inside a `{#…#}` Sanskrit mask — against a datasheet that states markup is **preserved**. Localised to the generation layer, not the exports: the sibling [`translation_memory.ru.publication.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/release/translation_memory/translation_memory.ru.publication.jsonl) loses the wrapper on the same 718 Russian sides while both stores keep it on the German side. **Three of this audit's own claims were falsified and the falsifications are recorded rather than dropped:** the "canonical export drops the wrappers" hypothesis (the first probe had searched the whole payload JSON and so matched the German field — re-run against the Russian field only, it inverted); a "whole sub-senses missing" reading of the 115-span `gam` record (equal byte offsets into texts 185 bytes apart — both sub-senses are present, the real defect is four span merges); and a `dhā` `addition` verdict refuted by another record in the same sample establishing «велеть» as the pipeline's standing convention for causative *lassen*. **A blinding defect in the apparatus was found by its own first reviewer and fixed:** control items shipped with ids literally reading `H5069.control:pos:001`, and the reviewer named both controls from the id strings before judging a single gloss — every packet item now carries an opaque `H5069.item:<hash>` id of one shape, the id→record map lives only in the sealed key, that first packet's results were discarded, and the regression is pinned by four assertions in [`tests/test_h5069_source_support_audit.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/tests/test_h5069_source_support_audit.py) (11 tests, all passing) plus the tool's own 15-check `selftest`. **The mandated independent review is PENDING, not PASS:** the mint names an independent Codex Astra (`gpt-6-astra`) logic critic, that lane was probed live and then returned `You've hit your usage limit … try again at Sep 21st, 2026 10:02 PM` mid-run, and no substitute family was promoted into the mandated slot — the retry command and the grading call are in the packet. **Nothing was applied to any store:** six confirmed errors are staged in `corrections_proposed.tsv` for the existing human-voted review-sheet path, since the mint forbids promotion of unreviewed records and hand-editing a content-addressed release artifact would invalidate its `target_hash`. Packet: [H5069_SOURCE_SUPPORT_AUDIT_30_SENSES_20-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h5069/H5069_SOURCE_SUPPORT_AUDIT_30_SENSES_20-09-2026.md).
