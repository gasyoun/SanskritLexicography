- **H4916 — the four-lease `staged-run` route no longer dispatches paid leases on the readiness probe alone, and a multi-profile bounded run can no longer pass on one canary receipt.** Since H4527 took the production TASK SHAPE block out of the readiness probe, the canary (`canary_gate.py judge`: production preamble plus a real synthetic card) is the only pre-dispatch check that sees the preamble. `max_account_orchestrator.py staged-run` now refuses before the call ledger and before `probe_fleet` unless every profile it will dispatch on holds its own fresh GO receipt (`--canary-receipt`, repeatable, at most 6 h old, no escape flag). The shared helper `enforce_canary_receipts` reuses `canary_gate.enforce` unchanged: one profile and one receipt keep the existing single-profile contract, and otherwise each receipt must name its `profile_slot`, slots must be distinct, and every dispatch profile must be covered. `bounded_staged_run --execute` uses the same helper over `gate_profiles` (the roster `run()` dispatches on), which closes its one-receipt-for-N-profiles gap; `--skip-canary-gate` and `--canary-max-age-seconds` are unchanged. Pins: the orchestrator selftest proves that no, stale, other-profile, duplicate and one-for-two receipts refuse with zero spawns and no call ledger, and that valid per-profile receipts reach the probe. `bounded_staged_run_selftest` (q4) proves that N profiles need N slot-named receipts. Both pins go red when the gate is removed. LANG_PARITY records a GAP: `canary_gate.judge_payload` counts only `russian` senses, so an EN canary can never GO and EN paid runs are fail-closed on both routes until the judge reads the run's own language field.
