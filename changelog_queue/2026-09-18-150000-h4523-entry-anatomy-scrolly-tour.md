- **H4523 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — EntryAnatomy scrollytelling tour: the two committed exemplars re-emit as 5-beat guided tours from one callout source, print contract intact.** [`EntryAnatomy/build_entry_anatomy.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/EntryAnatomy/build_entry_anatomy.py) grows a `--scrolly` emission path (H4523; storyboard MG-approved 14-09-2026): the SAME rendered sheet and callout set from `build_pwg`/`build_mw` are wrapped into beat containers — no forked template; new [`EntryAnatomy/scrolly_tours.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/EntryAnatomy/scrolly_tours.json) assigns every callout to beat 2 «Форма» (8 PWG / 9 MW labels) or beat 3 «Смысл» (16 / 12) by a target-substring first-match rule, and configures the injected beats 4 (digital `<L>` record raw-vs-rendered: PWG L 117557 / MW L 65898, same pipeline) and 5 (PWG↔MW sibling excerpts of the same lemma family, Duden plate excluded). Vanilla JS only: beat rail + progress bar, scroll-synced `body[data-beat]` dimming, IntersectionObserver sequential label emphasis, `prefers-reduced-motion` honored; no-JS and print fall back to the full static sheet at full opacity. **Print contract proven:** legacy HTML byte-identical on rebuild (`git diff` empty), legacy PDFs regenerate to the same single page, scrolly PDFs also single-sheet. New [`EntryAnatomy/test_scrolly.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/EntryAnatomy/test_scrolly.py) PASS — shell, self-containment (zero external resources), callout-target integrity via a static CSS-subset resolver replicating the layout-JS hide criterion (24/24 PWG + 21/21 MW targets resolve with `hits > nth`; browser probe confirms 0 hidden callouts live), beat assignment, fallbacks. Evidence: [`EntryAnatomy/reports/H4523_scrolly_verification.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/EntryAnatomy/reports/H4523_scrolly_verification.json) + 6 desktop/mobile screenshots; host decision = standalone self-contained HTML in EntryAnatomy/ (csl-guides embedding possible later), analytics unmeasured — accepted; publish-safety verdict SAFE-with-restriction (Cologne scans display-permitted, no Duden, no PII/secrets), no public hosting performed. `find_browser()` gains macOS Chrome/Edge/Chromium paths so `--scrolly` PDF export works on the Mac box.
