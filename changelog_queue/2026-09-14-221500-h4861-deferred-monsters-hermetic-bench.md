- **H4861 (Opus 5 `claude-opus-5`) — the h1339 offline bench no longer writes the tracked cap-and-defer ledger.** Measured per command: every command of the `Validate PWG production runtime and failure contracts` step, `window_selftest.py` included, leaves [`deferred_monsters.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/deferred_monsters.jsonl) byte-identical. The writer was the byte-identity control [`h1339_offline_bench.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/h1339_offline_bench.py). It drives the real `coordinator prepare-batch --allow-over-cost`, whose cost gate appended dated `nominal:ADAna` / `nominal:ABIra` rows on every run (H4528, #2201, committed two of them by accident). [`window_common.defer_monster`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/window_common.py) now honours a `PWG_DEFERRED_MONSTERS` path override on each call; production leaves it unset and keeps the tracked path. The bench sandboxes that path and exits 1 if the tracked ledger's bytes change. Negative control: removing the sandbox line fails the run even though the signature passes. `--selftest` pins the redirect, and the signature `5cfcedea1c` is unchanged. LANG_PARITY: 3 entries re-stamped SHARED and 1 new SHARED entry, via [`h4861_parity_restamp.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/h4861_parity_restamp.py).
