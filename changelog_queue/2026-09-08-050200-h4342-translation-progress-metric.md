- **H4342 — kitchen dashboard gains a translation-progress figure, not just DCS-attestation scope.** The 08-09-2026 [PWG-RU stall diagnosis](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/PWG_RU_TRANSLATION_STALL_DIAGNOSIS_08-09-2026.md) found nothing on [`progress_dashboard/`](https://github.com/gasyoun/SanskritLexicography/blob/master/progress_dashboard) reporting "translated share of PWG" — the headline "41.4% coverage" card is `dcs_attested_headwords / total_headwords` (scope in view), not translation output, so a 41-day production gap stayed invisible. `build_progress_data.py` gains a `translation_progress` block (promoted / target headwords, both lanes combined, plus a `verb.pct` matching the shape `nominal_lane()` already had) and `index.html`'s hero row now leads with it; live regen reads **1.54% of target headwords translated (89/5,788)**. In passing, the verb lane's correct in-scope denominator is `dcs_attested` (749, matching this repo's own `RussianTranslation/CLAUDE.md` "749 DCS-attested verb roots" statement) — the diagnosis doc's "48 promoted of 1,882" cited `universe`, a wider count, not the in-scope target.
