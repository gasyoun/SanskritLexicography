- **H4537 (OxAlpha `zai-coding-plan/glm-5.3-flash`) — MG's own 2013-2014 fuzzy MW↔PWK correction workbooks from yadisk `05_Sanskrit-Lexicon/corrigenda/` (4 files, ~21 MB) land as one derived pair table, plus a csl-corrections candidate-feed note.** [`HeadwordLists/mw_pwk_corrigenda_pairs.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pwk_corrigenda_pairs.tsv) — 260,568 MW↔PWK pairs across 5 source sheets, own-data parity re-derived and cross-checked against every workbook's dims from the [H4477 census §4](https://github.com/gasyoun/SanskritLexicography/blob/master/YADISK_05_SANSKRIT_LEXICON_TREES_CENSUS_10-09-2026.md) (all 5 OK). Three of the four workbooks turned out to be near-duplicates of one 2013-11-26 pass (plain `.xlsx` + a "peresortirovka" resort in `.xlsx`/`.xlsm`) — verified byte-identical on the MW/PWK/verdict columns, differing only in stale `LEN(PWK)`/`Book` helper columns the resort never recalculated; only the plain copy is read, the other two counted for parity and skipped as confirmed duplicates. Builder: [`mw_pwk_corrigenda_pairs.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pwk_corrigenda_pairs.py) (derived-only, raw workbooks stay on yadisk, fetched via the H4473 WebDAV recipe — no rclone on this Windows box). Report + candidate-feed note: [`MW_PWK_CORRIGENDA_PAIRS_11-09-2026.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/MW_PWK_CORRIGENDA_PAIRS_11-09-2026.md) flags the naming trap up front — MG's yadisk "PWK" is Cologne code `pwg` (Böhtlingk's kürzere Fassung), never Cologne's own `pwk` — and ranks filing priority for a future `/cologne-batch-pr` pass: 17 rows carry an explicit MG typo call (highest confidence), 24 are open `?` questions, 1,163 are exact-match duplicate-entry candidates (not typos), and the remaining ~257k rows are unreviewed/cross-dup/unverified fuzzy-match output that must not be bulk-applied. No writes made to csl-corrections or csl-orig.
