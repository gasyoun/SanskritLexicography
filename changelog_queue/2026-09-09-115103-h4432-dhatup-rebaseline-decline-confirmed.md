- H4432: **the DHĀTUP.→Palsule re-baseline H4386 declined is adjudicated row by row, and the decline is CONFIRMED — because the detector its numbers were measured with misfires on at least half of what it fires on, and the baseline it offered is not what the pipeline produces.** The shipped table does not move: **1465 of 1751 (83.7%), 1226 pwg / 140 mw / 99 mw-respell**, `src/data/dhatup_palsule.json` byte-identical (a rebuild today differs only in `built`), `builder_sha256` unchanged because the builder is untouched. New standalone probe [`RussianTranslation/src/pilot/dhatup_h4432_rebaseline_probe.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/dhatup_h4432_rebaseline_probe.py) restates the citation scan, the head-line weighting, the `<lex>` flag and H1333's resolution from the published prose, imports **nothing** from the builder, and asserts **25** figures: 1751 cited / 271 conflicted / 1480 resolved, 331 head-line claimants carrying the note, the blunt reading's **93 newly resolved / 130 lost / 18 reattributed** (241 moved), the narrow reading's **0 / 93 / 18** with `32,56 → cakk`, the funnel **74 → 43 → 31** (arithmetic only — see below), the **17** of 18 already shipped, and the 8 nominal + 10 no-head-citation split. **All 25 reproduce — H4386's arithmetic is confirmed exactly, including its own corrected 8 + 10.** What does not survive is what the arithmetic measures. The Böhtlingk-family variant-reading test is **line-scoped** (`_BOEHTLINGK_VL.search(line, mm.end())` — anywhere later on the same physical line), and a Cologne article is *one* physical line carrying six or eight citations, so a note attached to the fourth is read as disowning the first. **This is the same defect MW's side was corrected for on 07-09-2026** (`_vl_governs`, clause-scoped with parenthesis depth, after `paṇḍ`/32,130 shipped a root both dictionaries disown) — unnoticed on this side because the Böhtlingk note stands *after* its citation instead of before it. Mirroring that rule for a trailing note — no intervening `<ls` citation, no `.` or `;` at parenthesis depth 0 once markup and `{#…#}`/`{%…%}` braces are stripped, and the note not inside a parenthesis opened after the citation — cuts head-line claimants carrying a **governing** note from **331 to 148**, and the narrow reading's 18 reattributions to **8** (a variant admitting the `</ls>. <ab>v. l.</ab>` shape leaves 165 governing claimants and the identical 8, which is why the 8 are trustworthy and why the error rate is published as “at least half its firings” rather than “183 of 331”). The 10 that vanish are exactly the 10 the hand adjudication refused on scope, row for row. **The 17 flips, each with its quoted PWG sentence, in [`ABBREVIATIONS_RU.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/ABBREVIATIONS_RU.md): 4 justified outright, 1 with a reservation, 12 refused** — 10 because the note belongs to another citation (`9,76` laḍ's note is 19,53's; `17,80` cah's is 32,82's and reads `v. l. für {#cap#}`; `19,2` vyath's sits inside the artha parenthesis; `23,10` skand's reads `als v. l. von {#skund#}` at 2,8), 2 because the note governs but the winner is an artha noun (`17,13 tvakṣ → tvacana`, `{#tvacana#}¦ (von {#tvacay#}) <lex>n.</lex>`; `32,119 mlakṣ → mlecchana`, `(von {#mleC#}) <lex>n.</lex>` — where the right outcome is to drop, never to flip). The 4 justified are the ones where Böhtlingk disowns his own headword in the article's last token, twice **naming the winner**: `32,68` `√{#cal#}¦, {#cAla/yati#} {%ernähren%} <ls>DHĀTUP. 32,68</ls>, <ab>v. l.</ab> für {#bal#}.` and `33,55` `… <ls>DHĀTUP. 33,55</ls>, <ab>v. l.</ab> für {#las#}.`, plus `15,33 vehl → vell` and `35,11 sur → svar`, both sharing the artha. **The ordering question is answered and is not the load-bearing one:** "verbal beats nominal" *is* justified from Böhtlingk's practice (`√` on a verbal head line, `(von X) <lex>n.</lex>` on a derived noun) and running the tie-break last gives **0 flips, 0 losses, 30 net new → 1495/1751** (24 net new → **1489** clause-scoped) — but it blocks 12 of the 17 only because their challengers happen to be nouns, and leaves 5 rows attributed to roots the source explicitly disowns. Counterfactuals published beside the old baseline, never folded in: narrow + clause-scoped **1490/1751** with 7 flips, ordered + clause-scoped **1489/1751** with none. **Seven residues named rather than fixed:** the `<lex>` head-line test has the identical line-scope defect (`vell` is flagged nominal for `15,33` only because `<lex>n.</lex>` appears 1148 characters later on the same line, in an unrelated later sense); `23,39 → spardhā` is a live defect of a third class — a `<lex>f.</lex>` article that says in as many words the coordinate is `als Bed. von {#hvA#}`, shipped because a **sole** nominal head claimant is never tested; and `32,30 lañj → tuj` is the genuinely ambiguous row, the same trailing-parenthesis ambiguity MW's guard declares and leaves alone. Successor work is a clause-scoped port of `_vl_governs` to the Böhtlingk family with its own adjudication of the 148 governing notes — a screen correction, not a coverage project. `dhatup_h4349_verify.py` 29/29 green and still exit **2** with the corpora absent, `ls_enrichment_selftest.py` 21/21, pytest 401/401, ruff E9/F63/F7/F82 clean, LANG_PARITY 109 entries no drift, `--mw "" --pw "" --no-pwg-dotted` still rebuilds H1333 byte-for-byte (1226 / 70.0% / 59.9%). **Independently verified, and the verifier refuted four published claims** (Opus 5 `claude-opus-5`, separate context, own scanner and own clause rule written before reading the builder's, three end-to-end counterfactual builds against patched copies; report committed verbatim as [`RussianTranslation/reports/H4432_INDEPENDENT_VERIFIER_REPORT_09-09-2026.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H4432_INDEPENDENT_VERIFIER_REPORT_09-09-2026.md)). Confirmed: the baseline (its scanner reproduced the 1226-coordinate PWG set with symmetric difference 0), the blunt and narrow readings, 17-of-18, the 331 count, the clause-scoped **148** and the same eight flips reached independently, all ten scope eliminations, the `<lex>` defect, and **DECLINE CONFIRMED**. **Refuted: `1496/1751` — built end to end rather than added up, the narrow guard gives `1493/1751` and DELETES three shipped `source=pwg` rows (`28,1 tud`, `31,1 krī`, `31,41 grath`) whose new targets have no Palsule row; “17 shipped rows flip” — **21** change root, plus 46 changing witness only; “all 18 targets are nominal” — 8 are verbal root articles, 6 carrying Böhtlingk's own `√`, because the head/body flag also sees one physical line; and “wrong 183 of 331” — about 166.** **Two defects nobody had claimed:** the head/body discriminator's line scope, and a third `DHĀTUP.` citation form `<ls n="DHĀTUP.">4,13</ls>` that neither `_DHATUP` nor `_DHATUP_N` reads — **320 occurrences over 270 distinct coordinates**, while MW's reader handles the exact analogue — admitting which moves `coords_cited` **1751 → 1892** and the published rate **83.7% → 83.3%**, a larger effect than the ±31 under argument and in the opposite direction. **83.7% may not be published again without ruling on that form.**
