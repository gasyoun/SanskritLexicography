- H5468: **NKRYa gloss-audit flag sheets 2–14 (127 cards) + an append-only sheet-assignment
  ledger** — every one of the 136 currently-flagged PWG-RU gloss lemmas is now on a ≤10-card
  vote sheet in [`RussianTranslation/review/`](https://github.com/gasyoun/SanskritLexicography/tree/master/RussianTranslation/review),
  and card context is regenerated for all 136 flags (was 54).
  **The batching bug this fixes was live:** [`H5262_flags.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H5262_flags.json)
  is severity-ordered and the NKRYa drain keeps inserting new flags, so slicing it by
  position **renumbers sheets that are already published** — at 64 verdicts batch 1 ended on
  «кусывать», at 345 verdicts the same slice ends on «приформовывать», which would have
  silently dropped one card off published sheet 1 and duplicated another onto it. New ledger
  [`reports/H5262_sheet_assignments.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H5262_sheet_assignments.json)
  freezes `lemma → batch`: an assigned lemma never moves, new flags only ever open batches
  after the highest that exists, and a batch carrying a review-binding lock is sealed against
  rebuild (`assign()` / `sealed()` in
  [`build_h5262_nkrya_flag_sheet.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_h5262_nkrya_flag_sheet.py),
  new `--all` mode). Seeded from the published artifact by
  [`h5468_seed_sheet_assignments.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h5468_seed_sheet_assignments.py);
  [`reports/H5262_sheets.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H5262_sheets.json)
  rebuilt from the locks by
  [`h5468_update_sheets_index.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h5468_update_sheets_index.py).
  **Disclosed, not silently fixed:** «кусывать» is no longer flagged at all — H5468's own
  ellipsis-fragment precision filter retired it as a measurement artifact — but it sits on
  the already-prepared sheet 1; it is recorded under `retired` in the ledger and whether to
  re-cut sheet 1 is a human call. Two SLP1 leaks fixed in the sheet builder, both caught by
  the csl-pyutil evidence preflight: subcard ids (`rakz~~h0_03_sec_3`) are SLP1 by
  construction and are rendered on the card, so only the ids actually shown on that sheet are
  allowed; and the one context card without an `iast` fell back to raw `key1` («viS»), which
  now goes through `slp1_iast`. **Not done here:** the drain itself — 4,556 of 4,901 census
  lemmas are still unqueried, because the `ruscorpora-api` token lives only in the Mac
  keychain (`csl_pyutil.nkrya.load_token()` returns `None` on the Windows box). No store
  write, no paid call, no hub push (publishing is fenced to a human by `ALLOW_PUBLISH_PUSH`).
  Checks: `h5262_ipm_audit.py --selftest` PASS (33 checks, offline), `h5262_card_context.py
  --selftest` PASS, 13 sheets × evidence preflight + identity gate PASS. PR:
  [#2352](https://github.com/gasyoun/SanskritLexicography/pull/2352) · handoff:
  [H5468](https://github.com/gasyoun/Uprava/blob/main/handoffs/H5468-Opus_SanskritLexicography_nkrya-store-ipm-drain-remaining-sheets_24.09.26.md).
