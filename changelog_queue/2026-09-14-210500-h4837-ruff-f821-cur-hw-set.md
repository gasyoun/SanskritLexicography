- **Fixed — `master` CI red on "Python lint" (ruff `F821 Undefined name cur_hw_set`) from the H4837-support trial commit `9a3c55af`.** [`HeadwordLists/mw_pwk_nachtraege_missing_entries.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pwk_nachtraege_missing_entries.py) read `cur_hw_set` in the MW-annexure flush before its first assignment (runtime-safe only because `cur_hw` is `None` on the first headword) and reused `cur_hw` as the comprehension variable; now `cur_hw_set = set()` is initialised before the loop and the flush reads `clean(c) for c in cur_hw_set`, matching the end-of-file flush. Behaviour unchanged: re-run against local `csl-orig/v02` with `PYTHONHASHSEED=0`, original and fixed scripts produce byte-identical TSV, report and stdout (13,208 `sup_7` / 194,283 MW headwords / 6,082 annexure / 1,751 NO-MATCH), and every column of the committed [`mw_pwk_nachtraege_candidates_14-09-2026.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pwk_nachtraege_candidates_14-09-2026.tsv) except `mw_fuzzy` reproduces exactly — that column's tie-break between equal-score near-forms follows set iteration order, so it varies with the hash seed in the original script too.
