- **H4527 (Opus 5 `claude-opus-5`) — the cohort path dispatched its first live paid window ever (width 1 on `c1`), and the acceptance record stayed deliberately unwritten.** The gate chain ran end to end for the first time since the cohort wiring landed on 11-09: ration reporter exit 0 → `dq_canary_puregloss` **GO** receipt (`h4527-canary-160916`, 29 427 ms) → dry run (`live_admission.admitted: true`) → `bounded_staged_run.py --execute --cohort-path --cohort-width 1 --only-profile c1`. The readiness probe passed on both legs (warm-up 16 453 ms, measured 17 008 ms, `probe_prompt_sha: 90e2f1f6698b`) — a third independent clean reading confirming the 15-09 injection-shape repair, and the fastest yet. The wave bound, dispatched, audited and settled exactly once: `peak_concurrency: 1`, `effective_width: 1`, `calls_spent: 1` = `calls_reserved: 1`, one promote + one TM callback, single-promoter rule intact. The card itself came back **null** (`missing-or-mismatched-key`, transient class, requeued), so `accepted_order` is empty and `COHORT_LIVE_ACCEPTANCE.json` was **not** written — a window that accepts nothing cannot prove byte-identical acceptance against the serial route, and writing the record off it would be the silent gate-opening the handoff's own Fail-list bans. Also corrected: every armed command recorded in this handoff since 11-09 omits `--coordinator`, `--cwd` and `--events`, without which `--execute` exits at argparse; the runnable form (with a bare scratch `--cwd`, H2158) is in the packet. Packet: [H4527_LIVE_COHORT_WINDOW_FIRST_RUN_16-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_LIVE_COHORT_WINDOW_FIRST_RUN_16-09-2026.md) with the run report, events and checkpoint. 2 paid calls; `c1`'s 16-09 ration is now fully spent (next legal 17-09 00:00Z).
