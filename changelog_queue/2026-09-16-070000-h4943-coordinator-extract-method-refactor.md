- **H4943 (Sonnet 5 `claude-sonnet-5`) — `src/pilot/coordinator.py`'s four oversized functions split into private helpers, zero behaviour change.** `claim()`'s four `--kind` branches moved into `_claim_target_verb`/`_nominal`/`_rootmap`/`_defect_repair`; `prepare()`'s shared preflight→cost-gate→harness-gen sequence moved into `_prepare_run_preflight`/`_prepare_run_gen_harness`; `prepare_requeue()` split into `_hydrate_pending_requeue`, `_write_requeue_defect_fshas`, `_execute_requeue_attempt`; `promote_ready()`'s per-lease validation loop moved into `_validate_lease_for_promotion`. `main()`'s ~110-line argparse wiring was left alone — boilerplate subcommand construction, not genuine branching complexity. Independently verified by a second, distinct-tier session (Opus 5 `claude-opus-5`, satisfying the H4235 close gate): a differential harness comparing pre/post revisions side by side found byte-identical `prepare()` child argv and cost-gate calls across verb/nominal/defect-repair/rejected-kind, and identical `claim()` lease dicts and `SystemExit` text across all four kinds plus unknown; a `ruff` F821/F841 variable-leak audit was clean; `window_selftest.py` 225/225 and `coordinator_hardening_selftest.py` plus 19 further PWG gate selftests passed before and after; `lang_parity_check.py` showed no drift (115 entries, restamped hash matching the live file's sha256). Also added one-shot receipt script `h4943_parity_restamp.py` (same class as the H4935 siblings). [PR #2247](https://github.com/gasyoun/SanskritLexicography/pull/2247) merged as `d5f09878`; handoff [H4943](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H4943-Sonnet_SanskritLexicography_pilot-coordinator-extract-method-refactor_16.09.26.md) closed via `d5a90a9f8`.
