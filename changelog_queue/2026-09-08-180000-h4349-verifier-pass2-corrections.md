- H4349 (second verifier pass, verdict **pass** with corrections): a second independent adversarial verifier reviewed the merged correction at `d1fa56ad` and returned **pass** on the data — it re-derived the denominator 1751 with its own `<ls>`-scoped regex (a naive any-context regex gives 1708, so the scoping is load-bearing), confirmed coverage 1465/1751 = 83.7%, confirmed `set(table) ⊆ cited` with zero violations, confirmed both withdrawn rows are absent, re-verified both refutations against the corpora, recounted the 636 dotted-id articles and their single coordinate `15,89`, and — going beyond the brief — ran **four counterfactual builds** neutralising one guard at a time plus one with the screen order swapped. Those counterfactuals are the first real proof the screens are not decoration: deleting the same-book guard ships `32,56 → cukk` (table 1466), deleting the membership guard ships `33,67 → tras` (table 1466), and swapping ceiling/membership order leaves the table unchanged but moves the published counts (`pw_refused_out_of_coordinate_space` 2→0, `pw_refused_not_cited_by_pwg` 3→5, `_out_of_coordinate_space` empty) — so the shipped order is the one that produces the published adjudication. It also confirmed the MW exemption was neither widened nor narrowed, that no ceiling constant is hard-coded, and that the 12/4/11/3/2/8 refusal split reproduces exactly from `_stats`. **Three defects it raised are fixed here, none of them touching a shipped row.** (1) **A false invariant, repeated:** LANG_PARITY.md claimed "the artifact is byte-for-byte identical to the pre-H4349 one". The **`table` payload** is (1465 keys, zero added/removed/changed); the JSON **file** is not — it gains `_sibling_disagreements`, `_out_of_coordinate_space`, ~46 `_stats` keys and a later `built` date, so its sha256 moves (`9b199176…` vs `c5fda59c…`, 583360 vs 578916 bytes). This is the same class of false-invariant assertion the *first* verifier flagged, written by the same hand in the correction that fixed it; the wording is now exact and says so. (2) **The Böhtlingk v.l. guard is inert as shipped** — ABBREVIATIONS_RU.md called the latent defect "now closed", but deleting the guard leaves the artifact byte-identical. pw has exactly one such citation, `31,32` (pw:309560), and PWG cites that coordinate for three claimants (`plI` pwg:254914, `lvI` pwg:420914, `vlI` pwg:470515), so the same-book screen refuses it first and `pw_refused_variant_reading` is `0`. The doc now says the guard is **order-shadowed and refuses nothing today**, kept as a standing guard because the construction is real in pw and the next sibling pass need not be same-book conflicted; the verifier's check is renamed to assert what it actually tests (the construction exists), not that the guard fires. (3) **The standalone verifier failed OPEN** — with `csl-orig` not a sibling of the checkout it printed "skipped" and exited **0**, so the second verifier's own clean `/tmp` worktree scored a green run that executed **zero** checks, and any CI without the corpora would too. It now exits **2** and names the absent paths; `--skip-if-absent` restores the old behaviour for a caller that genuinely cannot supply the corpora and says so out loud. Verified in the exact failing condition: exit 2 in a corpus-less worktree, exit 0 with the flag. Checks after the fix: selftest **18/18**, pytest **401 passed**, ruff clean, LANG_PARITY **109 entries, no drift**, verifier **17/17**. **Declared residue the verifier named and this PR does not close:** nothing pins the artifact to the builder (a stale artifact beside a changed builder passes everything — the verifier rebuilt by hand and it reproduces); both screens are opt-in keyword arguments with permissive defaults, so a future third sibling pass that forgets to pass them is silently unscreened, which is the exact shape of the original defect; root attribution is unverified for 1463 of the 1465 rows; and the published nominal/body-only/already-present figures are asserted in no test.
