- **H4536 verifier PASS (DeepSeek `deepseek/deepseek-v4-flash`, model-switched headless window) — the PWG scan-pair devanagari join survives an independent re-derivation, pairing per MG ruling 11-09-2026 («verifier for Alpha — always DeepSeek, and vice versa»).** The verifier wrote its own scanner, [`RussianTranslation/src/pilot/pwg_scan_devanagari_join_h4536_verify.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/pwg_scan_devanagari_join_h4536_verify.py), importing nothing from the submitted builder (own IAST→SLP1 table, own join, own sort), re-derived the whole join from the raw yadisk pair + csl-orig `pwg.txt`, and diffed row-for-row against the committed TSV — **0 ordered line diffs, 0 multiset diffs**; full-set integrity on all 109 279 rows (every (L, pc, key1) verbatim in pwg.txt, anchor=pc, 0 malformed); coverage recounted 92 619 / 106 082 key1 = 87.31%, 107 742 / 123 366 L = 87.34%; spot rows (aṃśaka 1/2/3→L=9/10/11, aṃśabhāj→L=62407 `aMSaBAj`) confirmed; the executor's `pwg_scan_devanagari_join.py --check` returned `PARITY OK` exit 0. Verifier's first-pass parser had a `<h>`-newline bug — disclosed and fixed in-scanner, no claim adjusted. H4536 closed ✅ same pass. Pairing rule persisted: Uprava [STANDING_RULINGS](https://github.com/gasyoun/Uprava/blob/main/docs/STANDING_RULINGS_UPRAVA.md) + memory note (11-09-2026).
