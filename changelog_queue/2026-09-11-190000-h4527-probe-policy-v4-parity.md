- **H4527 prerequisite — MG's `production_v4` probe ceiling reaches `master`, and the one thing that kept it off is named: an un-updated parity hash, not a disagreement about the ruling.** The 240 000 ms wall ceiling MG ruled on 10-09-2026 («подними до 240») was committed the same evening as [`4092291ae`](https://github.com/gasyoun/SanskritLexicography/commit/4092291ae59d761d1edf0397e9aae1308fc38a67) on the `h4213-drain` branch, where it has sat unmerged behind [PR #2101](https://github.com/gasyoun/SanskritLexicography/pull/2101) — red on `RussianTranslation gates` for a single reason: `probe_log.py` is tracked by **two** LANG_PARITY ledger entries (`h1339_measurement_integrity`, `h1386_resume_recovery_and_medium50`) and neither hash was re-verified after the policy edit, so `window_selftest` failed 222/223 on `test_lang_parity_ledger_complete`. Both verdicts were re-checked and still hold SHARED — the new `POLICIES['production_v4']` row is lane-agnostic, reads and writes no target-language field — and the hashes are updated. MG's commit is cherry-picked here with authorship intact rather than re-typed.
- **H4527 prerequisite — the live-policy sentence in [`RUN_FREQ_MAX.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/RUN_FREQ_MAX.md) §A1 now names `production_v4`.** That paragraph exists to warn that a runbook naming a live threshold goes stale within days, and it had gone stale again — it still called `production_v3` (80 000 ms wall / 45 000 ms route) the live policy. The pass rule stays DERIVED (`probe_log.POLICIES[probe_log.CURRENT_POLICY]`); only the dated history sentence moved, and it records what v4 *is*: a host-degradation accommodation with the route ceiling subsumed at the wall number, not a route-health claim.
- **H4527 correction — «one canary GO receipt satisfies both lanes» was true in principle and false in fact on 11-09.** The handoff's *Armed launch* section deferred the day's remaining probe attempt to H4213 on the grounds that H4213's canary re-judge would produce the GO receipt H4527 needs. Probed: H4213's 05:29–05:36Z window was a **real card window** (`pwg.no_pwg_scale_plan.v1`, key `arvant~~h0_zz_nws00`, provenance `real`), and that key does **not** match `promote_final_cards.SYNTHETIC_KEY_RE` (`^(?:dq_canary_|zz~~synthetic|synthetic[_~-])`), so `canary_gate.judge` would refuse it as "NOT a synthetic-control key — refusing to judge a real window as a canary". H4213's re-judge therefore produces no receipt for H4527; a `dq_canary_puregloss` run is owed by whichever lane goes first.
