- **H4475 (OxAlpha `zai-coding-plan/glm-5.3-flash`) — the Sanskrityatina `04_Reverse` coding legend is landed as the `sanskrityatina-reverse-index` dataset: the 187,992-headword Schwarz-based reverse-dictionary stock's class taxonomy is now machine-readable, and the mint-time assumption is corrected on the record.** `187992 headwords.txt` (1,706 B, ASCII/CRLF, yadisk) turned out to be the coding LEGEND — 1 stock line (class 0 = all 187,992 "in two or more dictionaries") + 23 overlapping subclass lines (sum 197,019, delta 9,027) + 3 attested SLP1 example headwords — NOT the headword list; the legend is all that survives in `04_Reverse` of the coding stage. Landed under [ReverseDictionary/sanskrityatina/](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/): derive-don't-store builder [`tools/h4475_build_reverse_index.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/tools/h4475_build_reverse_index.py) (raw stays gitignored, refetch via rclone in `--help`), [coding-classes TSV](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/sanskrityatina_reverse_index_coding_classes.tsv) (24 rows), [examples TSV](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/sanskrityatina_reverse_index_examples.tsv) keyed via sanskrit-util `from_slp1`/`slp1_form_key`/`slp1_norm`/`norm` (page-ref column empty by construction — stated, not hidden), idempotent (double-run byte-identical). Verification: all 3 legend examples cross-checked against csl-orig v02 `<k1>` headwords — `paryantIkfta` in MW+BHS (2/2 tags), `aMSagaRa` in IEG (1/2; PD unprobeable), `paryavadApayitar` in BHS and correctly absent from MW (negative control) — and the SLP1 hazard mapping (**`R`=ṇ, `N`=ṅ, `f`=ṛ**) documented where the next reader will trip over it. Folder census (177 entries, 1.786 GiB) recorded with the SCH-experimental XML research cases as their own line: 16,243 UTF-16LE cases in csl-orig markup **with `<L>/<pc>` page-column refs** — the natural follow-up TSV; NCC (127,980/152,576), the 384k index binaries, the васту-суд expertise and the Schwarz 1974 source literature censused, none parsed. Verdict vs estate: REUSE_INDEX §2 "none known" confirmed — the canonical 266,820-reverse master is a different object; nothing superseded. [Format table + census](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/H4475_FORMAT_TABLE_10-09-2026.md) · [metadoc](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/sanskrityatina-reverse-index.meta.md) · [provenance.json](https://github.com/gasyoun/SanskritLexicography/blob/master/ReverseDictionary/sanskrityatina/provenance.json) · kosha row `sanskrityatina-reverse-index` · handoff: [H4475](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4475-OxAlpha_SanskritLexicography_sanskrityatina-reverse-index_09.09.26.md) (class:data — awaiting independent verifier PASS before ✅).
