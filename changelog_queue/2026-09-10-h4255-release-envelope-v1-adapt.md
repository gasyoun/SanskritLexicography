- **H4255 (OxAlpha `zai-coding-plan/glm-5.3-flash`) — this repo's FAIR Release #1 data exports are now pinned by a `release-envelope-v1` envelope, the second repo to adopt after the kosha pilot (additive only; canonical stores untouched).** [`data/manifest/envelopes/v1.144.164.envelope.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/data/manifest/envelopes/v1.144.164.envelope.json) wraps release `v1.144.164` (commit `971c8a6e`) around the three data-export TSVs plus the FAIR Release #1 deposit doc, each pinned by recomputed LF-canonical sha256 (`markup_tag_census.tsv` 671 rows / `headword_overlap_matrix.tsv` 105 pairs / `headword_unique_counts.tsv` 15 rows), with source pins re-derived from real bytes: the census's 44 csl-orig v02 main-text files at `78cbb257` (set-digest `e150c5bb…`; the pin is the methodology-doc-named checkout, deliberately NOT a `rev-list --before` re-derivation — origin/main had already moved by the 2026-07-11 run date) and the overlap builder's union input at `d3e7fecc` (323,425 data rows; the 05-09-2026 H4075 refresh post-dates the build and is a declared tolerance, pin authoritative). The gold V6 clause is proven, not asserted: a fresh `git archive` of csl-orig `78cbb257` plus a rerun of `data/markup_tag_census.py` reproduces the committed census byte-for-byte (`a8ebc33e…`), and the overlap builder against the pinned union blob reproduces `60d5f30c…` byte-for-byte — while rebuilds against CURRENT upstream differ exactly as the declared tolerances say (csl-orig v02 drift; union refresh), which is the envelope stating its own limits rather than hiding them. Repo-appropriate checker [`scripts/envelope_check.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/scripts/envelope_check.py) (stdlib-only, Python ≥ 3.9, adapted from kosha's H4239 reference) re-derives every declared digest — artifacts, output parity, both source pins, tag→commit — exits 0 only on all-pass, SKIPs (never fails) a source pin whose sibling clone is absent, and fails closed on an unknown `sha256_form`. Verified: full run **PASS (all checks, 2/2 source pins resolved and matching)** on this box; SKIP-path control green with `--source-root` pointed at an empty tree; tamper control (one flipped envelope digest) exits 1. Licence/citation/publication state recorded from [DATA_LICENSE.md](https://github.com/gasyoun/SanskritLexicography/blob/master/DATA_LICENSE.md) and [data/FAIR_RELEASE_1.md](https://github.com/gasyoun/SanskritLexicography/blob/master/data/FAIR_RELEASE_1.md) as they are: repo-level Zenodo software DOI live (H3961 ruling), dataset-level DOI still awaiting the prepared manual deposit (the open human @DO). Spec: [Uprava docs/SPEC_RELEASE_ENVELOPE_V6_PORTFOLIO_2026.md](https://github.com/gasyoun/Uprava/blob/main/docs/SPEC_RELEASE_ENVELOPE_V6_PORTFOLIO_2026.md) · reference: [kosha `data-v0.5.0` (H4239)](https://github.com/gasyoun/kosha/blob/main/data/manifest/envelopes/data-v0.5.0.envelope.json) · handoff: [H4255](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4255-OxAlpha_SanskritLexicography_release-envelope-adapt_06.09.26.md).
