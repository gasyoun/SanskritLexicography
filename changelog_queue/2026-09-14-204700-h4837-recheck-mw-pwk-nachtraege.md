- **H4837 support, recheck (deepseek-v4-flash `deepseek/deepseek-v4-flash`) — independent re-derivation of the MW-vs-Nachträge trial by two further methods, plus a two-round DeepSeek verifier pass.** New [`HeadwordLists/MW_PWK_NACHTRAEGE_TRIAL_INDEPENDENT_RECHECK_14-09-2026.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/MW_PWK_NACHTRAEGE_TRIAL_INDEPENDENT_RECHECK_14-09-2026.md) + [`mw_pwkvn_missing_entries.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pwkvn_missing_entries.py) (all 24,976 `pwkvn` headwords vs MW: 9,096 absent rows / 5,237 unique, 305 cite Mahāvyutpatti; annexure enrichment **7.84×** — a Nachträge headword is ~7.8× likelier among MW annexure entries than MW main) + [`mw_pw_sup7_missing_entries.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/mw_pw_sup7_missing_entries.py) (same `sup_7` slice, stricter tiers: 9,051 exact / 456 stem / 399 near / 3,302 absent; reverse 1,798/6,067 = **29.6 %**). Confirms the trial's headline independently: `sup_7` = 13,208 (exact), reverse ~30 % under every convention, `kAritra` absent from MW and present in `pw` L216013 + `pwkvn` L16013 (pc 7-331-d), `kArApaka` an MW annexure entry, `pwg` holds neither. **Verifier round 1 found a real defect, fixed here:** the trial report's §3 tier rows sum to 13,129, not 13,208 — the builder's `len(hw) < 3` guard silently drops 79 short headwords (verified by direct count); §3 now carries the accounting note. **Verifier round 2: PASS, 60/60 spot-checks, zero counterexamples** (20 Y + 20 N reverse-overlap rows, 20 absent rows, guard class, tier arithmetic) — it also caught four internal inconsistencies in the recheck note's first draft (enrichment denominator, a mislabeled 13,129 row, the conflated +1/+15 annexure deltas, a label collision), all fixed; the note now opens with a counting-conventions table. Residuals → H4837 (adjudication; posting = MG ruling).
