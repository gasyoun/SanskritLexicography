# 2026-09-24 13:30 — H5336 learner-layer user study protocol (G7)

**H5336 (Opus 5 `claude-opus-5`) — roadmap gap G7 gets the study it promises, written before any data exists.** New [`docs/USER_STUDY_PROTOCOL_LEARNER_LAYER_G7_24-09-2026.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/docs/USER_STUDY_PROTOCOL_LEARNER_LAYER_G7_24-09-2026.md): within-subject find-the-right-sense tasks, learner's reading layer vs plain Cologne lookup, 2-group Latin square with counterbalanced order, 20 items per participant, primary outcome log time-to-correct-sense, non-inferiority margin −0.07 on accuracy, preregistered mixed-model ladder with a named fallback, exclusion/timeout/stopping rules fixed in advance, and a 152-ФЗ section that keeps every identifier structurally out of every repo (pseudonymous codes, identity key deleted within 30 days, no recordings, aggregate-only reporting with a ≥5 floor per published cell). Protocol only — no recruitment, no consent collected, no participant approached.

New [`data/h5336_learner_study_power.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/data/h5336_learner_study_power.py) fixes the analysis plan in code: analytic paired-t sample size (d 0.5 → 34, **0.6 → 24**, 0.8 → 15) plus a Monte-Carlo power table over the crossed participant × item design, and a `frame` command that counts the item frame on our own live data — DCS-2021 bands from [`VisualDCS/dcs_lemma_summary.json`](https://github.com/gasyoun/VisualDCS/blob/main/dcs_lemma_summary.json) (83,239 lemmas) ∩ the H5330 typology pool (2,081 distinct `k1`, 853 attested) ∩ the shipped csl-atlas learner index (52,934 records) = **360 eligible items**, fail-closed when any input is missing. Target n = 24 (0.86 power against a 16% time reduction under the conservative analysis), pilot n = 6 for variance re-estimation.

**Finding the selftest forced:** dropping the item counterbalance makes the by-participant test reject a non-existent effect **45.8%** of the time (item-set offset masquerading as a condition effect) against **3.1%** with the Latin square — both rates are asserted in `--selftest`, so the design cannot be quietly simplified later. **Correction from a live probe:** the learner reading layer already ships (page + index, generated 2026-06-13) but carries **no per-sense ranking**, so condition B is evaluated as a *routing* aid and the MW sense index for answer options is the one genuinely missing prerequisite. Canaries: selftest PASS (type-I 0.031, monotone in n and |β|, deterministic at a fixed seed, refusals on invalid d); `frame` recomputed from live files.
