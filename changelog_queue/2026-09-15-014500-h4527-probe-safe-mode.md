- **H4527: the readiness probe now starts the CLI in the same stripped mode the paid lane uses. The 11-09 "retune the prompt" diagnosis is corrected.** Since H2251 `headless_worker` spawns every generation call with `--safe-mode`, which disables the profile's CLAUDE.md, skills, plugins, hooks and MCP. `max_account_orchestrator._probe_call` never adopted the flag. So the gate certified a call carrying the whole interactive `c1` profile, including the hook dumps: the 07-09 refusal cited them as *«the surrounding system-reminders»*, and a hook traceback sits inside the 10-09 refused envelope. That added about 15 K tokens per request (50 209 without the flag vs ≈35 K with it). The same-day counterexample on 11-09 (three lane calls passed while the probe refused) had been read as clearing the hooks. In fact all three ran with `--safe-mode`, so they clear the route, the credentials and the host, and leave the profile surface as the only input that differed. The probe now derives the flag from `headless_worker.resolve_safe_mode({}, claude)` (the lane's own resolver, never a literal) and records `cli_safe_mode_effective` on its row. Prompt, schema, plan mode, ceilings and the `{"ok": true}`-only check are unchanged. **Live reading on `c1`, 15-09 01:35Z: warm-up 26 208 ms / measured 38 468 ms, both `success`, GATE-0 PASS** (n = 1; 4–7× faster than both 11-09 readings). Pin added to `max_account_orchestrator_selftest`; 5 LANG_PARITY entries re-derived SHARED plus one new entry (`h4527_parity_restamp.py`). Packet: [H4527_PROBE_SAFE_MODE_DIAGNOSIS_15-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_PROBE_SAFE_MODE_DIAGNOSIS_15-09-2026.md).
