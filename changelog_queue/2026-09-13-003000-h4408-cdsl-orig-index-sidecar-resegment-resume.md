- **H4408 (OxAlpha `zai-coding-plan/glm-5.3-flash`) — csl-orig one-pass index sidecar + the per-record 69 MB rescan repaired.** New [`HeadwordLists/cdsl_index.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/HeadwordLists/cdsl_index.py): a committed SQLite key->byte-offset sidecar builder/reader (`<dict>.idx` under `data/cdsl_index/`, gitignored derived, rebuilt on demand; one streaming binary pass over pwg.txt indexes 123 366 records in ~0.4 s). Adoption: `EntryAnatomy/build_entry_anatomy.py` `load_records` serves from the sidecar (legacy whole-file parse kept as fallback) — **full-census parity proven: all 123 353 direct-parse records reproduced with 0 missing / 0 value mismatches, plus a 20/20 random spot-check**; `HeadwordLists/build_union.py` iterates via the sidecar instead of `read().split('<L>')` over ~300 MB — **byte-identical old-vs-new outputs on all 15 dicts (323 659 headwords, 237 folded, 3 995 candidates)**; `HeadwordLists/heritage_mw_crosswalk.py` streams line-by-line (both anchor regexes are line-local). `RussianTranslation/src/sanity_glyph_resegment.py` no longer re-runs the whole-dict `pwg_mask.records()` generator PER record: one streaming pass collects sampled bodies, verdicts append to an fsync-per-row JSONL cache (`reports/pwg_glyph_sanity.cache.jsonl`), and a rerun resumes with **zero re-paid DeepSeek verdicts** (offline `--selftest` proves one-pass + crash-resume). The five `h{3500,i2109,h2254,h2504,h3144}_parity_restamp.py` receipts now share [`RussianTranslation/src/pilot/parity_restamp.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/parity_restamp.py) (ledger block parse, one-run drift census over both checker output shapes, N-id in-process hash refresh instead of one interpreter spawn per entry); their docstring evidence and NOTE texts preserved verbatim.
