# 2026-09-24 12:20 — H5330 definition-typology 300×7 double-keyed pool frame

**H5330 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — definition-typology round 2 gets its reproducible pool frame.** New [`data/definition_typology_pool_sampler.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/data/definition_typology_pool_sampler.py) — seeded (5330) stratified draw of **300 entries × 7 dictionaries = 2,100 rows**, strata = dictionary × H1483-predicted WS2.4 class (classifier imported verbatim, never re-derived), proportional allocation with min-quota 10/class, largest-remainder rounding, fail-closed allocator; `--selftest` pins determinism + floor behavior + refusals offline. Committed frame: `definition_typology_pool_300x7_manifest.tsv` (ids only, no definition text), `_strata.tsv`, `_meta.json`. New [`data/build_typology_pool_sheets.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/data/build_typology_pool_sheets.py) renders 14 double-keyed sheet variants (a/b per dict, distinct sheet_ids, prediction-blind cards, 4-class force-choice via the rating row) through `csl_pyutil.render_review_sheet_packset` and its `--dry-run` re-parses the rendered HTML, synthesizes handin-shaped decisions payloads and validates pairability — **14 sheets / 4,200 card decisions / 14 decisions files PASS**. Rendered HTML stays gitignored (never-commit-csl-orig guard; regenerable from manifest + seed). Design + strata documented in [`data/DEFINITION_TYPOLOGY_POOL_300x7_2026.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/data/DEFINITION_TYPOLOGY_POOL_300x7_2026.md). Canaries: re-draw byte-identical; sampler selftest PASS.
