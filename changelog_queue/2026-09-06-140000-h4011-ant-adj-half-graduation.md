- H4011: GAPS §14 **graduated in full** — the `-ant` pool's ADJ half measured from the DCS master instead of estimated. H3984 reached only 23.5 % of the class because Sangram G2's universe is `upos='NOUN'`; this pass takes the per-lemma inventory straight from `dcs_full.sqlite` (read-only, never copied) and counts the surplus: **36,768 ADJ tokens over 1,880 lemma_ids**, adjudicated on the same MW+PWG headword **entry-id sets** into 34,809 / 1,015 `one_lexeme_two_spellings`, **0 / 0** `two_headwords`, 661 / 193 `at_only`, 95 / 13 `ant_only`, 1,203 / 659 left pooled. The pair GAPS §14 pins by name is answered from the inventory side at last: DCS `bhagavat` is lemma_id **48482, tagged ADJ** — exactly why G2 could not see it — and PWG indexes भगवत् and भगवन्त् under the identical id pair `53806,80057`, so it is **one lexeme under two spellings**; across all 1,880 ADJ lemma_ids not one comes out `two_headwords`, confirming the §630 presence-only inversion at seven times the NOUN-half scale. The `-ī` class's 664 ADJ tokens / 54 lemma_ids split 31 / 3 monosyllabic vs 633 / 51 polysyllabic. The `-ant` pool is now 100 % inventoried with 97.4 % of its token mass carrying a verdict; all 11 reconciliation rows pass and **no NOUN or G2 total moved** (the halves partition cleanly — zero lemma_ids carry both upos). Residual named, not blocking: 659 lemma_ids / 1,203 tokens with no dictionary witness stay pooled — 528 carry the productive `-vat`/`-mat` shape and 449 are hapax, so this is a productivity artefact, not unfinished work. [FINDINGS §630](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md) addendum 06-09-2026; builder + report + payload in [VisualDCS PR #126](https://github.com/gasyoun/VisualDCS/pull/126).
