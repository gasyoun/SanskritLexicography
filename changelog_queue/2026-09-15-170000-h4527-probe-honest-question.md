- **H4527: the readiness probe now asks an honest question instead of issuing an order that read as a prompt injection.** On 15-09 the cohort acceptance window on `c1` stopped at the warm-up probe again (`{"ok": false}`, 43 424 ms, 14:27Z), and this time `--safe-mode` was on. The session transcript gives the reason in the model's own words: the probe's embedded "READINESS CHECK" block "claims to share authority" with the task framing, over-justifies itself as "not a bypass", marks its sample "ignore", and wraps a translation-batch framing with no cards around it. Every one of those is a sentence of the H4277 bridge or the H3157 TASK SHAPE prepend. `_probe_prompt` now asks one question whose true answer is `{"ok": true}`: does the reference text mention the Petersburg Sanskrit dictionary? The text below the question does. The prompt keeps the old byte size (11 082 B, formerly 10 729 B), the same spawn, the same ceilings and the same `{"ok": true}`-only check. On the cohort-acceptance route (`bounded_staged_run --execute --only-profile`), the production-preamble check H3157 wanted is covered by the canary gate, which runs before the probe. There are two new pins, `test_health_probe_asks_an_honest_question` and `test_health_probe_carries_no_injection_shape`, 64 LANG_PARITY entries were re-derived (all verdicts stand), one new entry was added, and probe rows now carry `probe_prompt_sha` because readings across this change are not latency-comparable. `staged-run` has no canary behind the probe; that gap is routed to H4916. The pass also overran c1's probe ration: three attempts on one UTC day, one lock race with H4842, and a probe log split across evidence roots. That is recorded in LAUNCH_FUCKUPS and routed to H4915. The new prompt's first live reading is due no earlier than 16-09 00:00Z. Record: [H4527 injection-shape packet](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h4527/H4527_PROBE_PROMPT_INJECTION_SHAPE_15-09-2026.md).
