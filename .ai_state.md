# Project Objective: Repository documentation & housekeeping

Keep the SanskritLexicography data/research workspace documented and its git
state tidy.

## 📍 Current status (2026-06-26)

Two live tracks:
- **HeadwordLists / print-readiness — agent-prep COMPLETE.** Cross-dict union built
  (323k headwords, 15 dicts, gender-folded feminines); PRINT_READINESS A–F all
  pre-resolved (A 122-typo queue, B coverage ranked+cross-tagged → ~8 real adds,
  C 63 accent disagreements rendered, D key2 SLP1, E union, F folds+screened to ~7).
  Findings: **CDSL coverage of attested vocabulary is essentially complete**; the
  **MW/PWG spine is gated only by 16 typos**. Releases `v0.0.5`→`v0.0.26`. The remaining
  work is **human** (file typos vs scan, rule on the ~8/~7 candidates + accent QA).
  See [HeadwordLists/PRINT_READINESS.md](HeadwordLists/PRINT_READINESS.md).
- **pwg_ru (RussianTranslation) Track A — ongoing** (parallel session): the PWG→Russian
  Max bulk run + the Opus-judge/cost experiment; detail in
  [RussianTranslation/.ai_state.md](RussianTranslation/.ai_state.md) + the queue below.

## ➡️ Next Steps (Queue)

- [x] **pwg_ru full PWG→RU 30-day SLA documentation shipped (2026-07-04, Codex/GPT-5).**
      Added `RussianTranslation/FULL_PWG_RU_30_DAY_SLA_PLAN.md`, a docs-only hard-SLA
      plan for the full PWG Russian pass (**120,173 headword cards / ~120k-160k
      sub-cards**) under 30 calendar days, assuming 24/7 operation if caps allow while
      preserving the locked **≤3 concurrent LLM/Workflow translation lanes** rule.
      It separates H151 verb-root drain from the full-dictionary plan and names four
      workstreams: translation lanes, rootmap unblock, non-root/noun bulk, and
      publication validation. H151 and the economics note now point at the SLA plan;
      no pipeline code changed.

- [x] **A42 §2 Related work drafted from an ACL Anthology scan (2026-07-04, Sonnet 5
      `claude-sonnet-5`).** [`papers/A42_corpus_lexicon_resource.md`](papers/A42_corpus_lexicon_resource.md)'s
      §2 was a TODO stub; filled it with verified citations (SansTib LREC 2022, Itihasa
      WAT 2021, Punia et al. ICON 2020, Mao & Yu LoResMT 2024, Patel & Kulkarni ISCLS
      2024 word-sense-alignment precedent) and added ComputEL/LoResMT as live venue
      candidates alongside the existing Russian-orientalist journals. Cross-linked the
      Patel & Kulkarni precedent into A09 ([csl-atlas](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/PAPER_SENSE_ALIGNMENT.md))
      and A35 ([csl-orig](https://github.com/sanskrit-lexicon/csl-orig/blob/main/v02/etymology_stats/PAPER_DRAFT.md))
      since it's their closer Sanskrit-specific sense-alignment sibling, not A42's own.
      Mirrored to Uprava/ARTICLES.md (A42 → 2/5). Not yet committed.

- [x] **pwg_ru performance spend gate implemented (2026-07-03, Codex/GPT-5).**
      RussianTranslation now has a multi-root read-only `perf_preflight.py` matrix/JSON
      wrapper, fragment-TM empty/provenance warnings, conservative correction-prose handling
      for degenerate pass-through, and pinned selftests. Validation passed:
      `python src/pilot/window_selftest.py`, `python src/pilot/translation_memory.py selftest`,
      and `python src/pilot/perf_preflight.py sTA BU gam as i yuj vid han`. Current order:
      run `gam`, `vid`, `as`, `BU`, `yuj`; skip cached `han`; defer `sTA`/`i` until
      cache refresh or calibration. Details in
      [RussianTranslation/.ai_state.md](RussianTranslation/.ai_state.md).

- [x] **A33 (genetic-not-historical sense ordering) data + hypothesis pass — DONE (2026-07-03, Fable 5 `claude-fable-5`).** Verified A33's numbers against `RussianTranslation/research/`. **Found a committed-script reproducibility bug (NOT source drift — corrected mid-pass):** BOTH `analyze_sense_order.py` and `analyze_cross_dict.py` used a `)`-only PWG sense-delimiter regex, but committed csl-orig `pwg.txt` has ALWAYS used `N〉` (U+3009) — verified identical back to the 2026-06-24 commit (53,033 `〉` markers then and now). So the `)`-regex matched **0** delimiters: `analyze_sense_order` zeroed out entirely (split needs ≥2 hits), and `analyze_cross_dict` degraded silently to whole-entry units (PWG "density" 24.3%/87,680 entry-blobs instead of per-sense). Neither reproduced as committed. Fixed both to `[)〉]`. Rates reproduce EXACTLY (sense-1-oldest 73.5%, τ 0.375, adjacent ~76%, PWG Vedic-density **23.4%**) but current-source counts differ from the paper (dated-multisense 13,900→**11,882**; within-sense 32,254→28,280; cited senses 118,528→113,012; probe 57→51) — the paper's figures reflect an earlier citation state (2026-06-24 LS-correction commits), not a delimiter change. Regenerated `sense_order_metrics.{json,md}`. **Added the chance baseline the paper lacked** (`analyze_sense_order_null.py` → `sense_order_null.{json,md}`): sense-1-oldest floor **52.7%** → obs 73.5% → ceil 100% = **44% of span**; τ floor 0.00 → 0.375 → 1.0 = 38% — the calibrated form of "genetic, not historical". **Article prose NOT touched.** **Queued A33 paper edits:** update N to 11,882 (+ 28,280, 113,012, probe 51/72.5%); fix §5 Reproducibility (both scripts now `[)〉]`; pin the source snapshot); add the floor baseline to §3.1; author-scholarly related-work + venue remain. Delivered via worktree off origin/master (WIP on `heritage-h111...` untouched).
- [x] **Heritage/INRIA reuse — H099 Phases 0–2 DONE (2026-07-03, Sonnet 5
      `claude-sonnet-5`).** Mirror sparse-cloned (gitignored,
      `HeadwordLists/heritage_mirror/`); inventory
      [HERITAGE_MIRROR_INVENTORY.md](HeadwordLists/HERITAGE_MIRROR_INVENTORY.md);
      current-vs-2014 stem delta (38,343 vs 21,055 keys, scope caveat: full dict vs
      reader selection) in
      [Huet-INRIA-Wordlist-vs-Cologne.md §6](HeadwordLists/Huet-INRIA-Wordlist-vs-Cologne.md);
      187 unvetted DCS-no-CDSL candidates parked in
      [COVERAGE_ADDITIONS.md](HeadwordLists/COVERAGE_ADDITIONS.md); entry-level
      MW↔Heritage crosswalk (185,803 entries, 25,140 covered, 97.6% anchor-resolved) →
      [mw_heritage_crosswalk.tsv](HeadwordLists/mw_heritage_crosswalk.tsv) /
      [.md](HeadwordLists/mw_heritage_crosswalk.md); Huet outreach drafted + parked,
      **not sent**
      ([OUTREACH_2026-07-03_gerard_huet_heritage.md](https://github.com/gasyoun/Uprava/blob/main/handoffs/OUTREACH_2026-07-03_gerard_huet_heritage.md)).
      New: [FINDINGS.md §49](FINDINGS.md). Roadmap
      [HERITAGE_INRIA_ROADMAP.md](HERITAGE_INRIA_ROADMAP.md) phase table updated with
      outcomes; Phases 3–6 remain queued, each minting its own H### when its gate
      clears. Delivered via branch `heritage-inria-phase0-2` + PR (see commit log).
      ~~Human gates~~ ✅ ALL THREE CLOSED 03-07-2026 (was: LGPLLR×BY-SA @DECIDE, manual
      morphology-XML download @DO, review+send the Huet draft @DO) — Huet outreach SENT
      and APPROVED same day (composition OK; see GTD + FINDINGS §51), morphology XML
      downloaded + staged at `HeadwordLists/heritage_mirror/manual/`. Do not re-open.
      _(Refreshed by the 03-07 /gtd-sync hygiene sweep.)_

- [x] **Heritage/INRIA reuse — H105 Phase 4 forms-oracle DONE (2026-07-03, Opus 4.8
      `claude-opus-4-8`).** Diffed Heritage `SL_morph.xml` v3.81 (1,022,526 forms) vs
      kosha's DCS+vidyut layer (409,978 forms) on the SLP1 form key. **94,264 overlap,
      78.3% agree.** The small raw overlap is engine-surplus (928k Heritage-only paradigm
      forms) + transcription convention (anusvara `M`↔nasal recovers 18,636; 8,264 DCS
      avagraha artifacts), and 66% of the 20,496 disagreements are root↔derived-stem
      lemmatization policy, not error (40-row hand-adjudication; genuine one-sided
      divergences ≈ low-single-digit %, on both sides — DCS mis-tags AND Heritage stem
      oddities). Deliverables: [heritage_forms_oracle.md](HeadwordLists/heritage_forms_oracle.md)
      + `.tsv.gz` (94k rows) + `_disagreements.tsv` (20,496) + `.py`;
      [FINDINGS.md §52](FINDINGS.md); roadmap Phase 4 → ✅. **kosha-ingest of the 928k
      Heritage surplus forms is a NEW GTD @DECIDE — not built** (comparison only, per
      handoff). Delivered via branch `heritage-inria-phase4-forms-oracle` + PR (stacked
      on #96).

- [x] **H111 — kosha ingest of the 928k Heritage surplus forms DONE (2026-07-03,
      Sonnet 5 `claude-sonnet-5`).** MG ruled the H105 @DECIDE: ingest as a
      LOWEST-trust third witness, additive-only. This repo's side:
      [heritage_forms_oracle.py](HeadwordLists/heritage_forms_oracle.py) extended
      to retain stem+category for heritage-only forms and emit
      [heritage_only_forms.tsv](HeadwordLists/heritage_only_forms.tsv) (992,194
      rows) as the sole cross-repo ingest feed. Branch
      `heritage-h111-kosha-forms-feed` →
      [PR #100](https://github.com/gasyoun/SanskritLexicography/pull/100)
      (awaiting MG merge). kosha side (nullable `forms.category`,
      `build_forms.py` heritage load, trust-ordering docs) →
      [kosha PR #7](https://github.com/gasyoun/kosha/pull/7) — verified `dcs`
      397,843 / `vidyut` 28,567 unchanged, `heritage` +951,991 rows. The 6,966-row
      nominal disagreement pool (DCS-correction candidates) is out of scope,
      flagged for a future handoff.

- [x] **H106 — Heritage/INRIA reuse Phase 5 DICO gloss layer DONE (2026-07-03,
      Sonnet 5 `claude-sonnet-5`).** Extracted the French gloss prose for all
      24,549 crosswalk-resolved Heritage-covered MW entries from the local
      `HeadwordLists/heritage_mirror/DICO/` mirror →
      [heritage_dico_gloss.tsv](HeadwordLists/heritage_dico_gloss.tsv) (24,549
      rows, 100% of resolved crosswalk anchors, 99.98% join kosha's
      `lemmas.slp1`). Non-obvious defect fixed mid-session: DICO's named
      anchors nest three structural roles under one CSS class (fresh
      headword / compound sub-entry / inline cross-reference embedded
      mid-prose) — a naive per-anchor split truncated entries like `aṃśa`
      mid-sentence, and the first fix over-merged compound sub-entries back
      into their parent; the working extractor
      ([heritage_dico_gloss_extract.py](HeadwordLists/heritage_dico_gloss_extract.py))
      distinguishes them by preceding-tag context. 25-row hand-verified
      (shortest/longest/random), zero truncation/bleed remaining. Report:
      [heritage_dico_gloss.md](HeadwordLists/heritage_dico_gloss.md); already
      landed as [FINDINGS.md §56](FINDINGS.md) (renumbered from §54 by a
      concurrent §54 triple-collision fix); roadmap Phase 5 → ✅. **Standalone
      witness layer only — not merged into any canonical kosha field.**
      kosha UI-surfacing is a GTD @DECIDE, not built. Recovered via
      `/branch-contention-recover` (source-only re-apply off `origin/master`,
      original PR #102 CONFLICTING from base drift after #108/#112 landed;
      FINDINGS.md §54 entry already present on master under §56, not
      re-applied to avoid a duplicate).

- [ ] **VedaWeb 2.0 reuse programme — H096 re-attempted 03-07-2026, STILL blocked, outage
      persists hours later (Sonnet 5 `claude-sonnet-5`).** Roadmap:
      [ROADMAP_VEDAWEB_REUSE.md](https://github.com/gasyoun/SanskritLexicography/blob/master/ROADMAP_VEDAWEB_REUSE.md).
      Confirmed the export mechanism ([FINDINGS.md §48](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md)):
      `GET /resources/{id}/export` returns `202`+`pickupKey` (async task, no auth needed
      despite the OpenAPI spec), download via `GET /platform/tasks/download?pickupKey=…`.
      First attempt triggered the Casaretto export task, then `vedaweb.uni-koeln.de`
      degraded to `504`/timeout on every endpoint. Re-probed same day, hours later: now a
      full HTTP-layer hang (TCP+TLS connect fine, zero bytes back, 15–25s timeouts ×3) —
      `http://` root still 301s to the dead `https://` host. General internet +
      `uni-koeln.de` root both `200` in the same window, confirming the outage is isolated
      to the `vedaweb` subdomain/app and is server-side, not local. Nothing downloaded,
      nothing committed. H096 registry row stays 🔵 (attempted, not done) in
      [handoffs/README.md](https://github.com/gasyoun/Uprava/blob/main/handoffs/README.md).
      Next: `Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H096_vedaweb_feed_export.md and execute it.`
      (Sonnet-tier chat in `GitHub\VisualDCS`; do a cheap liveness check first —
      `curl -sI --max-time 15 https://vedaweb.uni-koeln.de/api/openapi.json` — before
      re-running the full mission; retry the whole export flow fresh, do not reuse the
      stale pickup key.) H097/H098 remain gated on H096; H063 unaffected.

- [x] **pwg_ru Stage C stale-input hold cleared structurally (2026-06-29).**
      In `RussianTranslation`, ran the handoff-required
      `root_window_status.py --prune-stale` pass for `gam`, `yuj`, `vid`, and
      `han`. All four now report PASS with 0 stale raw/portrait inputs
      (`gam` 127, `yuj` 60, `vid` 55, `han` 78 sub-cards). Next operator move:
      generate the optimized `gam` harness, run Max Workflow, save
      `wf_output.json`, and audit with `audit_window.py --root gam --write-requeue`.
      The F-gate-nws-fp fix and optional Opus retranslate of true
      `untranslated_braced_german_gloss` cards remain non-blocking follow-ups.

- [x] **pwg_ru fast print/DH acceleration review shipped (2026-06-29).**
      Added `RussianTranslation/PRINT_DH_ACCELERATION_REVIEW.md` and
      `RussianTranslation/roadmap/print_dh_acceleration_review.json`. The new
      review turns the recurring "what should we review next?" question into a
      ranked queue: fresh `sTA`, deterministic audit, sampled semantic review,
      G5/G6/G7 evidence, then print/DH renderer and front-matter decisions.
      Follow-up: `RussianTranslation/NEXT_REVIEW_PACKET.md` is the executable
      checklist for fresh `sTA` audit plus print feasibility from real examples.
      Follow-up v2: locked the no-10-root-blast Max policy; staged evidence run
      is `sTA`, then `BU`/`as`/`i`, then prune/recheck `gam`/`yuj`/`vid`/`han`.
      Follow-up v3: `RussianTranslation/HANDOFF_2026-06-29_claude_code_max.md`
      is the current Claude Code Max handoff for Stage A/B/C execution and
      audit artifacts to bring back.

- [x] **pwg_ru real print-entry examples review shipped (2026-06-28).**
      Added `RussianTranslation/PRINT_ENTRY_EXAMPLES.md` and
      `RussianTranslation/roadmap/print_entry_examples_review.json`, testing
      `PRINT_ENTRY_SPEC.md` against real local `agni`, `akzara`, and `ap`
      merged-card prototypes. The examples are explicitly layout/QA evidence,
      not accepted publication text; fresh `sTA` still precedes `BU`, and
      print remains blocked by G5/G6/G7/G10.

- [x] **pwg_ru human-review minimization + print-entry spec shipped (2026-06-28).**
      Added `RussianTranslation/HUMAN_REVIEW_MINIMIZATION.md`,
      `RussianTranslation/PRINT_ENTRY_SPEC.md`, and
      `RussianTranslation/roadmap/print_review_minimum.json`. G5/G6/G7 now have
      exact reviewer files, editable columns, validation commands, and done
      criteria; the print spec locks the target bilingual entry shape before
      renderer work.

- [x] **pwg_ru print/DH/speed readiness review pack shipped (2026-06-28).**
      Added `RussianTranslation/PRINT_DH_SPEED_REVIEW.md` plus
      `RussianTranslation/roadmap/print_dh_speed_review.json`. Verdict:
      engine is ready to continue bulk translation window-by-window after fresh
      `sTA` Max output, but not ready for reviewed core tranche, immutable
      digital edition, printed bilingual dictionary, or full PWG tail. P0s:
      fresh `sTA` output, G5/G6/G7 human gates, zero print-ready rows, and stale
      semantic-risk evidence.

- [x] **pwg_ru quick deterministic audit pack shipped (2026-06-28).** Added
      `RussianTranslation/src/pilot/prompt_rule_audit.py` to scan the committed
      template and generated optimized harness for manual-derived prompt rules
      without Max/LLM spend; added ignored `prompt_rule_audit.{json,md}` reports,
      cheap semantic-risk fixtures in `window_selftest.py`, explicit
      non-circular/cryptographic gloss guidance in `run_pilot_wf.js`, and docs
      clarifying that semantic judging remains deterministic-first locally with
      `judge_sample.keys.txt` as the later spend queue.
      Follow-up v2: `prompt_rule_audit.py --cards wf_output.json` now performs
      direct translated-card semantic triage, reports `card_risks`, and keeps
      `--fail-on-risk` opt-in so routine operator flow stays advisory.
      Follow-up v3: card-risk reports now include a ranked `review_queue`,
      formula inventory, markup/sigla leakage and sense-compression signals,
      `--review-limit`, and opt-in `--fail-on-high-risk`.
      Follow-up v4: `audit_window.py` now runs this as the canonical
      `prompt_semantic` gate, blocks only on missing manual-rule wiring, surfaces
      live manual coverage in audit/status reports, and lets only high-confidence
      mechanical semantic risks feed requeue while noisy evidence heuristics stay
      review hints.

- [x] **pwg_ru architecture hardening pass shipped (2026-06-28).** Split
      `RussianTranslation/src/pilot/audit_window.py` helper logic into importable workflow
      payload, root/input provenance, and report/status modules; added
      `window_selftest.py` for stale-refusal/requeue-preservation, optimized-harness scope/tools,
      nested payload parsing, and release-manifest hash validation; refreshed
      `PIPELINE_ARCHITECTURE.md` and marked `run_pilot_wf.js` as template-only.

- [x] **RussianTranslation repo-local AGENTS.md compiled (2026-06-28).** Added
      `RussianTranslation/AGENTS.md` with local operating rules for the PWG→Russian
      Max workflow, deterministic audit/requeue boundaries, generated artifact
      policy, translation/markup invariants, and session-state maintenance.
      Follow-up documented the immediate next operator action: rerun fresh `sTA`
      Max Workflow output, audit it, then targeted requeue or sampled semantic
      judging before advancing to `BU`.

- [x] **pwg_ru audit guardrails hardened (2026-06-28).** `RussianTranslation/src/nws_split.py`
      now resolves root-split `~~` sub-card stems literally before safe-name fallbacks and
      returns distinct `NO-RAW`/`NO-CARD`/`NO-NWS` verdicts; `audit_window.py` treats missing
      raw/card as requeue while leaving `NO-NWS` neutral and quarantining only true
      misattribution. `root_window_status.py` now compares optimized-harness `selected_keys`
      with the intended full/pending key scope before telling the operator to run Max. Verified
      with `nws_split.py selftest`, harness-scope assertions, compile checks, `root_window_status.py sTA`,
      stale `audit_window.py` refusal, `verify_root_glue.py`, and `node --check` on both workflow scripts.

- [x] **pwg_ru operator docs refreshed (2026-06-28).** Added
      `RussianTranslation/USE_CASES.md` with command-oriented flows for preflight, fresh Max
      windows, stale-output recovery, targeted requeue, sampled semantic judging, dashboard
      monitoring, release readiness, and corpus API retry. Updated `RussianTranslation/README.md`
      with guardrails + use-case links and `RussianTranslation/CHANGELOG.md` with the
      2026-06-28 audit/docs entry.
      Follow-up: fixed stale-artifact next-action wording to respect an already-current
      optimized harness, added normal `argparse` help/`--limit` to the portrait schema
      validator, and clarified that release validation targets immutable edition
      subdirectories such as `release\edition_v1`.
      Second follow-up: aligned stale-output runbook/use-case text with `root_window_status.py`,
      refreshed `JUDGE_POLICY.md` around `judge_sample.keys.txt`, and removed stale local
      working-tree/TODO claims from `RussianTranslation/.ai_state.md`.
      Third follow-up: added `gold_validate.py gold/_human_gold_review.csv` to the reviewer
      handoff so G6 completion has an explicit validation command after human labels are filled.
      Fourth follow-up: converted assembled-export and final-card schema validators to
      `argparse` so their `-h/--help` paths work while preserving fixture/default validation.
      Fifth follow-up: improved `validate_release.py release` diagnostics so staging-directory
      misuse points operators to an immutable edition subdirectory.

- [x] **pwg_ru production hardening landed (2026-06-27).** RussianTranslation now has
      provenance-bearing optimized Max harnesses (`meta` with root/mode/selected keys/rootmap
      + raw/portrait SHA-256), enforced translate-agent `tools: []`, stale-artifact fail-fast in
      `src/pilot/audit_window.py` before collect/gates/glue, append-only
      `window_ledger.jsonl`, root preflight hashes, and a print-gate snapshot dashboard
      (`release/gate_status_snapshot.{json,md}`). Verified against the stale 106-card `sTA`
      workflow (correctly refused against the 123-card rootmap) and a one-card matching fixture
      (normal collect + free gates + glue path).

- [x] **pwg_ru live dashboard shipped locally (2026-06-27).** `RussianTranslation/src/pilot/dashboard_server.py`
      serves `http://127.0.0.1:8765/` with a 5-second browser refresh, combining Max-run status,
      audit/print-gate events, ledger entries, file freshness, and G5/G6/G7/G10 readiness. The
      append-only event log is `src/pilot/output/dashboard_events.jsonl`; event logging is
      best-effort and never advances human review labels.

- [x] **pwg_ru bulk-throughput audit sampling landed (2026-06-28).** `RussianTranslation/src/pilot/audit_window.py`
      now writes `src/pilot/output/judge_sample.keys.txt`: all Python-gate failures plus a
      deterministic 10% clean-key sample (minimum 5 where available), with sample seed/counts
      recorded in `audit_window.report.*`, `window_status.*`, and `window_ledger.jsonl`. The
      mechanical requeue list stays separate in `requeue.keys.txt`. `RUN_FREQ_MAX.md` now states
      the decision-complete loop: root preflight → optimized harness → Max Workflow → audit →
      requeue-only rerun or sampled semantic judging. Follow-up in the same hardening pass:
      audit accepts Max wall-clock/token/cap fields and writes them plus a machine-readable
      `next_action` into status/ledger outputs.

- [x] **pwg_ru productivity review hardening landed (2026-06-28).** Added shared pilot helpers
      in `RussianTranslation/src/pilot/window_common.py`, refactored `gen_opt_harness.py` to
      explicit validation + importable `main()`, taught `root_window_status.py` to print one
      next action and exact next command, exposed `judge_sample.keys.txt`/production metrics/
      next action in the local dashboard, and marked `run_real_test.py` legacy/manual-test-only.
      Verified with compile checks, `root_window_status.py sTA`, `gen_opt_harness.py sTA`,
      strict stale-audit refusal, `node --check`, and a live `/api/status` dashboard smoke.
      Follow-up fix: root status now parses optimized harness provenance and, when the stale
      audit status is older than a matching generated harness, tells the operator to run Max
      next rather than regenerating forever; queue read failures are reported separately from
      queue keys.
      Review-fix follow-up: G10 print readiness now also checks concrete local G5/G6/G7
      summaries, and `audit_window.py` preserves provenance when Workflow output wraps the
      `{meta, results}` object.
      Edition-readiness consistency follow-up: G10 no longer appears in its own `blocked_by`;
      preflight and release-readiness reports now agree that G10 waits on upstream G5/G6/G7
      plus non-G10 roadmap blockers.
      Audit guardrail follow-up: stale `audit_window.py --write-requeue` refusals preserve
      existing `requeue.keys.txt`; print-readiness scripts stay report-only by default and
      add `--fail-on-blocked` for CI/go-no-go use.
      Report-clarity follow-up: stale audit Markdown now says the mechanical requeue was
      not generated rather than empty, and JSON records whether `requeue.keys.txt` was
      written or preserved.
      Workflow guardrail follow-up: `_pilot_collect.py` now refuses to write `.merged.md`
      files for null workflow cards, `root_window_status.py` refuses partial-window Max runs
      when the optimized harness points at a different root/rootmap, and
      `root_window_status.py --prune-stale` cleaned BU generated inputs to 59/59 raw+portrait
      with 0 stale files.

- [x] **pwg_ru Max pipeline prep refreshed (2026-06-26).** Ran local preflight only:
      `python freq_route.py 8`, `python _pilot_gen_merged.py --manifest freq --root-split --limit 3`,
      and `python verify_root_glue.py` from `RussianTranslation/src`. Results:
      43,968/106,082 DCS-attested in `scale_manifest.freq.json`; top queue
      `sTA/BU/gam/yuj/as/i/vid/han`; top 3 already root-split; split/glue audit
      ALL GATES PASS across 60 rootmaps. Added `RussianTranslation/src/pilot/RUN_FREQ_MAX.md`
      and corrected the stale handoff judge-pass note. No provider/Max run attempted.

- [x] **FREQ QUEUE RUN at volume + multi-homonym root-split fixed (2026-06-24).** — _status: DONE; source: current-session._
      Ran `_pilot_gen_merged.py --manifest freq --root-split` over the top of the DCS-frequency
      queue: **40 of the top 50 are giant roots → 2,316 sub-cards**, none overflow. Two fixes
      that unblocked the volume run: (1) **resumability composition** — `is_done`/`is_giant` so a
      giant root with only a *stale whole-card* input is still re-split (else `is_merged` masked
      it → 0 generated); split now also **removes the superseded whole-card** so the translator
      never sees both. (2) **multi-homonym bug** (hit **19 of top 50**): `gen_root_split` only
      segmented `bufs[0]`, so a giant verb root at a non-zero homonym index (√i at hom 2 = 114
      prefixes, mā/As/vā/iṣ) was **missed entirely** (→ giant whole card overflow) and extra giant
      homonyms (gam/as/dā) were **dropped**. Fixed: now segments EVERY homonym, splits each giant
      one, keeps small ones whole, attaches PW/SCH/PWKVN/NWS supplements ONCE; rootmap gains a
      `hom` field; `root_glue_translated.py` orders by (hom→seg→part) with homonym dividers +
      supplements last; secondary (caus/desid) stems preserved + nested with the simple verb.
      **Apresjan evidence on sub-cards (interim, was the gap):** `subcard_portrait` writes real
      `corpus_synonyms` per sub-card — head/secondary→root (`man`→считать), prefix→prefixed
      SURFACE form (`anu+man`→одобрять, `ava+man`→смотреть свысока), `evidence_scope` flags
      `prefixed-form` vs `root-fallback` (weak hint; prompt defers to the German gloss). Residual:
      sandhi/stacked prefixes need `pwg_preverb1.txt` join (proper fix). **Max plumbing wired:**
      `run_pilot_wf.js`(`fileOf`)+`_pilot_collect.py` keep `~~` sub-card stems verbatim →
      `<subkey>.merged.md` → glue. **38-unit freq test prepped** for Max:
      `src/pilot/FREQ_TEST_RUNBOOK.md` (8 nouns + giant `man`). **AUDITED ALL-GREEN**
      (`src/audit_root_split.py` + maintainer's `src/verify_root_glue.py`): corpus-wide lossless
      PASS (1226 rec, 0 fail); 60/60 top giants lossless·all-homonyms-split·glue-complete·portraits
      (3035 sub-cards); whole-card regression OK; csl-orig untouched.
      **38-UNIT TEST RUN + AUDITED (2026-06-24, this session):** translated all 38 via Workflow
      (38 Sonnet agents, 10.5 min, 1.61 M tok) → glued `man` 30/30 → `man.NESTED.md` (publishable
      quality; Apresjan register-judgment validated live on `ava`). Fidelity gate
      `src/audit_translation.py` = **38/38 clean** (<ls>≥90 %, {#..#}≥85 %, RU present); semantic
      spot-check (3 fact-check agents) anu/nara/ava PASS, 2 trivial nits (`ava` ein→один gloss
      prose; `nara` Hoernle NWS multi-cite compression = guard-4 follow-up).
      **OPUS JUDGE DONE (this session):** sev **{1:24, 2:13, 3:1} → 37/38 publishable (≤2)**, disc
      "good" everywhere. 1 sev-3 = `idam` translator-swapped NWS owner rows (Geldner↔Graßmann; map is
      CORRECT → translator slip the production `nws_split.py check` catches). sev-2 nits: `kArya`
      dropped 2 Nachträge patches; `jana`/`pw00` minor sigla (token merge; `Bed.`→«значением»).
      **Item-2 (sandhi-join) DROPPED as FUTILE** — only 3/15 prefixed forms corpus-attested
      regardless of spelling; limit is corpus coverage, interim already optimal.
      **NEXT = (1) prompt-tuning pass for the scale run (keep German abbrevs verbatim; render ALL
      Nachträge; `<is>`→siglum not `{%..%}`) + wire `nws_split.py check` into the run loop (catches
      idam-F12); (2) head-splitter for large non-giant nouns IF overflow confirmed (4/64 top freq
      >400 lines: kAla 530, ka 522, SrI 412, para 401 — overflow at ~520 untested); (3) scale the
      freq run on Max.**
- [x] **Sense-ordering study EXECUTED + quantified + folded in (2026-06-24).** — _status: DONE; source: current-session._
      Findings: European dicts (PWG/PW/MW/GRA) = **etymological-genetic**, not historical —
      PWG sense-1=oldest **73.5 %**, Kendall τ=0.375, Vedic-density PWG 23.4 % ≈ MW 24.8 % ≫
      AP90 2.3 % ≫ Koch 0 %; Apte/Kochergina = logical-semantic. Tooling reuses
      `src/renou.py`+`ls_source_map.json` (72.4 % citation coverage, validated). Deliverables:
      `research/analyze_sense_order.py` + `analyze_cross_dict.py` + `sense_order_metrics.md` +
      `cross_dict_metrics.md`; DH note **A33** (`papers/A33_sense_ordering_note.md`, 3/5, in
      Uprava/ARTICLES). Folded into production: `pwg_ru_prompts/6_merged_translate.md` gained
      an explicit **sense-order policy** (keep PWG order, NEVER re-sort by freq/date; «страт»
      badge = display metadata) + `src/sense_stratum.py` deterministic per-sense stratum feed
      (23,461 hw / 64,296 senses). **DECISION for pwg_ru: preserve PWG order, Renou = badge.**
- [x] **freq_route.py fixed + verified; root segmenter/glue verified (2026-06-24).** — _status: DONE; source: current-session._
      `freq_route.py` already existed but broke on `dcs_lemma_renou.json` format drift
      (`__sources__` meta key) → fixed (skip non-dict values); runs: 106,083 PWG hw, 44,135
      DCS-attested (42 %) → `scale_manifest.freq.json`; top of queue = giant roots
      sthā/bhū/gam. `research/root_segment_proto.py` + `root_glue.py` verified LOSSLESS /
      content-preserving on bhū+gam (PWG) and MW.
- [x] **Root-entry segmenter SUITE built + scale-proven; external resources wired (2026-06-24).** — _status: DONE; source: current-session._
      Per-build, one at a time (all in `research/`, see `ROOT_ENTRY_ARCHITECTURE.md` §BUILDS A–C):
      **#1** `src/root_units.py` segments a root mega-record into per-prefix translation units in
      the `compile_translatable` manifest shape (BU→380 units). **#2** `root_merge.py` = PWG↔MW
      merged comparative article (bhū 33/41 prefixes aligned). **#3** hardened `lex_noun_link.py`
      (30.9→34.8 %), folded `mw_deriv.py link` (MW link 133.7k rows from Funderburk's analysis),
      `scale_test.py` = **all 1,163 PWG verb roots segment 100 % LOSSLESS** (+227 FP-gap divisions,
      159 roots need verbs01's guard). Reuses Funderburk **verbs01** (don't rebuild sandhi/MW-align).
      **Resources (provided by user, vendored gitignored in `research/external/`):**
      (a) `funderburkjim/MWderivations` step4/analysis2.txt (220k MW headwords, method col classifies
      pfx/cpd/wsfx) — solves the MW derivation/compound side upstream (`mw_deriv.py`); its
      compounds.txt is byte-identical to `WhitneyRoots/MW_compounds_12610.txt`.
      (b) `indic-dict apte-hi.babylon` Apte Sanskrit-Hindi — `apte_parse.py` → independent root oracle
      (1,654 dhātus, **793 not in verbs01**), tracked `research/apte_roots.tsv`.
- [x] **UNBLOCKER DONE: `--root-split` hook wired into `_pilot_gen_merged.py` (2026-06-24).** — _status: DONE; source: current-session._
      `python _pilot_gen_merged.py --root-split <keys>` (or `--manifest freq --root-split`) detects a
      GIANT root (≥`ROOT_SPLIT_MIN`=8 prefix `<div n="p">` divisions, via `research/root_segment_proto`)
      and explodes it into one single-pass-sized sub-card per prefix: writes `<safe>~~NN[_upa].raw.txt`
      (HEAD seg-0 = simple verb + ALL supplements PW/SCH/PWKVN/NWS + owner map; each PREFIX seg = its
      `<div n="p">` block, root_key/upasarga-linked) + `<safe>.rootmap.json` for `root_glue` reassembly.
      Verified: `BU`→41 sub-cards (head 820 lines + supplements + 93-entry owner map; `anu` prefix card
      87 lines vs the 1315-line whole record), `gam`→63. Sub-cards land in gitignored `pilot/input/`.
- [x] **glue-after-translate BUILT + demoed on a small sample (2026-06-24).** — _status: DONE; source: current-session._
      `src/root_glue_translated.py BU` reads `<safe>.rootmap.json` + each sub-card's translated
      `pilot/output/<subkey>.merged.md`, orders by seg_index (head → prefixes), emits one
      `<safe>.NESTED.md` Russian article (missing sub-cards → pending placeholders). Sample:
      translated 3 BU prefix sub-cards (anu/aBi/ud) via Agent-Sonnet (German {%..%}→RU, kept
      {#..#}/sigla verbatim) → glued into the 41-sub-card NESTED article, 3 real + 38 pending,
      each in its correct slot. Confirms SPLIT→translate→GLUE round-trips. (outputs gitignored.)
- [x] **HEAD-CARD SENSE-SPLITTER built (2026-06-24) — gating decision resolved.** — _status: DONE; source: current-session._
      Validation revealed the 820-line `bhū` HEAD overflowed a single 32k-token pass (wrote NOTHING) →
      NOT 5/5 → built the splitter per the rule. `_pilot_gen_merged.py` `build_head_parts` +
      `chunk_lines`/`chunk_records` now split the head into single-pass parts: simple-verb senses chunked
      at `<div`/blank boundaries (budget 100, hard cap 1.5×), each supplement layer (PW/SCH/PWKVN)
      chunked, NWS owner map batched (25/unit); PREFIX sub-cards likewise chunked. `BU` → **56 sub-cards
      (14 head-parts + 40 prefix), every one ≤143 lines** (vs the 820 that died; ≈ the ~90-line prefix
      cards that translated cleanly). `gam` → 81. PWG side stays lossless (head pwg-chunks + prefix-chunks
      = a pure sub-partition of `RS.segment`). `root_glue_translated.py` orders by (seg_index, part) and
      labels head parts (senses part N / PW / SCH / PWKVN / NWS batch). Budgets tunable via
      `HEAD_SPLIT_BUDGET`/`NWS_OWNER_BATCH` env.
      **NEXT = (1) run the freq queue with `--root-split` at volume (top = sthā/bhū/gam) — now nothing
      overflows; (2) per-gloss keep-list alignment; (3) Apte roles b/c/d; (4) teaching: poster + quiz from
      affix_pedagogy.json + wire affix_explorer into the WhitneyRoots reader.**

- [ ] **Root-entry architecture DECIDED + 3 lexicography handoff researches created (2026-06-23).** — _status: SPAWNED as 3 handoff chats (task_740ea467 root-arch / task_2242dc13 sense-order / task_9b9ce8db microstructure); source: current-session._
      Triggered by the giant-root single-pass failure. DECISION (user): **two modes** — translate
      sopasarga dhātus as SEPARATE cards (like MW; fixes size) but keep a `root_key`/`upasarga`
      link so a `root_glue.py` can re-collect them into one NESTED root article on demand. Documented
      in `RussianTranslation/research/ROOT_ENTRY_ARCHITECTURE.md` (+ segmenter/glue build sketch).
      THREE handoff briefs in `RussianTranslation/research/` (each: question · dicts · sources ·
      method · output table · priors): (A) root architecture per dict, (B) sense ordering (read
      prefaces; hypothesis = 19th-c. European dicts historical, AP non-historical/logical, no print
      frequency dict exists), (C) microstructure conventions (homonym/gloss/citation/run-on per dict).
      Sources: OCRed prefaces (PWG/PW/MW/GRA/SCH) + `csl-orig/v02/<dict>/<dict>.txt`. Each runs as its own cold chat → appends a ## RESULTS table to its brief + commits. THEN fold findings → fold findings into the
      merged-translate prompt + build the root segmenter/glue BEFORE the scale-up.


- [ ] **Renou — what's left (handoff, 2026-06-24).** — _status: system complete; 3 items remain; source: current-session._
      DONE: 4 provenance signals (`ls`/`dcs`/`bhs`/`wl`) × 8 LS-rich dicts × **770,292 entries**,
      consolidated (`renou_pipeline.py --all` → `{code}.renou.jsonl`; `RENOU.md`), editorial badge
      (`renou_portrait.py`). The corpus + register sides are saturated. **Accuracy now VALIDATED**
      (2026-06-24): `renou_audit.py` inter-signal agreement audit + DCS over-tag **min-support fix**
      (per-state `state_support` + `renou.filter_dcs_states`, default ≥2 texts / ≥1 typed text) —
      pruned 9.9 % of `dcs` states (spurious IV/I; 0 II/V), shipped `ecc7bb9`+`9666591`. See the
      Completed entry below. **Renou *register* (subsection) axis ADDED 2026-06-25** — orthogonal
      multi-label `renou_register` (20-code lattice: épigraphique, bhāṣya, kāvya, …) from corpus +
      `<ls>` + dedicated épig detector; all phases done except a human bhāṣya precision check; see
      the Completed entry. **Remaining (state-badge):**
      (a) **@WAITING** — wire the Renou *badge* into the LIVE per-sense store: blocked on the PWG
      per-sense translated store (translation mid-pipeline, freq-first queue) and the MW Russian
      cards (separate working repo); join the canonical `{code}.renou.jsonl` by `key1`. This is a
      data-availability wait, not a build task.
      (b) ✅ **DONE 2026-06-24** — reviewed + squash-merged **SamudraManthanam PR #15** (the
      wisdomlib `definitions.py` fetcher; 6/6 CI green, selftest OK; ships the authoritative
      63-word `word_traditions.jsonl`). Also DONE: `renou_portrait` **low-information flag**
      (`renou_low_info=True` on an all-era span — `ca`/`idam`/`akāra`; shipped `b333454`).
      (c) **@DO** — a full wisdomlib **residential crawl** for broad `wl` coverage (only a 63-word
      sample exists; Cloudflare-gates this IP — needs a clear residential connection / the
      self-hosted runner, run gently). Low marginal value (BHS transfer already covers V), so
      optional. All three logged in `Uprava/GTD_NEXT_ACTIONS.md`.

- [ ] **FREQUENCY-FIRST ordering pivot (2026-06-23) — build `freq_route.py`.** — _status: decided, build next; date: 2026-06-23; source: current-session._
      User: translate in DCS-frequency order, not alphabetical. Decided design (AskUserQuestion):
      **hybrid token × breadth × richness**, **top ~2–5k DCS-attested core first**, then reassess.
      Data on hand: `../VisualDCS/dcs_lemma_summary.json` (freqBand 1–5, 83,239 lemmas, Hellwig
      DCS ~2021), exact counts in `../VisualDCS/src/DCS-data-2026/dcs_full.sqlite`, breadth =
      `n_texts` in `src/dcs_lemma_renou.json`; DCS→PWG join already exists (`build_dcs_renou.py`).
      BUILD: `freq_route.py` → composite score (log token × log breadth × log richness) over PWG
      headwords → `pilot/output/scale_manifest.freq.json` (ranked). Then the SAME chain runs on it:
      `_pilot_gen_merged.py --manifest freq` → owner-map → gated translate→audit. Only the order
      changes. ⚠️ richness for all ~106k needs a cheap proxy (PWG record sense-count/byte-size) —
      don't require full portraits up front. Roadmap updated: REVIEW_AND_ROADMAP.md §4 banner + §5.
      The a-section stays the validated gold/proof slice.

- [ ] **a-section translation on Max + the decisive cost experiment.** — _status: staged; date: 2026-06-23; source: current-session._
      Bulk handed to the Max workflow (see WIP). Run the prepped window 1 (`prep 50 0`)
      via `run_pilot_wf.js` on Max → save `wf_output.json` → `run_real_test.py audit`.
      **Instrument it**: record tokens + wall-clock, and keep running windows until the
      weekly cap fires, noting the cumulative-token number — that single run yields the
      per-card cost, harness throughput, AND the Max weekly quota together, the only
      thing blocking a clean "2-months-feasible? yes/no" for the whole dict. Analysis +
      data-gaps table = `RussianTranslation/PILOT_COST.md` §6 (a-section ~0.5–0.8 B tokens;
      whole PWG dict ~4–7 B; quota-bound to ~1–2 months on one Max seat; DeepSeek-API
      bulk ≈ $1.5–4 k as the 2-week alternative). **Open decision (human): vehicle for
      the bulk** — Max (quota-bound) vs DeepSeek API (fast, ~$1.5–4 k) vs judge-sampling
      to halve it. Cheap pre-measurements doable now w/o Max: typical small-card token
      cost (~10 small cards) + whole-dict total input bytes.

- [x] **NWS section audit m–z — COMPLETE (whole SLP1 universe).** — _status: complete; date: 2026-06-23; source: current-session._
      Ran `nws_audit_section.py` for m–z. Final Σ over **all of a–z**: 93,927 keys
      → 23,886 NWS-bearing → 61,072 entries; 5 bleeds (cosmetic, 0 residual
      everywhere); **114 real losses (0.187%)**, all accepted known limitations or
      1 source typo; **0 parser bugs**. Two NEW known-limitation classes documented
      (multi-page cite `: N, M`; `(pw)` lowercase parenthetical source name) — both
      no-code-change, consistent with the single-token-page / capital-initial-name
      design. Verified exhaustive: the only keys outside a–z are 10 non-SLP1 index
      artifacts (mojibake/junk) with 0 NWS fragments (coverage note in the report).
      Commits through `9548efe`; pushed.
- [ ] **Send the consolidated German erratum email to the Halle NWS team.** — _status: drafted; date: 2026-06-23; source: current-session._
      DRAFT WRITTEN: `RussianTranslation/NWS_ERRATUM_EMAIL.md` (German body +
      English context). SCOPE EXPANDED: a structural delimiter scan of the full
      NWS corpus (`src/_nws_defect_scan.py` / `_nws_defect_list.py`) found **70**
      entries with unbalanced parens/brackets — listed in
      `RussianTranslation/NWS_SOURCE_DEFECTS.md` — not just the 1 owner-breaking
      `vṛtrakhādá`. Most are a dropped closing `)` mid-gloss (invisible to
      attribution), inherited from condensing MW parentheticals (high confidence:
      excess-OPEN parens can't come from the `1) 2)` enumeration convention).
      Recipients = Hanneder & Demoto-Hahn (Marburg), Slaje/Einicke/Siegfried/Wilke
      (Halle) via nws.uzi.uni-halle.de. **Remaining = human-send only.**

- [x] **Materialize the human review queue for legacy/store cards.** — _status: complete; date: 2026-06-19; source: current-session._
      Ran `python src/run_batch.py status` + `python src/run_batch.py review` in
      `RussianTranslation/`: store has 217 translated legacy rows, 206 machine-ok,
      0 print-ready, all 217 `legacy_needs_review`. `_review_queue.jsonl` emitted
      217 human-review items sorted by severity (sev4=1, sev3=3, sev2=19,
      sev1=194). No reviewer/decision fields were advanced.
- [x] **Make the review queue spreadsheet-friendly.** — _status: complete; date: 2026-06-19; source: current-session._
      Added `python src/run_batch.py review_csv`: consumes canonical
      `_review_queue.jsonl` and emits gitignored `_review_queue.csv` with machine
      evidence plus blank `reviewer_id`/`decision`/`edit`/`notes` columns. Validated
      217 rows, severity-sorted, all `legacy_needs_review`, human columns blank.
- [x] **Design the human gold-set scaffold.** — _status: partial; date: 2026-06-19; source: current-session._
      Added `gold/HUMAN_GOLD_PROTOCOL.md` plus `python src/gold_review_csv.py`.
      The scaffold exports 320 balanced rows (40 in each period × kind cell) to
      gitignored `gold/_human_gold_review.csv`, keeps existing LLM labels separate,
      and provides blank human columns (`human_label`, `reviewer_id`, `confidence`,
      `needs_adjudication`, `human_note`). Roadmap G6 moved from not_started to
      partial. Still print-blocking until human labels/adjudication are complete.
- [ ] **Fill and adjudicate the human gold set.** — _status: queued; date: 2026-06-19; source: current-session._
      Assign reviewers, fill `_human_gold_review.csv`, double-review at least 10%
      stratified by period × kind, adjudicate disagreements, then aggregate a
      human-confirmed precision report with Wilson 95% confidence intervals.
- [x] **Promote Apresjan portrait to a schema contract.** — _status: partial; date: 2026-06-19; source: current-session._
      Added `schemas/pwg_ru_lexicographic_portrait.schema.json`, stamped
      `microstructure.portrait()` with `schema_version=pwg_ru.lexicographic_portrait.v1`,
      and added `python src/validate_portrait_schema.py`. Validator passed on 25
      live PWG portraits. Roadmap G8 moved from not_started to partial; still
      print-blocking until assembled-card schema and synonym-discrimination output
      are implemented.
- [x] **Cut v0.0.3.** — _status: complete; date: 2026-06-19; source: current-session._
      Released `v0.0.3` from commit `bbe7c8f` and published GitHub Release:
      deterministic June-22 pilot preflight (manifest mode, NWS rule 5,
      real-test driver, safe/exact filename handling, protected audit preflight,
      complete a-section input coverage, review queue notes).
- [ ] **PWG→Russian corpus gate — deterministic half BUILT & run.** — _status: queued; date: 2026-06-19; source: current-session._
      `src/corpus_gate.py` does the SLP1 join over the 5 gitignored dicts + the
      SamudraManthanam corpus query (reuses its FTS; `#sa`→`#ru` alignment); all
      modes run; coverage measured (correctness 16.4% / KOW 8.0% / corpus ≈14–15%);
      `THRESHOLD=0.5` + `HEAD_SENSE_ONLY` tuned (token overlap = confident-pass
      only — divergence is the LLM's job); Kochergina copyright confirmed
      (1924–2018, ©→~2088). See `RussianTranslation/SAMUDRA_INTEGRATION.md` §2.3.
      **Remaining** = the LLM verdict step (awaits the actual `pwg_ru` run) +
      broader ecosystem (below).
- [ ] **Broader SamudraManthanam wiring** (user: "broader ecosystem too") — _status: queued; date: 2026-06-19; source: current-session._
      per `SAMUDRA_INTEGRATION.md`: mw_ru validation/citations via `dic_mw.jsonl`,
      WhitneyRoots via `warnemyr.jsonl`, the parallel corpus as a shared citation
      backbone. Not started; design only.
- [x] Documentation gaps from `HANDOFF.md` — **all five closed.** — _status: complete; date: 2026-06-14; source: commit._
      HeadwordLists README + reference provenance + docs map + CONTRIBUTING done
      (v1.0.2); kosha import **decided external** (file not on disk, lives in the
      mw_ru working repo, documented in mw_ru.md §7). No doc gaps currently open.

## 🚧 Current Work-In-Progress (WIP)

- [ ] **FREQUENCY-FIRST validated + the GIANT-ROOT finding (2026-06-23).** `freq_route.py`
      built → `scale_manifest.freq.json` (43,968/106,083 = 41% DCS-attested; band 5=641,
      4=3170 → ~3.8k core). **Top of the dictionary = verbal roots** (sTA 379 senses, i 272,
      gam 213, yuj 151, BU 131, vid 74) + core particles/nouns. ⚠️ **Roots fail single-pass
      translation:** `vid`(74) and `BU`(131) both died "Connection closed mid-response", wrote
      nothing — the entries are too big for one agent. **Tractable freq-core NOUNS pass clean:**
      `rūpa` (26/26 NWS) and `rasa` (30, 0 mismatch) both CLEAN through the owner-map gated loop.
      → **Root-entry microstructure (how to SECTION a 379-sense root — by homonym? by prefixed-
      verb? sub-cards?) is the #1 question for the incoming lexicography-manuals review BEFORE
      scaling.** Core-tranche inputs generated (223 new + pre-existing owner-mapped). PAUSED for
      the manuals per user.


- [ ] **a-section scale-up STARTED (2026-06-23) — owner-map-fed gated batches.** First real
      batch of 6 coverage-first fresh cards (regenerated WITH owner map → Agent Sonnet translate
      → nws_split F12 gate): **5/6 CLEAN first-pass** — `as` (60 NWS owners, the densest case)
      CLEAN, `anu` 36/36, `arjuna` 8/8, `A` 37/37, `akza` 28; `Ap` MISATTRIBUTION (2, on generic
      glosses "extensive"/"ausfüllen" — likely locator collisions) → quarantined `_ap.merged.REJECTED.md`,
      re-queued. The owner-map feed holds at scale (the dense-NWS failure mode that broke pre-fix
      now passes). a-section rendered: 17/12,156.
      ⚠️ THROUGHPUT: each card ~80–150k subagent tokens, 2–12 min → full 12,156 via Agent one-by-one
      is a multi-day/~1.5B-token job, NOT in-session. Agent batches VALIDATE + seed; the FULL run
      should go via the Max-harness workflow (run_pilot_wf.js, now owner-map-aware via HARD RULE 5)
      or a batch API. NEXT = either keep Agent-batching coverage-first windows, or hand the bulk to
      the Max workflow; re-pass Ap.
- [x] **BULK HANDED TO MAX WORKFLOW — a-section inputs owner-map-ready (2026-06-23).**
      Decision: drive the full 12,156 via run_pilot_wf.js on the interactive Max harness
      (Workflow tool has no fs; Max does). Regenerated all NWS a-cards WITH owner map:
      `_pilot_gen_merged.py --manifest a` → **4,264 regenerated** (PW 94%, SCH 21%, PWKVN 16%,
      NWS-extra 100%); non-NWS cards skipped (no map needed). run_pilot_wf.js already
      owner-map-aware (HARD RULE 5 + Guard 7). Runbook = `src/pilot/RUN_ASECTION_MAX.md`
      (per-window loop: prep N OFFSET → run workflow on Max → audit wf_output.json; rejects
      auto-re-queue). NEXT (human-on-Max): execute the window loop; I prep/audit each window.


- [x] **Merged-translation gated pipeline VALIDATED end-to-end on FRESH a-cards (2026-06-23).**
      Ran 2 fresh coverage-first cards via Agent (Sonnet translate → render .merged.md →
      deterministic F12 gate `nws_split.py check`): **`ātman` CLEAN (13/13**, incl. French
      TAK "le soi"→RU — Guard 7 works); **`ās` MISATTRIBUTION** (3 owner-swaps + 11 unlocated)
      → gate correctly REJECTED a hard 22-owner NWS card. Gate discriminates, not rubber-stamps.
      Synced Guard 7 into run_pilot_wf.js CONV (de/fr/en). ⚠️ Mechanism note: run_pilot_wf.js
      uses node:fs → needs the interactive Max workflow harness; my Workflow tool has no fs,
      so batches run via the **Agent tool** (Sonnet/Opus). Don't hand-guess safe_name paths —
      compute via safe_filename.safe_name (safe_name('As')='_as', NOT '_a_s').
- [x] **DONE (2026-06-23): owner-map feed — F12 eliminated by construction.** `_pilot_gen_merged.py` appends an AUTHORITATIVE pre-parsed NWS owner map (deterministic `nws_split` triples); translator emits one row per entry, owner verbatim, no re-derivation. Re-validated `ās`: MISATTRIBUTION (3 owner-swaps) → **CLEAN** (0 mismatches; 9 gate-abstentions on generic glosses, not errors). Workflow HARD RULE 5 + 6_merged_translate.md updated. NEXT = scale the a-section (gen merged inputs already done; run translate→gate over scale_manifest.a in batches).


- [x] **NWS FULL scrape + fold + drift — ALL COMPLETE.** — _status: complete; date: 2026-06-18; source: commit._
      (1) **Scrape:** `nws_scrape.py section all --workers 8` → all **167,990**
      headwords (gitignored `src/pilot/nws/*.json`); `_nws_audit.py all` = CLEAN
      (167,990/167,990, 0 missing / 0 collisions / 0 dups / 0 refusals). Net-new
      `has_nws_extra` = **34,101 (20%)**; ~7h at ~300 cards/min, 26 transient skips
      all recovered on a resumable re-pass.
      (2) **Drift (all sections):** `_nws_drift.py all` confirms a-section's LOW
      staleness dict-wide — Schmidt 96.7% identical (mean Jaccard 0.987, only 0.8%
      major), pw 80.9% overlap (0.1% NWS-only; the 9.2% orig-only = Cologne growth
      since 2013, not NWS error). Saved as claude2 project memory `nws-staleness-validated`.
      (3) **Fold:** NWS wired into `dict_merge.merged()` as the 5th "external" layer —
      net-new only, per-key on demand, kept OUT of `LAYERS` (adds no new headwords;
      its keys derive from the csl layers). Commit `5d91f74`.
      (4) **Pilot scaled:** `_pilot_gen_merged.py --manifest <sec> --limit N` consumes
      `scale_route.py`'s coverage-first manifest → full layered inputs at volume,
      resumable. Top-300 a-heads: NWS-extra **95%** (vs 20% dict-wide). Fixed a
      resumable-skip bug (old PWG-only `_pilot_gen.py` writes same `<key>.raw.txt`
      names in `=== RECORD` format → now skip only `=== LAYER:` merged files).
      ⚠️ **F10 case-collision bug** (found in the a-section, gold-standard-first paid off):
      SLP1 case-sensitive keys as filenames collided on NTFS (aMSa.json==AMSa.json),
      silently dropping headwords; would've lost ~15k across the full dict. Fix =
      `safe_name()` (uppercase→'_'+lower, injective, proven collision-free); F10.
- [x] **NWS a-section scrape — RUNNING, resumable + reap-survivable.** — _status: history; date: 2026-06-17; source: commit._
      `src/nws_scrape.py section a 3` → gitignored `src/pilot/nws/*.json` (one card
      per merged a-headword). **Relaunch recipe** (env reaps long tasks; restart is
      now instant via the cached key list): just re-run that command — it skips done
      `*.json` and reads `pilot/nws/_keys_a.txt` (24,621 keys) instead of rebuilding
      the 4-dict index. **Rate** ≈ 1 card / 3.3 s (server responds in 0.1 s; 3 s is
      politeness) → ~22 h wall-clock for the a-section. NWS-extra hit ≈ 50 % on the
      common early words (will fall for the long tail).
      **Two bugs fixed this session**: (1) unguarded session-refresh `ReadTimeout`
      crash → wrapped all network in 3-try retry + backoff + skip-not-crash; (2) the
      success test `len(html) > 1500` wrongly rejected *valid empty* results
      (`$nws_lemmas = $("");`, ~1082 chars) → every word with no NWS addendum hit
      retries+skip and throughput collapsed to ~0. Now success keys on the
      `_lemmas = $(` JS structure; empty results are kept. (Both committed.)
      **Data-version ANSWERED + documented** in `RussianTranslation/DICTIONARY_CHAIN.md`:
      NWS = DFG project Marburg+Halle **1 Dec 2013 – 31 Dec 2016**, built **on** pw
      (Böhtlingk 1879–89) + Schmidt (1928) as a **~2013 Cologne snapshot**; its
      entries are condensed *„Kleines Zitat"* (new lemma/sense/grammar + lit-ref,
      **not** full articles) → NWS is a *completeness index + pointers*, low-volume
      for translation. Drift vs current `csl-orig` is measurable from the captured
      `pw`/`sch` fragments (the scrape stores them) — that diff is the next NWS step.
      **Then-decide checkpoint** (user: "Just the a-section first, then decide"):
      after a-section accumulates → run the drift-diff + report NWS-extra value →
      ask whether to continue to b–h or stop + move to Lexonomy export.

- [x] **P1 corpus word-alignment lexicon — COMPLETE: 1,091,528 alignments, 190,838 SLP1 keys, 116 works, 99% of the translatable corpus** — _status: complete; date: 2026-06-16; source: commit._
      (990k translation + 101k commentary), stratified Vedic/Epic/Classical/
      Medieval. Wired into `assemble.py` (corpus-first: stratified senses +
      Apresjan near-synonym candidate set; demoed on aṃśa). New: `add_corpus_text.py`
      + `ADDING_TEXTS.md` (incremental new-text pipeline); `REVIEW_AND_ROADMAP.md`
      (full retrospective + phased plan). NEXT = corpus-first re-translate of the
      216 a-section cards (awaiting scope/discrimination/microstructure decisions).
- [ ] P1 corpus word-alignment lexicon — was BUILDING. — _status: wip; date: 2026-06-19; source: current-session._
      `src/build_corpus_lexicon.py` aligns each verse-pair in SamudraManthanam via
      DeepSeek → gitignored `corpus_lexicon.jsonl` (SLP1-keyed, stratified by
      genre+date via `corpus_strata.json`). Batched ~8 verse-units/call, 12
      workers, biggest-first. Rate ≈ 440 groups/min; genuine corpus = **58,897**
      translated groups; projected DeepSeek total ≈ **$29**. Running in background;
      `_watch.py <pct>` posts a live cost/ETA bar each 10% + `_audit.py` runs a
      deterministic integrity check each milestone (all CLEAN so far).
      **DONE this session**: `<ls>`→stratum map (`build_ls_map.py` +
      `ls_source_map.json`: 45 sources = 72.4% of 772k citations, 29.8%
      corpus-harvestable) + harvest reader (`corpus_harvest.py`: SLP1 key →
      attested Russian, stratified by period/genre, counted, translation-vs-
      commentary, `<ls>`-cited stratum first — demoed rājan→царь/государь,
      dharma→дхарма/закон/добродетель/долг).
      **+ methodology review** (`METHODOLOGY_REVIEW.md`, grounded 5-lens) and its
      **Priority-1 items SHIPPED**: provenance stamping + persisted senses
      (`run_batch.provenance`), human-review state machine + `run_batch.py review`
      worklist, per-sense rights gate (`corpus_gate.RIGHTS`/`publishable()` +
      `assemble.py` flag) + `DATA_LICENSE.md` = CC BY-SA 4.0, and the
      coverage-by-stratum honesty check (`corpus_harvest.py coverage`).
      **+ project docs**: `CHANGELOG.md` (evolution), `failures/FAILURE_GALLERY.md`
      + `failures.jsonl` (8 bad→fixed samples for demonstration; live capture via
      `_audit.py` + `run_batch.py review`), and **`APRESJAN.md`** — grounds pwg_ru
      in the Moscow Semantic School (Apresjan, *Лексическая семантика* 1995):
      лексикографический портрет/типы, интегральность, регулярная многозначность,
      and **near-synonym discrimination of the harvest's multi-renderings** (the
      highest-leverage idea: царь/государь/владыка, dharma→дхарма/закон/добродетель
      are synonym SETS to discriminate, not flat lists).
      **Remaining**: ru-side lemma grouping (царь/царя/царю→царь); wire the reader
      into `assemble.py` harvest; LLM verdict step + corpus-first re-translate of
      the ~216 provisional a-section cards.
- [x] **PILOT DONE** — _status: complete; date: 2026-06-16; source: commit._ — 15 richest-reuse a-cards, corpus-first + per-sense
      Apresjan discrimination (Sonnet translate → Opus judge, `pilot/run_pilot_wf.js`,
      inputs via `_pilot_gen.py`, output via `_pilot_collect.py` → gitignored
      `pilot/output/`). Result: **12/15 publishable (sev≤2), ALL 15 discrimination=
      strong, register 15/15, sigla 13/15.** `arTa` showcase: 12 senses discriminated
      with corpus-grounded differentiae, sigla verbatim, source_type attested-vs-
      lexicographic, keeps B&R's own "Fehler für argha" + vol-2 correction. Judge
      flagged real fixables: antarikza coverage-fail + 1 fabricated sub-sense; arjuna
      sigla slip; api English leak. NEXT = re-pass the 3 flagged + prompt-harden
      (anti-fabrication / coverage-completeness / sigla-protect) before scaling.

## 🧠 Dev Notes & Hypotheses

- **PLACEHOLDER-FABRICATION BUG caught by a live integrity audit — the single most important corpus lesson.** — _status: history; date: 2026-06-16; source: existing-entry._ A 5-auditor + adversarial-verify
  workflow over the live `corpus_lexicon.jsonl` found that works whose corpus
  `seg=ru` is the placeholder `…` (no Cyrillic) — `12_mahabharata-shantiparva`
  (99.8% placeholder), `13_…-anushasanaparva` (100%) — were still aligned, and
  DeepSeek **hallucinated** fluent Russian for translations that do not exist:
  **166k of 204k rows (81%) were invention** attributed to real verse citations.
  They pass every surface check (fluent Cyrillic) → invisible to QA. **Lesson:
  truthiness ≠ translated; a real ru MUST contain Cyrillic.** Fix = `has_cyr()`
  guard in `pairs_of()` (placeholder → 0 API calls) + write-step Cyrillic/`ru!=sa`/
  refusal guards + `SYS_BATCH` never-invent + `[NOTE]` commentary instruction +
  `√`-strip + per-batch `(group,slp1,ru,kind)` dedup. `build_strata.count_groups`
  now requires Cyrillic ru (total 78,139→58,897). Recovery via `_purge_lexicon.py`
  → 26,277 genuine rows (0 untranslated-group / 0 non-Cyrillic / 0 `ru==sa` / 0
  dup, verified). Committed + pushed; build restarted clean. **Always re-scan a
  generative corpus build for source-placeholder leakage before trusting volume.**
- **Batching verses/call holds quality.** — _status: history; date: 2026-06-16; source: existing-entry._ A/B (5 Gītā verses, batched
  1-call vs 5 single-calls) = identical Russian glosses; batched returns cleaner
  dictionary-form keys (`samaveta` vs inflected `samavetā`) and keeps verses
  independent (no word-bleed). ~3-4× faster wall-clock, cost ~unchanged
  (content-bound). `_watch.py` rewritten to measure a live window (the append-only
  lexicon's ctime is stale across runs → bogus 24-min/$169 ETA).
- **pwg_ru scaffold built via a 4-phase workflow** — _status: history; date: 2026-06-14; source: existing-entry._ (analyze → draft →
  3-lens adversarial review → fix; 18 agents). Sources confirmed on disk: PWG
  text = `csl-orig/v02/pwg/pwg.txt` (53 MB); headwords ~106k. PWG card format is
  harder than MW: `{%…%}` wraps BOTH German glosses (translate) AND Latin
  (leave) — this is the kit's headline rule. The review caught fabricated data
  (invented "frequencies verified against real corpus" + a precise KOW count) and
  a garbled word; all scrubbed before write. Model version unified to Opus 4.8
  across the kit (mw_ru used 4.7; flagged the choice to the user).
- **KOW reality** — _status: history; date: 2026-06-14; source: existing-entry._ `KOW/` repo holds only CITATION/README/changelog,
  NO dictionary text; there is no `csl-orig/v02/kow/` and no KOW headword list.
  SKD (Śabdakalpadruma) and VCP (Vācaspatyam) are Sanskrit→Sanskrit (zero
  Cyrillic) — usable only as a Sanskrit-side sense backbone, never as a Russian
  authority. Sanskrit-key overlap with PWG: SKD∪VCP ≈ 34,804 keys (~33%); but
  Russian-gloss coverage is bounded by KOW alone (~10% ceiling).
  **UPDATE (user, 2026-06-14):** superseded — KOW/FRI/KNA/Кочергина are all
  digitized and will be supplied; KOW is a partial human PWG→RU (WIL-seeded), so
  it is the human *reference* signal, not the correctness authority. Design is now
  a 2-signal non-blocking annotator (independent-correctness Кочергина/FRI/KNA +
  KOW reference-agreement) — NOT inert and NOT KOW-bounded. SKD/VCP unchanged
  (Sanskrit-side only). FRI usable now from `csl-orig/v02/fri/fri.txt`.
- **CLAUDE.md already merged into master.** — _status: history; date: 2026-06-14; source: existing-entry._ `CLAUDE.md` (b64b4d3) was already merged into `master` via PR #13
  before we tried to commit it, so a fresh branch off `master` could not produce
  a reviewable diff for it. User chose to leave it on `master` rather than
  revert-and-re-add.
- **master / feature branch divergence resolved.** — _status: history; date: 2026-06-14; source: existing-entry._ `master` had diverged from `feature/particles-explorer` (dependabot
  commits on top of the merge), so the changelog commit was **cherry-picked**
  onto `master` (`44aed51`) rather than fast-forwarded. The feature branch's
  original commit (`7ab3651`) therefore had a different SHA from the one on
  `master`, which is why local `git branch -d` refused and `-D` was needed.
- **Encoding caveat documented in CLAUDE.md.** — _status: history; date: 2026-06-14; source: existing-entry._ BOM presence is **inconsistent**
  across `HeadwordLists/` exports (MW key1 has a BOM, key2 does not) — the
  org-level "no BOM" rule does not hold here. Check before transforming.
- **RussianTranslation external-data boundary.** — _status: history; date: 2026-06-14; source: existing-entry._ `RussianTranslation/` is docs + prompts only — the actual mw_ru
  translation data, `data/scripts/*`, `improvements.md`, `yandex_api.md`, and
  the glossary JSONs live in a **separate working repo**. Several docs linked to
  those external paths as if local, producing dead links. Pattern for such refs:
  demote to plain text with "(не входит в этот репозиторий)", do not link.

## ✅ Completed (Recent only)

- **Indische Sprüche dataset extracted — DONE (2026-07-03, Sonnet 5 `claude-sonnet-5`).**
  New [IndischeSprueche/](https://github.com/gasyoun/SanskritLexicography/tree/master/IndischeSprueche)
  data asset: full Böhtlingk *Indische Sprüche* (2nd ed. 1870–1873), 7,537 sayings,
  exported from `VisualDCS` archive.sqlite `subhashita` table via the new
  `VisualDCS/src/DCS-data-2026/export_subhashita_jsonl.py`. `subhashita_ramayana`
  intentionally excluded (separate, likely-positional pairing artifact). PWG cites
  this collection 6,666 times and PWK 138 times as `Spr. N` — the citation crosswalk
  is scoped, not built, in [Uprava H143](https://github.com/gasyoun/Uprava/blob/main/handoffs/H143_pwg_pwk_indische_sprueche_crosswalk.md).
  Released `v1.1.5`. PROJECT_INTERLINKS.md updated.

- **A33 referee + polish pass — DONE (2026-07-03, Fable 5 `claude-fable-5`, H123/S14 + follow-up).**
  Hostile pre-submission review [papers/A33_review_fable5.md](https://github.com/gasyoun/SanskritLexicography/blob/master/papers/A33_review_fable5.md)
  ([PR #109](https://github.com/gasyoun/SanskritLexicography/pull/109) merged): References
  section ADDED (the note had none — incl. **Renou 1956** for the language-state periodisation
  the dating map rides on), related-work § written from verified literature (the FABLE-index
  "Zaïane 2008 / Cysouw 2013 / Pereltsvaig 2019" suggestions were UNVERIFIABLE and deliberately
  not cited — flagged for the author), A33↔A02↔A06 anti-salami companion note, full-URL links.
  **Second pass** ([PR #110](https://github.com/gasyoun/SanskritLexicography/pull/110) merged)
  applied the paper edits queued by the parallel PR #103 data pass: PWG figures re-synced to the
  reproducible current-source run (N 13,900→11,882; cited senses 118,528→113,012; strict-chrono
  26.2→25.4 %; adjacent 76.3→76.8 % — rates 73.5 %/τ 0.375/density 23.4 % identical), the
  **chance-floor calibration** added to abstract + §3.1 (sense-1-oldest floor 52.7 %, τ floor ≈0;
  observed covers 44 %/38 % of the floor→ceiling span), §5 reproducibility note (`[)〉]` delimiter
  fix + snapshot pin + null-baseline links), MW column flagged as the 2026-06-24 run pending a
  same-snapshot re-run. **A33 → 4/5.** Remaining: venue `@DECIDE` (Lexikos/IJL/eLex — GTD row),
  AP90 Vedic-siglum hand-check, MW same-snapshot re-run, optional GRA/PW controls.

- **WhitneyRoots accent-axis validation run — DONE (2026-07-03, Sonnet 5
  `claude-sonnet-5`).** Executed
  [SONNET_WhitneyRoots_accent_validation.md](https://github.com/gasyoun/Uprava/blob/main/handoffs/SONNET_WhitneyRoots_accent_validation.md)
  in a sibling-repo worktree. Scored WhitneyRoots'
  [`crosswalk/accent_rules.json`](https://github.com/gasyoun/WhitneyRoots/blob/main/crosswalk/accent_rules.json)
  (18 rules, 19-cell matrix) against attested Rig-Veda accents from VedaWeb 2.0 + Casaretto
  et al. (2025), joined on this repo's
  [`RussianTranslation/src/headword_index.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/headword_index.tsv)
  udātta positions. **17/19 matrix cells GO, 1 GO-with-exceptions, 2 measurement-only, 0
  NO-GO** — clears the ZALIZNYAK_INDEX a–f axis emission on the 17 GO cells. Caught + fixed a
  mid-run scoring bug (case/number override lookup). Delivered as
  [WhitneyRoots#24](https://github.com/gasyoun/WhitneyRoots/pull/24), released
  [v1.2.0](https://github.com/gasyoun/WhitneyRoots/releases/tag/v1.2.0). This repo's
  [FINDINGS.md §54](FINDINGS.md) added; §1/§42 status lines updated
  ([SanskritLexicography#104](https://github.com/gasyoun/SanskritLexicography/pull/104)).
  **Branch-contention event during wrap-up:** a concurrent session switched this repo's shared
  working directory to `heritage-h106-phase5-dico-gloss` mid-edit; my FINDINGS.md commit landed
  stacked on their pushed H106 commit. Recovered per `/branch-contention-recover`: uncommitted
  cleanly (`git reset --mixed HEAD~1` + restore just `FINDINGS.md`, leaving the other session's
  files untouched), re-applied the delta on a fresh worktree off `origin/master`, and discovered
  the other session's local (unpushed) work had already claimed `§54` for an unrelated DICO-gloss
  finding — renumbered mine to the correct next-free slot against `origin/master`'s actual §53
  ceiling; flagged in the PR that whichever of the two §54 candidates merges second should
  renumber again. **Follow-ups queued in WhitneyRoots's own `.ai_state.md`:** the `samyaYc`
  (samyañc) exception-candidate review, and a wider VedaWeb pull to resolve the thin D3 split.
- **pwg_ru corpus-gate deterministic boundary tightened (2026-06-26):** local corpus-gate
  inspection no longer initializes DeepSeek credentials (`build_corpus_lexicon.py`
  lazy-loads the key only for live calls). `corpus_gate.py card` now exposes a
  deterministic precheck for candidate PWG-RU text, and `run_batch.py` preserves
  explicit provider-gate metadata through batch build/collect. Verified with
  `py_compile`, `build_corpus_lexicon.py status`, `corpus_gate.py card agni "огонь"`,
  `run_batch.py build 3 covered`, and bounded assembled export validation.
- **Cross-dict UNION headword index (2026-06-26, `[0.0.20]`):** print target chosen = **union**,
  feminines folded under masc. `build_union.py` merges the 8 key1 dicts from `now-2026/` →
  **295,298** headwords with provenance + gender (aggregated from `<lex>` per `<L>` record).
  Feminine fold split for safety: **238 `-inī`→`-in` auto-folded** (mf(ī) marker); **3,993
  `-ā/-ī` candidates** to `union/fold_candidates.tsv` for editor (āśā≠āśa risk). `union/UNION.md`
  + `union_headwords.tsv`. PRINT_READINESS: E done (union), F policy chosen+applied.
- **Fuller union + ranked candidates (2026-06-26, `[0.0.21]`):** `build_union.py` reads `<k1>`
  from csl-orig for **all 15 dicts** (added the 7 key2-only) → **323,425** headwords. `-ā/-ī`
  fold candidates **ranked**: 3,569 high (masc is `mfn` → true feminine, `parā←para`) / 426 low
  (masc `m`-only → distinct lexeme, `āśā≠āśa`) in `fold_candidates.tsv`.
- **Low candidates gloss-screened (2026-06-26, `[0.0.22]`):** `screen_candidates.py` pulls the MW
  gloss for both forms of each 426 low candidate → `union/low_candidates_screened.tsv`: **419
  likely-distinct (reject)** + **7 MAYBE-related** to eyeball. Editor low-set work: 426 → ~7.
  **Next human: confirm the 7 + spot-check the 3,569 high.**
- **B coverage + A typo queue (2026-06-26, `[0.0.23]`/`[0.0.24]`):** **B** —
  `coverage_additions.py` ranks DCS-absent lemmas by band (`COVERAGE_ADDITIONS.md`): 21,759
  absent, high-band = lemmatisation artifacts, priority = 409 band-3 nominal. **A** —
  `assemble_typo_queue.py` → `A_TYPO_QUEUE.md`: MW 4 + PWG 12 fileable typos w/ IAST + error
  type + evidence (PWG mostly b↔v Fraktur-OCR). PRINT_READINESS A🟡/B🟡.
- **A all-122 + B cross-tag (2026-06-26, `[0.0.25]`):** **A** — `assemble_typo_queue.py` auto-discovers
  all dicts → `A_TYPO_QUEUE.md` = **122 typos / 11 dicts** (spine first). **B** — `crosstag_additions.py`
  tags the 416 priority adds w/ Catalan+Huet: only **25 corroborated, ~8 genuine real words**
  (karkandhū/maṇikā/cittamātra…); rest = verb/affix artifacts. **Finding: CDSL coverage essentially
  complete.**
- **C accents rendered (2026-06-26, `[0.0.26]`):** `accent_review.py` → `Catalan-Pujol/accent_disagreements.tsv`
  = 63 Pujol-vs-Cologne udātta disagreements as accented IAST both sides (bhagá vs bhága), recommend
  = Cologne/RV. Union uses Cologne accents → C resolves to "use Cologne; 63 = Catalan-editor QA".
  **ALL PRINT_READINESS agent-prep done (A–F).** Remaining = human: file the typos (spine 16 first),
  rule on ~8 B adds + ~7 F candidates, confirm accents if printing. Findings: CDSL coverage complete;
  MW/PWG spine gated only by 16 typos.
- **Renou register glossaries shipped (2026-06-26).** — _status: complete; source: current-session._
  New `src/renou_glossary.py` filters the Renou-tagged dicts by register/state/provenance →
  deduplicated headword glossary (by IAST across 8 dicts; states · ls/dcs prov · dicts · senses;
  `--state`/`--prov`/`--min-dicts`/`--state-only`, md/tsv). Shipped `RussianTranslation/glossaries/`:
  **épigraphique** (709), **bhāṣya** (14,498; 10,320 in ≥2 dicts), **jaina** (286) + README. KEY
  FINDING: **484/709 épig words (68 %) are corpus-absent** (only in inscriptions — dynastic names,
  donative terms like `akṣayanīvī`), proving the register axis adds signal the state axis can't.
  Realises RENOU.md §Use cases. src/README.md gained a Renou subsystem section. Committed `181230b`.

- **Renou register — research layer: catalog, cross-axis, findings, nominal-style verification (2026-06-26).** — _status: complete; source: current-session._
  On top of the shipped register axis, a research layer landed: (1) **20-register catalog**
  `RENOU_REGISTERS.md` (chapter-relative + global numbering, per-register headword counts,
  rgveda…hors_inde); (2) **6-slice cross-axis deep-dive** `RENOU_CROSSAXIS.md` (bhāṣya∩I,
  kāvya∖I, epic∩bhāṣya, vyākaraṇa∩bhāṣya, bauddha∩kāvya, épig∖corpus); (3) **5 empirical
  findings** `RENOU_FINDINGS.md` F1–F5 (bhāṣya = cross-disciplinary syntactic register; BHS a
  self-contained lexical world; doctrinal = perennial vs narrative/poetic = period-bound;
  nāṭya/kāvya as coinage registers; the finite-verb recession); (4) **corpus verification of
  Renou's nominal-style thesis** `RENOU_NOMINAL_STYLE.md` + `src/nominal_style.py` — finite-verb
  density **halves** I 14.4 % → IV 6.7 %, bhāṣya 6.25 % < kāvya 7.48 %, Nyāyasūtra 0.18 % floor
  vs Taittirīyasaṃhitā 18.5 % ceiling (the subsystem's strongest single result); (5) **data note**
  `papers/A34_renou_register_note.md` (readiness **3/5**; target Lexikos/IJL/eLex; headline =
  68 % of épigraphique vocabulary is corpus-absent). All phases of `RENOU_SUBSECTIONS_PLAN.md`
  complete. Commits `797ef14`→`b5a17f1`. **Residuals:** cite Tubb & Boose 2007 + Speijer 1886 in
  the nominal-style note (Tubb PDF now local-only in gitignored `literature/`); human bhāṣya
  precision spot-check; inline-dict (ap/ben/bhs) `<ls>` register route.

- **`article-comparison/` polish + the table builder, committed (2026-06-25→26).** — _status: complete; source: current-session._
  Closed the Track-B tail and hardened the comparison tables, shipped as `v0.0.6`–`v0.0.12`:
  (1) **Residual per-sense pass** — Opus-4.8 assignment of every `### Не привязано` rendering in
  the four `*.persense-ru.md` to a PD sense (or honest *other*); coverage now agni 100 % / akṣara
  99 % / anya 97 % / ananta 97 %, frozen as a reproducible `LLM_ASSIGN` map in `_build_persense_ru.py`.
  (2) **Dictionary years on every quote** — all `*.table.md` rows labelled with their CDSL edition
  year (from `csl-guides/src/data/dictionaries.json`); the 7-volume Petersburg dicts name the
  **volume** containing the a-stem (PWG Bd. I 1855, pw/PWK Th. I 1879).
  (3) **Full untruncated entries** — the ~800-char cell cap (trailing ` …`) removed; 40 cells
  expanded. Committed the previously-uncommitted builder as
  [`RussianTranslation/src/_build_tables.py`](RussianTranslation/src/_build_tables.py) (rebuilds all
  four tables from the full `*.iast.md` + `*.pd-min.md`, nested-citation-safe bracket stripping).
  (4) **Rows sorted chronologically by edition year** (WIL 1832 → … → PE 1975 → PD 1976).
  (5) **agni gloss draft review** → [`article-comparison/agni.gloss-review.md`](article-comparison/agni.gloss-review.md):
  agent pass over the 130 hand-authored RU glosses (1 H = the *agnicayana* altar↔rite category error
  at 4i/4vi; 3 M; sign-off worklist, glosses untouched). **Pending = human sign-off.**
- **Catalan-Pujol dataset — KEPT as an intentional repo dataset (2026-06-26).** — _status: resolved; source: current-session._
  A `git add -A` in this session accidentally committed the then-untracked
  [`HeadwordLists/Catalan-Pujol/`](HeadwordLists/Catalan-Pujol/) files in `56564a0`; the parallel
  (M.G.) session then committed `75b917d` refactoring those scripts repo-portable. Decision (M.G.):
  **keep** them — they are now actively developed. No removal; one copy is permanently in history at
  `56564a0` (master is branch-protected, so a true purge would need an admin force-push). Lesson:
  **never `git add -A` here** — stage explicit paths; this repo carries untracked parallel-session WIP.
  **Added the DCS corpus axis (2026-06-26):** new `coverage_vs_dcs.py` cross-tabs CDSL dictionary
  coverage × DCS-2021 corpus attestation (`VisualDCS/dcs_lemma_summary.json`). Finding: 91 %
  dict-covered but only **46.4 % corpus-attested**; **44.9 % is dictionary-listed-but-unattested**
  ("dark matter"); only 0.3 % corpus-attested with no CDSL entry. **Triaged those 177**
  (§5): ~55 are lemmatisation/morphology convention (41 prefixed/denom verb roots, 9
  productive -tā/-tva/-tara/-tama/-vat, 5 bīja), 29 unheadworded compounds, ~93 simple/
  fem — within which a genuine residue of corpus-attested rare lexemes (plant/animal
  names: alasāndra/kustumburī/kaṅkolī…) is absent from all 43 CDSL dicts = real
  candidate additions. **Added §6 = Pujol's 11 headword conventions.** All in
  `Sanskrit-Catalan-Wordlist-vs-Cologne.md`.
- **HeadwordLists batch (2026-06-26):** (1) **now-vs-then** diff of the `*-unique-key*`
  snapshots → `headword_diff.py` + `NOW_VS_THEN.md`: key1 (SLP1) lists comparable —
  **AP 36,030→88,701**, **PWK(=pw) 131,918→151,349**, MW 193,978→194,084 (+753/−647),
  PWG/GRA/SKD/VCP/VEI small drift; key2 snapshots are legacy-numeric→SLP1 format
  migrations (not real diffs); PD absent from csl-orig; `_diff/` dumps gitignored.
  (2) **§7 accent compare** (`accent_compare.py`): Pujol udātta (combining acute) vs
  Cologne `/` — ~97 % same vowel (GRA 96.9 %, MW 97.1 %). (3) **§5 full 177 list** →
  `DCS-attested-no-CDSL.md`. (4) **Huet/INRIA control** (`huet_coverage.py` +
  `Huet-INRIA-Wordlist-vs-Cologne.md`): non-Cologne VH/Velthuis stem list, 86.2 %
  CDSL-covered, **60.0 % corpus-attested** (vs Pujol 46.4 % — reader's lexicon = denser
  corpus signal). All Pujol/Catalan work consolidated in changelog `[0.0.13]`;
  HeadwordLists tooling/Huet/accents/use-cases in `[0.0.14]`.
- **HeadwordLists/now/ regenerated (2026-06-26, `[0.0.15]`):** `headword_diff.py now`
  wrote current-csl-orig **key1** snapshots into `now/` (8: AP/GRA/MW/PWG/PWK/SKD/VCP/VEI;
  THEN files frozen for comparison; AP 88,867 / PWK 151,349 / MW 194,084 …). key1-only on
  purpose — the comparable set; key2 skipped (THEN=legacy numeric; several raw `<k2>` are
  `{#..#}` blobs — a naïve dump was 64 MB). Sanskrit-collated → compare by set not line-diff.
- **Foldered + dated (2026-06-26, `[0.0.16]`):** verified via git the snapshots were extracted
  **2014-10-05** → moved all 31 root `*.txt` to `then-2014/`, renamed `now/`→`now-2026/`
  (paths fixed in README/Huet doc/`huet_coverage.py`). `NOW_VS_THEN.md` now has a **growth %**
  column + **TOTAL** row (9 comparable lists 605,813→733,617 = +21.1 %; grand total of all 26
  then-counts = 1,721,983).
- **key2 as SLP1 + print-readiness (2026-06-26, `[0.0.17]`):** re-extracted key2 cleanly into
  `now-2026/` (was key1-only) — `key2_forms()` stops capture at `¦`, strips `{#..#}`, splits
  comma-lists → print form keeps `/` accent, `-`/`—`, `(...)`; SKD recovered 40,817 vs the 64 MB
  blob. 23 now-2026 files (key1+key2). **`PRINT_READINESS.md`** consolidates A–E for a printed
  headword list: MW/PWG = stable print-ready spine; gaps human/editorial (A clear
  SanskritSpellCheck's 122 typos = highest value, B coverage, C accents, E scope); **D (key2)
  closed**. Answers "ready to print in 2026?" → spine yes, union/AP/PWK needs the human passes.

- **Renou *register* axis — Renou's subsections as an orthogonal tag (2026-06-25).** — _status: complete; source: current-session._
  Reread the source (Renou 1956 *Histoire de la langue sanskrite*) from the scan → the five states
  = his five chapters, but his *subsections* are distinct registers a flat I–V tag can't express:
  **épigraphique** (Ch. II p.94) and **bhāṣya** (commentary, own grammar, Ch. IV p.133) fit none of
  the five. Faithful TOC → `VisualDCS/docs/Renou_1956_structure.md` (on VisualDCS `main`); design →
  `RENOU_SUBSECTIONS_PLAN.md`. Built an **orthogonal multi-label `renou_register`** (20-code lattice,
  parallel to the state — a word can carry registers across eras), provenance-graded, same lossless
  min-support policy as states. Routes: (1) **DCS corpus** — `build_dcs_renou.py` emits per-lemma
  `register`+`register_support`; genre→register + name-stem detectors (bhāṣya by `*bhāṣya/ṭīkā/vṛtti`).
  (2) **`<ls>` citation** — new `renou_register.py` maps siglum→register (PWG genre / MW name); only
  route for **jaina** (288 MW) + bhāṣya corroboration. (3) **dedicated épig** — inscription marker in
  `<ls>` (`Insch?r`) → `epig` (MW 687/AP 17/PWG 9). Inline dicts (ap/ben/bhs) via
  `renou_sigla.SIGLUM_REGISTER` + `bhs`→`bauddha` (17,839/17,839). Wired: `filter_dcs_registers`,
  taggers emit `renou_register`+`renou_register_provenance`, `renou_audit.py` register mode,
  `renou_portrait.py` sub-label. Every lattice register populated except `hors_inde` (no source);
  state axis untouched. Shipped `6ac5b11`→`9d841b0`. **`REGISTERS` canonical in `renou_register.py`**
  (build_dcs_renou imports it). Remaining = human bhāṣya precision spot-check (known `dīpikā` FP:
  Haṭhayogapradīpikā). Use cases documented in `RENOU.md`.

- **"a-letter" 4-word cross-dictionary comparison study built (2026-06-25).** — _status: complete; source: current-session._
  New `article-comparison/` study comparing a single headword across **every CDSL
  dictionary**: one non-samāsa + one a-samāsa pair, expanded to **4 finalists** —
  `agni`, `anya` (non-nañ) + `akṣara`, `ananta` (nañ-privative). Each picked as
  most-frequent-in-DCS (2026, `dcs_full.sqlite`, 5.69 M tokens) **AND** present in
  the unfinished Deccan **PD** dictionary — the binding constraint, since PD's "a"
  coverage stops at ~`apaca-` (kills artha/aśva/amṛta/asura despite high freq).
  Keys matched on both stem (`agni`) and nominative citation form (`agniH`) so no
  dict is missed. **Six views per word** (28 files + README): `.table.md`
  (side-by-side all dicts, SLP1→IAST, citations stripped, PD as skeleton),
  `.pd-min.md` (PD `{@..@}` sense outline), `.pd-min.ru.md` (bilingual EN/RU
  skeleton — agni 130 senses hand-authored), `.corpus-ru.md` (attested-RU
  distribution from the DeepSeek word-alignment lexicon + published verse pairs
  from SamudraManthanam), `.persense-ru.md` (each attested rendering hung under its
  PD sense, **88–99 % coverage**), `.verbatim.md`/`.iast.md` (full raw +
  transcoded). Builders in `RussianTranslation/src/`: `_build_corpus_ru.py`,
  `_build_skeletons_ru.py`, `_build_agni_ru.py`, `_build_persense_ru.py` (reuses
  `corpus_harvest.lemma_key` + pymorphy3; no re-alignment). **Audited**
  (data-integrity + per-sense): corpus lexicon 0-fabrication; fixed 2 assignment
  bugs (greedy name-routing → ananta «Ананта» now lands on the Śeṣa sense;
  gloss-scaffolding contamination → anya «Один» no longer matched a Bhṛgu-god name
  sense). **Headline finding** = the attested-RU split per sense:
  `agni`→Агни(theonym)/огонь, `akṣara`→слог/Непреходящее(Brahman),
  `ananta`→бесконечный(adj.)/Ананта(Śeṣa). Also cleaned the SamudraManthanam `#sa`
  Cyrillic leak (БхГ markers + 5 homoglyph words) that surfaced in the akṣara verse
  → SamudraManthanam **#16** + branch `feature/chronology-index`. Commits through
  `e08fdf9`, pushed to `origin/master`.

- **Audit tooling drift fixed (2026-06-25).** — _status: complete; source: current-session._ Restored the tracked deterministic
  corpus audit command `RussianTranslation/src/_audit.py`; hardened
  `release_readiness.py` so `--out <basename>.md` works as well as nested
  paths; ignored transient `_battery*`, `_judge*`, `_qa*`, `_out.txt`, and
  `_review_queue.triage.csv` sidecars while keeping `triage_review_queue.py` as
  reusable source. G5/G6/G7/G10 remain honestly blocked on human review, human
  gold, double-review, and a real edition cut.
- **Renou tag accuracy validation + DCS over-tag min-support fix.** — _status: complete; date: 2026-06-24; source: current-session._
  Anchor = `<ls>` (lexicographer citation); method = inter-signal agreement, no human labels.
  **Audit** (`src/renou_audit.py`, new): cross-tabs `ls`/`dcs`/`bhs`/`wl` per dict → `dcs`-widening
  is the dominant disagreement (MW 52 %, BEN 76 %, AP90 79 % of both-signal entries); 42–90 % of
  `dcs` assertions uncorroborated. Root cause = lemma-keyed index ⇒ homograph collapse (`akāra`→I–V)
  + discarded per-state evidence depth. **Fix:** `build_dcs_renou.py` now emits lossless per-state
  `state_support` `{n_texts, best_confidence}`; `renou.filter_dcs_states()` applies the policy at
  tagger time (`DCS_MIN_SUPPORT`=2, or ≥1 typed text), wired into both taggers (`--dcs-min-support`).
  **Effect:** 9.9 % of `dcs` state-assignments pruned (14.8 % of lemmas) — almost all spurious IV
  (9,736; date≥400 fallback) + I (2,923); **0 II / 0 V** (typed Vyākaraṇa/Buddhist texts untouched);
  33 maximal lemmas drop below 5. Residual `ca`/`idam`/`akāra` I–V left intact (corpus-accurate,
  uninformative → display concern). Index + all 8 `{code}.renou.jsonl` regenerated; `wl` layer
  reconstructed losslessly from intermediates. `RENOU.md` Validation section + post-policy coverage
  table; `CHANGELOG.md` entry. Shipped `ecc7bb9` (core, swept up by a parallel actor) + `9666591`
  (docs/audit), pushed to `origin/master`.

- **Renou language-state (I–V) tagging system — PWG + MW, two provenance-tagged layers.** — _status: complete; date: 2026-06-23; source: current-session._
  Every dictionary sense/headword classified into Renou's 5 states of Sanskrit (I Vedic ·
  II Pāṇinian · III Epic · IV Classical · V Buddhist/Jaina), multi-label, oldest citation
  flagged. **Layer 1 — `<ls>` citations** (deterministic): `build_ls_map.py` (+`renou` in
  CANON) / `build_ls_map_mw.py` → `ls_source_map{,_mw}.json`; `renou.py` resolver;
  `annotate_renou.py` tags final cards (`renou`/`renou_oldest`/`renou_oldest_sense`, schema
  updated, optional fields). Ran on legacy PWG store (217 cards, 84.8% tagged). **Layer 2 —
  DCS corpus attestation**: `build_dcs_renou.py` scans the 270-text/15.9k-file DCS CoNLL-U →
  `dcs_lemma_renou.json` (90,346 lemmas; gitignored); `enrich_renou_dcs.py` adds
  `renou_dcs`/`renou_enriched`/`renou_provenance` (`{state:["ls","dcs"]}`), per-lemma so it
  never overwrites the per-sense tag. **Scaled to all 120,173 PWG headwords** (45.4% DCS-hit;
  V 0→10,171) and **all 286,560 MW entries** from source (`tag_mw_from_source.py` →
  `mw_renou.jsonl`; 59.1% `<ls>`, 47.6% DCS; V 4,503 cite + 38,200 corpus, 1,508 by BOTH).
  `add_corpus_renou.py` (reusable GRETIL/IAST folder) applied to Skanda Revākhaṇḍa (redundant
  — DCS is built from GRETIL). **Extended to all 8 LS-rich dictionaries** (ranked csl-orig by
  `<ls>` richness): + AP, AP90, PW, BEN, SCH, BHS via `renou_sigla.py` (curated Apte/Benfey
  tables; BHS = default-V + meta blocklist) + `tag_dict_from_source.py` (generic; PW/SCH reuse
  PWG map) → `{code}_renou.jsonl` (gitignored), 360k more entries (BHS all-V 13,172; BEN 70% ls).
  **Two more signals (2026-06-24):** `bhs` — deterministic V transfer from Edgerton's BHS dict
  (`enrich_renou_bhs.py`, +23,337 new V across MW/PWG/AP, prov `bhs`); `wl` — wisdomlib tradition
  tags (`enrich_renou_wisdomlib.py` + SamudraManthanam `definitions.py`, **PR #15**; parser
  validated on real pages, 61-word high-yield sample, Cloudflare-gated per-IP). **Consolidated:**
  `renou_pipeline.py --all` → one canonical `{code}.renou.jsonl` per dict (8 dicts, **770,292
  entries**), documented in `RussianTranslation/RENOU.md`. **Editorial badge:** `renou_portrait.py`
  (Russian era label + first attestation + register-only-V flag, e.g. *viṣṇu* V=bhs-only). Per the
  DECISION **"Renou = badge, preserve PWG order"**, the badge is the editorial use — NOT sense
  reordering (the `order_senses_oldest_first` helper exists but is not the chosen path).
  Docs: CHANGELOG, RENOU.md, pwg_ru.md, mw_ru.md.

- **Corpus-gate index hygiene: 10 malformed join keys cleaned at source.** — _status: complete; date: 2026-06-23; source: current-session._
  The side task spawned by the NWS audit: `corpus_gate.load_index()` carried 10
  keys with a non-`[a-zA-Z]` initial. Traced to `build_src.py` blindly trusting
  upstream SamudraManthanam fields. Three classes — Kochergina scan-number/paren
  leak (`(nu`,`11`,`5)` → real headwords न्वै/अपकर्/स्कन्द), Frish Cyrillic↔Latin
  mojibake (`ргаdA`,`араnI`,`расб…`,`араbAD`,`араbrU`) + a multi-form/uppercase-`Ī`
  leak (`Īdfkza/…`), and a Smirnov Russian see-also (`РЕЧЬ … см. ↑`). Fix in
  `src/build_src.py`: a `bad_initial()` guard recovers the key from the row's
  first **pure-Devanagari** form (new `dev_to_slp1()`), else drops the row.
  Result: **4 recovered (3 koch + 1 fri), 6 dropped (5 fri + 1 smirnov)**; the 4
  re-united with their existing headword clusters (no legit headword lost). Total
  57 640 → 57 634. Verified: the audit one-liner now prints `[]`; jsonl BOM-free
  + valid. Rejected a strict SLP1 whitelist (over-flagged ~900 legit `ṁ`/IAST
  entries). README counts + format note updated. None carried an NWS fragment, so
  the attribution audit is unaffected. (`*.jsonl` are gitignored — fix lives in
  the committed `build_src.py`.)

- **NWS attribution audit COMPLETE across the whole SLP1 universe (m–z this run).** — _status: complete; date: 2026-06-23; source: current-session._
  Finished what b–l started: audited m,n,o,p,q,r,s,t,u,v,w,x,y,z via
  `nws_audit_section.py`, recording per-section bullets in `CHANGELOG.md` and
  rows + re-totalled Σ in `NWS_AUDIT_REPORT.md`. **Σ (a–z): 93,927 keys /
  23,886 NWS-bearing / 61,072 entries / 5 bleeds (0 residual) / 114 real
  (0.187%) / 0 parser bugs.** Classified every `?? OTHER` → two new
  known-limitation classes (multi-page cite, `(pw)` lowercase name), no code
  change. Corrected the errata: the `vṛtrakhādá` typo costs an owner in both
  the k and v section-fragments. Confirmed coverage exhaustive (10 non-SLP1
  outlier keys carry 0 NWS). Spawned a side task for those 10 junk index keys
  (index hygiene, separate from NWS). Erratum email to Halle now unblocked.
  Report `Status:` flipped to COMPLETE. Commits `004a952`→`9548efe`; pushed.

- **NWS section-audit sweep b–l + living documentation.** — _status: complete; date: 2026-06-23; source: current-session._
  Ran `nws_audit_section.py` per letter for **b–l** (a = original dev pilot).
  Cumulative: **32,348 keys → 8,591 NWS-bearing → 22,999 entries**; **5 bleeds**
  (all `Hillebrandt 1885 : IV` on `gam`/`kar`, owner correct, residual
  contamination 0 after the `nws_owner_map` debleed); **41 real no-owner =
  0.178%**, every one a known limitation (21 Meister `(2.1)`, 7 Böhtlingk
  `*NNN`, 6 roman page, 6 page-less x-ref) or **1 source-data typo** — the
  k-section `?? OTHER`, `vṛtrakhādá → … NṚV 2B : 79 (s. (2. khād )` = unbalanced
  trailing paren (siblings `amitrakhādá`/`vikhādá` with the same owner parse) →
  bad NWS input, NOT a parser gap, no code change. **Zero parser bugs found.**
  New tracked docs: living `RussianTranslation/NWS_AUDIT_REPORT.md` (roll-up +
  taxonomy + errata, linked from root `README.md`) and
  `RussianTranslation/src/NWS_AUDIT_HANDOFF.md` (per-letter runbook for m–z,
  updates BOTH changelog + report). Per-section bullets in
  `RussianTranslation/CHANGELOG.md`. A German erratum email to the Halle NWS
  team was drafted (hold until the m–z sweep finishes → one consolidated list).
  Commits `7bd4570`, `49f8a70`, `9a88ad2`, `c8f4f7f`, `97c5942`; pushed.

- **NWS attribution: F12 slide root-caused & gated; two audit false-positive/precision fixes.** — _status: complete; date: 2026-06-23; source: current-session._
  Audited all 10 NWS-bearing pilot cards with `nws_split.py check`; 8 CLEAN, 2
  flagged. (1) `ap` was a **false positive**: the cross-ref `apāṃ napāt → s.v.
  napāt` has no card row, and the locator `apāṃ` substring-matched
  `apāṃpitta`/`apāṃnidhi` — fixed by word-boundary locator matching
  (`located_in`). (2) `av` was a **real F12 slide** in the `+ upa` block (every
  gloss attributed to the source *preceding* it, not the one that *closes* it).
  Root cause traced through the pipeline: the deterministic layer
  (`compile_translatable` via `NS.split`) was already correct; the slide was
  introduced by the LLM assembly, **misled by an owner *bleed*** — a
  roman-numeral co-owner (`Rivelex (2) : XLV`) that `nws_split`'s digit-only
  OWNER can't tag rides onto the front of the next gloss's prose. Fix =
  `mask_nws_gloss` strips the leading bleed (fires only on the 5 real `av`
  cases); `nws_split.py` left untouched (parsing the roman cite there stuffs
  gloss text into the lemma field — tried and reverted). Also: OWNER name stops
  at `;` (trailing-tag sub-entries, `aYj`). Hand-corrected the slid `av` card
  (gitignored). **Defense in depth:** `run_real_test.py audit` is now a true
  gate — slid fresh cards are quarantined to `.merged.REJECTED.md` and the run
  exits non-zero (verified end-to-end). Commits `cb427d0`, `34bd924`, `b6a1b01`,
  `af9c333`; pushed. `selftest` + all 10 checks CLEAN.
- **Nonhuman/nonmax preflight bucket closed: audit runner hardened + missing a-card generated.** — _status: complete; date: 2026-06-19; source: current-session._
  (1) Pre-flighted the audit half of `src/pilot/run_real_test.py` with a synthetic
  `ap` `wf_output.json`: collect summary → protected card preservation → `nws_split.py
  check` → report. Result: publishable **1/1**, NWS audit **CLEAN 1**, F12
  misattribution **0**. (2) Removed the brittle `<key>.md` → `<key>.merged.md`
  bridge: `_pilot_collect.py` now writes `<safe>.merged.md` directly via shared
  `safe_filename.safe_name()`, and honors `PILOT_COLLECT_PROTECTED` so hand-authored
  `aMSa`/`anna`/`ap` cards are not overwritten. `run_real_test.py` now imports the
  same encoder (so `|` escapes correctly) and recursively finds workflow results.
  (3) Generated the previously-missing `arI|a` card as `ar_i~007ca.raw.txt` +
  `ar_i~007ca.portrait.json`; a-section pilot inputs are now **12,156/12,156**
  raw and **12,156/12,156** portraits. Verification: `py_compile`, synthetic `ap`
  audit, and `nws_split.py selftest` all pass.
- **June-22 prep refreshed with exact-case filename protection.** — _status: complete; date: 2026-06-19; source: current-session._
  Running `python src/pilot/run_real_test.py prep` exposed a Windows false-positive:
  `os.path.exists("Ap.merged.md")` could see `ap.merged.md`, making `Ap` look
  protected. Fixed `run_real_test.py` and `nws_split.py` legacy `.merged.md`
  fallback to require exact filename matches. Re-ran prep: batch `[0,10)` =
  `as As Ap api amfta agni Atman anu arjuna arTa`, all **10 fresh**, protected
  **0**. Re-ran `py_compile`, `nws_split.py selftest`, and synthetic protected
  `ap` audit: all pass.
- **Real-conditions pilot test + NWS format encode + June-22 prep.** — _status: complete; date: 2026-06-19; source: commit._
  (1) Validated the merged+NWS loop manually on a FRESH card `ap` (generate →
  translate per workflow conventions → `nws_split.py check` = **CLEAN**, 0
  misattributions). NWS fold surfaced genuine net-new material (verbal root
  ‘reach/attain’ = {#Ap#}; Pāṇinian kṛt-suffix {#a#}; juridical ‘water-ordeal /
  water-fort’) absent from PWG/PW. Test artifact `pilot/output/ap.merged.md`.
  (2) **Encoded the format lesson** as `run_pilot_wf.js` HARD RULE 5 (NWS = one
  entry per owner, `[NWS:]`-tagged, owner citation kept verbatim/last, F12 reading
  direction) — the first draft failed the audit purely on output format.
  (3) **June-22 trigger:** cloud routine was the WRONG tool (runs in cloud, no
  local gitignored data: pilot/input, corpus_lexicon 290MB, scraped nws; separate
  quota; GitHub not connected). User chose local prep → built
  `pilot/run_real_test.py` (`prep` selects batch + sets OFFSET/LIMIT;
  `audit <wf_output.json>` collect+nws_split+report; collector now writes
  `.merged.md` directly). On 2026-06-22 when Max resets:
  `python run_real_test.py prep` → run workflow on Max → `audit wf_output.json`.
- Released **v0.0.1** — _status: complete; date: 2026-06-18; source: tag._ ("NWS scrape + fold + scaled pilot"; tag + GitHub
  Release) — promoted changelog `[Unreleased]` → `[0.0.1]`. Scope: full NWS scrape
  (167,990, audit CLEAN), drift-validated LOW staleness dict-wide, folded as the 5th
  merge layer (`dict_merge`, commit `5d91f74`), and the merged+NWS pilot scaled to a
  manifest-driven resumable run (`_pilot_gen_merged.py --manifest/--limit`). NWS data
  stays gitignored. (NB: 0.0.1 starts a fresh version line below the repo's docs 1.x.)
- Shipped **v1.1.3** — _status: complete; date: 2026-06-15; source: tag._ (commits `96c33c9` fix + `662b031` changelog; tag
  + GitHub Release) — applied the same random-sample fix to `corpus_gate tune`
  (same seed as coverage). Random 4000-sample matches full-PWG (Jaccard ≥0.5
  ≈3.6% vs 3.7%); `n ≥ total` reports the full 106,085 / 2,585 pairs. Both
  `coverage` and `tune` now sample representatively.
- Shipped **v1.1.2** — _status: complete; date: 2026-06-15; source: tag._ (commits `29ef1e0` fix + `061330c` changelog; tag
  + GitHub Release) — `corpus_gate coverage` now uses a reproducible **random**
  sample, not first-N. First-N was alphabet-biased (a- section over-covered, esp.
  KOW): 3000-sample KOW read 39.8% vs the true full 8.0%. **True full-PWG coverage
  of 106,085 headwords: independent correctness ≈16%, KOW reference ≈8%, corpus
  ≈15%.** Random 3000-sample now matches the full run within ~1pt.
- **Validated the pwg_ru stage-4 corpus gate end-to-end and shipped v1.1.1** — _status: complete; date: 2026-06-15; source: tag._ (commits `085119d` fix + `a748a65` changelog; tag + GitHub Release).
  All four modes run (lookup/card/coverage/tune); data verified (5 dicts = 57,640
  entries); coverage on 3000-key sample = independent correctness 20.4%, KOW ref
  39.8%, corpus 20.7%; tune confirms THRESHOLD=0.5 (inter-dict agreement low, most
  cards route to LLM). **Found + fixed a real bug**: `corpus_examples()` returned
  0 verses for common headwords (agni/rāma/deva) because `corpus_lines` (FTS) also
  holds dictionary rows and the `LIMIT 400` ran before the `#sa` filter — pushed
  `#sa` into SQL; agni/rāma/kfzRa/deva now return 3 examples (was 0).
- Closed out the `HANDOFF.md` documentation gaps — _status: complete; date: 2026-06-14; source: commit._ (commit `652b580`) —
  gap 5 (kosha import) **decided "keep external"** (user): searched all sibling
  repos, `docs/dict/kosha_ai_translation.md` is not on disk (only unrelated
  `kosha_macrostructure.json` in csl-atlas), so importing would mean fabrication;
  it stays in the mw_ru working repo, documented in `mw_ru.md` §7. Rewrote the
  HANDOFF gaps section: all five closed, none open. (Gaps 1–4 shipped in v1.0.2.)
- **Wired SamudraManthanam into pwg_ru** — _status: complete; date: 2026-06-14; source: commit._ (samskrtam.ru parallel
  Sanskrit–Russian corpus tool). (1) Extracted the gate's 5 dictionaries from its
  bundled JSONL into `RussianTranslation/src/` (gitignored) via `src/build_src.py`
  — koch/kow/kna/fri/smirnov, ≈57,640 SLP1-keyed entries (commit `68245cd`).
  (2) Authored `RussianTranslation/SAMUDRA_INTEGRATION.md` (roadmap: PWG signal +
  mw_ru + WhitneyRoots + citation backbone) and revised `pwg_ru.md` §2/§3/§7 +
  `4_korpus_proverka.txt` to the 5-extracted-sources reality + a parallel-corpus
  usage/citation signal (soft, verse-level, reuses SamudraManthanam morph search;
  query-don't-duplicate). Built via workflow (author→review→fix); honesty review
  caught a stale "yandexgpt prompt not written" claim + FRI provenance, fixed.
  Lint-clean, 0 broken links. **KNA confirmed = Knauer.**
- **Committed & pushed the `pwg_ru` kit** — _status: complete; date: 2026-06-14; source: commit._ (commit `384fedb`, on
  `master`) — 7 files in `RussianTranslation/` + journal. Identified **KNA =
  Knauer** and named it across the kit; resolved the "what is KNA" open. Kit is
  the settled two-signal corpus-gate design; lint-clean, whitespace/BOM verified.
  Remaining PWG work is user-side provisioning of the Russian lexicography
  sources (Кочергина / Knauer / KOW → `RussianTranslation/src/`; added
  `src/README.md` documenting the drop contract).
- Released **v1.0.2** — _status: complete; date: 2026-06-14; source: tag._ ("documentation batch", commit `f876ed0`) —
  tag + GitHub Release. Promoted only the four committed doc items (HeadwordLists
  README, REFERENCES, README docs map, CONTRIBUTING expansion) to `[1.0.2]`; left
  the `pwg_ru` scaffold + Notes under `[Unreleased]` since its files are still
  uncommitted (user decision: release docs, keep pwg_ru unreleased).
- Expanded `CONTRIBUTING.md` — _status: complete; date: 2026-06-14; source: commit._ (commit `90dd2e5`) — formalised the
  data-change provenance expectation (source + transformation + counts/checksums)
  plus filename-count/BOM conventions, a Documentation-changes section, and a
  Hygiene section. Fourth HANDOFF doc gap closed; only kosha import remains.
- **Revised the `pwg_ru` corpus gate to the settled two-signal design** — _status: history; date: 2026-06-14; source: existing-entry._ (uncommitted). Rewrote `pwg_ru.md` §2/§3/§7, `4_korpus_proverka.txt`,
  and `pwg_ru_prompts/README.md`: stage 4 is a non-blocking annotator emitting
  (1) correctness vs independent S–R dicts (Кочергина/FRI/KNA) and (2)
  reference-agreement vs KOW (secondary, WIL-seeded); SKD/VCP Sanskrit-side only.
  Removed all "inert/blocked/KOW-only" wording; demoted external `csl-orig/...`
  paths to plain text; lint-clean, 0 broken links. changelog `[Unreleased]` Notes
  + this journal updated to match. Done across two sessions (resumed workflow
  `wf_799dc733-3d0` after a session-limit interruption).
- Added a "Documentation map" section to `README.md` — _status: complete; date: 2026-06-14; source: commit._ (commit
  `abcc97b`) — every doc grouped by purpose (Orientation / Contributors & agents
  / Material by area) with a one-line hook + link each. Third HANDOFF doc gap
  closed. (Deliverable committed; changelog + `.ai_state` bookkeeping held with
  the pwg_ru session's uncommitted edits per user decision.)
- Added `REFERENCES.md` — _status: complete; date: 2026-06-14; source: commit._ (commit `0a3537c`) — provenance for the root
  reference assets, read from each file's metadata (CDSL-2025.pdf = "Report on
  CDSL Project 2025", 2025-02-02; two DCS HTML exports; helpmorphids label set;
  the gasuns 2019 + WSC 2025 PDFs). Linked from README; covers the previously
  unlisted `WSC 2025 Reviews 7.pdf`. Second HANDOFF doc gap closed.
- Added `HeadwordLists/README.md` — _status: complete; date: 2026-06-14; source: commit._ (commit `5800139`) — index of the
  headword exports (naming scheme, key1/key2, variant patterns, BOM caveat, full
  16-code dictionary table). Resolved the two unknown CDSL codes via the CDSL
  site: PD = Encyclopedic Dictionary of Sanskrit on Historical Principles
  (Deccan College/Poona), CCS = Cappeller Sanskrit→German (companion to CAE =
  Cappeller Sanskrit-English). First HANDOFF doc gap closed.
- **Scaffolded the `pwg_ru` kit** — _status: history; date: 2026-06-14; source: existing-entry._ (PWG→Russian, mirroring `mw_ru`) —
  7 files in `RussianTranslation/`: `pwg_ru.md` (editor doc) + `pwg_ru_prompts/`
  (README, `1_perevod.txt` with full DE→RU glossary + mw_ru-seed, two QA judges,
  `3_pereperevod_opus.txt`, new `4_korpus_proverka.txt` corpus gate). User
  decisions baked in: direct German→Russian seeded with mw_ru; pipeline identical
  to mw_ru + a corpus-check stage; lives here, data external. Lint-clean (no BOM,
  no trailing WS, EOF newline), 0 broken links, framed as planned/not-run.
  Logged in changelog `[Unreleased]`. **Uncommitted** pending corpus decision.
- Added `HANDOFF.md` — _status: complete; date: 2026-06-14; source: commit._ (commit `e4a4c59`) — doc-development orientation
  for the next agent: repo nature, conventions (links, language-matched indexes,
  external-refs-as-plain-text, BOM caveat, release flow), link-sweep verification
  recipes, and five open doc gaps (HeadwordLists README, reference-file
  provenance, docs map, CONTRIBUTING, kosha import). Same commit removed the empty
  changelog `[Unreleased]` header left after the 1.0.1 cut.
- Released **v1.0.1** — _status: complete; date: 2026-06-14; source: tag._ (commit `520eea2`) — repo's first tag/release.
  Promoted changelog `[Unreleased]` → `[1.0.1] - 2026-06-14`, created annotated
  tag `v1.0.1`, and published a GitHub Release ("documentation pass") with the
  1.0.1 changelog section as notes. NB: `1.0.0` in the changelog remains
  untagged (user chose v1.0.1 only); a fresh empty `[Unreleased]` was left on top.
- Recorded the recent doc work in `changelog.md` — _status: complete; date: 2026-06-14; source: commit._ (commit `e30b5f5`):
  `[Unreleased]` Added entries for the Syntax-Lectures README and mw_ru section 7
  appendix, plus a Fixed subsection for the demoted dead links + prompt index.
- Added `Syntax-Lectures/README.md` — _status: complete; date: 2026-06-14; source: commit._ (commit `97c7661`) — Russian
  index of the particle materials (lectures, Zaliznyak schema, interactive
  explorer, 7 Apte_1957-*_RU.md entries). Closes the last structural doc gap
  from the `.md` sweep; Next Steps now empty.
- Swept all 19 tracked `.md` for gaps and added an external-documents appendix to `mw_ru.md` — _status: complete; date: 2026-06-14; source: commit._ (commit `bd3e329`): new section 7 tables the six
  working-repo files cited from the mw_ru docs (`kosha_ai_translation.md`,
  `improvements.md`, `yandex_api.md`, two glossary JSONs, QA scripts). Sweep
  result: no broken relative links anywhere; remaining file mentions are
  intentional external/cross-repo refs (ROADMAP cites csl-atlas/standards
  artifacts as bare text — correct as-is) or documented absences in `CLAUDE.md`.
- Fixed mw_ru doc inconsistencies — _status: complete; date: 2026-06-14; source: commit._ (commit `4036d45`): demoted four
  dead external links to plain text (`improvements.md`, `docs/yandex_api.md` in
  `qa_judge_v4.md`; two glossary JSONs in `mw_ru.md`) and indexed `qa_judge_v4.md`
  in the prompts `README.md` as a proposed v4 judge update. All relative links in
  `RussianTranslation/` now resolve.
- Earlier session housekeeping (now superseded) — _status: history; date: 2026-06-14; source: existing-entry._ wrote repo-level
  `CLAUDE.md` (on `master` via PR #13), logged it in `changelog.md`, and deleted
  the fully-merged `feature/particles-explorer` branch (remote + local).
