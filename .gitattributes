# Normalize text files to LF in the repository and working tree.
* text=auto eol=lf
# Common source, docs, and generated data files.
*.cff text eol=lf
*.css text eol=lf
*.csv text eol=lf
*.html text eol=lf
*.js text eol=lf
*.json text eol=lf
*.md text eol=lf
*.py text eol=lf
*.sh text eol=lf
*.tsv text eol=lf
*.txt text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
# Binary assets (never normalize or diff as text).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
*.gz binary
*.bz2 binary
*.sqlite binary
*.ifo binary
*.idx binary
*.dz binary

# --- non-Unicode-font document (H2173 follow-up, 04-08-2026) ---
# Measured, not assumed: 5,452 control codepoints across 3,818 runs (~35 KB, 1.4% of the
# file). They are NOT one defect. Figure 7.2's axis labels were a symbol font offset by
# exactly +32 and have been decoded to real text. The rest is largely comparative phonetic
# transcription (Tibeto-Burman cognate tables) rendered through non-Unicode fonts, plus at
# least one block on a DIFFERENT offset - each needs its own font mapping, so a blanket
# transform would corrupt them. Re-extraction was tested and rejected: PyMuPDF's text for
# the caption page does not contain the axis labels at all, so it would delete content
# rather than repair it. git therefore still reads this file as binary; declared so that
# behaviour is on the books. A real repair is a re-typesetting/OCR project.
"literature/md/Lexicography-Manuals/THE ROUTLEDGE HANDBOOK OF HISTORICAL LINGUISTICS.md" binary
