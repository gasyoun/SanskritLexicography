Consumer half of [H5282](https://github.com/gasyoun/Uprava/blob/main/handoffs/H5282-Opus_csl-pyutil_nkrya-client-shared-module_23.09.26.md). The client moved to csl-pyutil in [sanskrit-lexicon/csl-pyutil#48](https://github.com/sanskrit-lexicon/csl-pyutil/pull/48) (merged, `b1606944fe8d9bbb1c3f5447404b6ecdb58ed993`) per MG's ruling of 23-09-2026 ([GRILL_NKRYA_USES_ROUND2_DECISIONS_23-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/docs/GRILL_NKRYA_USES_ROUND2_DECISIONS_23-09-2026.md)), so RuWritingStyles, CommentaryStrategies and Systema consume one implementation instead of copying this file.

## Changed

- [`RussianTranslation/src/nkrya_client.py`](https://github.com/gasyoun/SanskritLexicography/blob/h5282-shim/RussianTranslation/src/nkrya_client.py) — 389 lines down to a shim holding only the repo-local halves: `DEFAULT_CACHE` (`pwg_ru/nkrya_cache`) and `FIXTURES` (`src/fixtures/nkrya`), a `NkryaClient` subclass defaulting to that cache, and a re-export surface so `import nkrya_client as nk` still resolves `nk.rank_in`, `nk.NkryaError`, `nk.FIXTURES`, `nk.NkryaClient`. A missing `csl_pyutil.nkrya` fails closed with the install line, never a silent fallback.
- Both csl-pyutil pins move together (the H2889 / #1802 rule): [`requirements.txt`](https://github.com/gasyoun/SanskritLexicography/blob/h5282-shim/requirements.txt) and [`.github/workflows/ci.yml`](https://github.com/gasyoun/SanskritLexicography/blob/h5282-shim/.github/workflows/ci.yml) now name the csl-pyutil 0.25.0 merge commit.
- Changelog-queue entry [`2026-09-24-010000-h5282-nkrya-shared-module.md`](https://github.com/gasyoun/SanskritLexicography/blob/h5282-shim/RussianTranslation/changelog_queue/2026-09-24-010000-h5282-nkrya-shared-module.md).

Inherited from upstream: a token-bucket throttle (6 live requests/min, burst 3, `rate_per_min=`/`burst=` or `NKRYA_RATE_PER_MIN`/`NKRYA_BURST`) and `Retry-After`-aware 429 backoff with up to five retries — live on 23-09-2026 the API refused after roughly ten calls in a minute, where this client slept 1 s flat, so a batch run no longer dies on one 429.

## Unchanged

[`src/nkrya_evidence_card.py`](https://github.com/gasyoun/SanskritLexicography/blob/h5282-shim/RussianTranslation/src/nkrya_evidence_card.py) is untouched, the CLI is verbatim, the fixtures stay in this repo, and a cache written by the old client is still read by the new one (identical request hashing).

## Checks

With csl-pyutil 0.25.0 on `PYTHONPATH`, the two commands the CI lane runs:

- `python src/nkrya_client.py --selftest` — PASS: `nkrya_client (shim -> csl_pyutil.nkrya) selftest OK (10 checks, offline fixtures: .../src/fixtures/nkrya)`
- `python src/nkrya_evidence_card.py --selftest` — PASS (3 checks, offline)
- `python src/nkrya_client.py freq кошка S --offline --cache src/fixtures/nkrya` — `{"ipm": 44.0418, "category": 3}`

## Risks

The shim hard-requires the new pin; a stale local venv needs `pip install -r requirements.txt` once. No live API run is included: neither `RUSCORPORA_API_TOKEN` nor a `ruscorpora-api` keyring entry exists on the executing box (both probed empty), so the 429 path is proven offline by injected refusals in the upstream tests.

## Inspect

The shim file first, then the two pin lines.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
