# AGENTS.md — SanskritLexicography

This file orients Codex and other non-Claude agent sessions. The section between the generated-block markers is machine-owned (regenerated from Uprava; hand edits there are overwritten) — add repo-curated content OUTSIDE the markers.

[generated-block: repo-context — do not hand-edit; regenerated by Uprava tools/gen_repo_context.py; content-hash: 1d95c5ef92]
## Shared org context (generated — H1393 layer)

- Canonical Sanskrit-domain facts (encodings, key1/key2, corpus gaps, transcoder traps): read the [Sanskrit context primer](https://github.com/gasyoun/github-spine/blob/main/SANSKRIT_CONTEXT_PRIMER.md) before touching encodings, transliteration, or corpus data.
- The curated context of record for this repo is [CLAUDE.md](https://github.com/gasyoun/SanskritLexicography/blob/master/CLAUDE.md) — read it in full. Its sections: What this repository is · HeadwordLists/ — naming and key semantics · Encoding — BOM is inconsistent, check before editing · RussianTranslation/ — mw_ru · RussianTranslation/ — pwg_ru (PWG→RU/EN, a separate pipeline from mw_ru above) · Authoring conventions · Operational hazard notes.
- Danger facts (destroy-work-if-missed; full registry with sources in the org-private hub):
  - VisualDCS/src/dcs_full.sqlite is a 0-byte DECOY; the real 921 MB DCS DB (IAST lemmas) is VisualDCS/src/DCS-data-2026/dcs_full.sqlite ([source](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H848-Sonnet_kosha_dcs_reading_packs_nala_gita_13.07.26.md))
  - The Bhagavadgita (MBh 6.23-40) is ABSENT from DCS; Nala (MBh 3.50+) is present — never assume canonical-text coverage ([source](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H848-Sonnet_kosha_dcs_reading_packs_nala_gita_13.07.26.md))
  - HeadwordLists exports have INCONSISTENT UTF-8 BOMs (MW-unique-key1 HAS one, its key2 sibling does NOT) — check before transforming, preserve BOM state on write ([source](https://github.com/gasyoun/SanskritLexicography/blob/master/CLAUDE.md))
  - pwg_ru store/TM are gitignored local-only; resolve the store ONLY via src/pilot/store_path.py canonical_store — worktree drains silently dropped promotions before that fix (H805) ([source](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H805-Opus_SanskritLexicography_pwg_ru_worktree_store_persistence_fix_12.07.26.md))
  - h1339_offline_bench.py is NOT hermetic — run in the main checkout it freezes fixture rootmaps into live src/pilot/input and appends to live dashboard_events; run only in a throwaway worktree ([source](https://github.com/gasyoun/Uprava/blob/main/handoffs/H1386-Fable_RussianTranslation_pwg-ru-post-h1339-resume-fixes-prepare-speed_20.07.26.md))
  - No-PWG windows degrade under ~10-wide concurrency on a slow API — requeue nulls with gen_opt_harness2 --max-wide=3 --stagger-ms=2000 (H811) ([source](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H811-Opus_SanskritLexicography_pwg_ru_lowwide_staggered_requeue_dispatch_12.07.26.md))
  - csl-orig + all Cologne upstreams are fenced: no direct commits/PRs; corrections only via the queued monthly batch-PR path ([source](https://github.com/gasyoun/github-spine/blob/main/CLAUDE.md))
  - sanskrit-util iast_to_devanagari is BROKEN (9/9 basic words wrong) — use the composition to_slp1() -> slp1_to_devanagari() instead ([source](https://github.com/gasyoun/github-spine/blob/main/SHARED_CODE.md))
  - git pull / rebase --autostash in a shared-contention clone can swallow another session's uncommitted work — prefer git fetch + a fresh worktree ([source](https://github.com/gasyoun/claude-config/blob/main/hooks/pre_shell_guards.py))
- Further operational hazard notes live in the org-private hub ([Uprava DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md); org members only).
- Cross-repo data edges (from [Uprava interlinks_edges.tsv](https://github.com/gasyoun/Uprava/blob/main/interlinks_edges.tsv)):
  - feeds → `SanskritRussian`: corpus_lexicon.jsonl → 3-layer Sa→Ru glossary (live)
  - feeds → `SanskritKaraoke`: corpus-aligned RU glosses baked into verses/data/*.json (live)
  - feeds → `kosha`: union_headwords + mw_roots + mw_etymology in data-v0.1.0 release (live)
  - feeds → `SanskritSpellCheck`: HeadwordLists/union/union_headwords.tsv attestation gate (live)
  - ← from `csl-guides` (vendors): mw_heritage_crosswalk aggregates → src/data/heritage-coverage.json (live)
  - ← from `VisualDCS` (feeds): frequency-first pwg_ru worklist ordering (live)
  - ← from `VisualDCS` (feeds): indische_sprueche.jsonl (7,537 sayings mirror) (live)
  - ← from `SamudraManthanam` (feeds): gate dicts + verse-aligned corpus_builder JSONL → L0/TMX + name glossaries (live)
  - ← from `telegram-sanskrit-corpus` (feeds): data/derived/headword_candidates.jsonl (queued)
  - ← from `ext:Heritage` (feeds): darkone23 mirror → mw_heritage_crosswalk.tsv (185,803 MW entries) (live)
  - ← from `PWG` (feeds): source dictionary text for the pwg_ru / pwg_en translation kit (live)
- Gitignored/off-git canonical assets are registered as pointer rows in the org-private [shadow-assets registry](https://github.com/gasyoun/Uprava/blob/main/SHADOW_ASSETS_POINTERS.md).

[/generated-block]
