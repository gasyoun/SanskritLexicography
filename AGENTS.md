# AGENTS.md — SanskritLexicography

This file orients Codex and other non-Claude agent sessions. The section between the generated-block markers is machine-owned (regenerated from Uprava; hand edits there are overwritten) — add repo-curated content OUTSIDE the markers.

[generated-block: repo-context — do not hand-edit; regenerated by Uprava tools/gen_repo_context.py; content-hash: 9469393f7d]
## Shared org context (generated — H1393 layer)

- Canonical Sanskrit-domain facts (encodings, key1/key2, corpus gaps, transcoder traps): read the [Sanskrit context primer](https://github.com/gasyoun/github-spine/blob/main/SANSKRIT_CONTEXT_PRIMER.md) before touching encodings, transliteration, or corpus data.
- Task routing: [CLAUDE.md](https://github.com/gasyoun/SanskritLexicography/blob/master/CLAUDE.md) — open the matching section; do not read the file end-to-end. Sections: What this repository is · HeadwordLists/ — naming and key semantics · Dual changelog — shared 1.144.x namespace (H3258) · Encoding — BOM is inconsistent, check before editing · RussianTranslation/ — mw_ru · RussianTranslation/ — pwg_ru (PWG→RU/EN, a separate pipeline from mw_ru above) · Authoring conventions · Agent skills · Operational hazard notes.
- Danger facts (destroy-work-if-missed; full text + sources in [DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md)):
  - VisualDCS/src/dcs_full.sqlite is a 0-byte DECOY; the real 921 MB DCS DB (IAST lemmas) is VisualDCS/src/DCS-data-2026/dcs_full.sqlite
  - The Bhagavadgita (MBh 6.23-40) is ABSENT from DCS; Nala (MBh 3.50+) is present — never assume canonical-text coverage
  - citation_tm.lookup(lang='en') returns a WRONG-HYMN status='hit' for RV 8.49-8.103 (678/10552 stanzas, 6.4%) — the valakhilya block shifts Griffith 1896 against its own key, and the call site gets no signal. No live consumer yet (corpus_gate defaults lang='ru'). Do NOT wire the EN lane until _canonical_to_griffith_loc refuses mandala 8 >=49 as a typed miss; verify with audit_griffith_en_alignment.py --selftest (exits 1 today)
  - HeadwordLists exports have INCONSISTENT UTF-8 BOMs (MW-unique-key1 HAS one, its key2 sibling does NOT) — check before transforming, preserve BOM state on write
  - pwg_ru store/TM are gitignored local-only; resolve the store ONLY via src/pilot/store_path.py canonical_store — worktree drains silently dropped promotions before that fix (H805)
  - h1339_offline_bench.py is hermetic from v1.54.0 / PR #667 via PWG_INPUT_DIR and PWG_EVENTS_PATH sandboxes + teardown; older revisions can still pollute live src/pilot/input and dashboard_events
  - No-PWG windows degrade under ~10-wide concurrency on a slow API — requeue nulls with gen_opt_harness2 --max-wide=3 --stagger-ms=2000 (H811)
  - A SECOND CLAUDE_CONFIG_DIR bypasses the one-active-call guard: a duplicate profile fingerprints differently, so ActiveCallClaim takes a DIFFERENT kernel lock while billing the SAME account. The H2189 minimal-profile twin was rejected as a cost lever (-8.7% vs --safe-mode's -88%) and is offline-diagnostic only — never point a duplicate profile at a paid run without its own roster slot
  - pdftotext blanks Cyrillic while returning plausibly-shaped output - never poppler on Russian PDFs.
  - Deleting opencode.db deletes all OpenCode chats - it is the session store, never treat as disposable.
  - Harness session stores are credential-bearing (sk-shaped keys in chat text) - treat all four stores as secret surfaces.
- Further operational hazard notes live in the org-private hub ([Uprava DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md); org members only).
- Cross-repo data edges (from [Uprava interlinks_edges.tsv](https://github.com/gasyoun/Uprava/blob/main/interlinks_edges.tsv)):
  - feeds → `SanskritRussian`: corpus_lexicon.jsonl → 3-layer Sa→Ru glossary (live)
  - feeds → `SanskritKaraoke`: corpus-aligned RU glosses baked into verses/data/*.json (live)
  - feeds → `kosha`: union_headwords + mw_roots + mw_etymology in data-v0.1.0 release (live)
  - feeds → `SanskritSpellCheck`: HeadwordLists/union/union_headwords.tsv attestation gate (live)
  - produces → `*`: RussianTranslation translation memory / TM-TMX lanes (build_l0.py; RU+oral registers) (live)
  - feeds → `kosha`: pwg-sense-attestation-window (PWG per-sense attestation window, Ceiling C2 phase 1) (live)
  - feeds → `csl-atlas, RussianTranslation`: heritage phase-6 witness cross-validation: heritage_phase6_crossval.md + h3171_results.jsonl + classified disagreement TSV (Heritage sktreader vs pinned local DharmaMitra ByT5, over the H1349 adjudicated glossary sample) (live)
  - vendors → `sanskrit-util`: German metalanguage detector classify_german_metalanguage + exported inventories (sanskrit-util v0.6.0, H2876) via the vendored shim RussianTranslation/src/sanskrit_util.py + _sanskrit_util_vendored.py (re-synced by /cologne-sanskrit-util-sync) (live)
  - ← from `csl-guides` (vendors): mw_heritage_crosswalk aggregates → src/data/heritage-coverage.json (live)
  - ← from `VisualDCS` (feeds): frequency-first pwg_ru worklist ordering (live)
  - ← from `VisualDCS` (feeds): indische_sprueche.jsonl (7,537 sayings mirror) (live)
  - ← from `SamudraManthanam` (feeds): gate dicts + verse-aligned corpus_builder JSONL → L0/TMX + name glossaries (live)
  - ← from `telegram-sanskrit-corpus` (feeds): data/derived/headword_candidates.jsonl (queued)
  - ← from `ext:Heritage` (feeds): darkone23 mirror → mw_heritage_crosswalk.tsv (185,803 MW entries) (live)
  - ← from `PWG` (feeds): source dictionary text for the pwg_ru / pwg_en translation kit (live)
  - ← from `ext:sanskrit-lexicon-scans` (feeds): ramayana-edition-alignment (scan-backed edition alignment feed) (live)
  - ← from `pwg-ru-data` (feeds): pwg_ru_nonstop_lane_store (live)
  - ← from `gasyoun.github.io` (vendors): 2,110 static per-adhyaya pages (live)
  - … 3 more edges in the TSV.
- Gitignored/off-git canonical assets are registered as pointer rows in the org-private [shadow-assets registry](https://github.com/gasyoun/Uprava/blob/main/SHADOW_ASSETS_POINTERS.md).

[/generated-block]

## Repo-curated orientation (hand-owned - outside the generated block)

| Read when | Surface |
|---|---|
| Touching PWG sense segmentation, enumeration markers, or anything that re-splits a `pwg_ru` card | [RussianTranslation/src/microstructure.py](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/microstructure.py) is the ONE sanctioned reading of PWG's **four** enumeration tiers (H3948) — never re-derive a tier from a marker's shape. A marker whose tier is genuinely ambiguous in the printed source is left unsplit and counted as an unresolved class, never guessed. Changing a tier rule ⇒ re-run [microstructure_four_tier_selftest.py](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/microstructure_four_tier_selftest.py) and re-measure in the same PR. |
| Asked "should we re-segment the store?" | Already measured, without writing a row: [RussianTranslation/src/pwg_four_tier_store_impact.py](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pwg_four_tier_store_impact.py) → [reports/H3948_four_tier_store_impact.json](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3948_four_tier_store_impact.json) — **3,212 of 11,462 `pwg_ru` rows affected (28.02 %)**, of which 12 are precisely resolvable, 990 unaffected and 2,210 unresolvable. The answer is a **range of 12–3,212 rows**, and the re-translation call belongs to a human ([FINDINGS.md](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md) §453). **No row has been rewritten.** |
| Mapping a Russian-language name index onto Sanskrit keys | Use the lookup table [RussianTranslation/data/cyrillic_proper_noun_slp1.tsv](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/data/cyrillic_proper_noun_slp1.tsv) (534 rows, IAST-witness-derived, `rule_derived_keys: 0`) — never invent Cyrillic→SLP1 character rules (FINDINGS §60 / §629, GAPS §6). Rebuild with [h3985_cyr_slp1_table.py](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/tools/h3985_cyr_slp1_table.py) and refresh [H3985_cyr_slp1_validation.json](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3985_cyr_slp1_validation.json) in the same PR. |
