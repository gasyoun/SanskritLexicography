# H1110 — post-H1080 execution-path audit memo

_Created: 17-07-2026 · Last updated: 17-07-2026_

**Audit only — no code was patched on this branch.** Executor: Opus 4.8 (`claude-opus-4-8[1m]`).
Base commit: [`11b16963`](https://github.com/gasyoun/SanskritLexicography/commit/11b169633864e074daf77212ce3a8815ee7cdc13)
(`origin/master` at audit time). Every claim below is bound to that base commit and to the
artifact hashes recorded in §1. Historical H963 measurements are preserved, not reinterpreted.

Handoff: [H1110](https://github.com/gasyoun/Uprava/blob/main/handoffs/H1110-Opus_SanskritLexicography_pwg-ru-post-h1080-audit-fix-skills-c4-restart_17.07.26.md).

---

## 1. Immutable baseline, independently remeasured

Recomputed from the canonical store and TM at audit time, not copied from the handoff.

| Invariant | Handoff expected | Measured | Verdict |
|---|---|---|---|
| store rows | 11,603 | 11,603 | ✅ |
| raw `{Tn}` rows | 0 | 0 | ✅ |
| null record `h` | 0 | 0 | ✅ |
| synthetic controls in store | 0 | 0 | ✅ |
| TM SHA-256 | `121d56fc…b7b4ab` | `121d56fc…b7b4ab` | ✅ |
| store SHA-256 | `f15caf7d…77b2f9` | `763b844b…deb40d` | ⚠️ drift — **explained + proven** |

### 1.1 The store drift is proven to be exactly PR #517, and is not a blocker

The handoff pins the pre-#517 store hash. [PR #517](https://github.com/gasyoun/SanskritLexicography/pull/517)
(commit `b7b3ebf8`, owner-authorised) stamped additive provenance markers onto the 468
identity-derived headwords **after** that baseline was recorded. The drift is not merely
*consistent with* #517 — it is proven:

```
sha256(src/pwg_ru_translated.jsonl.pre-hstamp.20260717T094406Z.bak)
  = f15caf7d1f65db0269498c50ecdcd6bf098572a543c12a055ae08c374377b2f9
  = the handoff's "expected" store hash, byte-for-byte
```

The pre-stamp backup **is** the handoff's expected baseline. Marker counts match #517's report
exactly (`h_reconstructed` 468, `iast_reconstructed` 462, `grammar_defaulted_empty` 468), and an
independent positional diff of live-store vs. pre-stamp backup shows `h` changed: 0,
`generator` changed: 0, `generated_at` changed: 0.

**Ruling:** the forward baseline is `763b844b…deb40d`. The handoff's `f15caf7d…` is historical.
This is an explained delta, not the "unexpected store drift" that blocks paid execution.

### 1.2 A measurement trap worth recording

`h == ""` (empty string) holds for **393** rows (`pw` 106 + `pwg` 287). These are a **separate,
pre-existing population** with **zero** overlap with the 468 reconstructed rows. Conflating
`h is None` with `h in (None, "")` produces a false "393 null headwords" alarm against an
invariant that in fact holds. The handoff's invariant is `h is None == 0`, and it holds.

---

## 2. Handoff defects found at audit time

The handoff is authoritative on intent, not on fact. Three of its premises are wrong at base
`11b16963`. Recorded here so Phase 2 does not implement fiction.

| # | Handoff claim | Reality at base commit | Consequence |
|---|---|---|---|
| D-1 | Phase 2 item 10: add the 468-row `h` reconstruction provenance marker | **Already delivered** by PR #517 (`src/mark_reconstructed_headwords.py`), applied to the live store | Item 10 is a no-op; re-implementing risks re-writing owner-authorised rows |
| D-2 | Phase 5: run `python src/repair_pwg_ru_store_selftest.py` | **No such file has ever existed** in git history; no `*repair*` script exists in `RussianTranslation/` | The mandated gate is unrunnable as written; nearest real gate is `src/backfill_tn_residue_selftest.py` |
| D-3 | Phase 5: "expected definition count is 131" | master runs **135** | Handoff self-corrects ("trust current master if the fix adds tests") — no action beyond recording |

PR #510's repair shipped as reports under
[`pwg_ru/h1080/`](https://github.com/gasyoun/SanskritLexicography/tree/master/RussianTranslation/pwg_ru/h1080),
not as a `repair_pwg_ru_store.py` module. D-2 is a naming fiction, not a missing deliverable.

---

## 3. Phase 6 — named stop: `STOP_PROFILE_UNBINDABLE`

**The paid c4 ladder was not entered, and no paid call was made.** This is a measured stop, not
an omission.

Phase 6 requires every call to carry an exact c4 profile fingerprint ("c4 only, exact
manifest/profile fingerprint"; Call 3 requires `provenance_class=real` + c4 fingerprint). The
executing session has **`CLAUDE_CONFIG_DIR` unset**, so it runs on the default profile, not
`D:\ClaudeTools\profiles\claude4\.claude`. Any call issued from here would be **mis-attributed**,
which is the brief's own manifest-provenance hard stop.

This is the identical, already-documented reason the H963 campaign fired **zero** live calls
(H963 §"Why zero calls", reason 1). The condition is unchanged. The handoff's own guardrails
settle the ruling:

> A confirmed P0 that cannot be tested safely is a legitimate stop, not permission to spend
> around it.

> A supplied `GO` cannot override measured policy or `gate_reason`.

**Terminal verdict for Phase 6 from this session: `STOP_PROFILE_UNBINDABLE`.** Not NO-GO (nothing
was measured about c4's health), not PRODUCTION_GO. Re-entry requires a session launched with
`CLAUDE_CONFIG_DIR` bound to the c4 profile — a human action, since it selects which account is
billed. Phases 1–5 are the prerequisites and are unaffected.

---

## 4. Execution-route parity — declared vs validated vs enforced

**Headline: the manifest's entire `budgets{}` block is write-only.** A grep for any read of
`budgets` across `RussianTranslation/src/` returns **zero matches**. It is emitted
(`gen_opt_harness2.py:1386-1393`), carried through the coordinator, hashed, validated-adjacent and
promoted against — and never read by the component that makes the model call.

The mechanism is the same structural trap R3 identified. Those ceilings are enforced only in the
generated JS harness. But manifest v2 hardcodes `execution_route: 'claude-cli-headless'`
(`:1344`) and refuses to generate any other route (`:1336-1337`). On that declared route the
executor is `headless_worker.py`, which reads `manifest['runtime']` but never `manifest['budgets']`.
**The safety rails minted after the pril10_w1 blow-up (H189, $79.83 for 3 cards) are inert on the
only route the manifest permits.**

| Field | Declared | Validated | Enforced | Verdict |
|---|---|---|---|---|
| `execution.profile_slot` | `:1342` | `execution_contract.py:29-34`, `:52-54` | `headless_worker.py:624-626` | ✅ enforced |
| `execution.config_dir_fingerprint` | `:1343` | `execution_contract.py:35-36`, `:55-58` | `headless_worker.py:624-625` | ✅ enforced |
| model identifier | `:1356`, `:1347` | `execution_contract.py:45-46` | `headless_worker.py:321` (`--model`) | ✅ enforced ¹ |
| `runtime.whole_attempts`/`fragment_attempts` | `:1383-1384` | none | `headless_worker.py:364`, `:407` | ✅ enforced |
| `runtime.binary_split` | `:1378` | none | `headless_worker.py:386` | ✅ enforced |
| `runtime.kill_timeout_no_bisect` | `:1382` | `window_selftest.py:4511` | `headless_worker.py:435` | ✅ enforced |
| `runtime.per_card_heal_*` | `:1379-1381` | `window_selftest.py:4455` | `headless_worker.py:411`, `:436`, `:449-454` | ✅ enforced |
| `meta.selected_keys` | `:1258` | `execution_contract.py:37-40`, `:64-66`; `coordinator.py:657`; `promote_final_cards.py:205` | `max_account_orchestrator.py:408-412` | ⚠️ enforced ² |
| `key_provenance` / `provenance_class` | `:1349-1351`, `:1358` | `execution_contract.py:37-44` | `promote_final_cards.py:224-228` — **promotion only, post-call** | ⚠️ validated-not-enforced ³ |
| **`budgets.max_translate_agents` / `max_heal_agents`** | `:1389-1390`; `agent_budget.py:125-133` | `window_selftest.py:4237` — against the **JS** `META`, not behaviour | **NOT ENFORCED** — `headless_worker.py:324-326` counts, never compares | ❌ validated-not-enforced |
| **`budgets.timeout_ceil_ms`** | `:1387-1388` | **none** — `validate_manifest` never inspects `budgets` | **NOT ENFORCED** — `:329` uses `--timeout`, default 7200 s | ❌ declared-only ⁴ |
| **`budgets.max_wide`** | `:1391`, `:219` | none | **NOT ENFORCED** — `run_all` (`:536`) is a serial loop | ❌ declared-only ⁵ |
| **`budgets.stagger_ms`** | `:1392` | none | **NOT ENFORCED** — never read | ❌ declared-only |
| `execution.execution_route` | `:1344` | `execution_contract.py:29-34` — **non-empty string only; value never checked** | **NOT ENFORCED at runtime** | ❌ validated-not-enforced ⁶ |
| `execution.executor_lane` | `:1345` | `execution_contract.py:29-34` | **NOT ENFORCED** — never read | ❌ validated-not-enforced |
| `execution.validation_method` | `:1346` | `execution_contract.py:29-34` | **NOT ENFORCED** — never read | ❌ validated-not-enforced |

¹ The CLI receives `manifest['model']`; `execution.model_identifier` binds only transitively via the `:45-46` equality check.
² Enforced as a *pre-call gate*, **not as the call driver**: calls are driven by `manifest['batches']` (`:536`) and `presplit_keys` (`:549`). **Nothing checks `batches ⊆ selected_keys`**; `:576-579` is post-hoc reconciliation. This is a hole R8 does not cover.
³ Enforcement is strictly downstream of the call. Nothing before/around the call treats a `synthetic_control` key differently — headless translates and **bills** it identically; only promotion refuses it. Phase 6 Call 2's "mechanically prove it cannot be promoted" therefore holds, but the synthetic control is still paid for.
⁴ Declared `KILL_CEIL_MS = 180000` ("NOTHING runs past 3 min (MG)", `:185`). Actual bound 7200 s — **40× looser than declared**.
⁵ H963 recorded the real consequence: two manifests launched concurrently and `max_wide=1` **did not serialise them**, invalidating that campaign's throughput/economy figures.
⁶ The generation-time route refusal is birth control, not a runtime gate — a hand-edited v2 manifest declaring another route still executes unmodified through `headless_worker.py`.

### 4.1 This is an unclosed finding, not a fresh discovery

[`H963_C4_PIPELINE_CALL_GRAPH_2026-07-16.md:241-243`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h963/H963_C4_PIPELINE_CALL_GRAPH_2026-07-16.md)
reached the same conclusion independently in July 2026 (D15: the only component that *executes* a
manifest reads **none** of the budget fields). Its line numbers have since drifted; the table above
uses current lines. Treat this as a finding that has persisted unclosed, not as new information.

### 4.2 The validation layer measures the wrong artifact

`window_selftest.py:4233` parses `const META` out of the *generated JS* to assert the agent budget
is well-formed. That check passes whether or not the executing route honours it — which is exactly
how `max_agents` became validated-but-unenforced. **The tests pin the declaration, not the
behaviour** (same class as N-2). The JS path is not dead code — `coordinator.prepare` emits both
harness and manifest — which is why the gap hid: the fields *are* live if a human runs the Workflow
harness, and inert on the bounded-run path every coordinator-imported lease actually takes.

---

## 5. H963 C-01…C-59 reconciliation

_(filled from the reconciliation passes)_

---

## 6. Seeded-risk verification

Each of the handoff's eleven seeded risks was verified against code at base `11b16963` by an
independent pass instructed to return REFUTED rather than invent a mechanism. Verdicts are bound
to this base commit.

**Headline: 6 of the 11 seeded risks are materially wrong as stated in the handoff.** Two are
refuted outright; four are real but mis-framed (wrong evidence, inverted impact, or wrong scope).
A seeded-risk list is a hypothesis set, not a finding set — this is the audit doing its job.

| ID | Seeded risk | Verdict | Severity | Correction to the handoff's framing |
|---|---|---|---|---|
| R1 | headless lacks JS SAN-LOSS/TNMASK fidelity path | **REFUTED** | P2 residual | Both JS guards are SOFT (`SANLOSS_HARD_REJECT=false` `:1547`, `TNMASK_HARD_REJECT=false` `:1558`) — the JS lane keeps the same card, so there is no divergence. SAN-LOSS is backstopped by a hard, lane-blind gate (`audit_window.py:496`). headless is not the c4 route. |
| R2 | Python token collection omits record `grammar` | **CONFIRMED** | **P1** | Real and reproduced: `card_token_multiset` (`headless_worker.py:290-295`) omits `record.grammar`; JS `cardTokens` (`gen_opt_harness2.py:1591`) includes it → opposite verdicts on one fixture. But "and/or sense German" is REFUTED (both read `sense.german`), and the *restoration* path does carry `grammar` (`card_fields.py:55`). Defect is confined to the token-collection guard. |
| R3 | headless does not enforce manifest agent budgets | **CONFIRMED** | **Med-High** | Scope corrected: the **heal** lane IS guarded (`:410`, `:436`). Only **translate** is unguarded. Decisive: the correct guard exists in JS (`:1567`) but that lane refuses v2 (`:2076`) while headless refuses v1 (`:617`) — production is v2/headless-only, the one lane with no ceiling. Measured: ceiling 13 → 30 spawns; `--max-agents=5` → 30 spawns (6×), a silent no-op. |
| R4 | CLI timeout can exceed `timeout_ceil_ms=180000` | **CONFIRMED** | Medium | Defect real: `--timeout` default 7200 s = 40× ceiling; `budgets` never read in `headless_worker.py`; JS enforces via `Promise.race` (`:1583`). **The H963 premise is REFUTED**: H963 shows the kill gate *firing correctly* at 180,000 ms on the JS lane, not a breach. Cite H963 as prior documentation of the gap, never as an observed breach. |
| R5 | wrapper usage/cost telemetry discarded | **CONFIRMED** | Med-high | Wrapper parsed (`:127-140`), dropped at the sole call site (`:349`). Pricer wants `subagent_tokens`, which headless never writes → `STOP_COST_UNEVALUABLE`. Aggravator: default `cost_fn` (`bounded_supervisor.py:163`) returns `0`, so `budget_cap` can never trip unless a ceiling is wired. |
| R6 | fragment-TM stitching loses `h`/`grammar` ownership | **CONFIRMED** | Med-high | Real, but "lost **or guessed**" is wrong — the stitch records owners as unknown by design (C-02 boundary, `:1870`); the *guessing* happened downstream in the H1080 repair. **The FINDINGS §94 citation is the wrong evidence**: §94 names the C-02 flatten bug, already fixed, which covered 403/468 rows. This lane is a separate, still-live residue and stands on its own code. |
| R7 | degenerate pass-through → schema-invalid records | **CONFIRMED** | High (operational) | **Impact inverted.** It fails **closed**, not open: all three live gates reject it, so no corrupt row reaches the store. The real harm is worse operationally — one xref stub makes `save_and_audit.py:131` refuse the *entire window*, discarding every paid card in it. Not a data-integrity issue. |
| R8 | manifest validation admits duplicate `selected_keys` | **CONFIRMED** | Low-moderate | Set-vs-set at `execution_contract.py:37-40` and `:64-66`, whose error text promises "exactly cover". **Not on the live route** — it is an offline validation gap. Three downstream guards (`promote_final_cards.py:209`, `coordinator.py:657` already uses `Counter`, `max_account_orchestrator.py:874`) catch it, but only *after* the spend. No upstream producer of duplicates was demonstrated. |
| R9 | active-call lock files survive crashes indefinitely | **CONFIRMED** | **High** | Worse than framed: not crash-only. The orchestrator tree-kills the lock holder on timeout (`max_account_orchestrator.py:274`, `taskkill /F` / `SIGKILL`), which never unwinds frames, so `__exit__` never runs — a *routine* timeout is the expected trigger. `ActiveCallClaim` writes a `pid` (`:87`) nothing reads back. `DirLock` (`coordinator.py:109-148`) does TTL + `pid_alive()` correctly. Scope: one config-dir fingerprint, v2 path. |
| R10 | bounded staged execution promotes before Opus review | **PLAUSIBLE** | Low | Mechanics true (`bounded_staged_run.py:317-326`; no `--stop-before-promote`). **Severity framing refuted**: the store is by design pre-review staging — rows land `review_status='ai_translated'` and `export_interop.approved_store()` keeps them out of the citable edition. Ergonomic gap, not "unreviewed AI published". |
| R11 | 468 `h` reconstructions lack a provenance marker | **REFUTED — superseded** | none | Closed by PR #517 (`b7b3ebf8`) and confirmed applied to the live store. Positional diff vs the pre-stamp backup: `h` changed 0, `generator` changed 0, `generated_at` changed 0; markers added to exactly 468/462/468 rows. Do not re-open or re-implement. |

### 6.1 Findings the seeded list did not contain

| ID | Finding | Severity |
|---|---|---|
| N-1 | **The 468 markers' recoverability depends on a gitignored backup.** `mark_reconstructed_headwords.py:84` refuses if the pre-repair backup is absent ("the 468 are no longer identifiable"). Both store and backup exist only in the canonical checkout on one machine. Restoring the store from any unstamped copy makes the 468 permanently unidentifiable. | Durability |
| N-2 | **`window_selftest.py:4057` pins only the ceiling *constant*** (`if gh.KILL_CEIL_MS > 180000: fail`), so the gate stays green while the headless route ignores the value it guards. A constant-pinning test that over-promises enforcement. | Test integrity |
| N-3 | **The degenerate lane is untested by construction.** `test_degenerate_passthrough_accounted` (`window_selftest.py:2197`) silently `return`s when its fixture is absent — and `src/pilot/input/` does not exist in this checkout — and never calls `validate_card`. | Test integrity |
| N-4 | **C-17's pin was specified in H963 and never written.** `test_card_tokens_twins_agree` does not exist; the existing `success_runner` fixture echoes `{T1}` into both `grammar` and `german`, accidentally masking R2. | Test integrity |

---

## 7. Ranked gap specification (input to Phase 2)

Ranked by live-route exposure × severity. Every item is confirmed on the live route and safely
testable offline with **zero** paid calls — each verifier demonstrated its case against the real
code, most via the existing runner-injection points.

| Rank | Gap | Fix shape | Pin required |
|---|---|---|---|
| 1 | **R3 translate-lane agent budget unenforced** — the money guard, silently inert on the only production lane | Thread `manifest['budgets']` into `HeadlessEngine`; check at the top of `call()` before the spawn, mirroring JS `:1567`. Refusal must consume no call and return a typed stop reason. | Stub-runner test asserting spawns ≤ ceiling, and that `--max-agents` binds |
| 2 | **R9 stale active-call lock** — permanent self-inflicted DoS on a credential profile, triggered by the pipeline's own timeout path | Replace bare `O_EXCL` with the `DirLock` pattern already in `coordinator.py:109-148` (TTL + `pid_alive()`), or a kernel-backed lock the OS releases on death | Crash-recovery case in `execution_contract_selftest.py` (claim → `os._exit(1)` → next claim must succeed) |
| 3 | **R5 cost telemetry discarded** — actual spend unreconcilable; `budget_cap` silently inert by default | ~3-line capture at `headless_worker.py:349`; surface additive input/output/cache tokens, observed cost, `cost_evaluable`, call/retry counts in `summary` | Injected-runner test asserting the wrapper's cost/usage survive into `summary` |
| 4 | **R4 timeout unclamped (40×)** | Clamp in `execute()` to `min(operator, manifest.budgets.timeout_ceil_ms, 180000)` | Runner-injection test asserting the `timeout=` kwarg actually handed to `subprocess.run` — closes N-2 |
| 5 | **R2 `card_token_multiset` omits `grammar`** — deterministic unhealable rejects on a broad card class | Route both twins through the `card_fields` shared constant (C-16/C-17) | Write C-17 `test_card_tokens_twins_agree` — closes N-4 |
| 6 | **R6 fragment-TM null ownership** — regenerates null-`h` rows on every warm-TM run | Carry `h`/`grammar` into `frag_prov` (`gen_opt_harness2.py:1890`) and the `build_frags` row (`translation_memory.py:587-598`); owner is known at harvest time. Fragment-TM v2; v1 readable for audit, live cache miss unless ownership is deterministically recoverable | Selftest that a frag-TM-served stitch retains its owner |
| 7 | **R7 degenerate card discards a whole paid window** | Build the degenerate card from immutable source-record identity so `validate_final_card_schema` passes without invented owners | Real `validate_card` assertion + a fixture that cannot silently skip — closes N-3 |
| 8 | **R8 duplicate `selected_keys`** | `Counter`/length reject at `execution_contract.py:37`, matching `coordinator.py:657`; multiset semantics wherever exact key coverage is asserted | Duplicate case in `execution_contract_selftest.py` |
| 9 | **R10 no `--stop-before-promote`** | Add the flag + hash-bound `AWAITING_REVIEW` checkpoint that does not promote, mutate the store or rebuild TM | Selftest asserting the flag reaches a terminal checkpoint with promotion uncalled |
| 10 | **P-1 `batches ⊄ selected_keys` unchecked** (§4 note ²) — `selected_keys` gates enqueue but does not drive calls; a manifest whose `batches` name a key outside `selected_keys` bills it anyway, reconciled only post-hoc (`:576-579`). Not covered by R8 | Assert `batches ∪ presplit_keys ⊆ selected_keys` before the first spawn | Test: out-of-set batch key refuses launch, consuming no call |
| 11 | **P-2 `max_wide` / `stagger_ms` declared-only** — H963's concurrent-launch incident is the measured consequence; `max_wide=1` did not serialise two manifests | Either enforce at the orchestrator (the only layer with concurrency) or delete from the manifest. A declared bound nothing can honour is worse than no bound | Test that two concurrent launches on one fingerprint serialise |
| 12 | **P-3 `execution_route` / `executor_lane` / `validation_method` never read at runtime** — generation-time refusal is "birth control, not a runtime gate" | Enforce the declared route at execution, or mark these fields advisory-only in the schema so no future reader trusts them | Test: a v2 manifest declaring a foreign route is refused at execution |
| — | **R1, R11** | **No action.** R1 refuted (P2 residual telemetry-parity only); R11 superseded by #517 | — |
| — | **N-1** | Backup-retention decision — a human's call, not a code fix | — |

**A cross-cutting rule for every pin above.** N-2/N-3/N-4 and §4.2 are one defect wearing four
hats: **the suite pins declarations, not behaviour** — a constant's value, a JS `META` block, a
fixture that silently skips. Every test added in Phase 2 must assert the value that reached the
*executing* boundary (the `timeout=` kwarg actually handed to `subprocess.run`, the spawn count
actually reached), never the constant or the manifest field that was supposed to govern it. That
is the discipline whose absence let a write-only `budgets{}` block sit green through H963 and
H1080.

**Phase 2 scope note.** Handoff item 10 (the 468 marker) is already delivered — see D-1. Item 4's
"restoration-field specification" is narrower than the handoff implies: restoration already shares
`card_fields.RECORD_MASKED`; only the token-collection guard diverged (R2).

---

_Dr. Mārcis Gasūns_
