# H4277 — c1 live-gate NO-GO, 07-09-2026: the measured readiness probe answered `{"ok": false}` (classification `content`)

_Created: 07-09-2026 · Last updated: 07-09-2026_

Executor: OxAlpha (`glm-5.3-flash`, opencode lane). Handoff:
[H4277](https://github.com/gasyoun/Uprava/blob/main/handoffs/H4277-OxAlpha_SanskritLexicography_hapta-4th-window-rule-tightening_07.09.26.md) (claimed 01:37Z, TTL 6 h) ·
prompt change under test: one `{Tn}`-verbatim tightening clause in `MASK_PREAMBLE` (commit `4a7998f22`, branch `SanskritLexicography-h4277-22912`).

**Headline: the paid window did NOT run — the gate fail-closed before it.** Warm-up call answered
`{"ok": true}` (success); the measured call, on BYTE-IDENTICAL prompt bytes, answered
`{"ok": false}` (schema-valid but content-false) → honest NO-GO per the gate's own class
machinery. The task is trivially answerable ("reply with exactly `{"ok": true}`"), so a `false`
is a real health signal about the sitting, not a gate bug. **Nothing is broken in the gate; the
open question for the diagnosing session is WHY the model answers `false` intermittently.**

## 1. What ran (both calls on c1, `claude-sonnet-5`, headless CLI, `--permission-mode plan`)

| | warm-up | measured |
|---|---|---|
| ts (UTC) | 01:40:54 | 01:41:10 |
| classification | `success` | **`content`** |
| structured output | `{"ok": true}` | **`{"ok": false}`** |
| wall / api (ms) | 19 076 / 5 674 | 15 371 / 8 696 |
| output bytes | 1 790 | 1 807 |
| free RAM at call | 3 185 MB | 2 932 MB |
| cost | (warm-up, evaluable in ledger) | **$0.0959** (`total_cost_usd`, costBasis list) |

Envelope facts on the measured call (`is_error: false`, `subtype: success`,
`terminal_reason: completed`): **`num_turns: 2`** (the task should be one turn),
**`stop_reason: "tool_use"`** before the final answer, 608 output tokens,
`ttft_ms 9330`, `structured_output: {"ok": false}`. The prompt is
`max_account_orchestrator._probe_prompt(6491)` — production TASK SHAPE block (which since
H4270/H4277 includes the GLOSS WRAPPERS + `{Tn}`-verbatim rules) + "You are a readiness probe…
replying with exactly the JSON object {\"ok\": true}…" + benign prose filler
(`_PROBE_FILLER_UNIT` — plain dictionary-description sentences, **no markup, no `{%…%}`, no
`{Tn}`**, so the new clause has nothing to bite on).

Raw envelope (durable):
`C:\Users\user\.pwg_ru_evidence\h963_c4_gate0_probe_raw_h963-c1-single-profile-gate0_2026-09-07T01_40_35Z-pid20052.txt` ·
event rows: `C:\Users\user\.pwg_ru_evidence\health_probe_log.jsonl` (tail 2).

## 2. Why the gate NO-GO is CORRECT (do not "fix" the gate past this)

`h963_c4_gate0_probe.py` fail-closes a measured reading whose structured content contradicts
the readiness task (`ok:false` on "confirm you respond by saying ok:true") — class `content`,
honest NO-GO, no retry inside a sitting. A model that cannot reliably answer a one-line
readiness task is exactly the risk the gate exists to catch before a paid window. **The
fail-close is right. Keep it.**

## 3. What the diagnosing (Claude-tier) session should investigate

1. **Why `false`?** The only defensible answers are `{"ok": true}`. Hypotheses to check, in
   order: (a) plan-mode reasoning (`--permission-mode plan`) + `num_turns: 2` — what tool did
   the model invoke in turn 1 (`stop_reason: "tool_use"`), and did its turn-2 answer
   "disagree" with the premise? The raw envelope's transcript fields will show it; (b) the
   TASK SHAPE block's growth (H3144 → H4270 GLOSS WRAPPERS → H4277 `{Tn}`-verbatim clause)
   — does the model now read the block as a task description that is "not ok" to confirm?
   Test by A/B: run the probe against a build WITHOUT the H4277 clause (worktree at
   `a67f96b19`..`4a7998f22`^) vs with it — **but note the warm-up passed on identical bytes,
   so any prompt-sensitivity conclusion needs more than one sitting**; (c) plain bimodality —
   the documented class (runbook: PASS/NO-GO flips on a hours-scale, same bytes, same
   profile); the 18:29Z sitting the day before had warm-up 56 s → measured 25.7 s PASS.
2. **Is `ok:false`-on-healthy-latency its own named class?** Today it rides the generic
   `content` class with no dedicated residual. If diagnosis shows this recurs, consider
   splitting the classification (content-contradiction vs content-garbage) so the ledger can
   count THIS failure shape across sittings — measurement only, no gate loosening.
3. **What NOT to do:** do not retry inside the sitting (ration + runbook), do not loosen the
   content check, do not raise ceilings, do not switch profiles to dodge the reading.

## 4. Spend + ration state (07-09 UTC)

Attempt 1 of 2 burned at 01:40Z (warm-up + measured, ~$0.19 total, `cost_evaluable` per
ledger rows). **Attempt 2 is ration-legal at ≥ 6 h spacing → ≥ 07:45Z today** (arm off the
last `probe_call` ts 01:41:10Z + margin — H2259), within the ≤2/UTC-day cap. The GO receipt
from 06-09 18:37Z is EXPIRED (>6 h); no canary was run; the window is unspent.

## 5. Exact resume procedure (the next sitting, after any fix lands)

```sh
cd ~/Documents/GitHub/SanskritLexicography/RussianTranslation   # or the session worktree off origin/master
PWG_EVIDENCE_DIR="C:\Users\user\.pwg_ru_evidence" python src/pilot/h963_c4_gate0_probe.py --account c1
# PASS -> 2a build canary manifest (printer gives the 2b line), 2b canary call, judge -> receipt
#   python src/pilot/canary_manifest_build.py --profile-slot c1 --config-dir "D:\ClaudeTools\profiles\claude1\.claude" --outdir src/pilot/output/<hid>
#   CLAUDE_CONFIG_DIR="D:\ClaudeTools\profiles\claude1\.claude" python src/pilot/headless_worker.py <outdir>\execution_manifest.canary.json ... (paste 2a's printed line)
#   python src/pilot/canary_gate.py judge <out.canary.json> --receipt <receipt> --manifest <manifest> --status <status> --call-reservation <calls> --run-id <id>
# GO -> H4277 window: manifest h4271rh shape (--keys=_apta --budget=1 --nominal --no-grammar --no-tm), preflight over_ceiling:false, worker in-session
```

Preconditions at fire: RAM reading recorded (MG 06-09 ruled "fire" at 3 148 MB for this
single-child shape; stop and re-ask near <2 GB — the H3654 death zone); `PWG_INPUT_DIR`
canonical input dir for build AND audit; claim H4277 fresh if the 01:37Z claim expired
(TTL 6 h → 07:37Z).

## 6. State of H4277

- Prompt clause + selftest pin + canary golden regen: **shipped** on branch
  `SanskritLexicography-h4277-22912` (commit `4a7998f22`, window_selftest 221/222 — sole
  failure the red-by-design parity gate). **PR not yet opened** — the branch also carries
  this doc; land it so the diagnosis is durable.
- Window: **unspent**. Acceptance contract unchanged (H4277 §5).

_Dr. Mārcis Gasūns_
