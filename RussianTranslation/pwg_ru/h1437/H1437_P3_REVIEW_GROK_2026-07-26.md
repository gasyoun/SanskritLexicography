# H1437 Phase 3 review — offline cohort width (Grok)

_Created: 26-07-2026 · Last updated: 26-07-2026_

**Verdict: PASS**

**Reviewer:** Grok 4.5 (`grok-4.5`) — H1654 Sonnet-lock override (MG 26-07-2026).
**Subject:** commit `ee44d774` on branch `h1437-phase3-cohort-width-8050`, worktree
`SanskritLexicography-h1437-p3-8050` (based on master `898a221c`, not pushed).
**Scope:** offline-only, read + verdict-write; no live/model call, no store/TM write, no
code fix, no push/PR. Codex (`gpt-5.6-sol`) re-checks post-hoc on 27-07 — this front-runs
that, it does not replace it.

---

## Contract checklist

| # | Requirement | Result |
|---|---|---|
| 1 | Serial route byte-for-byte unchanged at width 1 | **PASS** |
| 2 | Refusal fail-closed and early (before plan/db/coord/fleet) | **PASS** |
| 3 | Offline widths 1/2/3 equivalence (decisions, store, ledger, promote/TM, overlap, wall) | **PASS** |
| 4 | No second pipeline — thin `run_cohort_offline` over `CohortEngine` | **PASS** |
| 5 | LANG_PARITY honesty (SHARED justified; hash refreshes only) | **PASS** |
| 6 | Adversarial angles attacked | **PASS** (notes only; none block) |

### 1. Serial route at width 1

The live `--execute` path still builds `BoundedSupervisor` and calls `sup.run()` after the
gate; `run_cohort_offline` is never invoked from `run()`
([`bounded_staged_run.py`](https://github.com/gasyoun/SanskritLexicography/blob/h1437-phase3-cohort-width-8050/RussianTranslation/src/pilot/bounded_staged_run.py)
L753–857). Diff adds only:

- early `cohort_width` read + refuse when `execute and width > 1` (L758–764);
- `cohort_width=` passthrough into `plan_view` (L779–781);
- CLI `--cohort-width` default `1` (L877–881).

Stop policies, account probe (`probe_fleet`), model pin (`DEFAULT_GEN_MODEL_VERSION =
'claude-sonnet-5'`, L93), and promotion semantics are untouched. Width 1 is the default
and the production route.

### 2. Refusal is fail-closed and early

Mechanism (not only the test):

```text
run(args):
  cohort_width = getattr(args, 'cohort_width', 1) or 1   # L758
  if args.execute and cohort_width > 1:                 # L759
      raise SystemExit(... COHORT_LIVE_GATE ...)        # L760–764
  plan = json.load(open(args.plan, ...))                # L765 — first I/O
```

`COHORT_LIVE_GATE` (L86–89) names attempt/result/run binding, promotion-receipt
reconciliation, and the campaign reservation ledger. Message also states the serial
route stays default.

Test (o) proves early exit with a **nonexistent** plan path (would be `OSError` if plan
load ran) plus a boobytrapped `mao.probe_fleet`. Observed: PASS.

### 3. Offline equivalence (test p)

Same three-window fixture, delayed fake workers (`w1` 0.30s / `w2` 0.20s / `w3` 0.10s —
completion order reverse of plan order at width 3):

| width | accepted_order | requeue | calls reserved/spent | promote | TM | peak | wall (this run) |
|---|---|---|---|---|---|---|---|
| 1 | `w1,w3` | `w2~~h0_zz_pw` | 3/3 | once `['w1','w3']` | 1 | 1 | 0.67s |
| 2 | identical | identical | 3/3 | identical | 1 | ≥2 | 0.34s |
| 3 | identical | identical | 3/3 | identical | 1 | ≥2 | 0.32s |

Store bytes width 2/3 == width 1. Profile-less window → loud `SystemExit` naming
`profile`. Margins (serial−0.10 / serial−0.15) held with headroom.

### 4. No second pipeline

`run_cohort_offline` (L629–653) is a thin adapter:

1. shallow-copy windows;
2. refuse missing `profile` (fail-loud vs engine's quiet None-profile no-op);
3. construct `cohort_engine.CohortEngine(...)` with the caller's hooks;
4. `return engine.run()`.

No duplicated scheduling, barrier, or promotion logic. Unreachable from `--execute`
(gate + no call site on the live path).

**CLI wiring note (non-blocking):** the CLI never dispatches `run_cohort_offline`.
Without `--execute`, `--cohort-width N` only surfaces the `cohort` block in the dry-run
`plan_view`. Offline cohort execution is via the library entry + selftest (p). That
matches H1437 Phase 3 as written (flag + refuse live + fixture path); Phase 4 offline
bench will call the adapter, not invent a second CLI live path.

### 5. LANG_PARITY honesty

New SHARED entry `h1437_cohort_width_offline`: justified — control plane is
language-agnostic (bounded driver drains prepared leases; no target-language field
read/write; no per-language twin). Files pinned:
`bounded_staged_run.py` + `cohort_engine.py`.

Hash refreshes on `h1339_requeue_materialisation_unattended` and
`h1386_resume_recovery_and_medium50` only update
`src/pilot/bounded_staged_run.py` to the new content hash. Diff does **not** touch
requeue materialisation, resume recovery, promote, or TM code paths — only the early
gate, plan-view block, CLI flag, and offline adapter. Observed:
`lang_parity_check.py` → **84 entries, all verdicts complete, no drift**.

### 6. Adversarial angles

| Angle | Assessment | Severity |
|---|---|---|
| `getattr(args, 'cohort_width', 1) or 1` masks missing/typo attrs | Intentional for pre-P3 programmatic `Namespace`s. A misspelled attr (`cohort_with`) silently stays serial — safe for live (default serial), but a future offline caller can silently fail to widen. `or 1` also coerces `0` → `1`. | **LOW** (info; prefer keep default serial) |
| Timing flake margins 0.10s / 0.15s | This host: 0.67 / 0.34 / 0.32 — clear. On a heavily loaded CI box, sleep-based wall asserts can flake; engine already pins `peak_concurrency`. Phase 4 can prefer concurrency over wall time as the hard pin if CI flakes. | **LOW** (observed green) |
| Profile-less window refusal | Explicit `SystemExit` before engine; test (p) covers. | **OK** |
| `plan_view` additive `cohort` block | New top-level key; consumers that reject unknown keys could break. House dry-run view has been additive before; default consumers dump JSON. | **LOW** (no known strict consumer) |
| Negative `--cohort-width` | `width > 1` gate does not fire; live path still serial; plan_view labels it serial (`<= 1`). Cosmetic only until offline CLI dispatch exists. | **LOW** |

None of the above are contract failures. No code fixes required before Phase 4.

---

## Selftests observed (this session)

Workdir:
`C:\Users\user\Documents\GitHub\SanskritLexicography-h1437-p3-8050\RussianTranslation`

```text
python src/pilot/bounded_staged_run_selftest.py
  → (a)–(n) PASS; (o) PASS; (p) PASS wall 0.67s/0.34s/0.32s
  → bounded_staged_run_selftest: PASS   (16/16)

python src/pilot/cohort_engine_selftest.py
  → GREEN (1)–(7)
  → cohort_engine_selftest: PASS (7 pins)

python src/pilot/window_selftest.py
  → window selftest: ran 188/188 defined -- 188 passed, 0 failed

python src/pilot/lang_parity_check.py
  → LANG PARITY LEDGER: 84 entries, all verdicts complete, no drift;
    coverage: 23 language-aware files, all tracked or exempt
```

Matches H1654 expected: 16/16 · 7/7 · 188/188 · "84 entries, all verdicts complete, no drift".

---

## Routing

**PASS** → Fable may proceed to **Phase 4 closeout** (offline bench + CHANGELOG + hub
sweep) and open the PR the same day, before the ~20 queued PWG handoffs move master under
this branch. Codex adversarial re-check remains scheduled 27-07 via
`/adversarial-pass-review` post-merge (or pre-merge if Fable prefers).

LOW notes above are optional polish for Phase 4 / Codex, not a correction loop.

---

_Model: Grok 4.5 (`grok-4.5`) · H1654 · 26-07-2026_

_Dr. Mārcis Gasūns_
