# H3627 — c1 live gate: GO (health PASS + canary 3/3 clean)

_Created: 28-08-2026 · Last updated: 28-08-2026_

First `LIVE_GO` on the c1 lane since it became the sole live lane (MG ruling 19-08-2026).
Run under [`/pwg-live-gate`](https://github.com/gasyoun/claude-config/blob/main/commands/pwg-live-gate.md)
against policy `production_v3`, to unblock the 61-lemma re-ingest queue H3627 had parked.
Supersedes the [H3228 c1 NO-GO packet](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h3228/H3228_C1_LIVE_GATE_NOGO_21-08-2026.md)
(21-08-2026, weekly Pro cap).

## Verdict

```
gate_reason = LIVE_GO      (health PASS AND canary 3/3 clean)
verdict     = GO
```

Derived mechanically from the two measured readings, never asserted.

## Step 0 — slot

`c1`, config dir `D:\ClaudeTools\profiles\claude1\.claude`, passed explicitly as
`--account c1` / `--config-dir`. The script default is the **retired** `c4`, so omitting the
flag would have probed the wrong lane.

## Step 1 — health (one representative call, no retry)

`python src/pilot/h963_c4_gate0_probe.py --account c1 --config-dir "D:\ClaudeTools\profiles\claude1\.claude"`

| field | value | ceiling (`production_v3`) | verdict |
|---|---:|---:|---|
| measured wall `elapsed_ms` | **20 868 ms** | 80 000 ms | PASS |
| measured route `duration_api_ms` | **10 396 ms** | 45 000 ms | PASS |
| measured `api_gap_ms` | 10 472 ms | — | — |
| warm-up wall (advisory) | 17 746 ms | — | success |
| warm-up route | 4 584 ms | — | — |
| classification | `success` (class 1) | — | PASS |
| `schema_valid` | `true` | required | PASS |
| connection errors | 0 | 0 | PASS |
| prompt payload | 8 617 B | floor 5 120 B | PASS |
| executor | `claude-sonnet-5` | — | — |

Both ceilings are clear on their own numbers. The probe's own `GATE-0 VERDICT: PASS` line
prints only the wall reading, so `duration_api_ms` was read separately off
`src/pilot/output/h963_c1_gate0_probe_events.jsonl` — a wall pass with an unread route time
is not a health reading.

- run id `h963-c1-single-profile-gate0/2026-08-28T14:14:17Z-pid31368`
- evidence root `src/pilot/output` (canonical checkout, **not** a linked worktree — the
  probe refuses the disposable-checkout fallback that split the H2174 series)
- ration before firing: 0 c1 attempts this UTC day, last probe row 826.7 h old — inside
  `≥ 6 h` spacing and the `≤ 2/day` cap

## Step 2 — canary (`dq_canary_puregloss`, one paid call)

2a built the manifest offline (`canary_manifest_build.py --profile-slot c1`), sha256
`c59d7ed860c01b5056fa5dd43d7d1222c70c52960f2ebacf69017a9c5b9d23b5`. 2b ran through
`headless_worker.py` — the production headless route, never a Workflow session — with
`CLAUDE_CONFIG_DIR` exported **before** the command, since the builder's printed line omits
it (the paste trap H3144 paid an invocation to find).

| check | result |
|---|---|
| classification | `success` |
| senses returned | **3 / 3** |
| `tn_hits` (TNMASK) | **0** |
| `marker_hits` (SAN-LOSS) | **0** |
| `null_keys` | none |
| `reasons` | `[]` |
| judged verdict | **GO** |
| calls spent | 1 of `max_calls` 3 |
| `api_latency_ms` / `wall_latency_ms` | 14 568 / 12 014 |
| `cli_safe_mode_effective` | `true` |

Judged by `canary_gate.py judge`, not by reading the card: receipt
`src/pilot/output/h3627/canary_receipt.json` (`pwg.canary_gate_receipt.v1`), result sha256
`8d028597c0d4725851aa929fd5e03b6d0c76abbd0d19e7753a042bae4a0d92d4`, evidence commit
`b0bea41e51b3ce185201f957d25a66ba37654ab7`.

The synthetic card is `synthetic_control` provenance and is **never** promoted — the
promoter's C-05 refusal is expected behaviour, not a failure.

## Spend is NOT evaluable

The receipt carries `observed_cost_usd: 0.0` with **`cost_evaluable: false`** and
`unevaluable_calls: 1`. Per the gate's own rule, `cost_evaluable` must be read before either
zero is reported: this is **not** a measured $0, it is an unpriced call, consistent with
pwg_ru's dormant Max-credit billing. No dollar figure for this gate may be quoted from it —
the honest statement is "2 paid calls, cost not evaluable".

## What this authorises, and for how long

- `probe_log.py gate` now returns **GO** (`2026-08-28T14:20:51Z`, `production_v3`).
- `bounded_staged_run.py --execute` requires a **fresh GO receipt for the same profile**,
  `DEFAULT_MAX_AGE_SECONDS = 6 × 3600`. This receipt was judged at
  **2026-08-28T14:17Z**, so it expires about **2026-08-28T20:17Z**. After that it authorises
  nothing and the lane needs a new representative health reading — a stale or prior GO is
  never an override.
- The 61-lemma re-ingest queue is `src/pilot/output/H3627_reingest_worklist.json`
  (61/61 runnable, rebuild command in
  [the H3627 protocol](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3627_TM_MIRROR_REFRESH_REINGEST_QUEUE_28-08-2026.md)).
- **Every unit in that batch carries `--no-tm`.** The TM mirror is clean as of this
  handoff, so `--tm=auto` would no longer re-serve the quarantined cards — but a defect
  requeue must re-fetch the right PWG article rather than reuse any memory of the wrong one.

The windows themselves were **not** run under this gate: they are a separate and larger
spend, and a human should decide on them.

_Dr. Mārcis Gasūns_
