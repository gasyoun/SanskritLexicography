# H1447 — c4 live gate packet: health + canary → LIVE_GO (22-07-2026)

_Created: 22-07-2026 · Last updated: 22-07-2026_

Executor: Fable 5 (`claude-fable-5`), handoff
[H1447](https://github.com/gasyoun/Uprava/blob/main/handoffs/H1447-Fable_SanskritLexicography_pwg-ru-medium50-live-gated-start-today_22.07.26.md).
Baseline: `e603e47c` (`v1.55.0-4-ge603e47c`), session-unique worktree
`SanskritLexicography-h1447-1822`. Skill followed:
[/pwg-live-gate](https://github.com/gasyoun/claude-config/blob/main/commands/pwg-live-gate.md) c4,
invoked exactly once — no retry, no re-warm, no reroll.

## Step 0 — offline gates (pre-live floor)

| Gate | Result | Floor |
|---|---|---|
| `window_selftest.py` | **180/180 ran, 180 PASS** | 180/180 |
| `lang_parity_check.py` | **73 entries, no drift** | 73 clean |

## Step 1 — health: ONE representative ≥5 KB c4 attempt

Via [`h963_c4_gate0_probe.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/h963_c4_gate0_probe.py)
(the repo's D-K two-phase protocol, strict rule: EITHER reading ≥ 30 000 ms ⇒ NO-GO).

| Reading | Elapsed | Classification | Output |
|---|---|---|---|
| warm-up | **17 972 ms** | success | 1 465 B |
| measured | **16 621 ms** | success | 1 475 B |

- Prompt: 6 828 B actual (≥ 5 KiB floor), schema-carrying, natural load-representative (v1.9.17+).
- Connection errors: **0**. Verdict printed by the probe: **GATE-0 VERDICT: PASS**.
- Date (UTC): 2026-07-22T07:03:00Z. Events (append-only):
  `src/pilot/output/h963_c4_gate0_probe_events.jsonl`, run_id
  `h963-c4-single-profile-gate0-2026-07-16` (fresh dated rows appended this run).
- Contrast with prior NO-GOs on the same route (H963 16-07 104 870 ms; H1110 18-07
  98 625 ms): the degradation tracked in
  [SERVER_OUTAGES.md](https://github.com/gasyoun/Uprava/blob/main/SERVER_OUTAGES.md)
  is not present in today's reading.

## Step 2 — canary: ONE `dq_canary_puregloss` synthetic-control call

Headless CLI, manifest v2 — the production route, never a Workflow session.

**Manifest-v2 provenance (recorded as required):**

| Field | Value |
|---|---|
| `profile_slot` | `c4` |
| `config_dir_fingerprint` | `e96ee46471ec057e985b40d5ba6c5887bd4e17cc7cb89cb4dbb9c96a09704d4f` |
| `execution_route` | `claude-cli-headless` |
| `executor_lane` | `serial-whole-card` |
| `validation_method` | `audit_window+final_schema` |
| `model_identifier` | `claude-sonnet-5` (generation; session executor Fable 5 `claude-fable-5`) |
| `key_provenance` | `dq_canary_puregloss~~h0_zz_pw` → **`synthetic_control`** |

Command shape: `headless_worker.py <manifest> --output … --status-out … --only-profile c4
--max-agents 1 --timeout 180`, `CLAUDE_CONFIG_DIR` bound to the canonical c4 profile dir,
`PWG_RU_STORE` pinned to a scratch path (belt-and-suspenders; no promotion step was run).

**Result (one call, one profile):**

| Metric | Value | Policy |
|---|---|---|
| senses returned | **3/3** | 3/3 required |
| final-card schema | 1 checked, **0 invalid** | zero failures |
| SAN-LOSS (sense shortfall) | **none** | zero |
| TNMASK / markup fidelity (stage-2 mechanical) | **1/1 clean, 0 hard-flagged** | zero |
| unmapped tokens (translation fidelity) | **1/1 units clean** | zero |
| sense duplicates | **PASS** | zero |
| coverage (raw 3 → card 3) | **PASS** | complete |
| connection errors | **0** | zero |
| elapsed | 37 321 ms (translation-class call; kill gate 45–180 s — the 30 s ceiling applies to the health probe only) | — |
| usage | in 4 · out 2 843 · cache_read 81 229 · cache_create 83 476 | — |
| observed cost | **$0.5729587**, `cost_evaluable=true`, 1 priced call, 0 missing-usage | — |

Synthetic output was **not promoted and will never be** (`provenance_class =
synthetic_control`; promoter refusal C-05 is the designed behavior).

## Step 3 — mechanical verdict

```
health      = PASS   (both readings < 30 000 ms, 0 connection errors)
canary      = PASS   (3/3 senses; zero SAN-LOSS / TNMASK / unmapped / schema failures)
gate_reason = LIVE_GO
verdict     = GO
```

Derived only from the two measured results above; no prior or operator-asserted GO was
consulted. This row is what
[/pwg-bounded-run](https://github.com/gasyoun/claude-config/blob/main/commands/pwg-bounded-run.md)
consumes: same `--only-profile c4`, same fingerprint
`e96ee46471ec057e985b40d5ba6c5887bd4e17cc7cb89cb4dbb9c96a09704d4f`.

Health row also appended to
[GENERATION_API_PROBE_LOG.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/GENERATION_API_PROBE_LOG.md)
(2026-07-22T07:12:06Z, kind=warmup, GO). The canary row lives here, not there: that log's
`production_v1` policy latency-caps every row at 30 s, which is the health-probe ceiling —
a translation-class canary call structurally cannot be recorded there as GO (papercut filed).

## Phase 2 — bounded starter window: fleet-probe STOP (terminal, zero production calls)

With `LIVE_GO` recorded, `/pwg-bounded-run` was invoked once against the prepared plan
(`h1447_medium50_plan.v1.json`: 5 leases `h1447-m50-w1…w5`, 48 keys, starter `w1` = 3 keys /
3 projected calls) with `--only-profile c4 --max-accounts 1 --max-windows 1 --max-calls 3
--stop-before-promote --execute`, run-id `h1447-m50-starter-20260722`.

The run's own fleet probe (STOP-on-any-NO-GO, the honest-NO-GO stance) stopped at the c4
**warm-up**: a valid CLI result envelope (subtype `success`, 1 481 B, **19 477 ms**, 0
connection errors) whose structured schema result was **not `{"ok": true}`** →
classification **`content`**, `schema_valid=false` → immediate STOP per the warm-up policy
(2026-07-22T07:17:49Z, event in `h1447_bounded_events.jsonl`, committed below).

- **Zero dispatch, zero production translation calls, zero usage/cost.** No store or TM
  access; no lease consumed; no checkpoint written.
- Not a tooling defect: `claude_argv_prefix` resolved (`process` would classify otherwise),
  the envelope parsed (`malformed` would classify otherwise). The identical code path
  passed twice at 07:03Z (gate-0). A model-content flake 14 minutes after a clean gate is
  a **live host signal**, and the no-retry rule stands: this run does not re-warm or reroll.
- Probe-log row: 2026-07-22T07:20:08Z, kind=warmup, **NO-GO** (mechanical: schema not valid).

**Terminal state for H1447: `STOP_FLEET_PROBE(content)` after `LIVE_GO`.** Per the
/pwg-live-gate hand-off rule, the manifest-bound leases are left intact and unconsumed:
all 5 prepared leases and the plan survive for a successor session, which may resume ONLY
after a NEW representative ≥5 KB c4 health reading passes (a stale GO — including this
packet's — does not authorize resumption). The 48 medium50 keys all remain unrun; `yuvan`
and `ftvij` were already in the store before this handoff.

One code defect was found and fixed test-first on the way (execution/fix loop 1 of 6):
the `bounded_staged_run.py` CLI dereferenced `args.claude_bin` on the `--execute` path but
never defined `--claude-bin` — an AttributeError crash before any call, invisible to every
injected-runner selftest. Fixed (parser extraction + flag), pinned by
`bounded_staged_run_selftest` test (n); `window_selftest` 180/180 and `lang_parity_check`
73/no-drift re-verified after the fix (2 parity hashes refreshed, SHARED verdicts intact —
the change is language-neutral CLI plumbing).

_Dr. Mārcis Gasūns_
