# H3751 fold -- degraded `key1` census (#1767)

_Created: 31-08-2026 · Last updated: 31-08-2026_

Generated by [`src/h3751_key1_census.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h3751_key1_census.py). Store: 11519 rows; PWG source: 123366 records. The witness is the sub-card prefix before `~~`, per issue [#1767](https://github.com/gasyoun/SanskritLexicography/issues/1767).

## Verdict

**Zero** rows disagree with their sub-card witness, so nothing in this class is evidence-decidable to repair -- there is nothing left to repair. The corroborating name-by-name lookup of the issue's own worked examples (`src/h3751_key1_probe.py`, report `H3751_KEY1_1767_PROBE_31-08-2026.json`) finds each of `apta` / `gawa` / `asru`, `vasa` / `bara` / `vasin`, the `durg_a~~h0_zz_sch` junk key and the degraded `gaṭa` / `manorata` IAST values absent from the store: H2996's wrong-entry quarantine and the in-place key repairs that followed already closed #1767. This census is the regression guard, not a repair pass -- it reads the gitignored live store, so it is an operator check rather than a CI job.

## Classes

| Class | Rows | Repaired here |
|---|---|---|
| `decidable_subcard_only` | 0 | yes |
| `ambiguous_both_real` | 0 | no -- reported only |
| `ambiguous_neither` | 0 | no -- reported only |
| `witness_absent` | 0 | no -- reported only |

## What is deliberately NOT repaired

1. `ambiguous_both_real` -- both spellings name a real PWG entry, so which word the card translates is a reading of its German text, not a join. Issue #1767's `vasa` / `bara` / `vasin` conflations are this class.
2. `ambiguous_neither` -- neither spelling is in PWG at all; that is the wrong-entry ingest class H2996 quarantined, not a key-spelling defect.
3. `witness_absent` -- a whole-card row carries no `~~` witness, so nothing independent contradicts its `key1`.

_Dr. Mārcis Gasūns_
