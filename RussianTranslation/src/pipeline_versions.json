{
  "schema": "pwg_ru.pipeline_versions.v1",
  "doc": "Semver of the SOFTWARE that produces a translation, orthogonal to the Claude model version (provenance.model_version) and to the project CHANGELOG release. Bump by re-run impact: MAJOR = rows below MUST be re-translated; MINOR = re-run recommended; PATCH = no re-run needed. After a bump, run `python pipeline_version.py freeze` to record the new content SHA. `min_valid` is the re-run threshold: any stamped version < min_valid is stale.",
  "components": {
    "prompt": {
      "version": "1.1.0",
      "min_valid": "1.0.0",
      "sha": "9983b21651493256",
      "files": [
        "pwg_ru_prompts/*.txt",
        "pwg_ru_prompts/*.md"
      ],
      "note": "translate / judge / corpus-check prompt set"
    },
    "glossary": {
      "version": "1.0.0",
      "min_valid": "1.0.0",
      "sha": "ca1c632639627174",
      "files": [
        "glossaries/*.tsv",
        "glossaries/*.md"
      ],
      "note": "domain vocab (kavya/bauddha/jaina/vedic/epigraphic) + de-ru translation aids"
    },
    "script": {
      "version": "1.1.0",
      "min_valid": "1.0.0",
      "sha": "144e83f802c72828",
      "files": [
        "src/run_batch.py",
        "src/pwg_mask.py",
        "src/corpus_gate.py",
        "src/assemble.py",
        "src/promote_final_cards.py",
        "src/annotate_stats.py"
      ],
      "note": "deterministic mask / harvest / restore / corpus-gate + card promotion + derived-stats backfill (annotate_stats.py; 1.1.0 = H777 expanded stats block, forces stats recompute)"
    }
  }
}
