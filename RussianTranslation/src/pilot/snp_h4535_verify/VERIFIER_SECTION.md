## Verifier

- **Verifier:** headless opencode session (`zai-coding-plan/glm-5.3-flash`), model-switched window, 11-09-2026 — different model and session than the executor (pairing per MG ruling 11-09-2026)
- **Checks:** own scanners written from scratch over fresh sources (yadisk re-fetched via `rclone copy yadisk:Sanskrityatina/05_Sanskrit-Lexicon/1974-SNP` 12:23 local — 279 923 B, mtime 09-09-2014; csl-orig read from `origin/main` 785fc7e via `git show`, working tree verified identical for `v02/snp/`): yadisk **453** `<H1>` entries, L 1–453 unique+contiguous, `<H1>`/`<h>`/`<key2>`/`<body>` all 453/453 balanced, `<i>` 1185/1185, 0 chars outside entries; csl-orig **453** `^<L>` meta + **453** `^<LEND>`, k1==k2 for all 453; JOIN by L → 453/453 on both sides, **0 pc mismatches, 0 headword mismatches**; fold-diff (strip tags + `[Page…]`, NFKD, drop combining, letters-only, lowercase) → **450/453 identical, 3 diffs — not the claimed 451/453 two**: L2 «disagress»→«disagrees» and L440 hintāla end-matter (yadisk glues the 1159-folded-char «Additions and Corrections» list inside `<body>`; csl-orig keeps the 36-char entry and carries the same text unkeyed after `<LEND>`, lines 5744–5781 — both confirmed raw-byte) **plus L176 (barbara): yadisk has an EMPTY `<g></g>` Greek slot, csl-orig restored `<lang n="greek">ὀνυξ</lang>`** — the only Greek slot in either file; a real content difference the landed 451/453 claim does not account for (its fold evidently dropped non-ASCII letters, hiding it); difflib containment over all 453 pairs → **no yadisk letter content missing from csl-orig anywhere** (L2 replace is the misprint itself; L440 delete is the relocated end-matter) — zero lost readings, all three diffs favour csl-orig; XML gate (venv-mako): `preflight ok … snp: PASS — All records parsed by ET · Summary: 1 PASS, 0 FAIL, 0 SKIP (of 1)`; artifacts on origin/master 96db497e: parity doc present, census row 11 carries the SUPERSEDED strikethrough + evidence link, §7 item 2 struck RESOLVED no-action, changelog entry present (later swept into CHANGELOG.md by release cut v1.144.166 — content intact); `gh pr view 2181` → state MERGED, mergedAt 2026-09-11T06:44:07Z, file set = census + parity doc + changelog_queue entry, matches the brief. Two residuals flagged (neither changes the decision): (1) the 451/453 identity count should read 450/453 with L176 recorded as a third, csl-orig-favouring diff — parity doc, changelog text and census row 11 all carry the wrong count; (2) census §6 Verdict-summary line still lists «SNP (keyed markup)» under «unique-delta (nothing in Cologne)», contradicting the corrected row 11. This verifier is read-only per brief — it files no GTD rows for these residuals.
- **Verdict:** PASS — the load-bearing «no upgrade» conclusion is confirmed and in fact strengthened: csl-orig v02/snp strictly supersedes the yadisk keyed file (453/453 keying parity, zero lost readings, all three body diffs favour csl-orig — including the L176 Greek restoration the landed fold missed; filing the census's proposed upgrade would have destroyed the typo fix, the end-matter placement AND the ὀνυξ gloss); evidence: `/var/folders/17/xycv_hps0w5_b67s84q43vr40000gp/T/opencode/h4535/verify-glm/` (scan_yadisk.py · scan_cslorig.py · join.py · folddiff.py · containment.py + their JSON outputs) · https://github.com/gasyoun/SanskritLexicography/pull/2181 (MERGED)
