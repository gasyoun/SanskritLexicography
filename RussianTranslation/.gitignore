
# Claude Code local session config — not a deliverable, differs per machine/session.
.claude/settings.local.json

# H1457 nn_api.py embed/QE disk cache — regenerable, content-addressed, no deliverable content.
src/.nn_api_cache/

# H1632 pilot input: PWG per-sense <ls> loci for the frozen 500-headword frame.
# Rebuildable by research/export_frame_sense_loci.py from csl-orig — the same rule
# H1456 applied to the full pwg_sense_loci.tsv (generator committed, bulk table not).
# ~2.9 MB of PWG gloss text: public domain, but not a deliverable.
research/pwg_sense_loci.frame500.tsv

# H1632 scale-up bulk tables — same rule: reports + meta.json are the deliverable,
# these row-level tables are regenerable (28 MB universe export; 6 MB full-PWG join).
research/pwg_sense_loci.all.tsv
research/pwg_sense_dcs_attestation_all.tsv

# Generated per-root pwg_ru harnesses (derived from gen_opt_harness2.py et al.; inlined inputs)
src/pilot/run_fidelity_judge.js
src/pilot/run_pilot_wf.opt.js
src/pilot/run_pilot_wf.opt2.js
src/pilot/run_pilot_wf.batched.js
src/pilot/run_pilot_wf.sc.js
src/pilot/run_pilot_wf.sc_da_a.js
src/pilot/run_pilot_wf.sc_da_b.js
src/pilot/run_pilot_wf.tlonly.js
src/pilot/run_pilot_wf.run.js
src/pilot/run_pilot_wf.headtest.js
src/pilot/run_pilot_wf.sa.js
src/pilot/run_pilot_wf.sa_*.js
src/pilot/run_pilot_wf.nominal_*.js
src/pilot/run_pilot_wf.sc_B*.js
src/pilot/run_pilot_wf.sc_C*.js
src/pilot/run_pilot_wf.sc_S*.js
src/pilot/run_pilot_wf.sc_b*.js
src/pilot/run_pilot_wf.sc_g*.js
src/pilot/run_pilot_wf.sc_h*.js
src/pilot/run_pilot_wf.sc_i*.js
src/pilot/run_pilot_wf.sc_j*.js
src/pilot/run_pilot_wf.sc_m*.js
src/pilot/run_pilot_wf.sc_n*.js
src/pilot/run_pilot_wf.sc_p*.js
src/pilot/run_pilot_wf.sc_r*.js
src/pilot/run_pilot_wf.sc_s*.js
src/pilot/run_pilot_wf.sc_v*.js
src/pilot/run_pilot_wf.sc_y*.js

# The generated article site is PUBLISHED to the gh-pages branch (served at
# https://gasyoun.github.io/SanskritLexicography/). Rebuild with
# `python src/pilot/build_article_site.py`; publish by copying article_site/ onto gh-pages.
# (No longer ignored — MG asked for it public, 2026-07-01.)

# Translation-memory cache — DERIVED from the local-only store (pwg_ru_translated.jsonl) by
# translation_memory.py; regenerable, never a deliverable (like the store it comes from).
# The card cache is *.json; the fragment sidecar (harvested from wf_output frag_prov) is
# translation_memory.frag.<lang>.jsonl — both regenerable, neither a deliverable.
src/pilot/translation_memory.*.json
src/pilot/translation_memory.frag.*.jsonl
src/pilot/translation_memory.suggest.*.jsonl
src/pilot/translation_memory.denylist.jsonl

# H2683 fragment inventory is tracked; the 155 MB row dump is regenerable
# via `python src/pwg_tm_fragmentize.py` and is not a git deliverable.
release/pwg_tm_canonical/fragments.v1.jsonl

# H2684 full-wave generate dumps (resume locally). Compact receipt is
# wave1_b_receipt/; the first slice remains under wave1_b_slice/.
release/pwg_tm_canonical/wave1_b/
release/pwg_tm_canonical/wave2_b/

# H2685 four-format binaries (regenerable via pwg_tm_release.py; attached to
# the GitHub release). Tracked siblings: manifest.json, SHA256SUMS,
# coverage.json, loss_ledger.json, README, DATASHEET, CITATION, LICENSE-DATA.
release/pwg_tm/canonical.v1.jsonl
release/pwg_tm/pwg_tm.de-ru.tmx
release/pwg_tm/pwg_tm.tei.lex0.xml
release/pwg_tm/pwg_tm.ontolex.ttl

# Max workflow run output and A/B transcript artifacts (not deliverables)
wf_output.json
wf_output.*.json
wf_output_*.json
wf_sa*.json
wf_after*.json
wf_before*.json
wf_opt*.json
wf_prod*.json

# Local legacy runtime snapshots; keep on disk, never commit.
archive/legacy_runtime_*/
src/pilot/archive/legacy_runtime_*/

# Operator-generated key list files (local requeue/split artifacts)
da_keys_a.txt
da_keys_b.txt
sta_keys_a.txt
sta_keys_b.txt
i_keys_a.txt
i_keys_b.txt
i_keys_c.txt
*_keys_*.txt

# Generated status/dashboard snapshots
release/gate_status_snapshot.json
release/gate_status_snapshot.md
src/pilot/output/audit_window.report.json
src/pilot/output/audit_window.report.md
src/pilot/output/window_status.json
src/pilot/output/window_status.md
src/pilot/output/window_ledger.jsonl
src/pilot/output/dashboard_events.jsonl
src/pilot/output/requeue.keys.txt
src/pilot/output/judge_sample.keys.txt
src/pilot/output/prompt_rule_audit.json
src/pilot/output/prompt_rule_audit.md

# Local API retry ledger
src/corpus_lexicon.failures.jsonl

# H1456 PWG per-sense <ls>-loci export (microstructure.py export_sense_loci) —
# full run over all ~123k PWG records, regenerable, not a deliverable (same
# policy as the other derived tsvs above). The committed sample is the
# deliverable's reviewable/testable slice — never gitignore that one.
src/pwg_sense_loci.tsv
!src/pwg_sense_loci.sample.tsv

# H215 corpus TM TMX export (build_tmx.py, legacy full-text output) — rights-encumbered
# (mixes in-copyright named modern RU translations); no public release before
# per-translator clearance (H215 Slice 5 / /publish-safety-check).
release/corpus_tm/corpus_tm.sa-ru.tmx

# H1458 Track C2 (build_release_bundles.py) — public_full.* is own/PD (PWG) and
# TRACKED; derived_only.jsonl is a full 1.09M-row, ~350 MB regenerable structure-only
# extraction (NO Russian surface text either way) — same "full run is not a
# deliverable" policy as pwg_sense_loci.tsv above. derived_only.sample.jsonl is the
# committed reviewable/testable slice.
release/corpus_tm/derived_only.jsonl

# regenerable DCS frequency table (build_dcs_freq.py)
src/dcs_freq.json
src/pwg_ru_translated.jsonl.prefreq.bak
src/dcs_freq_dims.json
src/mw_en_tm.json
src/pilot/run_pilot_wf.en_*.js

# Per-root generated requeue/translate harnesses (same AUTO-DERIVED class as the
# run_pilot_wf.opt2.js patterns above; one gets created per root/slice, so a fixed
# list can't keep up — glob it).
src/pilot/run_pilot_wf.opt2.*_requeue*.js
src/pilot/run_pilot_wf.opt2.*_translate.js
src/pilot/run_pilot_wf.*_topup.js

# Pre-operation safety snapshots of the local-only translated store / wf_output
# (regenerable-adjacent backups taken before a risky merge/overlay, never a
# deliverable — same policy as the *.prefreq.bak entry above).
src/pwg_ru_translated.jsonl.*.bak
src/pwg_ru_translated.jsonl.*.keep
wf_output.*.keep

# Sa→Ru glossary — this repo holds only the generation pipeline (src/build_*.py) + docs.
# The data + live site live in gasyoun/SanskritRussian; regenerate here, don't commit it.
# Tracked: glossary/README.md, glossary/SAMPLE_root_glossary.md, and the src/ scripts.
glossary/*.jsonl
glossary/*.jsonl.gz
glossary/*.tsv
glossary/md/
glossary/surface/
glossary/index.html

# Auto-captured live failure incidents (runtime, dedup per root/day); the curated
# post-mortems in failures/failures.jsonl stay tracked. Read by the dashboard.
failures/auto_failures.jsonl

# H180 typology/re-glue/learner builders — DERIVED from the local-only store
# (pwg_ru_translated.jsonl) by build_relationships.py / build_learner_scores.py /
# build_reglue.py / synth_de_first.py. Regenerable, not deliverables, and useless
# without the (gitignored) store — so the bulk machine forms stay local. The
# COMMITTED deliverables are: the builders, the spec docs, the small summary
# tables (relationships_rollup.tsv, edition_deltas.tsv, reglue/PILOT_SUMMARY.tsv,
# reglue/synth_inputs/MANIFEST.tsv + *.prompts.json), and the rendered pilot
# cards reglue/*.md (the human-reviewable output).
src/pwg_ru_relationships.jsonl
pwg_ru/learner_scores.tsv
pwg_ru/reglue/*.json
pwg_ru/reglue/synth_inputs/*.de.txt

# H180 human review sheets (build_h180_review_sheets.py) — the generated HTML
# voting sheets embed translated `ru` bodies from the local-only/unpublished store,
# and THIS REPO IS PUBLIC, so the sheets stay local (open from file://, vote, then
# feed decisions.json to /decisions-apply). Only the generator + the metadata-only
# *_sample.jsonl audit trails (no translations) are committed.
review/h180_*_sheet.html

# H180 Arm-B synthesize-first outputs (synth_de_first.py + Opus-4.8 synth agents):
# the merged German entries embed `de` from the unpublished store (repo public) — local
# only. The numeric scores table (no text) IS committed.
pwg_ru/reglue/synth_outputs/*
!pwg_ru/reglue/synth_outputs/ARMB_SCORES.tsv

# H178 B-1 evaluation bake-off (h178_eval_bakeoff.py) — sample/baseline/judge/
# comet outputs and the 4 voting sheets embed unpublished `ru` translations
# (repo is PUBLIC): keep local-only; only the generator + protocol doc are
# committed. decisions.json votes land here too (personal review artifacts).
pwg_ru/eval/

# ---- Review sheets: DEFAULT-DENY (27-07-2026) --------------------------------
# Every /review-sheet HTML embeds unpublished `ru`/`de` from the gitignored
# store, and every downloaded decisions.json is a personal voting artifact —
# and this repo is PUBLIC. These used to be enumerated per generator
# (h178_/h1303_/h1306_/h1682_/g5_/g6_ prefixes, plus one line per
# compound-differs sheet), so every NEW generator leaked until someone
# remembered to add a line: the gorresio southern-map sheet did exactly that.
# Deny the shapes instead, and make publishing a sheet a deliberate `!` line.
# COMMITTED counterpart for a gated sheet: the metadata-only
# review/locks/<sheet_id>.lock.json binding record (H1404 standard) — never
# gitignore review/locks/. Sample-design frames (review/*_frame.tsv, no store
# text) also stay committed as the sample-design audit trail.
review/*_sheet.html
review/*_review.html
review/*_decisions.json
# Intentionally published sheets (evidence for committed findings):
!review/sanskritlexicography-renou-hypotheses_pilot_review.html
!review/sanskritlexicography-renou-hypotheses_pilot_decisions.json
!review/sanskritlexicography-kochergina-okas-guda-sphic_4rows_review.html

# Generated Workflow harnesses embed source/portrait payloads and are local-only.
src/pilot/run_pilot_wf.*.js
!src/pilot/run_pilot_wf.js

# H1691 crosswalk pass — same rule as above: the reports, the generators, the
# adjudicated verdicts and the .meta.json provenance are the deliverable; the
# regenerable row-level tables and the frame file are not.
research/frame_wide.tsv
research/pwg_sense_dcs_attestation_h1670base.tsv
research/pwg_sense_dcs_attestation_h1691.tsv
research/pwg_sense_dcs_attestation_h1691final.tsv

# H1210 A/B (src/pilot/h1210/): the generated payload/harness artifacts are large
# (22 MB) and, like every other card-bearing artifact here, embed unpublished RU/DE
# store-grade text. The COMMITTED evidence is the worklist, the slim audit verdicts,
# the telemetry, the report and the code — everything below is regenerable from those
# per src/pilot/h1210/README.md.
src/pilot/h1210/slice_payload.json
src/pilot/h1210/probe_payload.json
src/pilot/h1210/probe.args.json
src/pilot/h1210/probe.wf.js
src/pilot/h1210/h1210_ab100.manifest.json
src/pilot/h1210/h1210_ab100.opt2.js
src/pilot/h1210/ab100.chunk*.json
src/pilot/h1210/ab100.chunk*.wf.js
src/pilot/h1210/arm_a.chunk*.slice_result.json
src/pilot/h1210/arm_b.slice_result.json
src/pilot/h1210/arm_b.journal.jsonl
experiments/E1_deepseek_vs_c4/run/arm_b.journal.jsonl
experiments/E1_deepseek_vs_c4/run/e1_40.opt2.js
src/pilot/h1210/arm_b.run.log
src/pilot/h1210/arm_b.control_r*.payload.json
src/pilot/h1210/arm_b.control_r*.wf.js
src/pilot/h1210/arm_*.canonical_audit*.json
src/pilot/h1210/qc_*_senses.json
src/pilot/h1210/*.task_output.json
src/pilot/h1210/README_ARM_A_FILL.txt
