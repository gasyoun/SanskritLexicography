- H5403: **the `c1` volume wave is fully staged and its launch line is dry-run green — the
  `c1` probe ration is the only thing refusing.** A third unattended pass (Mac session over
  Tailscale SSH to `msi`) spent **zero paid calls** and changed no code. The expensive half
  is now durable state, not work the launching pass must redo: four `prepared` coordinator
  leases (`h5403vol25` `kunt_i`, `h5403vol35` `ma_d_uka`, `h5403vol39` `matsy_akz_i`,
  `h5403vol40` `mayo_bu`), a rebuilt `output\h5403vol\plan.json` (4 windows × 1 projected
  call) whose four `manifest_sha256` values all match a live `Get-FileHash`, and a canary
  manifest already built at 0 calls in `output\h5403vgate24\`. The `bounded_staged_run.py`
  dry run over those four leases exits **0** with `importable_prepared_leases` 4/4,
  `windows_not_prepared_skipped: []` and admission `serial route (width 1)` — so the paid
  line differs only by `--execute`, `--allow-unbounded`, a fresh canary receipt and the real
  repo paths. Route re-probed green (settings.json unchanged since 23-09 20:47:52Z, 0 hits
  for `BASE_URL|z\.ai|glm`, no `ANTHROPIC*` in User or Machine env). What refuses:
  `probe-ration --account c1` → exit 3, `legal_now false`, `next_legal_utc
  2026-09-24T21:20:17Z` — 2 h 14 min past this unit's budget — and that remaining slot is the
  day's last, contended with H5402's `kast_ur_i` repair. Nothing overridden; the store stays
  at 11 534 rows and the ration ledger keeps its single 15:20:17Z attempt. Evidence:
  [H5403_VOLUME_WAVE_STAGED_DRY_RUN_GREEN_RATION_RED_24-09-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h5403/H5403_VOLUME_WAVE_STAGED_DRY_RUN_GREEN_RATION_RED_24-09-2026.md).
