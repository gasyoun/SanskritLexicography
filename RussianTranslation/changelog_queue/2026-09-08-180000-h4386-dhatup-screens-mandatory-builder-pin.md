- H4386: **the DHĀTUP.→Palsule sibling screens are now mandatory, the artifact is pinned to its builder, and the `32,56` residue is adjudicated — measured, and declined.** H4349's independent verifier named four residues and closed none of them. Three are closed here and the shipped table does not move: **1465 of 1751 (83.7%), 1226 pwg / 140 mw / 99 mw-respell, byte-for-byte identical**. (1) `_sibling_pass`'s two screens defaulted to `frozenset()` and the membership test read `if pwg_cited and coord not in pwg_cited`, so a pass that simply forgot one ran **silently unscreened** — not a hypothetical, but exactly how the first H4349 cut shipped `32,56 → cukk`, passing the same-book screen to the `pwg-dotted` call and not to the `pw` one. Both are keyword-only and defaultless (omitting one is a `TypeError` at the call site), the membership test is unconditional, and an empty `pwg_cited` — the old default, now reachable only by hand — raises, because the set being screened against is also the denominator `match_rate` divides by. (2) Nothing connected `src/data/dhatup_palsule.json` to the code that wrote it: one harness reads the JSON, the other the JSON plus the corpora, neither rebuilds, so a stale or hand-edited artifact stayed green and H4349's verifier had to rebuild by hand to establish they agreed. `_stats.builder_sha256` now carries the sha256 of the builder's own source, checked **corpus-free** in the selftest and again in the standalone verifier — change the builder without rebuilding and CI fails. On landing, the rebuild was byte-identical but for that key. (3) `ABBREVIATIONS_RU.md` published pw's 40 citations as 12/4/11/3/2/8 with only **two** of the six terms asserted anywhere, and an earlier version of that table summed to 42 against its own header of 40; all six are pinned, plus the sum as a **partition** — every cited coordinate leaves the screen chain through exactly one of eight buckets — and the last row's wording is corrected (7 coordinates the table already holds, 1 whose root Palsule does not gloss, not 8 of the former). (4) **`32,56` was adjudicated by building the counterfactual rather than reasoning about it, and the guard stays out of H1333's PWG pass.** Applying H4349's variant-reading guard there moves **241 of 1751** coordinates: 93 newly resolved, 18 reattributed, and **130 lost outright** where the sole claimant carried the note (`2,15 mud`, `3,12 khād`, `5,35 valg`, `7,40 vraj` …) — in a sibling pass that guard can only refuse a *fill*, but run against PWG itself it deletes attributions for which PWG is the only witness. The narrow reading — `v. l.` as a tie-breaker among multi-claimant coordinates only — loses nothing and does resolve `32,56` to `cakk`, but **12 of its 18 reattributions hand the coordinate to a nominal claimant** (`19,2 vyath → saṃcalana`, `28,1 tud → vyathana`, `31,1 krī → vinimaya`), the `pad`/3,1 defect class in a new place: "verbal beats nominal" would have to run before the tie-break and be re-verified. Its arithmetic is published rather than folded into anything — 74 of the 93 gains have a Palsule row, 43 of those the MW pass already fills, **31 net new → 1496/1751 (85.4%), with 17 shipped attributions flipped**. That is a re-baseline of H1333 and H4339 together and belongs to its own handoff, not to a screen-hardening pass. Selftest **21/21** (3 added: screens mandatory by signature *and* behaviour, artifact pinned to its builder, the six pw refusal terms and their sum), `dhatup_h4349_verify.py` **27/27** (10 added; it now reads the builder's **syntax tree** for the signature contract, still without importing it, and still exits 2 with the corpora absent), pytest 401/401, offline contract pins 201/201, ruff E9/F63/F7/F82 clean, LANG_PARITY re-derived and re-stamped on both touched entries (SHARED and INTENTIONAL-DIVERGENCE both stand).
