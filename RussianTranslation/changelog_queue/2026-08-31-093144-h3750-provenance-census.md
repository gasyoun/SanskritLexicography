- **H3750 (W3) — provenance measured instead of asserted, and the obvious backfill refused on evidence (Opus 5 `claude-opus-5`, 31-08-2026; read-only, zero store writes, zero provider calls).** Measurement half of [issue #1804](https://github.com/gasyoun/SanskritLexicography/issues/1804). New read-only classifier [`provenance_census.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/provenance_census.py) buckets every store row as **measured** (the `prompt_sha`/`glossary_sha`/`script_sha` triple `pipeline_version.stamp()` computes from real bytes), **asserted** (`backfilled: true` + version strings with nothing behind them) or **absent** (no input identity at all): **10,773 asserted (93.52 %) · 737 measured (6.40 %) · 9 absent (0.08 %)** of 11,519 rows — the issue's 93.0 % confirmed at ×1.01, far under the ×2 halt of PLAN ruling 14. The eras do not interleave: asserted ends 2026-07-04T05:43Z, measured begins 2026-07-04T20:36Z.
  **The backfill that looks obvious is measurably wrong, and that is the unit's main result.** Recomputing `component_sha` from git at all 99 commits touching a component in 2026-06-01..2026-09-01 reproduces **1 of 9 measured stamp classes (20 of 737 rows, 2.7 %)** under both the strict test (newest component commit at or before `generated_at`) and a deliberately loose one (any commit in the window). The prompt set hashes to `dc0e1e2b65ca9731` on 66 August rows and that value exists at no commit at all: the pipeline runs against an **uncommitted working tree**, so git-derived hashes would replace one assertion with a larger one.
  **The input half survives and is corroborated:** all 10,773 asserted rows name a surviving `wf_output` sidecar and **10,100 (93.8 %)** have their `input_raw_sha256` independently confirmed by a file the promotion path never wrote. All 673 non-confirmations are one defect — 1,025 rows point at the generic `wf_output.json`, four copies of which exist on disk and which every later unnamed run overwrites.
  Deliverables: census [reports/PWG_RU_PROVENANCE_CENSUS_31-08-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/PWG_RU_PROVENANCE_CENSUS_31-08-2026.md) + its machine copy, design [docs/SPEC_PWG_RU_PROVENANCE_BACKFILL_31-08-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/docs/SPEC_PWG_RU_PROVENANCE_BACKFILL_31-08-2026.md) (field semantics, evidence source per era, ledger format — **execution explicitly out of scope**), [FINDINGS §621](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md), [GAPS §19](https://github.com/gasyoun/SanskritLexicography/blob/master/GAPS.md) for what stays permanently unrecoverable.
