- NKRYa corpus evidence can now ride inside the c1 generation prompt: `src/nkrya_prompt_evidence.py` renders a compact advisory block (head ipm, candidate ipm, word-sketch rank/dice, MAIN and 19c pair counts, plus the top-10 reading rule) from the same `nkrya_evidence_card.gather()` query path and the same committed cache as the H5261 evidence card, and `headless_worker.card_block()` appends it when — and only when — a card carries an `nkrya` input, so every existing manifest keeps byte-identical prompt bytes. Proven at 0 paid calls: `tools/h5263_prompt_evidence_probe.py` renders the C07 clouds block offline from `pwg_ru/nkrya_cache` (+1533 prompt bytes for two heads × five candidates, every figure reproducing the C07 card) into `reports/H5263_prompt_evidence_diff.json`; a missing pair count renders `n/a`, never `0`. The A/B itself did NOT run — no NKRYa API token exists on this box (MG's key is still the outstanding human step) and no live-gate GO exists for c1, so 0 of the 5 pre-authorized paid calls were spent and the keep/drop verdict stays open; full reasoning in `docs/H5263_NKRYA_C1_PROMPT_EVIDENCE_AB_24-09-2026.md` (H5263).
