- **H3959 — the `<ab>` German residue in the RU column is closed; MG's 02-09-2026 ruling rules [CONTRADICTIONS §4](https://github.com/gasyoun/SanskritLexicography/blob/master/CONTRADICTIONS.md) (Opus 5 `claude-opus-5`, 02-09-2026; read-only against the store, zero provider calls, no store writes).** MG voted the [registry-contradictions sheet](https://gasyoun.github.io/vote/sheets/uprava_registry_contradictions_02-09-26.html): «some remain Latin, none remain German, most German become Russian and do not become Latin». That keeps the 10-07 two-bucket policy, rejects the 19-07 `Caus.` = `кауз.` reading, and adds one constraint the policy never asserted — *none remain German*.
  **The defect it outlaws.** The 10-07 design fell back to the raw token for anything not in `RU_MAP`, so a Bucket-A residue rendered as German inside Russian prose by design. Census over all 11,519 store rows: 12,194 `<ab>` occurrences / 272 distinct tokens, of which **26 distinct / 409 occurrences** (`u.` 93, `v. l.` 283, `ved.` 57 …) were A-unmapped. Now **0**.
  **What changed in [`src/pwg_ab_ru.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pwg_ab_ru.py):** `RU_MAP` 96 → 122 entries, all Cyrillic; a new explicit `BUCKET_B` (138 grammatical sigla that deliberately stay international Latin) and `RESIDUE` (14 declared-undecided tokens / 18 occurrences) replace "no entry means Bucket B" guessing; the three sets are disjoint. New `python pwg_ab_ru.py census` prints the four-bucket table and **exits 1** if any token falls outside all three or if any `RU_MAP` value is non-Cyrillic — the mechanical form of "a Bucket-A token never becomes Latin". Final split: B 53.0 % · A-mapped 46.8 % · residue 0.1 % · A-unmapped 0.0 %.
  **Judgment calls named, not buried:** `u.` → `под` (pwgab expands it *unter*, not *und*); `v. l.` → `разночт.` (Latin but editorial, so "some remain Latin" = Bucket B does not cover it); `unregelm.`/`ungramm.`/`Ortsadv.` are Bucket A because they are German adjectives, not Latin termini technici; `Präs.` and PWG's own typo `instrans.` stay Bucket B. The 14-token residue is declared with a per-token reason — no `pwgab` entry, or a `pwgab` entry that is itself ambiguous (`d.` = "der/die/das", `pers.` = "Person/persisch") or garbled (`geder.` = "gedeutet?").
  **Store verdict (releases H3947): 65 rows need a sweep, not zero.** The `<ab>` policy stays render-time and needs no store write, but re-running the H2849 class check — German markers *outside* any `<ab>` tag, excluding `{#…#}` SLP1 spans — still finds 120 hits over 65 distinct rows in the `ru` field: `Akk` ×110, `Lok` ×8, `Ausgabe` ×1, `Präs` ×1, by layer `nws` 48 · `sch` 15 · `pwg` 2. These are Bucket B, so that sweep runs German→Latin; scoping and running it is not H3959. Method, census table and residue list: [ABBREVIATIONS_RU.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/ABBREVIATIONS_RU.md).
