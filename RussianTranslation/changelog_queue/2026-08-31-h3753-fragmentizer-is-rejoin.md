- **H3753 (W6) — pwg_tm_fragmentize rejoins `<is>`-interrupted glosses before fragmenting (Sonnet 5 `claude-sonnet-5`, 31-08-2026; offline, zero provider calls).** GAPS §18 census: 73 `{%...%}` gloss spans separated only by a single `<is>…</is>` run, in 58 senses across 53 records, over the live 2,392-record / 11,129-sense TM publication — the `viSveSa` 2 shape (`{%die%} <is>Viśve Devāḥ</is> {%zur Gottheit habend%}`, one gloss) that produced an orphan bare-article `definition_gloss` fragment with no lexical content. New `_rejoin_is_interrupted_glosses()` in [`RussianTranslation/src/pwg_tm_fragmentize.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pwg_tm_fragmentize.py) merges such spans into a single `{%...%}` fragment (chains included) before `extract_from_sense` runs `GLOSS_FULL` extraction; only the gloss extractor sees the rejoined text. Live re-fragmentize: `definition_gloss` 15,781 → 15,708, total fragments 112,133 → 112,060 (−73, matching the census exactly). RED-pinned on the `viSveSa` case via `test_is_interrupted_gloss_rejoined_before_fragmenting` (asserts `{%die%}` never emits standalone) plus `test_rejoin_is_interrupted_glosses_chain_and_noop`, both in [`RussianTranslation/tests/test_pwg_tm_canonical.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/tests/test_pwg_tm_canonical.py); `pwg_tm_fragmentize.py --verify` stays green. GAPS §18 graduated to [FINDINGS §619](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md) and closed. Existing store rows keep their pre-fix fragment identity — re-fragmenting/re-promoting them is out of scope, deferred to Wave-2+. Census artifact: [`RussianTranslation/docs/CENSUS_H3753_IS_INTERRUPTED_GLOSSES_31-08-2026.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/docs/CENSUS_H3753_IS_INTERRUPTED_GLOSSES_31-08-2026.json).
