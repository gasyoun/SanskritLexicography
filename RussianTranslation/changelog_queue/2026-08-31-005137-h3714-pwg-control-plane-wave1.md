- **H3714 Wave 1 — PWG translation control-plane strangler landed (Opus 4.8 `claude-opus-4-8`, 31-08-2026; verdict PARTIAL, USD 0.00, zero provider calls).** New standard-library [`RussianTranslation/src/pwg_pipeline/`](https://github.com/gasyoun/SanskritLexicography/tree/master/RussianTranslation/src/pwg_pipeline): unified `Campaign→Job→Attempt→Call→Artifact→Verdict→Promotion` model, transactional WAL-SQLite repository with compare-and-set transitions, one shared paid-call kernel (reserve→timeout→usage→seal→finalize, no retries, fails closed on unevaluable usage or route substitution), xAI/DeepSeek adapters plus a read-only Claude shadow adapter, pure audit, explicit apply intents, and journal-only promotion fenced to scratch stores. Recursive validator reproduces the canonical defect **exactly — 79 rows / 609 `{Tn}` occurrences over 2,392 rows, digest unchanged** — and fences them without rewriting a byte. Four frozen replay campaigns replay exactly; 11 named irreversible boundaries each recover idempotently (including a hard subprocess kill). 129 new tests, full RussianTranslation suite 358 passed, offline gate wired into CI. **PARTIAL, not GO:** no provider canary (both API keys absent — each adapter track stopped alone) and no independent reviewer receipt, so no legacy writer was disabled. Close report: [WAVE1_REPORT](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/docs/WAVE1_REPORT_RussianTranslation_PWG_CONTROL_PLANE_31-08-2026.md).
