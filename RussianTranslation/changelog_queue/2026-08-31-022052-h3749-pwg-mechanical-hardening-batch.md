- **H3749 (W2) — H1811 remainder H5 gate-pinned + 44 modules migrated off the sibling-path guess (Sonnet 5 `claude-sonnet-5`, 31-08-2026; offline, zero provider calls).** H3/H4/H7/H10 from the [H1811 fixlog](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/h1811/H1811_PIPELINE_REVIEW_FIXLOG_2026-07-29.md) were already landed with RED-verified pins on master; only H5 (corrupt/missing `window_status.json` / `audit_window.report.json` swallowed to `{}` under a clean-looking audit rc) had its code fix merged but no dedicated pin — added `test_h5_corrupt_status_report_is_a_typed_audit_error` to [`coordinator_hardening_selftest.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/coordinator_hardening_selftest.py), verified RED against the pre-H5 branch (temporarily disabling the guard reproduces the silent-unknown failure with no named `audit_errors` entry) and GREEN on current master.
  **#1804 migration half:** all real remaining call sites of the `os.path.join(HERE, '..', '..', '..')` sibling-path guess — the one that silently resolves to a directory with no sibling repos from a `git worktree`, since a worktree lands beside `GitHub/` rather than inside it (FINDINGS §503) — now route through [`sibling_root.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/sibling_root.py)'s upward-marker-walk resolver. 44 modules touched (13 had already migrated pre-wave); grep for the raw 3-up guess pattern across `RussianTranslation/src/` now hits zero real code, only `sibling_root.py`'s own canonical fallback and a docstring reference in `rv_org_root.py`. Also folded in two structurally-identical guesses the plan's file list undercounted: `store_path.py`'s non-git-tree fallback for locating the `pwg-ru-data` sibling repo, and `eval/probe_gold_strata.py`'s 4-up `sys.path` bootstrap for `sanskrit-util` (its hardcoded Windows-path fallback is untouched). Full offline gate battery green: `pytest RussianTranslation/tests -q` (372 passed), `coordinator_hardening_selftest`, `bounded_staged_run_selftest`, `bounded_supervisor_selftest`, `cohort_engine_selftest`, `headless_worker_selftest`, `sibling_root.py --selftest`, `mw_compounds.py --selftest`, `government_census.py selftest`, `store_path.py --selftest` — all PASS.
