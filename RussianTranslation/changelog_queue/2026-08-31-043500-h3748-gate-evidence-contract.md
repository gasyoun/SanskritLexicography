- **H3748 (W1) — the gate-evidence contract: a PASS must name what it checked (Opus 5 `claude-opus-5`, 31-08-2026; offline, zero provider calls).** New [`RussianTranslation/src/pilot/gate_evidence.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gate_evidence.py): a `GateEvidence` record carrying inputs examined (path, existence, byte size, **sha256**, units pulled out), predicates evaluated (evaluations + hits each), a verdict, a JSON sidecar, and `assert_nonvacuous()` turning a zero-work PASS into a hard FAIL. Legitimate emptiness is **declared, never silent**: `LEGITIMATE_EMPTY` enumerates it per gate up front (the spike — [SPIKE_PWG_GATE_EVIDENCE_LEGITIMATE_EMPTY_CLASSES_31-08-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/docs/SPIKE_PWG_GATE_EVIDENCE_LEGITIMATE_EMPTY_CLASSES_31-08-2026.md)), and an unregistered excuse cannot be improvised at the call site. All **nine gates of [#1803](https://github.com/gasyoun/SanskritLexicography/issues/1803)** are retrofitted one commit each, **predicate logic untouched** — so the underlying defects the issue also names stay filed, but stop being invisible: `lane_spotcheck_tick` records the 0-byte report it blesses, `gold_agreement` records `release_mode=false` when a non-default path disarms its κ guard, `spot_check_daily` records `judged=0` on an all-errors judge day, `prompt_compiler` names the golden it just overwrote before "comparing" against it. `prompt_rule_audit --fail-on-missing` over an absent template, which exited 0, is now a hard FAIL.
  **[#1798](https://github.com/gasyoun/SanskritLexicography/issues/1798) closed (step 1 only):** G9 gains a duplicate-entry-id predicate — TEI `xml:id` uniqueness, OntoLex parsed with `rdflib` instead of `text.count(...)`. Reproduced on our own shipped bytes through the gate that used to report them as validating: **12,374 duplicated `xml:id` across 26,465 occurrences** (`pwg-ac` ×6, `pwg-aS` ×5 …) and **120,173 OntoLex declarations against 106,082 distinct subjects — 14,091 entries silently merged**, matching the issue's measurements exactly. Re-cutting the artifacts (step 2) is deliberately NOT done: they are published against a Zenodo DOI, so re-cut vs supersede-with-erratum is a publication decision a human makes — and re-cutting before the gate had teeth would have put clean bytes behind a blind gate.
  **[#1800](https://github.com/gasyoun/SanskritLexicography/issues/1800) closed:** the promote claim now wraps the whole read-modify-write span on all four paths. `promote_ready_partial_clean` took the claim *after* reading and merging and had none of the three refusal gates its `--merge` sibling enforces — the measured 99.7 % content-mass shed that `--merge` refused and `--ready-partial-report --apply` applied in silence; it now refuses with the same message in the same gate order, and its CLI dispatch no longer bypasses `validate_store_target` and the H2089 route guard. `promote_en` reads inside the claim and gains the content-mass gate it never had; `lane_guard`'s executing revert takes a claim; the TM denylist's asymmetric lock is symmetric.
  Twelve RED pins in [`gate_evidence_selftest.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gate_evidence_selftest.py), sidecar presence wired into CI via `gate_evidence.py --require`, 31 LANG_PARITY entries re-affirmed (477 added lines grepped for language-keyed tokens; no new branch), `window_selftest` 219/219.
