- **H3751 (W4) — `~~h<N>` is an enumerate index, not a homonym number: 1,278 store rows repointed to their own printed column, ledgered (Opus 4.8 `claude-opus-4-8`, 31-08-2026; offline, zero provider calls).** [#1801](https://github.com/gasyoun/SanskritLexicography/issues/1801): a card's printed `volume`/`page`/`column` was chosen from the wrong PWG record because `pwg_page_index.annotate_cards` compared the sub-card key's `~~h<N>` against the source's printed `<h>` — but `<h>` starts at 1 and `<N>` is `gen_root_split`'s 0-based `enumerate` index over a headword's records, so `~~h0_` never matched, selection fell back to "every record sharing this `key1`", and `starts[0]` took the **lowest column across all homographs**. `export_de_edition.py` allowlists exactly those three scalars, so a homonym label shipped beside another word's locus.
  **Census first (plan ruling 14).** [`h3751_homonym_census.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h3751_homonym_census.py) reconciles every row **positionally** against `csl-orig/v02/pwg/pwg.txt`: **1,278 of 5,205 mappable pwg-layer rows wrong = 24.6 %**, ratio **1.00×** against the issue's claimed 24.5 % (1,278 / 5,211), so the halt-on-≥2× condition did not fire. Report: [H3751_HOMONYM_LOCUS_CENSUS_31-08-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3751_HOMONYM_LOCUS_CENSUS_31-08-2026.md).
  **Fix.** New [`pwg_homonym.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pwg_homonym.py) is the one place that knows what `~~h<N>` means; `compute_annotations` now takes the scalars from the card's **own** record and **omits** them (keeping the honest `pc_all`/`page_all`) when the index addresses none — an absent field cannot ship a wrong locus, a guessed one can. The operator counter no longer folds ambiguous guesses into `matched`. At the generation end, `gen_root_split` stamps the record's printed `<h>`/`<pc>` into the rootmap (`hom_printed`, `pc`) and into the translator-facing homonym header instead of `hom + 1`. Sub-card keys are deliberately unchanged — they are the identity of 11k promoted rows, so re-spelling them would fork the join rather than fix it.
  **Pin.** `python RussianTranslation/src/pwg_page_index.py --selftest` (8-row `vasa`/`akArya` multi-homograph fixture, wired into CI), **verified RED against pre-fix master `7435178e0`** by [`h3751_redpin_prefix_proof.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h3751_redpin_prefix_proof.py) — 5 of 7 fixture rows disagree pre-fix, and pre-fix reports `matched=7` even for the row it could not resolve.
  **Ledgered rewrite (H3591 pattern).** [`h3751_apply_homonym_locus.py --apply`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h3751_apply_homonym_locus.py) wrote **1,969 ledger rows / 7,198 row-field changes** (1,278 `scalar_repointed` + 365 `scalar_added` + 326 headword-wide `pc_all`/`page_all`) to [H3751_locus_ledger.jsonl](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3751_locus_ledger.jsonl) under the H2146 store lock; its G3 check re-read the store from disk and proved **delta == ledger exactly**, G2 proved no field outside the five pc fields moved. `audit_store_gates.py` before/after: rows 11,519 → 11,519, hard flags `{'SAN-LOSS': 3}` unchanged, `changed_ru=0`. Post-rewrite census: **0** rows carrying a foreign column. Mirror refreshed LAST — [gasyoun/pwg-ru-data#5](https://github.com/gasyoun/pwg-ru-data/pull/5), store and mirror byte-identical (`sha256 58c2172607c3`).
  **[#1767](https://github.com/gasyoun/SanskritLexicography/issues/1767) folded in and closed as already-fixed.** [`h3751_key1_census.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h3751_key1_census.py) finds **zero** rows whose `key1` disagrees with its sub-card witness across all 11,519 rows, and [`h3751_key1_probe.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h3751_key1_probe.py) finds every one of the issue's named examples absent — `apta`/`gawa`/`asru`, the `vasa`/`bara`/`vasin` conflations, the `durg_a~~h0_zz_sch` junk key, the degraded `gaṭa`/`manorata` IAST. H2996's wrong-entry quarantine already closed it; nothing was evidence-decidable to repair, so nothing was. Reports: [key1 census](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3751_KEY1_DEGRADATION_CENSUS_31-08-2026.md), [named-example probe](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3751_KEY1_1767_PROBE_31-08-2026.json).
  LANG_PARITY: `homonym_locus_positional_h3751` **SHARED** (source-structure-only, pre-`--lang`); `sense_count_sanloss_guard_h920` re-stamped Case C.
