- NKRYa ipm batch audit of the PWG-RU store's content words: `src/h5262_ipm_audit.py` (extract → query → flag → report, 17-check offline selftest) with the first artifacts in `reports/`. The extraction half ran over the whole store — 11 534 cards, 39 425 Cyrillic tokens in the `{%…%}` gloss spans, **4 912 distinct content lemmas** (1 832 of them hapax), lemmatized with pymorphy3 2.0.6; Sanskrit `{#…#}`, `<ab>`/`<ls>`/`<lex>` apparatus, function words, proper names and the `кого-л.`-class shorthand are skipped, and the store is never written. The ipm half is **blocked**: no NKRYa API token exists on either box (probed 24-09-2026, Windows credential store and Mac keychain both empty), so only the 3 lemmas cached by the H5261 C07 pilot carry a verdict (`туча` 42.4 ipm cat 3, `сгущаться` 4.90 cat 2, `связный` 2.71 cat 2) and no flags or vote sheets could be produced. `query` is resumable by construction — a failed or uncached lookup is never written to the ledger, so every unanswered lemma is re-asked once MG stores the key (H5262, ruling MG 22-09-2026 row 6).
