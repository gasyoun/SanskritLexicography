- **H3754 (W7) — coordinator perf: measure before touching a 1.0/10-health, untested hot path (Opus 5 `claude-opus-5`, 31-08-2026; offline, zero provider calls).** Repowise flagged `coordinator.py` at health 1.0/10 with 939 static I/O-in-loop findings; per the wave's own ruling those findings are the map, not the mission, so this unit profiled first. cProfile against a synthetic 480-lease fixture (incl. legacy manifest-derived nominal leases, matching production's un-backfilled shape) confirmed exactly one measured hotspot class in the claim/save path: `save_state()` calls `prepare_state_for_save()` (which already runs `expire_stale_leases` + scans `running_translation_leases`) and then `write_dashboard()`, which independently re-ran `expire_stale_leases` twice more via its own `running_translation_leases`/`reserved_translation_leases` calls — three redundant full-lease-list rescans per save on a state that provably had not changed in between. [`RussianTranslation/src/pilot/coordinator.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/coordinator.py): `running_translation_leases`/`reserved_translation_leases` gained a `skip_expire=False` opt-in (default preserves every existing call site byte-for-byte); `prepare_state_for_save` now returns `(paths, running)` and `save_state` threads `running`/`reserved` through to `write_dashboard(state, running=..., reserved=...)` instead of letting it rescan from scratch. Measured: 806011 → 632881 function calls (−21.5 %), 1.100 s → 0.978 s pure-Python cProfile tottime over 30 claim-shaped load/scan/save cycles (fsync/mkdir/replace excluded — this box's disk jitter swamps wall-clock at this fixture size, so call-count + tottime is the reproducible signal). Behavior equality proven two ways: the pre-change and post-change `coordinator.py` were run against an identical 5-cycle scenario (142 leases, a legacy manifest-derived nominal lease, and a TTL-expiring claimed lease) and their `state.json`/`dashboard.json` diffed byte-for-byte identical; a permanent pin (`test_h3754_save_state_dashboard_reuse_matches_full_rescan`) landed in [`coordinator_hardening_selftest.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/coordinator_hardening_selftest.py) asserting the reused-scan path matches a from-scratch rescan and that TTL expiry still fires exactly once. No characterization test existed for `coordinator.py`'s claim/save cycle before this pass — the new pin is also step 1 of the wave's own "test before any edit" bar. Nine other coordinator call sites still call `expire_stale_leases`/`normalize_lease_reservations` defensively per-invocation and were deliberately left alone: they are not called back-to-back on an unchanged state within one synchronous operation the way `prepare_state_for_save`→`write_dashboard` are, so removing their guard would risk the exact TTL-release correctness this same entry's [LANG_PARITY](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/LANG_PARITY.md) ledger governs, for a saving the profiler did not confirm as material. No behavior change, no store write, `window_selftest` 219/219, `lang_parity_check` 104 entries re-verified with 4-entry note (the one this fix's mechanism belongs to plus three incidental re-stamps).
