- **H3752 (W5) — the relation label re-derived from its own attachment result (Opus 5 `claude-opus-5`, 31-08-2026; offline, zero provider calls).** [Issue #1736](https://github.com/gasyoun/SanskritLexicography/issues/1736) closed on the code side: `subtype` was assigned from the **layer** and never re-read after the attachment lookup, so **4,187 rows** said `restate` — "PW пересказывает *этот* смысл PWG" — while the same row's `target_sense` read `*new`, the pipeline's own marker for *no such sense was identified*. Wave 1 (H2879) put the truth in a new `placement` boolean and deliberately left the label alone; but every surface that reads `subtype` **on its own** — sheet chips, `relationships_rollup.tsv`, the published "86 % of the re-glue is PWG paraphrase" headline — never saw the boolean. The fix adds no third field: in [`edition_rel.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/edition_rel.py) `subtype` becomes a **function of the placement result**, rewritten in the same expression that produced it, so the three labels naming a PWG *sense* as the relation's other end take an unplaced twin when no target was found: `restate` 562 placed / **4,637** `restate_unplaced`, `nws_at_sense` 6 / **317**, `a2a` 10 / **112** — **5,066 rows** relabelled.
  **Census first, per PLAN ruling 14** ([`h3752_relation_label_census.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h3752_relation_label_census.py), read-only): the live headline population is **4,187** against the issue's 4,132 — **×1.01**, worst divergence across all four claims ×1.11, far under the ×2 halt, so the rewrite half was authorised on a confirmed premise rather than a repeated one.
  **`direction` and `op` are untouched** — "the PW layer abridges PWG" needs no target — which is what separates this from the issue's rejected variant B (it would have erased the ＋/≈/✕ distinction from ~90 % of supplements). Three subtypes deliberately take no suffix: the additive ones assert a new sense rather than a relation to one; `pw_correct` rests on the gender index, a lookup that already succeeded; `pwg_internal_correction` was ruled on by wave 2, where unplaced-by-design is a published result.
  **Wave 1's anti-drift objection is answered mechanically, not promised:** one computation site, `placement_label_consistent()` stating the invariant in both directions, and new gate **W5a** in [`placement_axis_check.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/placement_axis_check.py) applying it to every row as a STOP — **RED at 5,066 rows** on the pre-fix sidecar (the wave's required RED pin), 0 after, alongside W5b–W5e. Consumers learn no second vocabulary: `base_subtype()` resolves a twin back to the key `TYPOLOGY`/`CLASS_OF`/the h180 strata already use, and a selftest pins that a twin can never fall through to a lookup default — which would have painted these very rows as green ＋ additions.
  **The canonical store was NOT rewritten, and that is measured:** its `edition_rel` field exists on 86 rows, all `base` (76) or `pwg_internal_correction` (10), none sense-asserting, so **0 store rows change** — proved by gate A5 with the sha pinned (`58c2172…`, 11,519 rows, byte-identical) and by `audit_store_gates.py` producing **byte-identical output** before and after, `changed_ru=0`. The label lives in the derived sidecar, and the 5,066 moves are ledgered anyway: `pwg-ru-data/tm/h3752_relabel_ledger.jsonl`, joined on the H3300 `row_key` rather than the bare pair (132 pairs repeat; a bare-pair join would ledger the wrong sibling, FINDINGS §551) — **5,066 in scope, 0 out of scope**.
  **What did not move:** the checkable population is 637 placed rows before and after, so the evidence sheet's cards are unchanged and it is **not re-cut** and no vote lock re-bound. Mode B (521 rows whose target number leads nowhere) is the issue's variant D — content alignment — and stays out of scope, still split into `out_of_range` (387, the renumbering phenomenon) and `not_found` (134). Waves 1–3 proved untouched: A1–A9, W2a–W2d, W3a–W3e, W7a–W7c all 0. Report: [reports/H3752_RELATION_LABEL_REVISION_31-08-2026.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/reports/H3752_RELATION_LABEL_REVISION_31-08-2026.md); spec: [REGLUE_SPEC §13](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/REGLUE_SPEC.md).
