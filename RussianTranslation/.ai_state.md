# Project Objective: PWG (Petersburg Dictionary) → Russian edition + the CDSL article-comparison study

Two live tracks (full cold-start brief: [`H025-Opus_RussianTranslation_pwg_ru_max_25.06.26.md`](H025-Opus_RussianTranslation_pwg_ru_max_25.06.26.md)):

- **Track A (PRIMARY):** scale PWG→Russian on the **Max** harness.
- **Track B (SECONDARY):** lock the article-comparison flagship pair (**agni + akṣara**)
  and finish its Russian to 100 %.

> This is the pwg_ru-detail journal, **subordinate to the root** [`../.ai_state.md`](../.ai_state.md).
> Keep both in lockstep — the prior divergence (judge gate, Renou) came from updating only
> the root copy. Audited + reconciled 2026-06-26.

## ➡️ Next Steps (Queue)

- **H405 Stage-2 mechanical pre-gate — RU WIRING DONE 09-07-2026 (Opus 4.8 `claude-opus-4-8`); only the EN port remains.**
  [`src/stage2_pregate.py`](src/stage2_pregate.py) built + selftest-green + LANG_PARITY SHARED; measured 99.72% CLEAN / 0.10% hard-FAIL over the 11,261-row store (13 real defects surfaced). `--wf` window-gate mode wired into [`src/pilot/audit_window.py`](src/pilot/audit_window.py) as `stage2_mechanical`; mechanical criteria stripped from both judge prompts ([`2_qa_sudya_opus.txt`](pwg_ru_prompts/2_qa_sudya_opus.txt) + YandexGPT twin); `window_selftest.py` green. **Remaining:** wire the pre-gate into the EN auditor [`src/pilot/audit_window_en.py`](src/pilot/audit_window_en.py) — it audits in-process per-card (not the file-pair subprocess model), so it needs an in-process adapter feeding `audit_card`'s (source, translation) to `pregate()`. Tracked as GAP `stage2_pregate_en_wiring_h405` in [LANG_PARITY.md](LANG_PARITY.md).

- **H317 pwg-ru medium50 test — resume once a clean environment is confirmed.** Closed this
  session at 0/50 promoted (transient API instability + kill-gate cascade, see the Completed
  entry below and [`MEASUREMENT_2026-07-08_H317.md`](MEASUREMENT_2026-07-08_H317.md)). A future
  session should first launch ONE solo diagnostic Workflow window; if it comes back mostly/fully
  clean, resume from `src/pilot/output/requeue.h317_w1a/w1b/w2a.keys.txt` (h317_w2b still needs
  its first launch), promote clean rows, and finish the n=50 metrics table.

- **H309 corpus-lexicon commentary-tail + adverb recall fix — ✅ DONE 08-07-2026 (Sonnet 5 `claude-sonnet-5`).**
  Fixed the two H136-measured recall failure classes ([recall_report.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/gold/recall_report.md)):
  widened [build_corpus_lexicon.py](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_corpus_lexicon.py)
  `SYS`/`SYS_BATCH` prompts to include ADVERB (was missing entirely from the content-word
  category list) + an explicit exhaustiveness instruction. Root-caused "commentary-tail
  unaligned" to single-pass dilution on a dense fused verse+commentary `sa` blob (not a
  skipped `comm1` segment as originally hypothesized). Targeted REPLACE re-harvest of the
  two genre-tagged commentary works (`gitartha-samgraha_yamunacharya` + `gitarthasamgraha-
  abhinavagupta`, 780 groups; 12,841→14,704 rows). **Medieval recall 84.0%→95.1%** (re-measured
  on the same 8 frozen `recall_set.jsonl` groups, 9/13 misses resolved, 4 residual documented);
  **overall recall 92.1%→95.4%** (Wilson CI 92.2–97.3). `PR_MEMO_A42.md` refreshed. The
  adverb/exhaustiveness prompt fix applies to all future harvests; the ~58k groups outside
  the targeted population were deliberately NOT re-harvested (documented residual, matches
  the "targeted, not full rebuild" mandate). [PR #250](https://github.com/gasyoun/SanskritLexicography/pull/250)
  MERGED (`30da847`), delivered via isolated worktree (`SanskritLexicography-h309`, removed
  after merge). `corpus_lexicon.jsonl` (gitignored) updated in the main checkout to match.

- **H335 pipeline capability audit — ✅ DONE 08-07-2026 (Fable 5 `claude-fable-5`; audit-only, no behavior change).**
  [`PIPELINE_CAPABILITY_AUDIT_2026-07-08.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/PIPELINE_CAPABILITY_AUDIT_2026-07-08.md)
  answers MG's four capability questions: **W1** 3-account concurrency = MISSING (no locking on the
  direct production path; collision matrix + 3-account protocol: worktree-per-account, root-sharding,
  single-promoter, global ≤3-wide; hardening list H-1..H-4); **W2** per-sense evidence provenance =
  MISSING (corpus_gate assembles 7 lanes then discards; the 4_korpus gate never ran; retrofit is a
  deterministic offline join — spec'd); **W3** case-government = PARTIAL (new
  [`src/government_census.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/government_census.py):
  **3,853 markers / 3,222 sense units / 1,476 entries** in raw PWG; 30/30 spot-check clean; store
  backfill surface 510 rows; extractor+backfill+prompt-rule spec'd); **W4** per-sense genre =
  mostly EXISTS (annotate_renou/sense_stratum already per-sense; literal genre = one join via
  `ls_source_map.json` 45 works ≈ 70% of 801,788 `<ls>`; taxonomy `@DECIDE` w/ recommendation).
  LANG_PARITY: `government_census` SHARED entry added. 4 MG `@DECIDE` forks + follow-up handoffs
  registered in Uprava (see the audit doc's fork table).

- **Research-grade 30-capability roadmap — ✅ DONE 09-07-2026 (Codex/GPT-5).**
  Added [`RESEARCH_CAPABILITY_ROADMAP_2026-07-09.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/RESEARCH_CAPABILITY_ROADMAP_2026-07-09.md):
  30 additive PWG→RU research capabilities, each framed as a falsifiable claim
  with data source, metric, acceptance test, and dependency flag. It is explicitly
  anchored to H335, [`ACL_TM_CROSSWALK_MEMO.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/ACL_TM_CROSSWALK_MEMO.md),
  and [`DECISIONS_PIPELINE_CAPABILITY_H335.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/DECISIONS_PIPELINE_CAPABILITY_H335.md);
  first implementation packages are evaluation spine, sense intelligence, TM
  safety/release, external acquisition, and graph/publication layer.

- **H290 (H215 Slice 4a) — oral TEXT + PDF front-end — ✅ SCAFFOLD DONE 07-07-2026 (Opus 4.8 `claude-opus-4-8`).**
  New [`src/build_oral_l0.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_oral_l0.py):
  reads ONE transcript (+optional PDF/DOC companion), **detects** the source shape (`bi`/`sa+pdf`/`ru+cit`,
  ambiguous→flag), **classifies** each companion PDF role (`edited-ru`/`sanskrit-source`/`commentary`),
  and emits the SAME `corpus_builder/<work>.jsonl` seg-rows `ingest_oral` does (tagged `orality`+`source_type`),
  so `build_l0`→`tm_grade`→`build_tmx` consume it unchanged. Multi-reference (spoken vs edited-ru handout)
  = separate `work` sharing the verse `passage` → consensus signal. Companion PDF/DOC→`.mdx` via the
  `/docx-to-md` skill only. Reuse: generalized `ingest_oral.to_corpus_rows(extra=)`, threaded `orality`/
  `source_type` through [`build_l0.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_l0.py).
  `selftest` green; all Slice-4 selftests + written TMX byte-unchanged. Design: [`src/ORAL_INGEST.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/ORAL_INGEST.md) "Slice 4a".
  - **Gated next (needs a real sample — MG `@DO`):** calibrate detector/classifier on one real transcript+PDF;
    replace the `sa+pdf` placeholder aligner with LaBSE/LASER+Vecalign; ingest a first series; report the oral
    A/B/C distribution + multi-ref agreement rate.
  - **`@DECIDE` (oral→A policy):** MG ruling 4 (consensus with a *written* work promotes oral to A) conflicts
    with the merged Slice-4 `build_tmx.oral_cap()` (oral A→B unless human-adjudicated). NOT flipped blind —
    reconcile once real oral graded output exists. See ORAL_INGEST.md "⚠️ Open policy conflict".

- **H215 Slice 4 — oral register — ✅ DONE 07-07-2026 (Opus 4.8 `claude-opus-4-8`).**
  New [`src/ingest_oral.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/ingest_oral.py):
  cleaned VTT/SRT/JSON transcript of spoken Sanskrit + Russian → `corpus_builder/<work>.jsonl`
  tagged `modality=oral` + `t_start`/`t_end`/`source_media`/`asr_conf` + canonical `iast_to_slp1`
  key; index-pairing with a hard mismatch guard; never-invent guards. Threaded `modality`/anchors
  through [`build_l0.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_l0.py)
  → [`build_tmx.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_tmx.py)
  (`l0_tu_xml` emits the oral props; written TMX byte-unchanged). **Lowered base grade** in one
  place: shared `build_tmx.oral_cap()` (A→B unless human-adjudicated) + `tm_grade.ORAL_PENALTY=0.15`.
  Shared schema/design (coordinated with [H174](https://github.com/gasyoun/Uprava/blob/main/handoffs/H174-Opus_spoken-sanskrit-corpus_spoken_sanskrit_corpus_scaffold_04.07.26.md)):
  [`src/ORAL_INGEST.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/ORAL_INGEST.md).
  Fixture pilot end-to-end (ingest→L0→grade→TMX validates); same corroborated units grade **6×A written
  vs 6×B oral** (mean composite 0.892→0.743). Selftests pass: `ingest_oral.py`, `build_tmx.py`,
  `tm_grade.py`, `build_l0.py`, `py_compile` all green. **➡️ Scale is asset-gated** — no oral recordings
  in a tracked repo yet (H174 unbuilt); scale-ingest runs once H174 delivers cleaned transcripts.
  Slice 5 (FAIR release) stays rights-gated (per-source clearance, human).

- **Launch-failure guardrails — ✅ DONE 07-07-2026 (Codex/GPT-5).**
  Added the mandatory post-launch incident ledger [`LAUNCH_FUCKUPS.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/LAUNCH_FUCKUPS.md)
  with a compact failure typology and seven seeded incidents (Slice D, H189/H191,
  H201, H178, H214, H220, H234). Added
  [`src/pilot/check_launch_ledger.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/check_launch_ledger.py)
  (`--handoff H###`, `--since YYYY-MM-DD`) and wired two selftests into
  `window_selftest.py`. Refreshed [`PIPELINE_HISTORY.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/PIPELINE_HISTORY.md)
  with H220 + post-launch discipline, and [`RUN_FREQ_MAX.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/RUN_FREQ_MAX.md)
  with the closeout checklist and lane-specific calibration rule. Verified:
  `check_launch_ledger.py`, `check_launch_ledger.py --handoff H220`,
  `check_launch_ledger.py --since 2026-07-05`, `py_compile`,
  `lang_parity_check.py`, and full `window_selftest.py` all pass.

- **H283 docs pass — ✅ DONE 07-07-2026 (Fable 5 `claude-fable-5`).** [`README.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/README.md) rewritten researcher-first (mw_ru + pwg_ru framing, method paragraph, FAIR datasets table, consumer use cases for student/lexicographer/NLP-researcher/CAT-translator, 14-phase milestone table, operator quickstart); [`PIPELINE_HISTORY.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/PIPELINE_HISTORY.md) Timeline extended with Phases 10–13 (H189→H191→H201 nominal arc · H184/H186/H224/H215 TM industrialization · H180 re-glue verdict · H234 orchestration guards) + "Current state" refreshed to 07-07; [`USE_CASES.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/USE_CASES.md) +§§14–18 (TM mining, TMX grading/export, re-glue, synth_dispatch fan-out, learner/addenda builders). All four scope forks ruled by MG in-session; handoff record [H283](https://github.com/gasyoun/Uprava/blob/main/handoffs/H283-Fable_SanskritLexicography_russiantranslation_readme_timeline_usecases_07.07.26.md).

- **H178 ACL-grade verify-and-improve pass — ✅ DONE 06/07-07-2026 (Fable 5 `claude-fable-5` orchestrating; generation Sonnet 5 `claude-sonnet-5`).**
  **Part A** ([PR #206](https://github.com/gasyoun/SanskritLexicography/pull/206) merged): 65-artifact historical gate re-audit ([`pwg_ru/H178_REAUDIT_2026-07-06.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/H178_REAUDIT_2026-07-06.md)) — `dah`/`no_pwg_w1` CLEAR, nominal allow-stale misfire reproduced-known, legacy flags decomposed (render drift ≠ defects, 16-key oversample list); A-1(b) date partition **CONFIRM-CLEAN, 0 pre-merge roots**; **`dah` 31/31** (nws00 1/1; pw topup 16/17 across 2 attempts, `dah~~h0_zz_pw__s10p0` = documented 🟡 residual, StructuredOutput retry-cap class); store **11,185 → 11,261**, TM 2,302/217/2,392 all green; provenance 11,261/11,261 stamped; execution-model + 5-layer source docs corrected (RUN_FREQ_MAX + H151).
  **Part B** (second PR): [`EVALUATION_PROTOCOL.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/EVALUATION_PROTOCOL.md) — four-rubric bake-off (MQM/Likert/DA/pairwise) machinery [`src/h178_eval_bakeoff.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/h178_eval_bakeoff.py) with 30-gloss held-out sample (22 roots, 10-key flagged oversample), DeepSeek `deepseek-chat` second channel + pairwise baseline run 30/30 (gotcha: the canonical `deepseek()` pins JSON mode — prompts must mention JSON, and SLP1 accent `\v` needs a lenient-escape fallback), 4 HTML sheets in `review/` (gitignored, registered in [REVIEW_SHEETS_INDEX](https://github.com/gasyoun/Uprava/blob/main/REVIEW_SHEETS_INDEX.md)); [`DATA_STATEMENT.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/DATA_STATEMENT.md) · [`REPRODUCIBILITY.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/REPRODUCIBILITY.md) · [`ERROR_ANALYSIS.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/ERROR_ANALYSIS.md) · paper **A51** ([`PAPER_LLM_LEXICOGRAPHY.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/PAPER_LLM_LEXICOGRAPHY.md), novelty verified, registered in ARTICLES).
  **➡️ Human gates:** MG votes the 4 `review/h178_*_sheet.html` sheets (+ minutes per sheet) → `pwg_ru/eval/h178_<rubric>.decisions.json`; HF license/token for COMETKiwi (`Unbabel/wmt22-cometkiwi-da` is gated). Then:
  ```
  Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H274-Fable_RussianTranslation_pwg_ru_bakeoff_compute_07.07.26.md and execute it.
  ```

- **H220 no-PWG single-card throughput — ✅ ROOT-CAUSED + FIXED 06-07-2026 (Opus 4.8 `claude-opus-4-8`; gen Sonnet 5 `claude-sonnet-5`).**
  The no-PWG lane's ~36 % single-card yield was root-caused (fresh 10-card diagnostic window through the Workflow tool + its own `journal.jsonl` / kill-log) to **two compounding failure modes**, both fixed in [`gen_opt_harness2.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py): **(A, dominant)** the wall-clock **kill gate** abandons valid-but-slow single supplement cards — calibrated on dense `tyaj` root batches, its byte-scaled budget (53–104 s) is below a tiny single card's fixed StructuredOutput latency (~55–105 s), and single-fragment no-PWG cards have no selfheal lane to route a kill to → permanent null (6/6 nulls were kill-timeouts, 4 of them would have passed `accept()`). Fixed: a no-fallback single now gets the **CEIL budget (180 s)** via `killBudgetForCur(cur)`; the aggressive gate stays for multi-card/splittable batches. **(B, secondary)** **key-echo mismatch** — the portrait's clean SLP1 `key1` (`CAyA`) pulls the model off the mangled `=== CARD _c_ay_a~~h0_zz_pw ===` header, so strict `km[k]` drops it. Fixed: nominal windows re-key via `nominal_keymap` when the SLP1 maps to exactly one pending stem (gated on `META.nominal`; PWG strict matching untouched). Plus observability: `selfHeal` no longer overwrites the real upstream reason with `no-selfheal-fallback`. **Verified: same 10-card window re-run → 10/10 ok (100 %, was 4/10 = 40 %), 0 kill-timeouts (was 6), 9 agents (no retries).** 3 new `window_selftest.py` tests, full suite green (83 PASS), `lang_parity_check.py` clean (new SHARED entry). Detail: [`RUN_LOG.md` H220 block](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/RUN_LOG.md). **The 232-lemma no-PWG lane is UNBLOCKED for scaling** (standard drain discipline: branches+PRs from `origin/master`, `--no-tm` on requeue, ≤3-wide, `--gen-model-version claude-sonnet-5`; heed `perf_preflight.py`; no-PWG cards are single-fragment so keep small batches).

- **H188 full-pipeline correctness + readiness audit — ✅ DONE 06-07-2026 (Claude Opus 4.8 `claude-opus-4-8`; handoff was written for Codex/GPT-5, but the mandate is model-agnostic so Opus ran it).**
  Deliverable [`pwg_ru/CODEX_AUDIT_2026-07-05.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/CODEX_AUDIT_2026-07-05.md) ([PR #198](https://github.com/gasyoun/SanskritLexicography/pull/198)). **Verdict: 🟢 GO on BOTH translation paths** — the verb drain (H151) and the nominal-core queue (H179). **0 🔴 blockers.** 1 🟠 correctness finding — autosplit merges emitted a stripped meta so promoted autosplit rows landed SHA-less (9 `vid` rows) — **fixed forward** by [`_autosplit_meta()`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/autosplit_requeue.py) in [PR #197](https://github.com/gasyoun/SanskritLexicography/pull/197) (pinned by `autosplit_requeue.py selftest`). 5 🟡 hygiene (filename-order card selection in `promote_final_cards.collect_cards`; three child gates' `find_results(...) or []` false-clean, parent-mitigated; H170 primitive unpinned; `gen_opt_harness.py`+`_pilot_gen.py` tracked-but-dead archive candidates; one doc nit). Seed #1 reconciled as already-landed (PR #155). Deterministic suite all green (window/TM selftests, TM validate card 2301/frag 217/pub 2392, lang-parity 20 no-drift, provenance 11,185 rows/0 stale, layer-presence all-benign). **The translation run can start at scale.** Follow-on = the ACL-grade methodological pass, still owned by [H178](https://github.com/gasyoun/Uprava/blob/main/handoffs/H178-Fable_RussianTranslation_pwg_ru_acl_verify_improve_05.07.26.md) Part B (NOT folded in, per MG ruling #4).

- **H234 orchestration post-mortem + guards — ✅ DONE 06-07-2026 (Fable 5 `claude-fable-5`, across a session-limit split).**
  The H180 Arm-B 10-agent synthesis fiasco (six failures: false-stall kill on buffered transcripts, mid-stream API stalls at 10-wide, a 34-min silent hang with no kill-guard, watcher-wiped gitignored outputs, a zombie overwriting the scored `viS`, free-form generation collapsing on >800-`<ls>` entries) is now guarded by [`src/synth_dispatch.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/synth_dispatch.py) — deterministic dispatch-and-monitor wrapper: ≤3 concurrency (hard cap 4, staggered), 10-min **output-file-growth** kill-guard (never transcript bytes), confirmed-dead-before-redispatch + single-owner sealed outputs, watcher-safe staging outside the repo with post-land sha re-verify, and a zero-LLM programmatic assembler auto-routed for inputs >800 `<ls>` (7-case selftest passes). `viS.de_synth.txt` regenerated via that assembler (1597 raw `<ls>`, `synth_score.py` row byte-identical to committed [`ARMB_SCORES.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/reglue/synth_outputs/ARMB_SCORES.tsv)). Post-mortem in [`PIPELINE_HISTORY.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/PIPELINE_HISTORY.md) + [Uprava/FINDINGS.md §42](https://github.com/gasyoun/Uprava/blob/main/FINDINGS.md). **Registry audit:** 10 stale pwg_ru rows moved 🟡→✅ (H056 H068 H184 H186 H189 H191 H201 H212 H214 H224), H215 → 🔵; **truly-unapplied pwg_ru queue = H178** (H188 ✅ audit, H220 ✅ throughput fix both landed 06-07-2026; + H143 blocked on MG `@DECIDE`, H151 legacy-skip). **Standing rule: any future pwg_ru multi-agent fan-out goes through `synth_dispatch.py`, never a bare 10-wide dispatch.**

- **H180 addenda typology + content-aware re-glue + learner apparatus — IMPLEMENTATION LANDED. ✅ 06-07-2026 (Opus 4.8, `claude-opus-4-8`; deterministic, ZERO LLM/translate call).**
  First-pass specs (PR #172) are now implemented as four no-re-translation builders in [`src/`](https://github.com/gasyoun/SanskritLexicography/tree/master/RussianTranslation/src):
  - [`build_relationships.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_relationships.py) — classified **5,603** non-`pwg` sub-card senses into the `provenance.relationship` typology (non-destructive sidecar; canonical store byte-identical). Rollup [`pwg_ru/relationships_rollup.tsv`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/relationships_rollup.tsv): `restate` 5,054 · `nws_at_sense` 211 · `a2a` 89 · `sch_star` 88 · `foreign_fragment` 62 (all `en`) · `derived_sense` 98 · `pw_correct` 1.
  - [`slp1_norm.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/slp1_norm.py) + [`build_learner_scores.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_learner_scores.py) — scored **106,079** PWG headwords by Russian-student-dict retention: **learner-core 22,772 (21%)**, Kochergina 15,254, ≥2-dict 6,516. Edition deltas normalized: AP90→AP dropped **3,489**, MW72→MW **5,257** (confirms LEARNER_APPARATUS §3).
  - [`build_reglue.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_reglue.py) — **15/15** pilot headwords re-glued, **byte-identity asserted on every `ru`** (re-glue is free); rendered cards in [`pwg_ru/reglue/`](https://github.com/gasyoun/SanskritLexicography/tree/master/RussianTranslation/pwg_ru/reglue). Finding: descendant sense-numbering diverges from PWG (PW renumbers; store is verb-root-first) → supplements fall to flagged `*new`, **nothing lost** — true sense-alignment is the deferred gold pass.
  - [`synth_de_first.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/synth_de_first.py) — Arm-B (synthesize-first) **inputs assembled** for all 10 bake-off words (deterministic, zero-cost); the tagged model run (Opus 4.8 synth → Sonnet 5 translate) + human coherence/fluency HTML sheet are **deferred** (Arm B emits new translations — only run paired with the sheet).
  Large derived outputs (relationships.jsonl, learner_scores.tsv, reglue/*.json, synth *.de.txt) are gitignored (regenerable from the local-only store); builders + specs + small summaries + rendered `.md` cards committed.
  **Review sheets stood up** ([PR #191](https://github.com/gasyoun/SanskritLexicography/pull/191)): 3 HTML voting sheets — typology κ (73), learner threshold (60), reglue spot-check (15) — via [`build_h180_review_sheets.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_h180_review_sheets.py); gitignored (embed unpublished `ru`, repo public), regen `python src/build_h180_review_sheets.py`, registered in [REVIEW_SHEETS_INDEX](https://github.com/gasyoun/Uprava/blob/main/REVIEW_SHEETS_INDEX.md). The 4th (Arm-A-vs-B coherence) waits on the Arm-B run.
  **➡️ Deferred next:** MG votes the 3 sheets → `/decisions-apply` → compute κ + calibrate the learner threshold, then run Arm B (Opus 4.8 synth → Sonnet 5 translate) + build the coherence sheet. Register paper **A49** movement per Deliverable 5. Resume:
  ```
  Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H180-Opus_RussianTranslation_pwg_ru_addenda_typology_glue_learner_05.07.26.md and execute it.
  ```

- **H186 running-text term mining — Track B SHIPPED (pilot); Track A BLOCKED. ✅/⛔ 06-07-2026 (Opus 4.8, `claude-opus-4-8`; extraction DeepSeek `deepseek-chat`).**
  New [`src/mine_running_text.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/mine_running_text.py) mines Sa→Ru term glosses from Russian running prose into a quarantined `mined` TM tier (`src/corpus_lexicon.mined.jsonl`, gitignored, `tier:"mined"`), **never** the clean 1.09M `corpus_lexicon.jsonl`. Reuses `build_corpus_lexicon.deepseek()/to_slp1()/has_cyr()`; never-invent prompt + deterministic verbatim-in-passage guard. **Pilot: 421 pairs** (induizm/mify/syrkin), 0 API failures; 30-row precision gate = **97% correct-equivalence, 80% meaning-gloss, 3.3% hard error** → scale approved ([PR #187](https://github.com/gasyoun/SanskritLexicography/pull/187)). Design/guards/precision: [`pwg_ru/RUNNING_TEXT_MINING.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/RUNNING_TEXT_MINING.md).
  **➡️ Track A blocked (needs a human):** none of MG's ~12 parallel texts exist in verse-aligned jsonl the aligner can consume (`corpus_lexicon` already covers all 116 verse-aligned SM works; raw GRETIL Sanskrit + separate Russian glossaries/prose only). Aligning them needs an **upstream SamudraManthanam `corpus_builder` ingestion job** + per-text edition/alignment decisions — routed to [GTD](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md). **✅ Track B scale (H224, 06-07-2026):** DONE — see Completed below.

- **H224 Track B scale — `mineall` batch over the whole SM folder. ✅ 06-07-2026 (Opus 4.8, `claude-opus-4-8`; extraction DeepSeek `deepseek-chat`).**
  Added a `mineall` command to [`src/mine_running_text.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/mine_running_text.py): scans all 148 `*.jsonl` in SamudraManthanam, applies a **deterministic, logged selection rule** (skip 116 verse-aligned + 18 dictionary/glossary + 1 index + 5 low-yield `<min-tb`; mine the rest cheap-first, `kommentarii` LAST), and mines each resumably. Selected **8 sources → 10,132 mined pairs** (8,576 distinct `(slp1,ru)`): `kommentarii` 7,244, `stepanyants` 1,248, `stati` 596, `mify` 333, `induizm` 323, `biruni` 280, eliade 72, syrkin 36. `kommentarii`'s first pass logged **227** transient DeepSeek timeout failures; one resumable mop-up re-run recovered **all 227** (+385 pairs, 0 residual). All other sources 0 failures. Fresh 30-row precision gate on the NEW sources = **97% correct-equivalence, 73% meaning-gloss, 3.3% hard error** — matches the pilot; the one hard error is the known list/context mis-scoping mode. Skip-set backed by committed [`pwg_ru/aligned_works.txt`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/aligned_works.txt); sample at [`pwg_ru/running_text_mining_precision_sample_scale.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/running_text_mining_precision_sample_scale.jsonl); full record in [`pwg_ru/RUNNING_TEXT_MINING.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/pwg_ru/RUNNING_TEXT_MINING.md). Tier stays quarantined + gitignored. `re-run mineall` to extend as new SM sources land.

- **H214 no-PWG lane — SHIPPED + first run + blockers fixed; throughput follow-on = H220. ✅/🟡 06-07-2026 (Opus 4.8, `claude-opus-4-8`; gen Sonnet 5 `claude-sonnet-5`).**
  The no-PWG supplement-chain render path shipped ([PR #178](https://github.com/gasyoun/SanskritLexicography/pull/178)); first live run (`no_pwg_w1`, 24 headwords / 58 sub-cards, 3 Workflow passes, ~3.4 M tokens) validated the path end-to-end. Two blockers found + fixed ([PR #183](https://github.com/gasyoun/SanskritLexicography/pull/183)): `{{Lbody=NNNN}}` alternate-headword-pointer leak (`dict_merge.resolve_lbody()`) and the nominal audit crash (`audit_window.py` skips glue for no-rootmap windows). **5 verified-clean sub-cards promoted** (store 11,163→11,185, `ai_translated`, held for G5). Full write-up: [RUN_LOG.md `no_pwg_w1` block](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/RUN_LOG.md). Released as **pwg_ru 1.4.0** (rolls up H182 + H214). **➡️ Throughput now SOLVED by H220 (06-07-2026): ~36% → 100% on the diagnostic window (kill-gate CEIL budget for no-fallback singles + nominal key-echo re-key). The 232-lemma lane is UNBLOCKED for scaling — see the H220 entry at the top of this queue.**

- **H215 Slice 1 — corpus Sa→Ru TM → TMX 1.4b exporter — ✅ DONE 06-07-2026 (Opus 4.8,
  `claude-opus-4-8`; deterministic, no LLM call).** New
  [`src/build_tmx.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_tmx.py)
  reads the gitignored `corpus_lexicon.jsonl` (DeepSeek L1 word/phrase units) + `corpus_strata.json`
  and emits standards-compliant TMX 1.4b: one `<tu>` per L1 unit, `srclang="sa-slp1"` (source seg = SLP1
  key, printed surface kept as `<prop type="surface">`), full provenance/grade `<prop>` spine
  (`layer/grade/modality/kind/work/passage/group/genre/period/date`). **Every unit stamped `grade=C`**
  until the composite grader (Slice 2, `src/tm_grade.py`) lands. Re-applies the never-invent guards
  (drops non-Cyrillic `ru`, empty key, `ru==sa`) at export; content-derived deterministic `tuid`s;
  `--sample N` reviewable slice; `build` self-round-trip-validates; `_audit.py` validates the release
  TMX when present. Output `release/corpus_tm/` gitignored (rights — in-copyright named RU translations;
  no release before Slice 5 clearance). Docs:
  [`src/BUILD_TMX.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/BUILD_TMX.md);
  committed fixture
  [`src/fixtures/corpus_lexicon.fixture.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/fixtures/corpus_lexicon.fixture.jsonl).
  Validation: `python src/build_tmx.py selftest` (3/6 rows kept, escaping + props clean) + a real
  fixture `build`.

- **H215 Slice 2 — composite A/B/C TM grader — ✅ DONE 06-07-2026 (Opus 4.8, `claude-opus-4-8`;
  [PR #193](https://github.com/gasyoun/SanskritLexicography/pull/193)).** New
  [`src/tm_grade.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/tm_grade.py)
  assigns the publication-grade stamp from a versioned composite (qe 0.35 · source 0.30 · consensus
  0.20 · align 0.15): **source_weight** (real, [`tm_source_weights.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/tm_source_weights.json))
  + **consensus** (real, whole-corpus — distinct works agreeing on one `(passage,slp1)`) + **qe**
  (pluggable: deterministic reference-free `proxy` default / COMET-QE `comet` hook) + **alignment**
  (Slice-3 proxy, weighted low). `A` = composite ≥ 0.70 AND corroborated (≥2 works, ≥50%) OR
  human-adjudicated; `B` ≥ 0.55; `C` else. `build_tmx build --grades <sidecar>` stamps real grades;
  `_audit.py` runs the grader selftest. **Measured over the full 1,091,528-unit corpus (qe=proxy):
  A 5.7% / B 94.0% / C 0.3%**, all 62,503 A units consensus-gated (0 violations). Calibration on the
  320-row `gold/gold_set.jsonl`: 0 defective rows reach A, but proxy QE separates acceptable/defective
  at only AUC ≈ 0.58 (a surface heuristic can't detect wrong-sense → semantic lift is consensus +
  A-gate; `--qe comet` / Slice 3 aligner are the next steps — [FINDINGS §60](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md)).
  Grades sidecar + TMX stay gitignored (rights). Docs: [`src/BUILD_TMX.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/BUILD_TMX.md).
  **Next:** Slice 3 (L0 segment layer + real SimAlign/awesome-align word-align cross-check → real
  `alignment_confidence`), Slice 4 (oral ingest, coordinate H174), Slice 5 (FAIR release — the real
  blocker: per-translator rights clearance). Handoff:
  [`H215`](https://github.com/gasyoun/Uprava/blob/main/handoffs/H215-Opus_RussianTranslation_pwg_ru_publication_grade_tm_tmx_and_oral_06.07.26.md).

- **H215 Slice 3 — L0 verse-segment layer + real word-align cross-check — ✅ DONE 07-07-2026
  (Opus 4.8, `claude-opus-4-8`).** Two new modules + wiring, worktree `SanskritLexicography-h215`.
  (1) [`src/build_l0.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/build_l0.py)
  rebuilds the **L0** layer from the SamudraManthanam verse-aligned source (`seg=sa/ru/comm*` per
  `group`, the L0↔L1 join key), stripping `БхГ 1.1`/daṇḍa/verse-number noise from the Sanskrit →
  **99,733 units (58,893 verse translations + 40,840 commentary) over 116 works**, gitignored
  `release/corpus_tm/corpus_l0.jsonl`. (2)
  [`src/tm_align.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/tm_align.py)
  cross-checks every L1 word-pair vs its L0 verse: `proxy` (deterministic grounding — sa word in
  verse? ru stems in translation?) ran on the **full 1,091,528-pair corpus** (mean
  `alignment_confidence` **0.684**, **93.4 % grounded**, 6.6 % ground to nothing); `embed`
  (SimAlign-style mBERT cosine, Jalili Sabet 2020) genuinely ran on a 400-pair Vedic sample
  (`bert-base-multilingual-cased` L8, mean 0.305 — weak on translit Sanskrit, documented). (3)
  Wiring: `tm_grade.py grade --align <sidecar>` supersedes the Slice-2 align proxy →
  **A 5.7 %→5.3 %, C 0.3 %→0.9 %** over the full corpus (ungrounded pairs correctly demote);
  `build_tmx.py build --l0 <file>` emits real `<tu segtype="block">` L0 verse segments (full graded
  TMX round-trip-validates). 4/4 selftests green (`build_l0`, `tm_align`, `tm_grade`, `build_tmx`).
  [FINDINGS §65](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md);
  [`src/BUILD_TMX.md` § L0 + alignment](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/BUILD_TMX.md).
  **Next:** Slice 4 (oral ingest, coordinate H174), Slice 5 (FAIR release — per-translator rights).

- **H214 no-PWG supplement-chain cards — ✅ DONE 06-07-2026 (Opus 4.8, `claude-opus-4-8`; no
  Max/Workflow generation run — deterministic render-path only).** Implemented the newly-ruled
  no-PWG render path (supersedes the H206/H212 ambiguity and decisions #2/#4 of
  [`PWG_MISS_RENDER_PATH_DECISIONS.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/PWG_MISS_RENDER_PATH_DECISIONS.md)):
  a PWG-missing headword that carries a PW/SCH/PWKVN/NWS record now renders **standalone
  supplement-chain sub-cards** (`<key>~~h0_zz_<layer>`) with **no fabricated PWG base portrait**.
  - [`_pilot_gen_merged.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/_pilot_gen_merged.py):
    new `no_pwg_parts()` + `gen_no_pwg_card()` (reuse `dict_merge.merged()` — no parallel merge
    path; renders raw NWS + authoritative owner map, unlike `supplement_parts`); `main()` falls
    through to it for empty-`pwg_bufs` keys instead of `MISSING`. Runs via the **nominal** harness
    (no rootmap): `gen_opt_harness2.py --nominal --keys=<...~~h0_zz_*>`.
  - Layer identity survives raw card → sub-card id (`layer_of`) → provenance → promoted `layer`
    field, verified end-to-end. **Per-card `source_profile`** (H214 + MG mixed-cards ask):
    [`gen_opt_harness2.card_source_profile()`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py)
    stamps `meta.source_profiles` (per sub-card) →
    [`promote_final_cards.provenance()`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/promote_final_cards.py)
    carries it to every row: `no_pwg_supplement_chain` / `pwg_with_supplements` (MIXED) /
    `pwg_only` / `pwg_supplement_subcard`. Filter `pwg_with_supplements` to find all mixed cards.
  - [`nominals_worklist.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/nominals_worklist.py):
    the 232 `other_layer_hits` become a **`no_pwg_runnable`** lane (176 in pril5), kept SEPARATE
    from PWG-rooted counts; true misses never reclassified as runnable. Backfill queue flipped
    BLOCKED → runnable.
  - Validation: `window_selftest.py` PASS (main tree; 7 new no-PWG tests), `dict_merge.py
    selftest`, `promote_final_cards.py --selftest`, `py_compile`, `lang_parity_check.py` clean;
    `Bagavat` (pw+nws) and `Akulita` (sch) render without PWG. **Concurrency note:** a parallel
    Codex/GPT-5 session implemented the same handoff in the main working tree; both converged, and
    this deliverable was re-applied source-only onto `origin/master` via a fresh worktree
    (branch-contention discipline) to avoid a racy commit.

- **PWG→RU code-hardening fix plan — ✅ DONE 06-07-2026 (Codex/GPT-5; no Max/Workflow generation run).**
  Implemented the local safety half of the fix plan before any production Max work: strict
  key-based response matching in `gen_opt_harness2.py` (no full-card positional fallback;
  fragment fallback only if the echoed fragment key matches), zero-marker TM sanity checks
  (`<ls>` / `{#` counts compared even when cached count is 0), exact-card TM exclusion for
  partial selfheal cards, defensive `supersedes` string coercion, nominal-window provenance
  without rootmaps, `reverse_index --show` representative lookup, and an explicit
  `--expected-cards` exact-count validator mode. Pinned by new `window_selftest.py`
  coverage for strict key matching, TM marker drift, partial-card TM exclusion via
  `translation_memory.py selftest`, and nominal stale checks. Validation: `py_compile`
  PASS, `translation_memory.py selftest` PASS, `reverse_index.py --show "m·8n*"` PASS,
  `validate_assembled_export.py --min-cards 1` and `--expected-cards 1` PASS on fixtures,
  `lang_parity_check.py` clean after re-verifying touched ledger hashes, and
  `window_selftest.py` PASS. **Not run:** H201 Sonnet/Max Workflow translation; external
  Max Workflow surface still required.

- **H182 upstream-change watcher — ✅ DONE 06-07-2026 (Opus 4.8, `claude-opus-4-8`).** Built the
  layer-version log + monthly Cologne/NWS drift watcher on branch
  `claude/h182-upstream-watcher`. [`src/pilot/layer_versions.py`](src/pilot/layer_versions.py)
  appends per-layer upstream provenance (csl-orig HEAD + each layer's last-touching commit +
  record/headword counts; NWS scrape counts) to append-only
  [`src/pilot/layer_version_log.jsonl`](src/pilot/layer_version_log.jsonl); backfilled once.
  [`src/pilot/watch_upstream.py`](src/pilot/watch_upstream.py) `cologne` diffs each csl-orig
  layer `last-seen..HEAD` (git `show`, read-only) → changed headwords (reusing
  `dict_merge`/`pwg_mask`/`corpus_gate.form_key`) → cross-refs the promoted store by
  `(form_key, layer)` → monthly `upstream_changes/<YYYY-MM>.md` + `.stale.json` worklist
  carrying each flagged headword's `input_raw_sha256` (H170 primitive). `nws` re-fetches only
  the ~48 promoted headwords (polite, downtime-resilient). Validated end-to-end on real data:
  a rewound PWG diff (`aec92ed..HEAD`) detected 6 changed headwords incl. the promoted root
  **`dA`**, correctly flagging its pwg-layer rows. Baseline seeded at csl-orig `78cbb257`.
  Both modules carry selftests; schedule wired via
  [`.github/workflows/upstream-watch.yml`](../.github/workflows/upstream-watch.yml) + a local
  `schtasks` recipe in [UPSTREAM_WATCHER.md](UPSTREAM_WATCHER.md). **Guardrail:** flags only —
  re-runs stay on the H179/H151 drain discipline; the watcher never re-translates.
  Merged: [PR #174](https://github.com/gasyoun/SanskritLexicography/pull/174) (watcher) +
  [PR #176](https://github.com/gasyoun/SanskritLexicography/pull/176) (fuller `--counts` backfill
  entry: csl-orig records/headwords **pwg 123,366/106,082 · pw 170,556/151,349 · sch 29,125/28,455 ·
  pwkvn 24,976/14,995**; NWS `net_new` left null — reading all ~168k gitignored cards is
  filesystem-bound >15 min). **Baseline re-confirmed 06-07-2026**: an incremental `cologne` run
  (not a re-seed) diffed `last-seen..HEAD` and found **0 drift** across all 4 layers — csl-orig
  HEAD still `78cbb257`, matching the seeded baseline in
  [`upstream_changes/_state.json`](src/pilot/upstream_changes/_state.json). Steady-state path
  verified: nothing upstream moved → nothing flagged. Note: the csl-orig 4-layer index is ~5s
  run alone (earlier "minutes" was disk thrashing from parallel runs); only the 168k-file NWS
  net-new tally is genuinely slow.

- **H191 deterministic verify + 100-small nominal staging — ✅ DONE 05-07-2026 (Codex/GPT-5; no
  Max/Workflow generation run).** Executed the H191 verify/optimize handoff on branch
  `codex/h191-pwg-ru-verify-optimize-100small`: H189 economics re-derived to the digit
  (**42,316,604 tokens / ~$79.83**, cache-write **12,738,510**), `gam` guardrail replay now
  shows `agent_expected_after_tm=6`, and the H189 verification report is tracked at
  [`src/pilot/VERIFY_H189.md`](src/pilot/VERIFY_H189.md). Three shared code fixes landed with
  pinning selftests: B1 prunes `INPUTS`/`PH` to agent-reachable keys only, B2 sub-splits
  single-line citation monsters without tearing `<ls>` spans, and B3 emits `cost_partition`
  (`run_now` / `defer_monster`) with grouped cost totals. Validation: `window_selftest.py`
  PASS, `lang_parity_check.py` clean (20 entries), `translation_memory.py selftest` PASS,
  `py_compile` PASS, and the generated 100-small JS passes `node --check`.
  **Next runnable handoff:** Sonnet/Max should read
  `C:\Users\user\Documents\GitHub\Uprava\handoffs\H201-Sonnet_RussianTranslation_pwg_ru_nominal_w1_100small_run_05.07.26.md`
  and execute it. The staged window has **100 selected keys**, **95 live inputs**, **5
  degenerate pass-through**, **0 defer_monster**, **3 expected agents**, harness **274,848
  bytes**, and preflight **~745,200 tokens / ~$1.41**. Staging files:
  [`src/pilot/NOMINAL_W1_100SMALL.md`](src/pilot/NOMINAL_W1_100SMALL.md),
  [`src/pilot/NOMINAL_W1_100SMALL.keys.txt`](src/pilot/NOMINAL_W1_100SMALL.keys.txt),
  [`src/pilot/NOMINAL_W1_100SMALL.selection.json`](src/pilot/NOMINAL_W1_100SMALL.selection.json),
  [`src/pilot/NOMINAL_W1_100SMALL.preflight.json`](src/pilot/NOMINAL_W1_100SMALL.preflight.json).

- **H201 run — ✅ RUN & PROMOTED 100/100 on 06-07-2026 (gen Sonnet 5 `claude-sonnet-5`;
  orchestration Opus 4.8 `claude-opus-4-8`, Max/Workflow surface).** The staged window is fully
  AI-translated and promoted. Ran in **three Workflow passes**: pass 1 (full) hit a transient
  `Connection closed mid-response` outage on **all** batches (incl. the 4-card batch-0), tripping
  the 19-agent kill switch → **5 ok / 95 null**; a clean rerun (`run_pilot_wf.opt2.js`, all 100
  requeued) recovered **93 ok / 7 null**; a 7-key transient requeue
  (`run_pilot_wf.nominal_w1_100small.requeue7.js`) closed the residual to **7 ok / 0 null** in 42s.
  Totals: **37 agents, ~2.50 M subagent tokens, ~23 min** wall-clock. Promotion
  (`promote_final_cards.py --merge --gen-model-version claude-sonnet-5`): **100 non-null cards /
  306 sense rows**, store `src/pwg_ru_translated.jsonl` = **11,163 rows** (`ai_translated`, held
  from the citable edition until **G5 human review**); TM rebuilt to **2,297 cards**; no
  `frag_prov` → no `build-frags`; **no requeue residuals**. **⚠ Audit caveat (logged in RUN_LOG):**
  nominal keys-based windows have **no rootmap**, so `audit_window.py` needs `--allow-stale`, in
  which forensic mode the glue gates crash and `missing_required_sense_field` misfires (bogus 86
  "defect" on pass-2 — all four spot-checked cards were complete; the flagged ones are legit
  *see-under* stubs). Trust Workflow `summary.null_keys`, not the stale-audit defect count.
  Detail: [`RUN_LOG.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/RUN_LOG.md) (2026-07-06 RUN & PROMOTED block).

- **✅ H189 cost-blow-up fixes LANDED 05-07-2026 (Opus 4.8, `claude-opus-4-8`; gen of the aborted run
  Sonnet 5 `claude-sonnet-5`).** The `pril10_w1` blow-up (42.3 M tok / ~$79.83 / ~3 of 8 cards; every
  §4 hypothesis CONFIRMED, economics reproduced to the digit) is fully diagnosed and guardrailed.
  Verified post-mortem: [`POSTMORTEM_pril10_w1.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/POSTMORTEM_pril10_w1.md).
  Five SHARED fixes (68/68 selftests green, parity ledger clean), all in
  [`gen_opt_harness2.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py)
  + [`perf_preflight.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/perf_preflight.py):
  (1) **presplit-lane re-batching** (budget 60/18: `pril10_w1` 174→69 groups, real `gam` giants 18→6
  agents); (2) **kill-gate recalibration** (floor 120→45 s, ceil 480→180 s); (3) **live budget
  kill-switch** (`MAX_AGENTS=⌈expected×3⌉+10 (size-proportional)`, self-abort + requeue); (4) **harness-size guard**
  (warn + key-split > 480 KB); (5) **preflight cost gate** (tokens/$/per-card, `--refuse-over-cost`).
  Plus a latent nominal crash fixed (`_slp1_lex_for_key` on empty `[]` portrait). Run detail:
  [`RUN_LOG.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/RUN_LOG.md)
  (2026-07-05 H189 block).
  **⚠️ Standing rule — a window of `kAla`-class monsters must NOT run as bulk:** the cost gate flags
  `pril10_w1` at ~$4/card **even post-fix** (8 top-heads are intrinsically expensive). MG `@DECIDE`:
  choose the monster-card handling (dedicated human-budgeted lane vs cap-and-defer) before the next
  nominal window; the first nominal window should be *typical* heads, not top-8-by-size.

- **H151 verb-root drain — ▶ UNPARKED 05-07-2026 (Opus 4.8, `claude-opus-4-8`).** The park condition
  ("until H189 §5.B/§5.C guardrails land") is now **met** — the per-fragment cost blow-up that also
  threatened dense verb roots is fixed (presplit-lane re-batching) and bounded (recalibrated kill
  gate + live budget kill-switch + preflight cost gate). H151 may resume with the new guardrails
  active. **Discipline for the resume:** run [`perf_preflight.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/perf_preflight.py)
  on each candidate root FIRST and heed the cost-gate verdict — a root the gate flags `OVER-CEILING`
  is a `kAla`-class monster and goes to the human-budgeted lane, not the bulk drain; the kill-switch
  now self-aborts a runaway instead of a manual kill.

- **H179 nominal lexical-core queue adapter + `layer` field — ✅ DONE 05-07-2026 (orchestration
  Opus 4.8, `claude-opus-4-8`; NO generation ran — Step 3 drain is the Sonnet 5 follow-on).
  [PR #152](https://github.com/gasyoun/SanskritLexicography/pull/152) merged.**
  Step 2 (the switch-enabler for the Приложение 5 nominal drain) + Step 1 items 1 & 3:
  - [`extract_lexical_cores.py`](src/pilot/extract_lexical_cores.py) — VisualDCS Приложение
    10/5 + Сборное ядро `.xls`/`.xlsx` (lemmas are **IAST**, not SLP1) → deduped SLP1
    wordlists + TSV via `build_src.iast_to_slp1`.
  - [`nominals_worklist.py`](src/pilot/nominals_worklist.py) — flat-nominal sibling of
    `verb_worklist.py`; `form_key` join into merged PWG index, hit/miss coverage, DCS-score
    ordering (period-spread tiebreak), cumulative dedup vs the store. Verbs (POS=`v`) set
    aside to the H151 verb drain.
  - [`lexical_cores/`](src/pilot/lexical_cores/) — committed SLP1 lists + coverage: **pril10
    286 / pril5 4292 / sbornoe 4798 runnable nominals**. Flat-nominal generation validated on
    `indra`/`agni`/`kAla`.
  - **Step 1.1** — explicit `layer` field (pwg/pw/sch/pwkvn/nws) on every promoted row via
    new `dict_merge.layer_of()`; local store backfilled (10,857 rows). SHARED across RU/EN
    (EN inherits via `promote_en` attach); LANG_PARITY `layer_field_on_promoted_rows` added.
  - **Step 1.3** — [`layer_presence_audit.py`](src/pilot/layer_presence_audit.py) report-only
    `LAYER-DROPPED` gate; `--exclude-nws` surfaces 10 real overlay gaps (e.g. `dah dropped=pw`).
  - **⚠️ FINDING (needs a human ruling):** H179's "Приложение 5 = 3,493" is the single
    **longest period column**; Step 2.1's requested **7-period union is 6,337 lemmas → 4,292
    runnable nominals**. Used the union (per the Step-2 instruction); flagged in
    [`lexical_cores/README.md`](src/pilot/lexical_cores/README.md).
  - **⚠️ Concurrency:** a **separate session** has in-flight failure-gallery/dashboard work
    uncommitted in the main working tree (`audit_window.py`, `dashboard/*`,
    `evolution_stats.py`, `pwg-layers.md`) — this PR was authored via a fresh worktree and
    deliberately excludes those files; do not clobber them.
  - **➡️ RESUME (Step 3 — the actual nominal drain):** lead with Приложение 10 (tier 2), then
    Приложение 5 (tier 3), under the standard drain discipline (branches+PRs from
    `origin/master`, `--no-tm` on requeue, ≤3-wide, `--gen-model-version claude-sonnet-5`):
    ```
    python src/pilot/nominals_worklist.py src/pilot/lexical_cores/pril10.slp1.txt --top 30
    python src/_pilot_gen_merged.py <top keys>
    python src/pilot/gen_opt_harness2.py pril10 --nominal --keys=<ordered> --gen-model-version claude-sonnet-5
    ```
    Then audit → promote → TM rebuild per batch. **Park H151 the moment this drain starts**
    (not before — no idle window). **Deferred: Step 1.2** (`provenance.relationship` inline
    sidecar) belongs to the FIRST batch that runs a live translate pass — wire it into the
    translate prompt then.

- **H151 `dah` drain — 🟡 PARTIAL DONE 04-07-2026 (orchestration Opus 4.8, `claude-opus-4-8`;
  generation Sonnet 5, `claude-sonnet-5`, hardcoded `model:'sonnet'` in the harness).**
  Resumed the standing H151 drain after a prior **Sonnet** session stopped on H151 believing the
  Workflow surface was unavailable — that was a **session-tooling gap, not a pipeline bug**: that
  session genuinely had no Workflow/agent tool, whereas this Opus 4.8 Claude Code session does, so
  the harness (`run_pilot_wf.opt2.js`, already a valid `export const meta` Workflow script) was
  driven directly per the H145 guardrail (no manual Max-surface save; `.result` captured to
  `wf_output.json` programmatically). **Base run** (15 agents, ~1.13M tokens, ~6.5 min):
  **31/31 cards ok, 0 null, 0 failures, 1 healed.** Audit: **26 clean, 5 flagged** (4 gate-defect +
  1 partial). One **`--no-tm` requeue round** (4 keys, 3 agents, ~1.5 min; `--no-tm` verified on the
  harness) cleared 3 → **29/31 clean**. Promoted the **29 clean sub-cards**
  (`promote_final_cards.py --merge --gen-model-version claude-sonnet-5`, store now 10,857 rows,
  other roots preserved); TM rebuilt (`translation_memory.ru.json` **2,204 cards**); no `frag_prov`
  among the promoted cards so `build-frags` not needed. **2 cards HELD unpromoted** (MG directive
  mid-session: *finish what's started, do not start new translations*) — `dah~~h0_zz_nws00`
  (NWS-layer gate-defect, survived 1 requeue) + `dah~~h0_zz_pw` (partial PW-addenda card, 17 missing
  fragments in groups g2/g3 of 7). Full 31-card run preserved at
  [`src/pilot/output/dah.run31.hold.json`](src/pilot/output/dah.run31.hold.json) (outside the
  promote glob) so the 2 held cards can be finished later without re-running the clean 29:
  requeue `dah~~h0_zz_nws00` (`--no-tm`) + `autosplit_requeue.py topup` `dah~~h0_zz_pw`, then
  `promote_final_cards.py --merge`. Worklist after: **REMAINING 701, promoted 48**; `dah` is off
  the runnable queue. **Next runnable head (`verb_worklist.py --top`): `sTA`(61.6, defer-calibrate)
  `BU`(60.2) `yuj`(58.2) `as`(57.5) `i`(57.4) `tap`(49.4)** — do NOT start `sTA` without its own
  calibration session.

- **PWG→RU SLA coordinator ops window — WIP/BLOCKED on Workflow result capture (04-07-2026, Codex/GPT-5).**
  Created branch `codex/pwg-ru-sla-ops-20260704` from the coordinator branch and claimed/prepared
  the first max-throughput coordinator leases: `tyaj` (`verb-drain-tyaj-tyaj-20260704T182838Z-28924`,
  19 cards / 5 batches / 9 expected agents), `dah`
  (`verb-drain-dah-dah-20260704T182845Z-3332`, 31 cards / 6 batches / 13 expected agents), and the
  nominal batch `afRin`–`aMSu`
  (`nominal-nominal-a001-nominal_afRin-20260704T182846Z-27816`, 12 cards / 3 batches / 3 expected
  agents). Harnesses and preflight JSON are under `src/pilot/output/coordinator/artifacts/`.
  **Blocked step:** run each generated `run_pilot_wf.*.js` in the Claude/Max Workflow surface and
  save the full `.result` wrapper as `workflow_result.raw.json` in its lease artifact directory,
  then run `python src\pilot\coordinator.py record-output <lease-id> <artifact>\workflow_result.raw.json`.
  Deterministic side lanes completed: rootmaps generated for `muh`, `nu`, `hu`, `sev`, `pU`
  (glue audit PASS; runnable pool now 18 roots / 930,856 bytes / 684 missing rootmaps), `SuD` stayed
  blocked because `_pilot_gen_merged.py --root-split SuD` produced a whole-card input but no rootmap,
  and calibration manifests were emitted for `aS` and `kzip`. `window_selftest.py` PASS.

- **Full PWG→RU 30-day SLA documentation — ✅ DONE 04-07-2026 (Codex/GPT-5).**
  Added [`FULL_PWG_RU_30_DAY_SLA_PLAN.md`](FULL_PWG_RU_30_DAY_SLA_PLAN.md) as the
  docs-only hard-SLA plan for a full PWG Russian pass (**120,173 headword cards /
  ~120k-160k sub-cards**) under 30 calendar days, assuming 24/7 operation if caps
  allow while preserving the locked **≤3 concurrent LLM/Workflow translation lanes**
  rule. The plan separates H151 verb-root drain from the broader full-dictionary
  work: translation lanes, rootmap unblock, non-root/noun bulk, and publication
  validation. Cross-noted H151 and the economics note; no pipeline code changed.

- **H151 runnable-queue hardening — ✅ DONE 04-07-2026 (Codex/GPT-5).** Follow-up to the
  readiness review that found two non-token-spend blockers before starting `sTA`: the live
  worklist could advertise roots with no rootmap (`SrI`), and H151/pwg-drain still carried
  stale `sTA`/`i` agent estimates. [`verb_worklist.py`](src/pilot/verb_worklist.py) now keeps
  the full unpromoted backlog in `remaining*` fields but exposes runnable operator order via
  `runnable_remaining`, `runnable_count`, `runnable_bytes`, with missing-rootmap roots preserved
  in `blocked_missing_rootmap`; `--top` prints runnable roots by default, and
  `--include-unrunnable` is audit/debug only. Pinned by
  `window_selftest.py::test_verb_worklist_excludes_missing_rootmaps`. H151, `/pwg-drain`, and
  [`RUN_FREQ_MAX.md`](src/pilot/RUN_FREQ_MAX.md) now say to trust live `perf_preflight.py`
  output and record the current `sTA` calibration warning (123 cards, 19 batches, 241 expected
  agents, `defer-calibrate`) instead of the old ~30-agent prose estimate. **Next safe resume:**
  create the actual drain branch from `origin/master`, run
  `python src\pilot\verb_worklist.py --top 20`, then preflight the runnable roots; do not start
  a Max Workflow run from stale fixed-order prose.

- **`pipeline_version_stamp_en_gap` closed same day — ✅ DONE 04-07-2026 (Sonnet 5,
  `claude-sonnet-5`).** Follow-up to the H169 parity re-affirm below: PR #140 stamped
  `provenance.pipeline` on RU rows (`promote_final_cards.py`) but never on EN
  (`promote_en.py`). [`promote_en.py`](src/promote_en.py)'s `en_index()` now calls
  `pipeline_version.stamp(model_version=gen_model_version)` per sub-card and stores it as
  `en_provenance.pipeline` (a distinct field from RU's `provenance.pipeline` since EN
  attaches onto an existing row rather than owning it). Pinned by a new assertion in
  `promote_en.selftest()`. LANG_PARITY entry `pipeline_version_stamp_en_gap` re-verdicted
  GAP → SHARED. `window_selftest.py` 52/52 green, `lang_parity_check.py` clean (17
  entries), `promote_en.py --selftest` and `pipeline_version.py --selftest` both pass.

- **H169 QA-gate teeth (real `DUP` hard flag + RU-gate verdict-parse guard) — ✅ DONE
  04-07-2026 (Sonnet 5, `claude-sonnet-5`).** Fixed the 2 🟠 "broken validators" findings
  from [`CODE_REVIEW_2026-07-04.md`](CODE_REVIEW_2026-07-04.md) deferred out of PR #138.
  **(1)** [`audit_window_en.py`](src/pilot/audit_window_en.py): the advertised HARD `DUP`
  gate was never emitted — two senses with identical english slid through `--strict`
  clean because the only signal was soft `SAME-GLOSS`, gated on `>=3` content words. Now
  emits a real HARD `DUP` flag on any identical-english pair within a record regardless of
  length; added to `HARD`. **(2)** [`audit_window.py`](src/pilot/audit_window.py): the RU
  gate recovered `translation`/`coverage`/`sense_dupes` verdicts by regex-scraping child
  stdout — any wording drift silently returned `[]`, dropping flagged cards from the
  requeue with the gate reporting clean. [`audit_translation.py`](src/audit_translation.py),
  [`audit_coverage.py`](src/audit_coverage.py), and
  [`audit_sense_dupes.py`](src/audit_sense_dupes.py) now also emit a strict
  `FLAGGED_JSON: [...]` line; the parent parses it strictly and treats a missing/malformed
  line as unparseable — requeueing every card in the window and marking the gate crashed,
  never silently passing. Dead `parse_nws` removed. Pinned by 2 new
  `window_selftest.py` cases (`test_en_gate_dup_has_teeth`,
  `test_ru_gate_fails_loud_on_unparseable_child`); full suite green (52 tests).
  **Parity:** DUP fix logged as `en_dup_hard_gate_20260704` (SHARED, GAP-closing per the
  handoff's classification — see [`LANG_PARITY.md`](LANG_PARITY.md) note for the nuance
  that RU's own within-record duplicate signal is softer, all-senses-only). Re-affirmed
  drifted hashes on 7 unrelated entries this branch's edits touched (all Case-C
  re-snapshots, no behavior change). **Incidental finding while re-affirming parity:**
  PR #140's pipeline-version stamping landed only on `promote_final_cards.py` (RU), not
  `promote_en.py` — logged as a new GAP ledger entry `pipeline_version_stamp_en_gap`,
  tracked in [`Uprava/GTD_NEXT_ACTIONS.md`](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md)
  @DO, not fixed here (out of H169's scope).

- **Publication-grade TM schema hardening — ✅ DONE 04-07-2026 (Codex/GPT-5).** Upgraded
  the translation memory from cache-only toward a scholarly audit artifact: added a tracked
  publication schema contract (`schemas/translation_memory.schema.json`), a reusable datasheet
  template (`TRANSLATION_MEMORY_DATASHEET.md`), valid/invalid row fixtures, validation helpers
  + `translation_memory.py validate`, and suggestion-row filtering so malformed advisory rows
  cannot enter prompts. Fuzzy evidence now preserves separate German-fragment, Sanskrit-headword,
  and semantic-tag score channels; generated harness prompts render those channels separately.
  RU raw MW-English suggestions remain blocked; MW influence is only via curated
  Sanskrit→Russian terminology rows with provenance. Validation passed:
  `python src\pilot\translation_memory.py selftest`, `python src\mw_en_tm.py --selftest`,
  `python src\pilot\translation_memory.py validate --lang ru`,
  `python src\pilot\window_selftest.py`, JSON schema parse, and py_compile.

- **Production-speed TM hardening — ✅ DONE 04-07-2026 (Codex/GPT-5).** Implemented
  trust-ranked exact TM metadata and loading (`reviewed_exact` > `machine_exact` >
  legacy), defect-dominant card eligibility (any bad row blocks card auto-reuse),
  best-reusable fragment selection, language-prefixed local denylist invalidation for defect/all
  requeues, exact-cache sanity checks before auto-serve, and an advisory-only
  `translation_memory.suggest.<lang>.jsonl` lane (`--suggest-tm`) that never changes
  `tm_hits` or agent accounting. MW suggestion build is EN-only
  (`build-suggestions --lang en --mw-tm src\mw_en_tm.json`); RU direct MW-English suggestions
  are blocked in favor of `--terminology` curated Sanskrit→Russian rows. Fragment TM rows now
  carry low-cost audit metadata (`raw_sha256` when available, splitter/formula/source file), and
  malformed nested workflow results are skipped with a warning instead of crashing.
  TEI/OntoLex export remains
  human-approved by default with explicit `--preview-machine` for non-citable machine rows.
  Validation passed: `python src\pilot\translation_memory.py selftest`,
  `python src\pilot\window_selftest.py`, `python -m py_compile ...`, suggestion build smoke
  (EN MW suggestions), and default/preview `export_interop.py all --limit 10` +
  `validate_interop.py`.

- **H151 `vid` drain — ✅ DONE 04-07-2026 (Sonnet 5, `claude-sonnet-5`), driven from a second
  concurrent Claude Code session (main checkout, not a worktree — 5 other worktree sessions
  were active on the same H151 queue at the time; MG explicitly authorized continuing the
  main-tree `vid` state rather than starting fresh).** Took over an in-flight `wf_output.json`
  (root `vid`, `needs_requeue`, 47/55 clean) and drove 3 more `--no-tm` requeue rounds via the
  Workflow tool directly (H130 pattern — no manual Max-surface save): round 2 (8 keys, 62
  agents) → 51/55; round 3 (4 keys, 48 agents) → 52/55; round 4 (3 keys, 36 agents) → 54/55,
  coverage + sense-dupe gates PASS. The 2 remaining partial cards (`h0_10_ni`, `h2_00_pwg01`,
  1 missing fragment group each) cleared via `autosplit_requeue.py topup`/`topup-merge` (2
  agents) instead of a full requeue. **1 documented residual left:** `vid~~h0_00_pwg01`'s
  `foreign_gloss_translated` high-confidence flag fired on a CORRECT translation ("Sieger sein
  werde" → «будет победителем») across all 3 requeue attempts — suspected the same
  German-capitalization false-positive class as the gam `LATIN_BINOMIAL` bug fixed in
  [PR #119](https://github.com/gasyoun/SanskritLexicography/pull/119), NOT re-investigated
  this session (worth a follow-up gate audit if it recurs on other roots). Promoted
  `claude-sonnet-5` (2 merge passes: full 55-card file, then the 2-card topup file to override
  the stale partials), TM rebuilt (2,175 cards, +177 fragments). Ledger row added to
  [H151](https://github.com/gasyoun/Uprava/blob/main/handoffs/H151-Sonnet_RussianTranslation_pwg_ru_verb_batch_drain_04.07.26.md).
  **Next queue head (per `verb_worklist.py --top 20`): `sTA`(61.6) `BU`(60.2) `yuj`(58.2)
  `as`(57.5) `i`(57.4)** — `vid` now clears the Wave-1 head.

- **H155 follow-up — failure-mode taxonomy + wall-clock KILL GATE — ✅ DONE 04-07-2026
  (Opus 4.8, `claude-opus-4-8`).** MG: "sense-count is just one case — what other cases are
  possible? Plan in advance. And add a gate: if a translation runs too long, kill it, don't
  wait for miracles." **(1) Taxonomy** — [`FAILURE_MODES_AND_KILL_GATE_2026-07-04.md`](FAILURE_MODES_AND_KILL_GATE_2026-07-04.md)
  enumerates every whole-card/batch StructuredOutput-stall driver from the timeline: citations
  (#1, handled), senses (#2, handled H155), **gloss-prose volume (#3), masked-token count (#4),
  multi-layer nesting (#5), batch-level sums (#6)** — all the same disease (too much output),
  surfacing via whichever attribute spikes first; any single-metric budget is blind to the
  rest. **(2) Kill gate** — runtime backstop for the un-triggered drivers: every schema-bearing
  `agent()` call is raced against a `setTimeout` budget scaled to its masked-skeleton byte
  volume (best time predictor — output ≈ 2× skeleton); overrun by `KILL_FACTOR=2×` → abandoned,
  cards routed to the bounded fragment lane instead of the full ~5-deep retry cap. Runtime
  probe: `setTimeout`/`Promise.race` work (relative timer; `Date.now()` banned), **no
  `AbortController`** so a killed call keeps running in the background until its own cap — the
  harness just stops blocking. Constants calibrated from a 13-call `tyaj --no-tm` benchmark
  (legit ≤84 s @ ~25 ms/skel-byte): BASE 30 s, SLOPE 45 ms/byte, FLOOR 120 s, CEIL 480 s →
  budgets 127 s (744 B) … 416 s (3956 B). On kill, `resolveGroup` breaks to BINARY_SPLIT and
  `healGroup` bisects, so a killed card is **recovered, never dropped**. Proven: zero-token
  behavioural test (`kill_mechanism_test.mjs` 9/9) + static wiring selftest
  `test_kill_gate_wired`; full suite green (47 tests). LANG_PARITY entry `wall_clock_kill_gate`
  (SHARED, lang-agnostic — keys on skeleton bytes, no RU/EN branching); gate clean (14 entries).
  **Discipline:** when a new stall class appears in a real run, promote it from kill-gate-caught
  to its own structural trigger (as senses did) so the next one spends zero doomed tokens.
  Files: [`src/pilot/gen_opt_harness2.py`](src/pilot/gen_opt_harness2.py),
  [`src/pilot/window_selftest.py`](src/pilot/window_selftest.py),
  [`LANG_PARITY.md`](LANG_PARITY.md), [`PIPELINE_HISTORY.md`](PIPELINE_HISTORY.md) Phase 8 #6.

- **H155 — `tyaj~~h0_zz_pw` StructuredOutput stall root-caused + FIXED — ✅ DONE 04-07-2026
  (Opus 4.8, `claude-opus-4-8`).** `tyaj`'s drain stalled ~7 min on one agent retrying the
  identical `agent()` call (`root: must have required property 'cards' / must NOT have
  additional properties`) and never producing valid output; MG stopped it. **Root cause:** the
  presplit router (built for 150-`<ls>` citation giants) prices output complexity as `1+<ls>`,
  which is **blind to SENSE density**. `tyaj~~h0_zz_pw` is a PW addenda card compressing a whole
  root article (base verb + Caus/Desid + every prefix combo) into **35 senses / 47 divs / 67
  gloss-spans** carrying only **11 `<ls>`** → weight `1+<ls>=12` ranked it among the *lightest*
  cards while its real output surface was the *heaviest* of the root, so it deterministically
  blew the whole-card StructuredOutput retry cap (the model can't emit the full card as valid
  StructuredOutput, so it never returns `{cards:[…]}`). NOT schema-broken (the schema is one
  shared const across all 5 batches; only this batch failed) and NOT stochastic (identical error
  every retry). The `_zz_pw` addenda class is a known cap-trigger (`gam~~h0_zz_pw01` hit it
  before, recovered only via selfheal after the doomed retries). **Fix** (lang-agnostic →
  SHARED): a **second, orthogonal presplit trigger** keyed on the deterministic fragment count
  (== sense-objects the model must emit) vs a new `SENSE_PRESPLIT_BUDGET=20` — routes sense-dense
  cards straight to the proven fragment lane, converting up-to-5 doomed retries + binary-split
  cascade into zero. Threshold 20 sits on a clean shelf (survey: only ~0.2% of 8,391 sampled
  cards exceed 20 senses; every known-good whole-card head is far below — sam 6, pari 8).
  Independent of the citation trigger and of byte/citation batching mode; tunable via
  `--sense-presplit-budget=N` (`off` disables). **Validated live** (fresh Workflow, store
  untouched): the `[sam, zz_pw]` pair that stalled now returns **cards:2 / ok:2 / null:0**, with
  `zz_pw` routed via presplit and healed **complete** (4 fragment groups, `partial_keys:[]`),
  6 agents / ~4 min. Pinned by `window_selftest.py::test_sense_dense_card_presplit` (synthetic
  30-sense/1-`<ls>` card; 46/46 green). LANG_PARITY entry `sense_presplit_trigger` (SHARED) +
  all drifted hashes re-affirmed → parity gate clean (13 entries). Files:
  [`src/pilot/gen_opt_harness2.py`](src/pilot/gen_opt_harness2.py),
  [`src/pilot/window_selftest.py`](src/pilot/window_selftest.py),
  [`LANG_PARITY.md`](LANG_PARITY.md), [`PIPELINE_HISTORY.md`](PIPELINE_HISTORY.md) (Phase 8 #5 +
  recurring-pattern). `tyaj` is otherwise clean — resume its drain via the standing
  [H151](https://github.com/gasyoun/Uprava/blob/main/handoffs/H151-Sonnet_RussianTranslation_pwg_ru_verb_batch_drain_04.07.26.md)
  loop, no special procedure.

- **FULL-RUN READINESS AUDIT + scope ruling — ✅ DONE 04-07-2026 (Fable 5, `claude-fable-5`).**
  MG asked "is everything ready for full PWG→RU; I get bugs and half translations all the time;
  next batch = all the verbal roots." Audit verdict: **READY, no open stoppers** — selftests
  green (`window_selftest.py` 31+, `translation_memory.py selftest`), TM fresh (2,122 cards,
  rebuilt 03-07 22:42), all historical silent-loss classes FIXED with pinned tests (S10 F1–F14;
  FL1–FL5+FL8 via PRs [#79](https://github.com/gasyoun/SanskritLexicography/pull/79)/[#80](https://github.com/gasyoun/SanskritLexicography/pull/80)/[#81](https://github.com/gasyoun/SanskritLexicography/pull/81)/[#82](https://github.com/gasyoun/SanskritLexicography/pull/82)/[#92](https://github.com/gasyoun/SanskritLexicography/pull/92)/[#93](https://github.com/gasyoun/SanskritLexicography/pull/93)/[#94](https://github.com/gasyoun/SanskritLexicography/pull/94);
  schema-classifier + 3 gate FPs in [#119](https://github.com/gasyoun/SanskritLexicography/pull/119)).
  The "half translations" causes were: silent-null key loss (fixed by total-accounting), TM
  re-serving gate-flagged content on requeue (fixed: `--no-tm` mandatory on requeue), wide-
  concurrency rate-limit collapse (process-fixed: ≤3-wide), giant-head overflow (fixed: presplit
  router + fragment TM), and the manual Max-surface save gap (H145 `vid` — see next line).
  Known-open non-blockers: FL6/FL7 (low sev), G5/G6/G7 human print-gates (downstream only).
  **H148 executed inside this session:** MG confirmed the `vid` output is LOST (nothing on
  disk — repo/Downloads/Desktop swept); `wf_output.json` verified still the promoted `gam`.
  Hardening: save-verification checklist added to [`RUN_FREQ_MAX.md`](src/pilot/RUN_FREQ_MAX.md)
  + production preference = drive the Workflow from the Claude Code session (H130 pattern),
  no manual save step.
  **Scope ruling (MG): next batch = ALL remaining DCS-attested verb roots.** New
  [`verb_worklist.py`](src/pilot/verb_worklist.py) enumerates them reproducibly (verbs01
  universe 1,882 ∩ freq manifest = 749 attested − 46 promoted = **703 remaining, ~5.3 MB
  source ≈ 2.3× everything done so far**). Standing resumable drain handoff =
  [H151](https://github.com/gasyoun/Uprava/blob/main/handoffs/H151-Sonnet_RussianTranslation_pwg_ru_verb_batch_drain_04.07.26.md)
  (Sonnet 5 sessions, roots one-at-a-time ≤3-wide, RUN_FREQ_MAX loop verbatim, promote + TM
  rebuild per root; Wave-1 head = `vid` clean re-run → `as` → `BU` → `yuj` → `sTA`/`i` solo;
  one-time pre-wave task = the historical gate-fix re-audit sweep from the 03-07 open
  question). H145 is superseded — do not execute its file.
  **Next:**
  `Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H151-Sonnet_RussianTranslation_pwg_ru_verb_batch_drain_04.07.26.md and execute it.`
  (Sonnet 5 chat in this directory.)

- **H145 `vid` Max run — ~~🟡 BLOCKED~~ ✅ SUPERSEDED by H151 04-07-2026 (output confirmed
  lost; clean re-run = H151 Wave-1 head). Original record: 🟡 BLOCKED 03-07-2026 on a Max-Workflow hand-off gap, NOT a code
  bug (Sonnet 5, `claude-sonnet-5`).** Preflight was clean (55 keys, 13 expected agents,
  `run-now-low`) and `gen_opt_harness2.py vid` wrote a fresh `run_pilot_wf.opt2.js`
  (`meta.name: 'pwgru-opt2-vid'`). M.G. reported running it in the Claude Max Workflow
  surface and saving the result as `wf_output.json`, but the file on disk never changed:
  `meta.root` stayed `"gam"`, `meta.generated_at` stayed `2026-07-03T16:34:44Z` (the same
  `gam` output `window_status.json` already recorded `state: clean` for at `19:42:01Z`,
  hours earlier), across two independent checks ~15 min apart (23:22 and 23:38
  wall-clock). No other `wf_output*.json` in the directory carries `root: vid`. The
  stale-provenance check worked exactly as designed — it just has nothing to compare
  against because no new file ever landed. **Routed to
  [`H148`](https://github.com/gasyoun/Uprava/blob/main/handoffs/H148-Fable_RussianTranslation_max_workflow_handoff_gap_audit_03.07.26.md)
  (Fable 5, `claude-fable-5`)** to characterize whether the Workflow output is
  recoverable (wrong save path / still visible in Workflow run history) or genuinely
  lost, since that requires either M.G.'s direct input on what happened in the Workflow
  surface, or evidence this repo's Python tooling has no visibility into. **Do not
  re-run `vid`'s harness until H148 resolves** — re-running blind risks a second
  untracked Max spend on the same hand-off gap. Once resolved (either a recovered
  output or a confirmed clean re-run), the very next step is
  `audit_window.py wf_output.json --root vid --write-requeue`, same as any other root.
- **EN audit gate ported the 2026-07-03 RU gate-bug fixes — audited + 1 real fix ported,
  2026-07-04 (Sonnet 5, `claude-sonnet-5`).** Audited [`audit_window_en.py`](src/pilot/audit_window_en.py)
  against the 3 RU gate-bug fixes in [`audit_coverage.py`](src/audit_coverage.py) /
  [`prompt_rule_audit.py`](src/pilot/prompt_rule_audit.py) (see the 2026-07-03 "3 gate-bug
  fixes" entry above). Verdict per fix:
  1. **Multi-layer raw-marker over-count (`raw_markers`/`layer_sense_count`) — N/A.**
     `audit_window_en.py` never calls `audit_coverage.py` or compares raw `.raw.txt`
     sense-marker counts against card senses at all; its only coverage check is per-sense
     `MISSING-EN` (empty english when german had gloss prose). No analogous mechanism exists
     to carry the bug.
  2. **`braced_gloss_risks` Sanskrit-span leak (`SANSKRIT_SPAN` scrub) — already safe.**
     `audit_window_en.py`'s `prose()` helper already strips `{#..#}` Sanskrit citation spans
     (via `SAN.sub`) before any residue/DE-RESIDUE/RU-RESIDUE matching runs — the EN gate was
     never vulnerable to the SLP1-substring collision that hit RU's `braced_gloss_risks`.
  3. **German/Latin/French misclassification (LATIN_BINOMIAL/FRENCH_WORDS/LATIN_WORDS) —
     REAL EN analogue found and fixed.** `prompt_rule_audit.py`'s per-braced-gloss
     foreign-vs-German classification (`looks_foreign_literal`/`looks_german_gloss`) has no
     EN counterpart (EN's `MARKUP-LOSS` is a pure `{%..%}`/`<div>` occurrence count, not
     content classification), so that half of fix 2 doesn't apply. But EN's `DE-RESIDUE`
     check (`DE_WORDS` regex over the translated english prose) has the SAME collision class:
     `des` is both the German genitive/plural article ("des Todes") and a French
     partitive/plural article, and `gen_fidelity_judge_en.py`'s own judge prompt explicitly
     instructs the EN generation to preserve French/Latin literals verbatim (e.g. "de basse
     extraction", "inire feminam") — exactly the material RU's fix 2 was protecting. Verified
     the collision live (`DE_WORDS.findall('des chevaux') -> ['des']`) before fixing.
  **Fix:** extracted `LATIN_WORDS`/`FRENCH_WORDS` out of `prompt_rule_audit.py` into a new
  shared module [`foreign_literal_guards.py`](src/pilot/foreign_literal_guards.py) (imported
  by both `prompt_rule_audit.py` and `audit_window_en.py`, per the "shared helper" pattern),
  added `AMBIGUOUS_DE_FR_WORDS = {'des'}` and a `FRENCH_CONTEXT_WORDS` regex (FRENCH_WORDS
  minus `des`/`du`, so the French-confirmation check can't trivially match on the very token
  being disambiguated), and guarded `audit_sense`'s DE-RESIDUE: a bare `des` hit with no other
  unambiguous German marker, alongside a confirming French word elsewhere in the prose, is
  now suppressed as a preserved French literal rather than flagged. Pinned by
  `test_en_de_residue_french_guard` in
  [`window_selftest.py`](src/pilot/window_selftest.py) (French "des basse extraction" must
  NOT fire DE-RESIDUE; German "des Todes" alone still must). Full `window_selftest.py` run
  confirmed green on every test this change could affect (`test_en_gate_strict_has_teeth`,
  both `test_markup_loss_soft_flag_*`, `test_coverage_gate_multi_layer_and_presplit`,
  `test_en_residual_coverage_complete`, the new test) — `test_perf_preflight_small_tm_and_no_tm`
  fails on this worktree both before and after the change (`no rootmap for 'gam'`, an
  unrelated pre-existing environment/data gap, confirmed via `git stash`).
  **Also found (out of scope, fixed by a separately-spawned task):** this file
  (`.ai_state.md`) carried literal unresolved `git stash`/merge conflict markers committed
  into HEAD around the H148 vid-run entry — resolved by `task_5568538f` (already landed on
  `master` by the time this branch rebased onto it).
- **H130 gam Max run — Workflow-tool schema bug FOUND + FIXED, gam translated + mechanically
  audited 2026-07-03 (Sonnet 5, `claude-sonnet-5`).** Executing
  [`H130-Sonnet_RussianTranslation_pwg_ru_gam_max_run_03.07.26.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/H130-Sonnet_RussianTranslation_pwg_ru_gam_max_run_03.07.26.md)
  hit a hard blocker before any translation happened: **100% of the harness's `agent()`
  calls (61/61) were rejected pre-execution** by the CLI Workflow tool's safety classifier
  with `output schema too large to classify safely` (0 tokens spent — rejected before any
  model call). Three isolated single-agent tests pinned the exact cause: a toy 1-`$def`
  schema succeeded; the real `CARDS_SCHEMA` (5 `$defs`: card/record/sense/judge/judge_issue,
  ~4.7 KB) failed even with a trivial one-line prompt; the SAME schema with the unused
  `judge`/`judge_issue` `$defs` pruned out succeeded instantly. Root cause in
  [`gen_opt_harness2.py`](src/pilot/gen_opt_harness2.py) `build()`: it read the **entire**
  `$defs` block from the shared
  [`schemas/pwg_ru_final_card.schema.json`](schemas/pwg_ru_final_card.schema.json) (which
  also carries the judge-report `$defs` for a different pipeline step) instead of only the
  subset reachable from `card` — dead weight that pushed every StructuredOutput call over
  the classifier's threshold. **Fix shipped** (branch `codex/pwg-gam-gate-fixes-v2` — the
  original `codex/pwg-performance-spend-gate` branch it was developed on hit a merge
  conflict against `master` after PR #116 landed a parallel perf-gate change; re-applied
  source-only via `/branch-contention-recover`, see the closing bullet below): new
  `_ref_names`/`_reachable_defs` helpers walk
  `$ref` pointers from `'card'` and filter `$defs` to the reachable subset before building
  `CARDS_SCHEMA`; `window_selftest.py` 40+/40+ green after the change. **This unblocks every
  root's production run through the CLI Workflow tool, not just gam** — committed and merged
  (see PR link in the closing bullet below). Finding + fix detail cross-posted to
  [`Uprava/FINDINGS.md`](https://github.com/gasyoun/Uprava/blob/main/FINDINGS.md).
  **gam outcome after the fix:** the 4 real (non-TM) keys — `gam~~h0_01_sec_1_0` (presplit,
  13 fragment groups + binary-split retries), `gam~~h2_36_upasam`, `gam~~h3_05_ni`,
  `gam~~h3_07_vinis` — all translated clean (16 agents, ~759 K tokens, ~13 min wall-clock,
  0 failures). Merged in Python with the 123 TM-resolved cards (order-preserving —
  `window_provenance.stale_check` compares key **lists**, not sets, so the merge must
  follow `meta.selected_keys` positional order, not TM-first) into a canonical
  `wf_output.json` (127/127 accounted, 0 null). `audit_window.py --root gam --write-requeue`:
  **109/127 clean, 18 requeue** (`coverage` 13, `prompt_semantic` 6, 1 key in both);
  `nws`/`sense_dupes`/`translation` gates all PASS; 0 transient nulls. The 18 requeue keys
  are the **same known residual class** documented since the original 2026-06-29 gam run
  (dense `pw00`/`pw03`/`pw04`/`pwkvn`/`sch` head-layer coverage gaps + `untranslated_braced_
  german_gloss`/`likely_circular_gloss` prompt-semantic flags on `ava`/`ni`/`upa`/`anu`/
  `a_di`/`sam_a`/`sam_0` cards) — not new defects introduced by this run.
  **⚠️ TM-staleness-on-requeue trap discovered + fixed (same session).**
  `requeue_from_audit.py gam`'s default harness generation resolved **18/18 of the flagged
  keys straight from `translation_memory.ru.json`** (`agent_expected_after_tm: 0`) — i.e. it
  would have silently re-served the EXACT SAME already-audit-flagged content, since the
  content-addressed TM keys on input SHA, not on whether the cached translation actually
  passed the gates. Confirmed concretely: the TM entry for `gam~~h0_zz_pw03` has exactly
  **47 senses**, matching the audit's `COVERAGE-LOW(47/59)` flag byte-for-byte. Generated a
  targeted `--no-tm` harness instead (`gen_opt_harness2.py gam --keys=<18> --no-tm`) to force
  real re-translation. See [`Uprava/FINDINGS.md`](https://github.com/gasyoun/Uprava/blob/main/FINDINGS.md)
  for the generalized finding — **any requeue-for-audit-failure harness must pass `--no-tm`**,
  since `gen_opt_harness2.py`'s TM lookup has no concept of "this cached translation already
  failed the gates".
  **Two requeue rounds run (both `--no-tm`, order-preserving Python merge into `wf_output.json`
  each time — `window_provenance.stale_check` compares key lists positionally, not sets):**
  round 1 (18 keys, 16 agents, ~759K tokens, ~21 min) → **109→119/127 clean, 18→8 requeue**
  (`h2_10_ava`/`h2_24_upa`/`h0_zz_pw01`-adjacent keys cleared); round 2 (the remaining 8 keys,
  14 agents, ~757K tokens, ~18 min) → **119→121/127 clean, 8→6 requeue**. **Diminishing
  returns confirmed — the SAME 6 keys failed BOTH fresh-translation attempts**:
  `gam~~h0_02_sec_2`, `gam~~h0_38_sam_a`, `gam~~h0_zz_sch` (prompt_semantic, high-confidence
  `missing_required_sense_field`-adjacent flags) and `gam~~h0_20_ava` (`COVERAGE-LOW(6/8)`),
  `gam~~h0_63_sam_0` (`COVERAGE-OVER(13/8)`, also the presplit giant, `partial:true` was even
  set on a *different* key `gam~~h0_45_upa` from round 1's presplit lane), `gam~~h0_zz_pw04`
  (`COVERAGE-OVER(8/5)`) — assessed at the time as "a genuine sense-segmentation disagreement
  between the model and the deterministic `<ls>`/sense counter... not random noise." **This
  assessment was WRONG for most of these 6 keys — a second session (same day) continued past
  this point, root-caused 3 real gate bugs, fixed them, and got gam to 127/127 clean.**

  **✅ CLOSED 2026-07-03, same day (Sonnet 5, `claude-sonnet-5`).** Concurrency note first: a
  second session picked this handoff up mid-flight and, before realizing another session
  (this one) was already running requeue round 2 live, briefly overwrote the shared
  `wf_output.json` with an incomplete duplicate. Fully recovered with no data loss by
  reconstructing the merge from both sessions' scratchpad artifacts (TM-resolved cards +
  original translate run + both requeue rounds) — logged here as a caution: **two chats can
  race on the same non-git-tracked `wf_output.json` with no lock**, and a session resuming a
  requeue handoff should re-check `window_status.md`'s timestamp against wall-clock before
  assuming a stale state is safe to overwrite.

  **Root-caused, fixed, and selftest-pinned 3 real gate bugs** (not translation defects):
  1. **`audit_coverage.py` multi-layer over-count** — `raw_markers()` used
     `t.split('===', 2)` (only 2 splits), so any sub-card bundling >1 `=== LAYER: ... ===`
     block (PW/SCH/PWKVN addenda cards, e.g. `gam~~h0_zz_pw04`: 3 layers) had its `<div n="p">`
     cross-reference continuations from layers 2+ counted as if they were numbered senses.
     `<div n="p">` is a **structural** prefix/secondary-conjugation divider elsewhere in this
     codebase ([`root_units.py`](src/root_units.py), [`scale_preflight.py`](src/scale_preflight.py)'s
     `_DIVP`, [`verify_root_glue.py`](src/verify_root_glue.py)), never a sense boundary — fixed
     by splitting into per-LAYER bodies and counting only the `〉` glyph / numbered `<div n="N">`
     per layer, floored at 1 (a pure-cross-reference layer is still one translatable unit).
     Also added a presplit/fragment-card carve-out: those cards produce one JSON row per
     fragment chunk by construction (not per raw sense), so their row count was tripping
     `COVERAGE-OVER` as a false positive (`gam~~h0_63_sam_0`: `27/8`) — now skips the
     threshold check and labels them explicitly instead.
  2. **`prompt_rule_audit.py` German/Latin misclassification, three distinct instances:**
     (a) `LATIN_BINOMIAL` (meant for species names like "Homo sapiens") lacked the
     `and not GERMAN_GLOSS_WORDS.search(...)` guard its sibling checks (`LATIN_WORDS`/
     `FRENCH_WORDS`/`ENGLISH_GLOSS_WORDS`) all have — German capitalizes every noun, so an
     ordinary "Jmd zusammenkommen" (Jmd = common B&R abbreviation for "jemand") matched the
     Capitalized-word+lowercase-word shape unconditionally; (b) `FRENCH_WORDS`' short-word
     entry `\bdu\b` collided with the German pronoun "du" in colloquial questions ("wie
     kommst du darauf?"), and `GERMAN_GLOSS_WORDS` had no short pronouns/interrogatives to
     break the tie — added `Jmd/jmdm/jmdn/du/wie/woraus/dieses`; (c) `LATIN_WORDS` didn't
     recognize `sequi, obsequi` (Latin, B&R's own untranslated-euphemism convention) — added.
  3. **`braced_gloss_risks`'s "leaked" check scanned the WHOLE Russian text including
     verbatim `{#Sanskrit#}` citation spans** — a 3-letter gloss like `{%mit%}` (correctly
     translated to `{%с%}`) coincidentally matched as a substring of an unrelated SLP1
     citation token (`miTunO` → lowercased `mituno` contains "mit"). Fixed by scrubbing
     `{#...#}` spans (new `SANSKRIT_SPAN` regex) before the substring search.
  All 3 fixes are pinned by new `window_selftest.py` cases
  (`test_coverage_gate_multi_layer_and_presplit` + 3 assertions added to
  `test_braced_gloss_audit`); full suite green after each fix.

  **After the fixes:** re-audited gam from the SAME merged `wf_output.json` (no re-translation
  needed) → **121→126/127 clean, 6→1 requeue.** Ran one more `--no-tm` requeue round on the
  last 2 non-gate-bug residuals (`h0_63_sam_0` fragment-drop, `h0_zz_sch` untranslated "die")
  — `h0_63_sam_0` got WORSE across attempts (2→3→7 missing self-heal fragments on 3
  independent tries, confirming it's a genuine API-reliability gap on this 115-`<ls>`/9-group
  presplit card, not a code bug); **recovered by using the OTHER session's original round-2
  attempt for this one key, which was actually complete (0 partial)** — a reminder that
  "latest requeue wins" is the wrong default when a retry can regress a complete card.
  `h0_zz_sch`'s hedge word (Schmidt 1928's own `"die"?`, question-marked as an uncertain
  reading) survived 2 independent fresh-translation attempts unchanged — genuinely ambiguous,
  not a miss. **Manually resolved** using an independent third attempt's own rendering
  (`«которая»?`, preserving the same hedge) rather than inventing one. **Final: 127/127
  clean, 0 requeue.** Promoted: `promote_final_cards.py --gen-model-version claude-sonnet-5`
  (2122 non-null cards / 10432 senses across the full store, not just gam),
  `audit_translation_provenance.py` clean (0 unresolved, 0 missing SHA), card TM + fragment TM
  both rebuilt (`translation_memory.ru.json` 2122 cards, `translation_memory.frag.ru.jsonl`
  +40 new fragments).

  **⚠️ Open question for MG / a follow-up session:** the 3 gate-bug fixes above were verified
  narrowly (the specific evidenced instances + a hand-built pinning fixture) but **not
  re-run across the full historical corpus** — other already-promoted roots may carry the
  SAME false-positive residuals (multi-layer PW/SCH addenda cards, short-gloss/Sanskrit-
  substring collisions) that were previously accepted as "documented residuals" but are
  actually gate bugs now fixed. Worth a `python src/audit_coverage.py` + `prompt_rule_audit`
  sweep over all promoted roots to see how many historical residuals silently clear.
  **Next preflight-recommended root: `vid` (13 agents expected), then `as`, `BU`/`yuj`.**
  The [`H130-Sonnet_RussianTranslation_pwg_ru_gam_max_run_03.07.26.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/H130-Sonnet_RussianTranslation_pwg_ru_gam_max_run_03.07.26.md)
  handoff is now SPENT (gam is done) — do not re-execute it; a fresh `H###` handoff for
  `vid` is still needed (not yet minted as of this session).

- **pwg_ru performance preflight/spend gate shipped 2026-07-03 (Codex/GPT-5 implementation pass).**
  New read-only [`perf_preflight.py`](src/pilot/perf_preflight.py) reports TM sidecar
  presence/hits, fragment-TM reuse, degenerate pass-through keys, near-degenerate candidates,
  presplit keys, batch count, and `agent_expected_after_tm` without writing harnesses or
  touching the RU store. [`calibrate_perf_harness.py`](src/pilot/calibrate_perf_harness.py)
  now has named scratch grids: `--arm-set conservative` (90/110 × selfheal 12 × TM auto/off)
  and `--arm-set wide` (90/110/120 × selfheal 8/12/16 × TM auto/off). This is not a new
  Fable/Sonnet judgment session and made no live Workflow/Max runs. Follow-up implementation:
  `perf_preflight.py` now accepts multiple roots and emits a compact matrix / JSON wrapper,
  preserves the single-root JSON shape, warns when presplit keys exist but
  `translation_memory.frag.<lang>.jsonl` is empty (including the exact `build-frags` command
  and "no fragment provenance available yet" when applicable), and recommends run order.
  Current staged-root preflight:
  **run `gam` (2 agents), `vid` (13), `as` (14), `BU`/`yuj` (19); skip cached `han` (0);
  defer `sTA` (30) and `i` (35)** until cache refresh/calibration.
  **Executable next handoff:** open a Sonnet 5 (`claude-sonnet-5`) chat in
  `C:\Users\user\Documents\GitHub\SanskritLexicography\RussianTranslation` and run:
  `Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H130-Sonnet_RussianTranslation_pwg_ru_gam_max_run_03.07.26.md and execute it.`

- **pwg_ru performance hygiene shipped 2026-07-03 (Codex/GPT-5 hygiene/verification pass).**
  `gen_opt_harness2.py` now defaults to `--tm=auto` (card + fragment sidecars when present,
  no-op with explicit metadata when absent), records `tm_cards` / `frag_tm_cards` /
  `agent_expected_after_tm`, and adds a conservative deterministic no-LLM lane for tiny
  cross-reference stubs (`degenerate_passthrough:true`, accounted rows, never silent skips).
  New scratch-only [`calibrate_perf_harness.py`](src/pilot/calibrate_perf_harness.py) builds
  comparable harness arms/report templates across output budgets, selfheal budgets, and TM
  modes; it records the AB_TEST_LEAN_TR cache-cooldown warning and does not touch the RU
  store. Current performance truths: article-site lazy loading is already done; lean TR was
  rejected; `OUTPUT_BUDGET=90` is the default; refresh TM after promotion/heal harvest and
  let the generator use it by default. Degenerate pass-through remains deliberately narrow:
  editorial correction prose (`lies:`, `zu streichen`) stays in the LLM lane and is only
  reported as near-degenerate. Validation: `python src/pilot/window_selftest.py` PASS,
  `python src/pilot/translation_memory.py selftest` PASS,
  `python src/pilot/perf_preflight.py sTA BU gam as i yuj vid han` PASS (warnings only:
  fragment TM empty / no local `frag_prov`). Remaining speed work is TM/fragment-TM coverage
  and wider measured calibration; no defaults change without live sequential A/B.

- **Audit-tail (FL4 + FL8 + `ka` missing-group topup) — ✅ CLOSED 2026-07-03 (Opus 4.8,
  `claude-opus-4-8`), 3 merged PRs, each with a pinning `window_selftest.py` test (now 31 green).**
  The three deferred items from the S10 audit
  ([`ARCHITECTURE_AUDIT_2026-07-02.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/ARCHITECTURE_AUDIT_2026-07-02.md) §3):
  - **FL8** [#92](https://github.com/gasyoun/SanskritLexicography/pull/92): audit fixture guard —
    [`audit_window.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/audit_window.py)
    writes status+report to a scratch dir whenever the run is ephemeral (explicit `--ephemeral`,
    or auto when the wf lives under the OS temp dir), so a self-test/fixture can no longer clobber
    the live `window_status.json`/`audit_window.report.json`.
  - **FL4** [#93](https://github.com/gasyoun/SanskritLexicography/pull/93):
    [`en_residual_keys.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/en_residual_keys.py)
    "done" now = coverage-complete (every German-bearing sense carries English, not ≥1); a 1/40
    card is a residual. [`en_split_triage.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/en_split_triage.py)
    keeps missing-input nulls visible (own section) and no longer crashes on a heal-merge store
    lacking `selected_keys`.
  - **Item 1 — `ka`'s 2 missing heal groups** [#94](https://github.com/gasyoun/SanskritLexicography/pull/94):
    new [`autosplit_requeue.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/autosplit_requeue.py)
    `topup` / `topup-merge` consume a partial card's `missing_fragments` (+`frag_prov`) to
    re-translate ONLY the failed fragment groups and stitch a complete card — no full re-split.
    **Validated on `ka`:** a 2-group-missing partial targets 10 of 81 fragments (reuses 71) and
    `topup-merge` reassembles a complete 63-sense card. Cards translated before `frag_prov`
    existed are skipped with a warning (use full `gen`).
  The [`H055-Opus_SanskritLexicography_pwg_audit_tail_02.07.26.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/H055-Opus_SanskritLexicography_pwg_audit_tail_02.07.26.md)
  handoff is SPENT — do not re-execute.

- **Translation-memory (TM) sidecar SHIPPED + wired + live-validated (2026-07-02, Opus 4.8
  `claude-opus-4-8`; [PR #87](https://github.com/gasyoun/SanskritLexicography/pull/87)
  merged).** New [`src/pilot/translation_memory.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/translation_memory.py):
  a **content-addressed** cache keyed on `f"{lang}:{input_raw_sha256}"` — the address is
  computed identically at harvest (each store row's `provenance.input_raw_sha256`) and at
  generation (`sha256_file` of the `.raw.txt`), so it is exact, **version-safe** (changed
  source → new SHA → miss → re-translate; a stale translation can never be reused), and gives
  **cross-card reuse** for byte-identical sources. Built from the local-only store: **2,121
  cards / 10,841 senses / 49 roots** (4 legacy no-SHA rows skipped). Wired into
  [`gen_opt_harness2.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py)
  as default `--tm=auto` (explicit `--no-tm` opt-out; `--tm=PATH` override): TM-hit keys go
  to a `TM_RESOLVED` lane (canonical, restored-markup cards, **no `agent()` call**) and are removed from every translate lane
  (batches / presplit / selfheal frags); total-accounting invariant preserved
  (`tm ∪ batched ∪ presplit == selected`, disjoint). `window_selftest.py` **24/24** (+2 TM
  tests). Validated: **gam 127→3** cards route to translation (124 pre-resolved); **jIv 17/17
  served from TM live via the Workflow runtime at 0 agents / 0 tokens / 70 ms**. Build/refresh
  the TM after any promotion: `python src/pilot/translation_memory.py build --lang ru`. TM json
  is gitignored (derived from the local-only store, regenerable).
  **NEXT INCREMENT (sub-card sense/fragment reuse) — ✅ SHIPPED 2026-07-02 (Opus 4.8
  `claude-opus-4-8`); see the fragment-TM bullet below. The
  [H068-Opus_RussianTranslation_pwg_tm_sense_level_02.07.26.md](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/H068-Opus_RussianTranslation_pwg_tm_sense_level_02.07.26.md)
  it pointed at is SPENT — do not re-execute.**

- **Fragment-level TM (sub-card sense/fragment reuse) SHIPPED + wired + LIVE-VALIDATED
  (2026-07-02, Opus 4.8 `claude-opus-4-8`; branch `feat/pwg-tm-fragment-reuse`, PR pending).**
  The higher-value increment on top of the card-level TM: reuse ONE fragment inside a
  still-failing giant flat headword (kAla/ka/SrI) and the same meaning shared byte-for-byte
  across a root and its derived noun. **Approach (a) — capture ground truth at heal time,
  NOT re-align the store.** New in
  [`translation_memory.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/translation_memory.py):
  `frag_address(lang, frag_source)` = `sha256(lang + NUL 'frag' NUL + plan()-chunk)` — the
  chunk is the EXACT deterministic `autosplit_requeue.plan()` fragment **including** the
  header it attaches to fragment 0 (real translated source on both sides; the handoff's
  "strip the header" step was an artifact of the discarded store-`de` alignment and would
  have broken the round-trip — DEVIATED, documented in the module). `build_frags` harvests
  the harness's new per-fragment `frag_prov` channel from `wf_output` into an append-only,
  fsha-deduped sidecar `translation_memory.frag.<lang>.jsonl` (gitignored, regenerable);
  `load_frag_tm` reads it. Wired into
  [`gen_opt_harness2.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py)
  under the SAME `--tm` switch: `build()` addresses each precomputed fragment and emits a
  `FRAG_TM[k]` map (mirrors `FRAGS[k]` group shape); JS `selfHeal` serves cached fragments
  with **NO `agent()` call** and heals only the uncached, emitting `frag_prov` for the
  freshly-resolved — so a partial giant card re-runs only its missing fragments, a
  fully-cached card heals at zero cost. Every fidelity guard preserved (per-fragment {Tn}
  multiset + whole-card `<ls>/{#` reassembly). `window_selftest.py` **25/25**
  (+`test_frag_tm_reuse`); `node --check` clean on empty + populated FRAG_TM. **Live proof
  (`gam~~h0_02_sec_2`, 7 fragments / 23 senses, store untouched — harvested from wf_output,
  no promotion):** run-1 cold = **5 agents / 312,393 tokens / 117 s**; harvest `build-frags`;
  run-2 `--tm` = **0 agents / 0 tokens / 31 ms**, card returned complete and **byte-identical**
  to run-1 (23/23 senses, `<ls>` 60==60). Refresh the sidecar after every heal run:
  `python src/pilot/translation_memory.py build-frags --lang ru` (stale = misses, never wrong
  reuse). The card-level TM shadows a whole-card hit first, so fragment reuse lands exactly
  where it's needed: cards never promoted whole (partial giants) + cross-card shared fragments.

- **Opt2 harness: S10 recovery machinery is now DEFAULT + presplit router SHIPPED
  (MG decision 2026-07-02, Fable 5 (`claude-fable-5`) approach-review session; this PR).**
  [`gen_opt_harness2.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py):
  selfheal, binary-split, and citation-weighted `--output-budget=90` (raised from the
  S10-era 60 by the 2026-07-03 calibration) are ON by default; a NEW generation-time
  **presplit router** sends any card whose
  `1+<ls>` units exceed the whole per-batch output budget straight to the proven fragment
  path — no more paying up to 5 doomed whole-card StructuredOutput retries on giant heads
  (verified: 150-`<ls>` `gam~~h0_00_pwg00` routes to fragments; `presplit:true` + reason
  ledger + total accounting preserved). Backward compat: an EXPLICIT `--budget=N` keeps
  byte mode (FU1's documented `--budget=6000` unchanged); `--no-selfheal` /
  `--no-binary-split` / `--output-budget=off` restore old behavior; `--selfheal` /
  `--binary-split` accepted as no-ops so all queued runbooks work verbatim.
  `window_selftest.py` 17/17 PASS, `node --check` PASS, presplit + byte-mode + defaults
  smoke-verified on vas/gam.
  **Knob calibration A/B — ✅ EXECUTED 2026-07-03 (Sonnet 5, `claude-sonnet-5`),
  [PR #101](https://github.com/gasyoun/SanskritLexicography/pull/101).** 4-arm live
  Workflow run in a fresh worktree off `origin/master` (branch
  `knob-calibration-20260703`), scratch-only (never touched the RU store).
  `--output-budget` 60 vs 90 on the 56-card `hA` root: **90 wins clearly** — 60 agent
  calls vs 66 (−9%), 4.03M vs 4.68M tokens (−14%), 496s vs 1,082s wall-clock (−54%),
  identical 0/56 null rate → **shipped as the new `OUTPUT_BUDGET` default.**
  `AUTOSPLIT_LS_BUDGET` 18 (stock) vs 10 (finer) on the 150-`<ls>` `gam~~h0_00_pwg00`
  giant head: finer fragmentation made things **worse** — 21 agent calls vs 13 (+62%),
  1.46M vs 925K tokens (+58%), 1,207s vs 615s wall-clock (+96%), same outcome →
  **left unchanged at 18.** Full numbers:
  [`KNOB_CALIBRATION_2026-07-03.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/KNOB_CALIBRATION_2026-07-03.md);
  finding appended to [`FINDINGS.md` §55](https://github.com/gasyoun/SanskritLexicography/blob/master/FINDINGS.md).
  `window_selftest.py` 31/31 green after the `OUTPUT_BUDGET` change. The handoff file
  is SPENT — do not re-execute.

- ~~**Dense-residual re-run through the fixed stack — QUEUED**~~ **✅ EXECUTED 2026-07-02
  (Sonnet 5, `claude-sonnet-5`) — recorded in the GTD hub; this queue item was stale.**
  Outcome per [`Uprava/GTD_NEXT_ACTIONS.md`](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md)
  Agent-Ready: 4 gam RU keys + `en.DA`'s 7 nulls + the 2 real remaining EN residuals
  (jYA, yat — the historical "77" was already drained) all **ok**; EN overlay re-run
  (`promote_en.py` + `annotate_dcs_freq.py`; store 8,574/10,856 rows carry `en` — closes
  the S7 leftover). 2 new hard flags logged, not retried (`LS-LOSS` on
  `_d_a~~h0_56_pra_ri`, `SAN-LOSS` on `yat~~h0_zz_pw/r0/s1`). `ka`'s 2 missing heal groups
  now have a targeted-requeue mechanism — ✅ **[#94](https://github.com/gasyoun/SanskritLexicography/pull/94)
  `autosplit_requeue.py topup`** (see the audit-tail bullet at the top of this queue).
  The handoff file carries a SPENT banner — do not re-execute.

- ~~**Audit-churn fix — QUEUED (Opus/Fable-tier judgment, from S10 2026-07-02).**~~
  **✅ EXECUTED 2026-07-02 (Opus 4.8, `claude-opus-4-8`) — FL1/FL2/FL3/FL5 all fixed +
  merged, each its own PR with a pinning `window_selftest.py` test (now 20 tests, green).**
  FL5 [#79](https://github.com/gasyoun/SanskritLexicography/pull/79): `has_text_signal`
  redesign — the flag now fires only on a sense that makes a `{%..%}` meaning claim and lacks
  any citation/stratum/NWS-owner/**lexicographic** signal (37→6 fires on 39 Slice-C files);
  report-only enforced by a `REPORT_ONLY_RISKS`×`HIGH_CONFIDENCE_RISKS` disjointness invariant;
  reasoning in [`AUDIT_CHURN_FL5.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/AUDIT_CHURN_FL5.md).
  FL2 [#80](https://github.com/gasyoun/SanskritLexicography/pull/80): EN gate `--strict` now
  fails on nulls/crash; strict-by-default + report in `save_and_audit.py`. FL3
  [#81](https://github.com/gasyoun/SanskritLexicography/pull/81): `ru_coverage.py` wired into
  the RU save path (advisory); corrupt EN denom → FAIL, missing denom → loud `UNVERIFIABLE`
  (no more silent gam-6/127 exemption). FL1
  [#82](https://github.com/gasyoun/SanskritLexicography/pull/82): `grammar_for` returns
  empty + warns on an absent homonym instead of ALL homonyms. **FL4 + FL8 + the `ka`
  missing-group topup are now ✅ CLOSED 2026-07-03** (see the audit-tail bullet at the top of
  this queue; PRs [#92](https://github.com/gasyoun/SanskritLexicography/pull/92)/[#93](https://github.com/gasyoun/SanskritLexicography/pull/93)/[#94](https://github.com/gasyoun/SanskritLexicography/pull/94)).
  The handoff file carries a SPENT banner — do not re-execute.

- **S7 quality follow-through — QUEUED as its own handoff (MG decision 2026-07-02;
  runs AFTER the churn-fix — the re-run leg of the locked order is already ✅).** The two S7 judge
  recommendations that neither queued session owns: (a) deterministic `{%..%}`
  pair-count soft flag in the audit (dropped German-echo markers = the dominant
  residual: RU 17/50 cards, EN 32/68 rows, systematic on the EN path); (b) prompt
  tightening — "translate-don't-annotate" (all 3 S7 unfaithfuls were unsourced
  ADDITIONS) + MW-TM guard in the EN TR (the one confirmed MW leak). Memo:
  [`FABLE_JUDGE_S7_2026-07-02.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/FABLE_JUDGE_S7_2026-07-02.md).
  **Start (Opus 4.8+ chat in this repo):** `Read C:\Users\user\Documents\GitHub\Uprava\handoffs\H056-Opus_SanskritLexicography_pwg_s7_quality_gate_02.07.26.md and execute it.`
  ([handoff file](https://github.com/gasyoun/Uprava/blob/main/handoffs/H056-Opus_SanskritLexicography_pwg_s7_quality_gate_02.07.26.md))

- **Renou hypothesis programme (H1–H7) — STEP 3 DONE 2026-07-08 (Sonnet 5,
  `claude-sonnet-5`), step 2 still awaiting MG's Step-0 votes.**
  Full executable spec (Sonnet-tier, fresh-session-ready):
  [`RENOU_HYPOTHESES.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/RENOU_HYPOTHESES.md).
  Step 1 (H4 citation bias + Step-0 pilot sheet generation) shipped in
  [PR #61](https://github.com/gasyoun/SanskritLexicography/pull/61) (merged):
  **H4 confirmed** — epic under-cited in all 8 dicts (log2 bias −0.97 to −6.86, all
  CIs exclude zero), rgveda under-cited 7/7 reachable, kāvya over-cited 5/8 — see
  [`RENOU_H4_CITATION_BIAS.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/RENOU_H4_CITATION_BIAS.md)
  (F6 in RENOU_FINDINGS.md). Step-0 pilot: deterministic 70-item stratified sample +
  interactive HTML review sheet ready at
  [`review/renou_pilot_sheet.html`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/review/renou_pilot_sheet.html)
  — **@DO for MG: open the sheet, vote all 70 items (approve/reject/defer + note),
  download `decisions.json`, hand it back** → triggers step 2 (`RENOU_VALIDATION.md`
  analysis) — still pending, so step 3 ran ahead of it (ungated per the spec).
  **Step 3 (H6 Zipf agreement) shipped 2026-07-08** (own PR, see repo PR list):
  **H6 confirmed, steeply** — `ls`–`dcs` exact agreement 66.7% → 0.2% and `dcs_adds`
  9.3% → 97.8% across the frequency range, 50%-crossing at just ≈2.7 DCS texts — see
  [`RENOU_H6_ZIPF.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/RENOU_H6_ZIPF.md)
  (F7 in RENOU_FINDINGS.md); recommends (no code change) a frequency-gated confidence
  flag as an alternative to `renou_portrait.py`'s state-count `LOW_INFO_MIN_STATES`
  heuristic — a human decides whether/how to wire it in. Next after step-2 votes land:
  (4) H1 survival → (5) H5 MW–PWG lineage → (6) H3 register disjointness → (7) H2
  compounds (VisualDCS) → audit-report charts → gated GH-Pages portrait demo. Spec
  authored by Fable 5 (`claude-fable-5`); steps 1 and 3 executed by Sonnet 5
  (`claude-sonnet-5`).

- **PWG gold-sample judge — VERDICT: PROCEED (Fable window S7, 2026-07-02).** Fable 5
  (`claude-fable-5`) adjudicated an independent 50-card seed-7 Slice C+D RU sample (96.0 %
  faithful+acceptable, unfaithful 4.0 % CI [1.1, 13.5]) + 68 EN sense rows covering ALL 16 pilot
  roots (98.5 % clean, 1 unfaithful). **All 3 unfaithful = unsourced ADDITION** (interpretive
  gloss ×1, invented attribution «и другими» ×1, MW-TM leak "(of a heavenly body)" ×1 — the only
  confirmed MW contamination). Dominant residual = dropped `{%..%}` markers (RU 17/50 cards, EN
  32/68 rows, systematic on EN path) — deterministic gate recommended, non-blocking. Verdict
  gates Phase 2 + future tranches: **proceed, tighten prompts** ("translate-don't-annotate" +
  MW-TM guard in `tr_en.txt`), add `{%..%}`-pair-count soft flag to the audits. ⚠️ State finding:
  store `en` layer superseded — only DA carries `en` (700 rows); **re-run `promote_en.py` before
  any Phase-2 sampling**. Memo (binding sampling plan §): [`FABLE_JUDGE_S7_2026-07-02.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/FABLE_JUDGE_S7_2026-07-02.md);
  evidence: [`fable_s7_verdicts_2026-07-02.json`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/fable_s7_verdicts_2026-07-02.json).

- **PWG→EN FU1 — Phase 1 GENERATION + REQUEUE COMPLETE on Sonnet 5 (2026-07-01).** All 30
  worklist roots translated with **generation = Sonnet 5 (`claude-sonnet-5`)** (pinned
  explicitly on the `--lang=en` path — the bare `model:'sonnet'` alias previously resolved to
  4.6; probe-confirmed this env routes `claude-sonnet-5` → Sonnet 5). **Coverage 1,432/1,509
  cards = 94.9%** (12 roots at 100%) after TWO requeue rounds: round-1 small-batch
  (`--budget=6000`), then round-2 after an **EN-schema relaxation** — trim the per-sense
  `required` to `[tag,german,<field>]` (the 4 annotator fields equivalence_type/source_type/
  stratum/differentia made optional, EN path only; RU keeps all 7). ⚠️ **Systemic finding:**
  Sonnet 5 hits the StructuredOutput retry-cap (5) on citation-dense HTML-entity-heavy
  main-head `pwg00` cards **even solo**; the schema relaxation recovered many medium-dense
  cards (+33) but the **truly-giant root-article heads remain output-size-bound** (gam 23 of
  the 77 residual) — clearing them needs sense-level head-splitting (RU pipeline Finding-5),
  a separate larger change, NOT more re-runs. Some residual is fidelity-guard nulls (han/vah/
  diS cards where restored {Tn} `<ls>`/`{#..#}` counts mismatch). **NEXT (MG-locked 2026-07-01,
  in order; do NOT start Phase 2 until 1–3 done): (1) giant-head recovery via the head-splitter
  (`_pilot_gen_merged.py --root-split`, lower `HEAD_CIT_BUDGET`) toward ~100%; (2) hard-flag
  remediation (4 cards); (3) wire the EN audit into the :8765 dashboard; THEN (4) Phase 2**
  (promote_en → annotate_dcs_freq → Opus 4.8 judge → human gold). Full resume spec:
  [`H037-Opus_RussianTranslation_pwg_en_fu1_finalize_01.07.26.md`](H037-Opus_RussianTranslation_pwg_en_fu1_finalize_01.07.26.md).
  Hard flags: 8 on 4 cards (AB-LOSS ×4 = banD upasam_0 / gam __a_1 caus / jan a_di / laB sec_1;
  SENSE-DUPE roots vac/su/laB + siD-expected). siD SENSE-DUPE = KNOWN
  RU-h1 defect (expected, not a regression); gam/jan/vac/laB/su carry SENSE-DUPE/AB-LOSS on
  specific dense multi-record cards that persist across re-runs (structural). Store files:
  `wf_output.en.<root>.json` (dA saved as `wf_output.en.d_a.json` — Windows case-collision
  with pilot DA). **Code shipped:** `gen_opt_harness2.py` (meta language-aware + Sonnet-5 pin
  on en path + LF harness write, RU path unchanged); `save_and_audit.py` (drops null result
  slots from failed batch thunks). Phase 2 (promote_en → Opus 4.8 judge → human gold) still
  pending. Runbook: [`H034-Opus_RussianTranslation_pwg_en_fu1_run_30.06.26.md`](H034-Opus_RussianTranslation_pwg_en_fu1_run_30.06.26.md).

- **PWG→EN FU1 — Phase 0 SHIPPED, Phase 1 cost-check IN PROGRESS (2026-06-30).** RESUME spec:
  [`H034-Opus_RussianTranslation_pwg_en_fu1_run_30.06.26.md`](H034-Opus_RussianTranslation_pwg_en_fu1_run_30.06.26.md) §"RESUME HERE".
  Branch **`feat/pwg-en-fu1-phase0`** (off master post-#26). Phase 0 = PR #27 (MERGEABLE, CI
  green): Opus EN judge (`fidelity_sample_en.py`+`gen_fidelity_judge_en.py`), `en_provenance` in
  `promote_en.py` (+`--judge`), `gold_sample_en.py` (MW hidden) — all reuse RU tools, selftested.
  Phase 1: **`nI` done clean** (97 cards/447 senses, 0 null, 0 hard flags, sense-dupe PASS →
  `wf_output.en.nI.json`; Sonnet gen, ~4.4k tok/card). **`yA` harness staged, NOT run** — that is
  the next action (finish cost-check → MG go/no-go on remaining 27). `dA` stale-blocked (run
  `root_window_status.py dA --prune-stale` first). ⚠️ harness gen needs `--lang=en` (with `=`;
  space form silently → RU). [original locked plan immediately below]

- **PWG→EN FU1 — original locked plan (2026-06-30).** Full spec:
  [`FU1_PLAN.md`](FU1_PLAN.md). Six MG-locked decisions: (1) bilingual single-pass for *new*
  freq roots, EN-only+merge for already-RU'd roots → **this tranche = EN-only**; (2) Sonnet
  generate + **Opus judge**; (3) scope = **match current RU coverage**; (4) validation = FU2
  audit + Opus judge + **human gold sample** (Cohen κ, error rate); (5) **full per-sense
  provenance** (model, judge verdict+score, generated_at, harness SHA, mw_used); (6) ground
  truth = **faithful to the PWG German** (MW = cross-check only). **Worklist = 30 RU'd roots
  with no EN: 1,260 subcards / 5,259 sense rows, ~$42 Sonnet** (none >512KB, no `--keys`
  split). Freq order: `nI yA dA han car viS jYA vas hA vA diS mA vah iz muc su Ap jan banD man
  Sru siD vac ji paS brU laB jIv As gam`. ⚠️ siD mirrors the known RU h1 sense-dupe defect.
  **Build first (no quota), ADAPTING existing RU tools (don't reinvent):** Opus EN judge
  (adapt `fidelity_sample.py`/`gen_fidelity_judge.py`/`fidelity_aggregate.py`, rubric =
  faithful-to-German), provenance-aware `promote_en.py` (`en_provenance` block), human gold
  sample (adapt `gold_sample`/`gold_packet`/`gold_validate`/`gold_ingest`/`gold_agreement`).
  Then the Max run at ≤3-wide. Execution runbook:
  [`H034-Opus_RussianTranslation_pwg_en_fu1_run_30.06.26.md`](H034-Opus_RussianTranslation_pwg_en_fu1_run_30.06.26.md).

- **Process audit + pipeline hardening (2026-06-29, session 3).** Ran a 7-dimension
  ultracode audit (speed/tokens/python/gates/printable-DH/orchestration/quality;
  8 Opus agents, ~1.05M tok) → [`PROCESS_AUDIT_2026-06-29.md`](PROCESS_AUDIT_2026-06-29.md)
  (26 ranked recs + 17 MG decisions). **Verified keystone**: the ~1,989 translated
  cards are STRANDED — `export_interop.py` reads raw `assembled_cards.jsonl`, NO
  exporter reads `wf_output.*.json`. **MG decisions taken**: workstreams = hardening
  + speed/token + print-bridge (no Slice E yet); store = SUPERSEDE old 217-row
  run_batch; siD = investigate source first; fidelity = Opus-judge interim.
  **Tier-1 hardening SHIPPED this session** (window_selftest 14/14 PASS incl. new
  `test_sense_dupe_batch_override`): (1) `save_and_audit.py` — killed `\.` SyntaxWarning,
  fixed hardcoded-`sc`-tag misfile bug (now `[tag]` arg), honest name (actually audits),
  robust result extraction; (2) `requeue_from_audit.py` → calls `gen_opt_harness2.py`
  + `run_pilot_wf.opt2.js` (was the dead legacy per-card path); (3) **reproducible
  `batch_of` exemption** — committed `src/pilot/rootmap_overrides.json` merged by
  `audit_sense_dupes.py` (jan no longer depends on a gitignored hand-edit); (4) harness
  path canonicalized opt.js→opt2.js across window_common/root_window_status/
  window_provenance/window_selftest/prompt_rule_audit (`OPT_HARNESS` constant);
  (5) `gen_opt_harness2.py --out=PATH` flag kills the gen→copy race; (6) AGENTS.md
  runbook → opt2 canonical.

- **Speed/token #1 SHIPPED (2026-06-29, session 3).** (a) **Transient-vs-defect requeue
  split** (rec 3/10): `audit_window.py` now writes `requeue.transient.keys.txt` (null cards
  = rate-limit dropout, cheap re-run) vs `requeue.defect.keys.txt` (gate flagged real content
  = rework); new `transient_only` window state (backward-compat guarded so pre-split reports
  stay `needs_requeue`); summary prints the split; `requeue_from_audit.py --transient|--defect`
  routes the cheap re-run. Verified live: Ap 20 requeue = 12 transient + 8 defect; mA 0+13.
  (b) **Concurrency doctrine** in RUN_FREQ_MAX.md: ≤3-wide, never N parallel chats (Slice D
  18× → 117 nulls; single-root = 0). window_selftest **16/16 PASS** (+`test_requeue_transient_vs_defect_state`).
  ⚠️ Caught+reverted a self-inflicted regression: pointing `prompt_rule_audit.DEFAULT_HARNESS`
  at opt2 made the masked harness's 4 absent SOFT judge-7 rules hard-fail prompt_semantic →
  every root `blocked` (that file was NOT in the audit's fix list; its rule-check is the
  template build-contract). **NEXT (queued)**: portrait-slim (rec 4, opt-in flag + cold A/B;
  gated on MG decision #11 — does the translator ever need `citations_resolved`?); the print
  bridge (`promote_final_cards.py` + per-card provenance + repoint export_interop, supersede);
  siD source investigation; Opus-judge interim over a stratified sample.

- **Track A — SLICE D COMPLETE + AUDITED (2026-06-29).** 18 next-freq roots translated
  in parallel on the `gen_opt_harness2` batched+masked 3-layer harness. TAG=`sd`.
  Freq ranks #9–#57. Total: **879 cards, 620 clean (70.5%), 259 requeue** — of which
  **117 are transient rate-limit nulls** (this run hit heavy server-side rate limiting at
  18× concurrency; ~80+ `Server is temporarily limiting requests` failures across batches).
  Net clean rate is depressed purely by rate-limit nulls, not quality; nulls are recoverable
  via a null-requeue pass (same as Slice C). **nws GATE: PASS all 18.** sense_dupes GATE:
  **siD FAIL** (4 dupes), all other 17 PASS.

  | root | cards | clean | requeue | null | nws | sense_dupes | decision |
  |---|---:|---:|---:|---:|---|---|---|
  | dA   | 87 | 67 | 20 | 5  | PASS | PASS | ACCEPT |
  | car  | 90 | 59 | 31 | 19 | PASS | PASS | ACCEPT |
  | jYA  | 66 | 49 | 17 | 11 | PASS | PASS | ACCEPT |
  | viS  | 66 | 47 | 19 | 10 | PASS | PASS | ACCEPT |
  | vas  | 66 | 44 | 22 | 11 | PASS | PASS | ACCEPT |
  | hA   | 56 | 48 | 8  | 2  | PASS | PASS | ACCEPT |
  | vA   | 53 | 40 | 13 | 3  | PASS | PASS | ACCEPT |
  | vah  | 51 | 30 | 21 | 12 | PASS | PASS | ACCEPT |
  | diS  | 49 | 42 | 7  | 1  | PASS | PASS | ACCEPT |
  | Ap   | 45 | 25 | 20 | 12 | PASS | PASS | ACCEPT |
  | muc  | 42 | 27 | 15 | 8  | PASS | PASS | ACCEPT |
  | mA   | 41 | 28 | 13 | 0  | PASS | PASS | ACCEPT |
  | ji   | 34 | 26 | 8  | 7  | PASS | PASS | ACCEPT |
  | Sru  | 29 | 18 | 11 | 1  | PASS | PASS | ACCEPT |
  | vac  | 29 | 22 | 7  | 1  | PASS | PASS | ACCEPT |
  | siD  | 29 | 21 | 8  | 1  | PASS | **FAIL** | FLAG — dedup pass required |
  | laB  | 26 | 16 | 10 | 8  | PASS | PASS | ACCEPT |
  | As   | 20 | 11 | 9  | 5  | PASS | PASS | ACCEPT |

  **siD sense_dupes FAIL** = genuine cross-part over-production (NOT a citation split):
  PWG homonym-1 main entry was split into parts pwg00–03; `pwg02` redundantly renders
  senses `3`, `4`, `ppp-siddha-2`, `ppp-siddha-3` already emitted by `pwg00`/`pwg03`.
  The `batch_of` rootmap exemption CANNOT express this (one sub-card can't carry 4 shared
  tags), so unlike `jan` it needs a targeted requeue/dedup of the siD h1 parts, not a
  rootmap patch. Flagged for the coordinating pass.
  **Requeue harnesses READY (2026-06-29, Codex).** 17 per-root requeue harnesses generated
  and committed (`run_pilot_wf.sd_rq_ROOT.js`, `node --check` all 17 pass, commit `1c0892c`).
  Audit improvements also shipped: `audit_window.py` now splits requeue into
  `requeue_transient` (rate-limit nulls → cheap re-run) and `requeue_defect` (content
  failures → rework); `window_reports.py` adds `transient_only` state; 15 selftests pass.
  **Next**: run each `run_pilot_wf.sd_rq_ROOT.js` harness via Max Workflow (<=3-wide
  concurrency to avoid rate-limit recurrence); merge results into `wf_output.sd.ROOT.json`;
  run `audit_window.py ROOT --wf wf_output.sd.ROOT.json --write-requeue` per root.
  Then siD h1 dedup; then Slice E (freq ranks #58+:
  BU/dah/ram/tyaj/tap/kzip already have rootmaps + jIv/brU/iz/paS/su/banD/man/nI/yA etc.
  Saved outputs: `wf_output.sd.ROOT.json`. Task IDs: scratchpad `sd_task_map.txt`.

- **Slice C requeue pass COMPLETE (2026-06-29).** 16 targeted null-card requeues finished.
  Results: 6 roots fully resolved (hi, vad, naS, pat, Cid, Bid → 0 null after requeue);
  8 roots partially resolved (Sam 3 null, Buj 1, pA 1, mad 1, gA 2, yat 1 persistent second-attempt nulls);
  4 roots all-null persistent (jan 5/5, vraj 2/2, yaj 1/1, rakz 1/1 — structural failures,
  no third requeue). Total 18 cards remain null = accepted as residual. Jan sense_dupes
  locally fixed via rootmap `batch_of` patch (gitignored; must be re-applied if rootmap is regenerated).

- **Track A — SLICE C BULK RUN COMPLETE (2026-06-29).** All 26 roots translated
  and audited on the `gen_opt_harness2` batched+masked 3-layer harness via
  two parallel chats (Slice A + Slice C). TAG=`sc`. Total: **1,110 cards,
  842 clean (75.9%), 268 requeue**. nws GATE PASS all 26. sense_dupes GATE:
  **jan FAIL** (1 duplicated sense) → flagged for coordinating requeue pass;
  all others PASS. Coverage residuals on `pw*`/`nws*` cards and dense homonym
  heads = known F-gate-nws-fp pattern. Documented residuals: prompt_semantic
  risks (untranslated_german_residue, likely_circular_gloss,
  foreign_gloss_translated) on standard cards. **Next action: coordinating
  requeue pass for `jan` (sense_dupes FAIL) + all deferred requeue keys;
  commit Slice C results + state update; continue freq queue beyond Slice C.**

  | root | cards | clean | requeue | sense_dupes | decision |
  |---|---:|---:|---:|---|---|
  | DA   | 145 | 110 | 35 | PASS | ACCEPT |
  | yA   | 90  | 73  | 17 | PASS | ACCEPT |
  | nI   | 97  | 76  | 21 | PASS | ACCEPT |
  | man  | 30  | 19  | 11 | PASS | ACCEPT |
  | jan  | 38  | 25  | 13 | **FAIL** | FLAG — requeue pass required |
  | banD | 32  | 22  | 10 | PASS | ACCEPT |
  | pat  | 73  | 53  | 20 | PASS | ACCEPT |
  | su   | 34  | 25  | 9  | PASS | ACCEPT |
  | vad  | 47  | 36  | 11 | PASS | ACCEPT |
  | iz   | 37  | 32  | 5  | PASS | ACCEPT |
  | mad  | 21  | 13  | 8  | PASS | ACCEPT |
  | hi   | 20  | 18  | 2  | PASS | ACCEPT |
  | Bid  | 35  | 27  | 8  | PASS | ACCEPT |
  | Buj  | 39  | 29  | 10 | PASS | ACCEPT |
  | paS  | 27  | 24  | 3  | PASS | ACCEPT |
  | pA   | 34  | 25  | 9  | PASS | ACCEPT |
  | Sam  | 38  | 25  | 13 | PASS | ACCEPT |
  | yat  | 28  | 18  | 10 | PASS | ACCEPT |
  | gA   | 65  | 50  | 15 | PASS | ACCEPT |
  | Cid  | 32  | 23  | 9  | PASS | ACCEPT |
  | brU  | 23  | 20  | 3  | PASS | ACCEPT |
  | naS  | 26  | 17  | 9  | PASS | ACCEPT |
  | vraj | 34  | 29  | 5  | PASS | ACCEPT |
  | jIv  | 17  | 14  | 3  | PASS | ACCEPT |
  | yaj  | 25  | 17  | 8  | PASS | ACCEPT |
  | rakz | 23  | 22  | 1  | PASS | ACCEPT |

- **Track A — Stage C root `gam` RUN + AUDITED + ACCEPTED (2026-06-29).**
  First Stage C window executed via the in-chat Workflow tool from the optimized
  harness (`gen_opt_harness.py gam` → `run_pilot_wf.opt.js`). Result: **127/127
  translated, 0 null**, ~4.18 M total tokens, ~15 min wall-clock, weekly cap not
  fired. Audited with `audit_window.py wf_output.json --root gam --write-requeue`:
  **101/127 clean**; gates `nws` PASS and `sense_dupes` PASS; `prompt_semantic`
  (18), `coverage` (6), `translation` (3) flagged 26 requeue keys. Decision
  (operator, 2026-06-29): **accept `gam` with documented residuals — no second
  Max run now.** The 26-key residual decomposes as: (a) **3 known F-gate-nws-fp
  false positives** — `gam~~h0_zz_pw00/pw01/pw02`, top-scored
  `suspicious_attested_without_text_signal` on the NWS-dense PW layer (do NOT
  re-loop; code fix only); (b) the named **untranslated-German follow-up** —
  `untranslated_braced_german_gloss`/`untranslated_german_residue` on
  `h0_45_upa`, `h0_zz_sch`, `h0_11_anu` (+ `unbalanced_sanskrit_delimiters`),
  `h0_17_a_bi`, `h0_31_up_a`, `h0_06_ati`, `h0_20_ava`, `h0_00_pwg01`,
  `h0_09_a_di_0`, `h2_06_vyapa`, `h3_05_ni`, `h2_01_sec_1` (Opus-escalation
  candidates); (c) **6 real coverage failures** — `h2_01_sec_1` (5/9),
  `h2_24_upa` (4/6), `h0_zz_pw03` (47/59), `h0_zz_pw04` (8/5 over),
  `h0_zz_pwkvn` (8/1 over), `h0_01_sec_1_0`. Artifacts:
  `src/pilot/output/audit_window.report.md`, `window_status.md` (state
  `needs_requeue`), `requeue.keys.txt` (26), `judge_sample.keys.txt` (37),
  glue `src/pilot/output/gam.NESTED.md`. **Two known code follow-ups carried
  forward (NOT this run):** (1) ~~fix F-gate-nws-fp~~ — **F-gate-nws-fp FIXED
  2026-06-29** (`has_text_signal()` scans all sense values for `[NWS:\s`; card-level
  aggregation on attested senses suppresses per-sense FPs; 13 selftests pass);
  (2) optionally Opus-retranslate the real untranslated-braced-German cards (bucket b). Next operator action: consider
  `yuj` (Stage C, structurally PASS at 60 sub-cards) one root at a time, same
  loop. Do not batch `yuj`/`vid`/`han`.
  - **Gate de-noise SHIPPED 2026-06-29** ([`src/pilot/prompt_rule_audit.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/prompt_rule_audit.py)).
    Investigation of the untranslated-German bucket found it was **~40 gate false
    positives + 1 real delimiter typo**, NOT translation failures — Sonnet rendered
    every simple German gloss correctly (verified on `anu`/`upa` sense 1). FPs:
    (a) B&R **Latin** euphemisms (`inire feminam`, `legere`, `inveteratus`,
    `convenire`) and **French** (Renou layer) flagged as German; (b) **Sanskrit
    preverb** braces on the SCH layer (`abhi`, `upanis`); (c) required **side-by-side
    `{%German%}`** echoes kept beside the Russian. Fixed `looks_foreign_literal`
    (Latin/French sets), `looks_german_gloss` (skip IAST/preverb tokens),
    `braced_gloss_risks` (suppress side-by-side echoes adjacent to Cyrillic; still
    flag bare echoes), `untranslated_german_residue` (strip retained
    `{%..%}`/`{#..#}`/`<ab>`/`<ls>`/`<is>` before the residue search). Re-audit:
    **clean 101→109, requeue 26→18**; `window_selftest.py` +2 fixtures, all pass.
    Remaining genuine German residual = `anu`'s one `{#`→`{%` delimiter typo +
    ~8 cards with trivial `und`/`mit`/`auch`/`für` connectives in xref/corrigendum
    tails (optional tiny Sonnet polish, low value). The
    `suspicious_attested_without_text_signal` *F-gate-nws-fp* (NWS owner citations
    in `has_text_signal()`) was a DIFFERENT FP family — **FIXED 2026-06-29** (see
    Completed below).
  - **Harness redesign PROTOTYPED + INTEGRATED 2026-06-29** (full writeup
    [`TLONLY_PROTOTYPE.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/TLONLY_PROTOTYPE.md)).
    Investigated the "4.2M tokens / one verb" cost concern → the dominant cost is the
    FIXED per-card subagent overhead (~27–46k tokens of system-prompt `cache_create`
    paid ONCE PER CARD), not the translation. Built the batched+masked production
    harness [`gen_opt_harness2.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py):
    masks each card (reuses [`pwg_mask.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pwg_mask.py),
    whose attributed-`<ls n=...>` leak I fixed), bin-packs N cards per agent call,
    restores `{Tn}` to source markup IN-JS → canonical `wf_output.json` (audit consumes
    unchanged). **A/B on 8 mixed gam cards: \$1.72 → \$0.17 = −90% cost**, 0 leftover
    `{Tn}`, markup fidelity 8/8, quality parity. Cost parser
    [`parse_workflow_cost.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/parse_workflow_cost.py)
    reads the transcript cache split (caveat: `output_tokens` under-reports structured
    runs → estimated from tool-call size). Hardening: positional card mapping + length
    check + post-restore `<ls>`/`{#..#}` fidelity guard (nulls mismatches → requeue).
    **Open before rollout:** tune batch budget for dense roots; run a full mixed root
    end-to-end through `audit_window.py`; wire `gen_opt_harness2.py` into `RUN_FREQ_MAX.md`
    as default. THEN resume Stage C (`yuj`…) on the −90% harness.
  - **STAGE C COMPLETE 2026-06-29 — gam + yuj + vid + han all translated** on the opt2
    batched+masked 3-layer harness (lexicon + corpus/Renou + Whitney grammar), all canonical
    (markup-fidelity 100% on non-null), accepted with documented residuals (standard
    F-gate-nws-fp on `pw` cards + coverage on dense/homonym heads):

    | root | cards | clean | null | $ cost | notes |
    |---|---:|---:|---:|---:|---|
    | gam | 127 | 110 | — | — | accepted; gate de-noise + connective Python-fix |
    | yuj | 60 | 47 | 2 | $3.52 | post file-read fix |
    | vid | 55 | 42 | 1 | $3.20 | 9 homonyms (h0–h8); both Whitney recs injected |
    | han | 78 | 62 | 0 | $2.94 | perfect fidelity 78/78, 1 retry |

    Whole staged run (Stage A/B sTA/BU/as/i + Stage C gam/yuj/vid/han) translated. Next: the
    requeue cards (F-gate-nws-fp needs the `has_text_signal()` code fix; coverage/heads → targeted
    requeue), then the freq queue continues beyond `han`. Per-root cost now ~$3 on the validated
    harness (cost lever = agent count; see [`TLONLY_PROTOTYPE.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/TLONLY_PROTOTYPE.md)).
  - **Stage C `yuj` TRANSLATED 2026-06-29 on opt2 with the 3-layer pipeline** (lexicon +
    corpus/Renou + **Whitney grammar** injected once per run via
    [`gen_opt_harness2.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py)
    `grammar_text`). 60 cards/14 batches, canonical (3 guard-nulled→requeue, 0 leftover
    `{Tn}`, fidelity 57/57); audit clean **47/60**, requeue 13. **⚠️ Cost $4.96** — heavy
    schema-validation retries (49 tool_uses/14 batches) eroded the win to ~−35% vs legacy
    (not −72% like gam). **Retry rate is the open tuning lever** (smaller batches / leaner
    schema / additionalProperties).
  - **Retry blowup DIAGNOSED + FIXED 2026-06-29.** The "schema retries" were the model
    calling FILE TOOLS to list the input dir — opt2 reused the production `TR` verbatim,
    which still said "INPUTS for headword KEY (read both): KEYFILE.raw.txt / .portrait.json"
    (the original `gen_opt_harness.py` strips this; opt2 didn't), contradicting the
    masked-inline design → model read unmasked files + globbed the dir. Fix: neutralize that
    block (explicit do-NOT-open-files/list-dirs/call-tools). **yuj re-run: tool_uses 49→19,
    cost $4.96→$3.52 (−29%), `cache_rd` 1.81M→0.25M; masking integrity restored;** per-card
    ≈ −54% vs legacy. Remaining cost = per-batch prompt cache-creation (long full TR); a
    leaner masked-regime TR is the next (quality-risky) lever. gam's earlier $4.45 was
    inflated by the same bug → would improve on re-run.
    Grammar layer (3rd axis) shipped:
    [`src/whitney_grammar.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/whitney_grammar.py)
    (PWG root→Whitney crosswalk join, vendored `whitney_grammar.json`),
    [`GRAMMAR_LAYER.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/GRAMMAR_LAYER.md).
    Non-root nominal grammar = designed (vidyut subanta + Whitney nominal-§ ingest +
    **MW `<k2>` em-dash compound segmentation = Jim's samāsa analysis, 182k compounds**);
    not built (queue is roots). Corpus provenance + Renou register layers also shipped this
    session (`corpus_provenance.py --renou`, `renou_corpus_map.py`, `CORPUS_PROVENANCE.md`).
  - **`anu` requeued (1 card, Sonnet) 2026-06-29** — fixed its one `{#`→`{%`
    delimiter typo; merged into `wf_output.json`. (Side note proving the economics
    problem: that 1-char fix cost a 34k-token re-run — see the translation-only
    model I/O design proposal in [`../../Uprava/GTD_NEXT_ACTIONS.md`](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md).)
  - **German-connective Python cleanup SHIPPED 2026-06-29**
    ([`src/pilot/fix_german_connectives.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/fix_german_connectives.py)).
    Deterministic substitution of stray lone German connectives left in the Russian
    field (`und`→`и`, `mit`→`с`, `ohne`→`без`, `auch`→`также`, phrase `fehlerhaft
    für`→`ошибочно вместо`) — applied ONLY to free text, never inside retained
    `{%..%}`/`{#..#}`/`<ab>`/`<ls>`/`<is>` markup or `«…»` verbatim German quotes;
    ambiguous multi-word phrases (`aber nur in der Saṃhitā`) left for review. No LLM
    tokens — Python owns this cleanup. Applied to `gam`: 8 cards, 9 subs →
    **clean 110→116, requeue 17→11**. `window_selftest.py` +1 fixture, all pass. This
    tool is reusable on every future root's `wf_output.json`. Remaining 11 requeue =
    F-gate-nws-fp (`pw00`/`pw02`/`pwkvn`), coverage (`pw03`/`pw04`/`sec_1_0`/
    `sec_2`/`upa`/`sam_0`/`sec_4`), and `vyapa`'s intentionally-kept `«Von Sternen»`
    quote — no genuine translation defects remain.
- **Track A — FIRST REAL freq window `sTA` DONE (2026-06-27), instrumented.** Ran the first
  production root (106 sub-cards) from chat via the **Workflow tool** — the old "needs a
  human-driven Max session" caveat is **obsolete/disproven**. Result: 106/106 returned, 0
  dropouts, **6.3 M cache_read / 10.3 M total / 19 min**; per-card cache_read **flat vs tyaj**
  (59.3 k vs 60.5 k) → optimization holds at 7.6× scale (Finding 9, [`PILOT_COST.md`](PILOT_COST.md) §6.1).
  Gates: NWS PASS, fidelity 104/106, coverage 103/106, **sense-dupes FAIL ×4**. **~7 cards to
  re-queue.** Glue → `src/pilot/output/s_t_a.NESTED.md`.
  - **Finding 10 — FIXED (2026-06-27).** `_pilot_gen_merged.py` `_marker_tags()` threads the
    top-level sense number → letter sub-senses tagged `<number><letter>`; `head_sense_parts()`
    injects an explicit per-part "render EXACTLY these sense(s)" directive. Re-split → canonical
    collision-free tags (`9a/9b/9c,9d`, `19a..19f`); `gen_opt_harness.py` got a `--keys=` re-queue
    filter; re-ran the 11 affected cards (370k tok, 5 min, 0 drops). **sense-dupes PASS**, pwg01
    fabrication gone, pratyupa coverage recovered. Now safe for `BU/gam` (which may share the
    two-`<div n="2">`-block shape). See [`PILOT_COST.md`](PILOT_COST.md) §6.1.
  - **Finding 5 dense-sense splitter — TOOLING SHIPPED locally (2026-06-27):**
    `_pilot_gen_merged.py` now citation-batches any single over-budget head sense with rootmap
    metadata (`batch_of`, `batch_index`, `batch_count`); `audit_sense_dupes.py` allows only
    rootmap-declared same-sense batch duplicates; `root_glue_translated.py` labels citation
    packages clearly. `sTA` regenerated to 123 sub-cards; 26 declared citation-batch entries,
    max 18 `<ls>` per batch, canonical tags include `9a/9b` (no bare-letter drift). Verified:
    `py_compile`, `verify_root_glue.py`, `audit_sense_dupes.py`, temp duplicate-batch smoke.
    Review follow-up fixed stale generated sub-card inputs on rootmap reshapes (0 stale raw /
    portrait files after `sTA` regen) and made `gen_opt_harness.py sTA headtest` select
    `s_t_a~~h0_00_pwg00b00` from rootmap metadata.
    **Still needs a Claude translation re-run of the new batch cards** before marking `pwg00`
    fidelity closed; this can be done in Claude/Max outside this Codex shell.
  - **Residual re-queue backlog (separate from Finding 10 / dense-split tooling):** `pw07` tiny
    4/6; `ud` (21/33) + `ni` (3/5) COVERAGE-LOW on prefix cards. Defer unless they block the
    post-batch sweep.
  - **Audit workflow orchestrator — SHIPPED locally (2026-06-27):**
    `src/pilot/audit_window.py` now runs NWS attribution, markup fidelity, sense coverage,
    sense-dupes, and optional root glue against the same `wf_output.json` key set, then writes
    `audit_window.report.json`, `audit_window.report.md`, and `requeue.keys.txt`.
    `audit_translation.py --wf` audits workflow keys instead of the default manifest, while
    `audit_coverage.py` now treats stale/missing raw inputs as `NO-RAW`. Current stale `sTA`
    workflow output produces 20 deterministic requeue keys (old unbatched `pwg00*` labels plus
    `ud`, `ni`, `pw07`) and exits non-zero by design.
    Speed follow-up: `audit_window.py` no longer shells through `run_real_test.py audit`; it
    collects once, runs NWS in-process via `nws_split.check_result()`, and parallelizes the free
    gates. Measured current `sTA` audit: ~1.26 s vs ~8.62 s legacy path.
    Production-line follow-up: `audit_window.py` now writes `window_status.json/.md`;
    `root_window_status.py` preflights rootmaps/generated inputs/citation batches before Max
    spend; `requeue_from_audit.py` turns `requeue.keys.txt` into the exact rerun harness.
    `RUN_FREQ_MAX.md` now treats `audit_window.py` as the canonical audit path.
    Drift-proofing follow-up: `gen_opt_harness.py` now embeds workflow `meta` (root/mode,
    selected keys, rootmap SHA-256, per-input raw/portrait SHA-256) and enforces `tools: []`
    for translate agents; `audit_window.py` refuses stale artifacts before collect/gates/glue
    unless `--allow-stale` is passed, writes `window_ledger.jsonl`, and records state
    `stale_artifact` for the old 106-card `wf_output.json` against the regenerated 123-card
    `sTA` rootmap. Normal path smoke: one-card matching fixture passes collect + NWS/fidelity/
    coverage/sense-dupes + glue. Print-gate dashboard added to `preflight_remaining_gates.py`
    (`release/gate_status_snapshot.{json,md}`).
    Live dashboard follow-up: `src/pilot/dashboard_server.py` now serves a local browser page
    at `http://127.0.0.1:8765/`, refreshing every 5 seconds from `/api/status`; it combines
    Max-run status, recent audit/print-gate events, ledger entries, file freshness, and the
    G5/G6/G7/G10 print gates. `dashboard_events.jsonl` is append-only under `src/pilot/output/`;
    event writes are best-effort and do not make audits fail.
    Bulk-throughput follow-up (2026-06-28): `audit_window.py` now writes a deterministic
    semantic judge queue `judge_sample.keys.txt` (all Python-gate failures + default 10% clean
    sample, minimum 5 where available), records sample seed/counts in reports/status/ledger,
    and keeps this separate from the mechanical `requeue.keys.txt`. `RUN_FREQ_MAX.md` now has
    the decision-complete loop: preflight → optimized harness → Max Workflow → audit → requeue
    or sampled semantic judging. Production-line metrics follow-up: `audit_window.py` also
    accepts Max wall-clock/token/cap fields, writes them into `window_status.json/.md` and
    `window_ledger.jsonl`, and prints a `next_action` so each root status says whether to
    regenerate, requeue, translate pending cards, judge the sample, or advance.
    Productivity follow-up: added `window_common.py`, refactored `gen_opt_harness.py` to
    importable `main()` with explicit `FAIL:` validation, extended `root_window_status.py`
    with a deterministic next command, surfaced `judge_sample.keys.txt`/production metrics/
    next action in the local dashboard, and marked `run_real_test.py` legacy/manual-test-only.
    Verified with `py_compile`, `root_window_status.py sTA`, `gen_opt_harness.py sTA`,
    strict stale audit refusal, `node --check`, and a live dashboard `/api/status` smoke.
    Non-looping status follow-up: `window_common.py` now parses optimized harness provenance
    and uses safe queue reads; `root_window_status.py` recognizes that the current `sTA`
    harness already matches the 123-card rootmap, so stale audit status now points to
    "run Max, then audit" instead of repeatedly telling the operator to regenerate.
    Final productivity-loop follow-up: `root_window_status.py` now applies the same harness
    check when latest status belongs to another root, prioritizes queue read errors before
    requeue commands, and `dashboard_server.py` no longer carries stale local file-read helpers.
    Review-fix follow-up: `preflight_remaining_gates.py` now blocks G10 on concrete local
    G5/G6/G7 evidence as well as roadmap blockers, and `audit_window.py` reads workflow
    provenance from the same nested object that contains `results`.
    Edition-readiness consistency follow-up: G10 no longer self-blocks when roadmap still
    marks `G10_immutable_edition_cut` partial; both preflight and release readiness now derive
    G10 from upstream G5/G6/G7 plus non-G10 roadmap blockers.
    Audit guardrail follow-up: stale `audit_window.py --write-requeue` refusals preserve any
    existing `requeue.keys.txt`, while print-readiness scripts remain report-only by default
    and add `--fail-on-blocked` for CI/go-no-go use.
    Report-clarity follow-up: stale audit Markdown now says the mechanical requeue was
    not generated (rather than empty), and JSON records whether `requeue.keys.txt` was
    written or preserved.
  - **Item 1 (Max weekly cap) STILL OPEN** — one root is under the cap, so it never fired; the
    absolute quota divisor needs a **run-to-cap** window (the deferred scope option).
- **Track A — Step 1: grammar-parity re-runs COMPLETE (2026-06-29).**
  sTA, BU, as, i, gam re-translated on `gen_opt_harness2.py` (3-layer batched+masked)
  for uniform Whitney grammar coverage. All 5 gates PASS. Findings documented in
  [`STEP1_GRAMMAR_PARITY_FINDINGS.md`](STEP1_GRAMMAR_PARITY_FINDINGS.md).

  | root | cards | clean | requeue | $/card | wall-clock |
  |------|------:|------:|--------:|-------:|-----------|
  | sTA  | 123   | 96    | 27      | $0.041 | ~15 min   |
  | BU   | 59    | 44    | 15      | $0.051 | ~7.5 min  |
  | as   | 98    | 87    | 11      | $0.029 | ~7.5 min  |
  | i    | 204   | 178   | 26      | $0.027 | ~25 min   |
  | gam  | 127   | 111   | 16      | $0.029 | ~7 min    |
  | **total** | **611** | **516 (84.5%)** | **95** | **$0.033 avg** | **~62 min** |

  Cost: **$20.16 total**, ~8× cheaper per card than old 2-layer single-card runs
  (A/B baseline: $0.215/card old → $0.033/card new). Token ratio: 8.4× reduction
  (sTA old 10.3 M tokens → new 1.41 M for ~same card count).
  **Next: Step 2 (Slice A: 26 roots starting dA, jYA, …).**
  Harness: `run_pilot_wf.sa_{root}.js` (TAG=`sa`); output: `wf_sa_{root}.json`.
  Fixed `gen_opt_harness2.py` main() unpacking (was crashing with "too many values to unpack").
  **sTA half-A (61/123):** DONE — 61/61, 0 nulls, $1.51, ~4 min.
  **sTA half-B (62/123):** running.
  Next: BU (59 cards), as (98), i (68+68+68), gam (127).
  After Step 1: proceed to Step 2 (Slice A: 26 roots starting with dA, jYA, …).
- **Track A — BULK RUN continues** window-by-window per [`src/pilot/RUN_FREQ_MAX.md`](src/pilot/RUN_FREQ_MAX.md)
  using the **opt2** harness (batched+masked 3-layer; `gen_opt_harness2.py`; per-card LLM judge
  dropped). **Next action after Step 1:** Step 2 Slice A per the handoff.
  Freq queue: `sTA`(done) → `BU, gam, yuj, as, i, vid, han`. **`gen_opt_harness.py` rootmap-lookup
  fixed for non-ASCII roots** (commit `f450d10` — safe_name resolution; was FileNotFoundError on `sTA`).
  **Head lane LOCKED (2026-06-27):** the optimized harness is regenerated per root by
  [`src/pilot/gen_opt_harness.py`](src/pilot/gen_opt_harness.py) (portable). Both lanes now
  inline their own part only (no file roaming → no sense over-production); giant heads are
  sense-split at `<div n=>` by `<ls>` density (`HEAD_CIT_BUDGET=18`); causative tails keep
  their `<ab>caus.</ab>` marker and are tagged `caus. N`. **New 4th gate:**
  [`src/audit_sense_dupes.py`](src/audit_sense_dupes.py) deterministically catches cross-part
  duplicate senses (free). Final tyaj A/B: 19 cards, 640k tok, 3.4 min, 0 fails; dup PASS,
  NWS PASS, fidelity 18/19 (1 normal re-queue). `audit_sense_dupes.py` is now wired into
  `audit_window.py` alongside NWS, fidelity, and coverage.
- ~~**Track A — pre-run prompt nits (agent-doable, do at run start).**~~ DONE in the optimized
  harness/runbook: German abbrevs verbatim, all Nachträge rendered, `<is>` as siglum, and the
  deterministic `nws_split.py` owner-map gate in the audit loop.
- **Track B — agni human sign-off (open gate).**
  [`../article-comparison/agni.gloss-review.md`](../article-comparison/agni.gloss-review.md)
  is the worklist; decide its 7 substantive (esp. **H = 4i/4vi agnicayana category error**)
  + 4 optional proposals. Accepted edits then apply to `agni.pd-min.ru.md` col. 3 (still
  carries un-fixed originals, last touched 2026-06-24).
- **Track B — akṣara human sign-off (open gate).** Agent draft review now shipped:
  [`../article-comparison/aksara.gloss-review.md`](../article-comparison/aksara.gloss-review.md)
  (55/55 senses; flagship слог↔Непреходящее split verified correct; **2 M proposals** + 4
  optional). Decide the 2 M proposals (sense 7 ⟨кӯ⟩ glyph + Śrīvidyā hint; 2Eiii Mahat
  category-term), then apply accepted edits to `aksara.pd-min.ru.md` col. 3.
- ~~**Track A — fold the literature harvest into prompts/glossary.**~~ **DONE 2026-06-26**
  — see Completed. The harvest, the per-manual audit + five-manual deep-dive, the prompt
  edits (`1_perevod.txt`/`2_qa_sudya_opus.txt`), and the hand glossary
  `glossaries/de_ru_translation_aids.md` are committed. Remaining pre-run nits are the four
  prompt items above (abbrevs verbatim, Nachträge, `<is>`, `nws_split.py` gate).
- **Hub upkeep:** update [`../../Uprava/GTD_NEXT_ACTIONS.md`](../../Uprava/GTD_NEXT_ACTIONS.md)
  after the preflight/runbook commit is pushed.

## 🚧 Current Work-In-Progress (WIP)

- **FIX_PLAN_2026-07-09 P0 safety patch — PR READY 09-07-2026 (Codex/GPT-5).**
  P0 requeue/TM safeguards are present and now testable without old ignored runtime fixtures:
  `requeue_from_audit.py` forces `--no-tm` for defect/all requeues, appends card + fragment
  denylist rows with atomic JSONL appends, `translation_memory.load_denylist()` fails closed on torn
  JSONL, and `window_selftest.py` covers defect fragment denylist, coordinator defect `--no-tm`,
  window-tagged requeue output, and torn denylist failure. Added hermetic `gam`/`ab` pilot
  fixtures inside `window_selftest.py` for the preflight tests and made stale harness provenance
  checks skip only when the old ignored `dah` input/rootmap artifacts are absent (the `tools: []`
  guard still runs). `LAUNCH_FUCKUPS.md`'s H317 `residual_status` was already fixed to the
  checker-valid `open-paused` by another upstream commit before this landed — kept that value
  as-is rather than overwriting with a different (also valid) status. Restamped
  `LANG_PARITY.md`'s 17 affected entries via `lang_parity_check.py --update-hash` after rebasing
  onto origin/master (the selftest-file hash changed again from upstream drift). Verified:
  `python src\pilot\window_selftest.py`, `python src\pilot\lang_parity_check.py`, and
  `python src\pilot\check_launch_ledger.py --since 2026-07-05` all PASS.

- **H188/H255 Codex deterministic scale prep — 07-07-2026 (Codex/GPT-5; no Workflow tool in this session).**
  H188 itself was already complete and explicitly says not to re-run; H255 is the live scale handoff,
  but this Codex session cannot run Sonnet Workflow generation. Deterministic guardrail work done
  instead: added `src/pilot/no_pwg_scale_plan.py`, which reads the 232-lemma backfill queue, drains
  `no_pwg_w1.still_null.txt` first at sub-card granularity, keeps windows <=30 headwords, generates
  sidecars through `_pilot_gen_merged.py`, emits unique `run_pilot_wf.no_pwg_wNN.js` harness paths
  with `--output-budget=1`, and writes `src/pilot/output/no_pwg_scale_plan.json` with preflight
  evidence and exact next commands. Also taught `perf_preflight.py --output-budget=1` so cost/agent
  accounting matches the actual no-fallback single-card harness. Prepared `no_pwg_w02`: 20 still-null
  headwords / 34 sub-cards / 34 one-card batches / est. 34 agent calls / ~$15.93, under H189 gates;
  harness syntax OK. Validation: `python src/pilot/window_selftest.py` PASS, `python
  src/pilot/lang_parity_check.py` PASS, `node --check src/pilot/run_pilot_wf.no_pwg_w02.js` PASS.
  **Next physical action:** open a Workflow-capable Opus/Sonnet session, run
  `src/pilot/run_pilot_wf.no_pwg_w02.js`, save as `src/pilot/output/wf_output.no_pwg_w02.json`,
  then `python src/pilot/audit_window.py no_pwg_w02`; promote only clean rows and regenerate the next
  window with `python src/pilot/no_pwg_scale_plan.py --window-size 20 --limit-windows 1 --start-index 3`.

- **Nominal grammar layer SHIPPED 2026-06-29** ([`H028-Sonnet_RussianTranslation_nominal_grammar_layer_29.06.26.md`](H028-Sonnet_RussianTranslation_nominal_grammar_layer_29.06.26.md)).
  All 5 build steps complete:
  (0) vidyut subanta confirmed (`Pratipadika.basic('deva')` → `['devaH']`);
  (1) [`src/mw_compounds.py`](src/mw_compounds.py) + [`src/mw_compounds.json`](src/mw_compounds.json) — 106,603-entry MW compound
  segmentation lookup, accent marks stripped;
  (2) [`src/nominal_grammar.py`](src/nominal_grammar.py) — `nominal_grammar_for(slp1, lex)` with static
  Whitney §§ concordance (ch. IV–V, XVI–XVIII from already-complete `whitney_sections.json`);
  (3) §§ concordance hand-verified, 9 stem classes, all cases tested;
  (4) `nominal_grammar_text()` added to [`src/pilot/gen_opt_harness2.py`](src/pilot/gen_opt_harness2.py)
  (harness-level injection ready for nominal windows);
  (5) [`src/pilot/enrich_portrait_nominal_grammar.py`](src/pilot/enrich_portrait_nominal_grammar.py) — portrait-level
  attachment script.
  `GRAMMAR_LAYER.md` updated (non-root section rewritten from design to shipped state).
  `py_compile` clean.
  **Follow-ups all DONE 2026-06-29 (the 4 deferred items):** (1) batch-enrich bug fixed
  (`enrich_portrait_nominal_grammar.py` reads `key1`+`pos`, safe-name aware); (2) vidyut
  **paradigm display** (`nominal_grammar.py --paradigm`; feminine ā/ī/ū use `nyap` — senā
  not *senāḥ); (3) Whitney **exception §§ folded into the ROOT layer** (`whitney_grammar.json`,
  289 records by whitney_no, capped vs over-match); (4) **A/B RUN → translation injection
  REJECTED** ([`NOMINAL_GRAMMAR_AB.md`](NOMINAL_GRAMMAR_AB.md)): wired per-card injection
  (`gen_opt_harness2.py --nominal [--no-grammar]`), ran arm B (grammar ON) vs A (OFF) on 8
  stratified headwords + blind Opus judge → **A 5 / tie 2 / B 1**; grammar ON gives no DE→RU
  quality gain (PWG German already carries the cues; the one compound's segmentation was already
  in the source). Both arms 0 nulls, 100% fidelity. **Nominal windows run grammar OFF by default.**
  Layer NOT retired — its value is structured grammatical data (stem-class index, paradigm,
  compound segmentation, exception §§) for apparatus/display/FAIR export (Scope B/C), not
  translation. Zaliznyak `gramdict/zalizniak-2010` = design model for a compact per-word
  inflection-index ([[reference_zaliznyak_gramdict]] in memory).
  - **Zaliznyak inflection-index + reverse dictionary SHIPPED 2026-06-29**
    ([`ZALIZNYAK_INDEX.md`](ZALIZNYAK_INDEX.md)). `zaliznyak_index(slp1, lex, accented=)` →
    compact token `G·T S F` (gender · Whitney type 0–8 · Vedic stress a/b · flags `*°+N`),
    e.g. agni `m·3b`, rājan `m·8n*`, abaddhamukha `mfn·1+2`. `reverse_index.py` applies it to
    all of PWG: **123,366 entries → 98,639 indexed → 335 paradigm tokens**; FAIR outputs
    `reverse_paradigm_index.json` / `headword_index.tsv` / `paradigm_stats.tsv` (committed).
    Flag sanity: `+N` 47.3% (≈MW 44.5%), `*` 3.6%, `°` 0.04% (after fixing ° to fire only on
    genuine deviations, not normal-class markers). **Accent-mobility correction:** Whitney DOES
    carry the per-case accent rules (whitney_sections.json §§315–319/350/372/390/423/446) +
    PWG key2 `/` gives per-word accent → full Zaliznyak a–f is an ENCODING task, not a missing
    source (logged in [[reference_findings_registry]] SanskritLexicography/FINDINGS.md).
  - **Declension display SHIPPED** (`nominal_grammar.py --table`, `reverse_index.py --show`):
    vidyut paradigm table per headword / per paradigm token (shared template). Released as
    **v0.0.32** (GitHub release published after an api.github.com TLS-timeout recovered).
  - **Per-word grammar dataset SHIPPED** — `headword_index.tsv` now carries the full per-word
    record (`k1·hom·lex·accented·index·stem_class·compound·irregularities`, 98,639 rows, 5.8 MB);
    recorded as STRUCTURED DATA, **portraits deliberately untouched** so translation never inlines it.
  - **VedaWeb accent-axis probe CONFIRMED 2026-06-29** ([[reference_vedaweb]]): VedaWeb 2.0 live
    (`vedaweb.uni-koeln.de/api`, FastAPI); `POST /api/search {type:quick,q:agni}` → RV 6.59.3
    udātta word-split from the Casaretto 2025 annotation resource `66695e4a…` (`…agnī́; ávasā; …
    devā́`). Accent data is per-word, accented, position-aligned, lemma+morph layers co-located,
    bulk `export` endpoint → axis fully de-risked; only the Whitney-rule encoding + join remain.
  - **FAIR data package SHIPPED** — `src/datapackage.json` (Frictionless, CC-BY-SA-4.0) over the 5
    grammar resources with field schemas + sources + provenance; DOI_PLAN notes it is archivable on
    its own track, independent of the translation edition.
  **Next**: the freq queue continues (`yuj`/`vid`/`han` requeue + next roots, parallel chat's lane);
  the grammar layer's only open extension = the full a–f accent axis (encode Whitney accent §§ →
  case-accent table, validate vs VedaWeb — turnkey API recorded in ZALIZNYAK_INDEX.md).
- **Token optimization landed (2026-06-27), local generated-harness follow-up done:**
  the BALANCED reshape is present and the ignored/generated optimized Max harness
  now disables translate-agent tools (`tools: []`) so inlined cards stay true
  single-turn. `wf_output.json` remains a local tyaj A/B artifact, not a deliverable.
- **Cleanup pass in progress (2026-06-28):** superseded a-section/old Workflow files moved
  under `src/pilot/archive/legacy_max_2026-06-27/`; generated dashboard snapshots are ignored;
  `build_corpus_lexicon.py` now logs flaky API batches to a retry ledger and supports
  `--retry-failed`. Workflow guardrail follow-up shipped: `_pilot_collect.py` no longer writes
  `.merged.md` files for null workflow cards, `root_window_status.py` refuses partial-window
  Max runs when the optimized harness points at a different root/rootmap, and
  `root_window_status.py --prune-stale` cleaned BU generated inputs to 59/59 raw+portrait with
  0 stale files.
- Track A judge gate cleared (`b9e27fb`) **and** the cost reshape done; pipeline is scale-ready
  on the optimized lane. `sTA` is structurally ready after re-batching; the next concrete action
  is a Max rerun for its new citation-batch cards, then `BU/gam/yuj/as/i/vid/han`.
- **Stage C preflight done (2026-06-29):** pruned stale generated raw/portrait inputs for
  `gam`, `yuj`, `vid`, and `han`; all four are structurally ready. No LLM/Max spend was used.
- Working tree is active as of the Stage C preflight pass; several documentation/pipeline
  edits from the current handoff remain unstaged. Ignored local research/runtime files remain
  present (including large `literature/` PDFs and generated `src/pilot/output/` artifacts), but
  they are not pwg_ru deliverables.

## 🧠 Dev Notes & Hypotheses

- **Cost model is fragment-count-driven, not card-count-driven — the `pril10_w1` lesson (2026-07-05).**
  The opt2 harness amortizes the ~30 K fixed framework by packing many cards per `agent()` call.
  That amortization **only holds when cards are batched**. In nominal+presplit mode each fragment is
  its own agent call, so a window's true cost ≈ (agent count) × (framework re-cache + per-fragment
  I/O), and **agent count = Σ fragments, not Σ cards**. Measured: 8 cards → 230 agents → cache-write
  = 60% of a $80 bill. **Any cost/throughput estimate must be over expected *agent* count** (the
  generator already prints `agent_expected_after_tm` — surface it as tokens+$ pre-flight). Hypotheses
  to confirm in H189: (1) batch fragments per call to re-amortize; (2) share one cached system prefix
  across a window's agents if the runtime allows; (3) route `kAla`-class monster heads (349 `<ls>`)
  to a separate human-budgeted lane; (4) recalibrate the kill gate (`KILL_CEIL_MS` 8 min → well under
  3 min; > ~60 s single-fragment = suspicious, per MG). See H189.

- **Translation-memory policy questions answered (2026-07-04, MG; do not re-ask in fix plans).**
  Standing decision record: [`TRANSLATION_MEMORY_DECISIONS.md`](TRANSLATION_MEMORY_DECISIONS.md).
  Decisions: machine-gated exact reuse is acceptable for draft windows; fuzzy TM should suggest
  wording to the model only (advisory, not pre-resolving output); speed is the priority for this
  TM engineering pass; MW English may influence RU only through a curated Sanskrit-to-Russian
  terminology layer, not directly.

- **--selfheal VALIDATED LIVE (2026-07-01).** Ran the in-harness auto-split fallback on 6 marquee dense EN heads. **ji~~h0_00_pwg00 HEALED end-to-end** (whole-attempt failed → split 8 fragments → stitched **48 senses**, fidelity OK, escalated:true) — the batch-fails→heals cycle works in the wild; the compact `summary` (ok/null/healed/null_keys) reported it cleanly. Added **per-fragment retry (3×)** so one flaky fragment can't sink a multi-fragment heal. Re-run: man recovered, brU still the lone holdout (198 `<ls>`, 13 frags). **Quality insight:** a giant that passes on the WHOLE-card attempt (variance) is fidelity-valid but STRUCTURALLY COARSE — man came back 3 senses, banD 1 sense (all citations crammed in), vs ji's proper 48 via heal. So prefer the split-heal for structure even when the whole attempt would pass. Cost: ~2.2M tok/6 giants — the fragment-GROUPING tier (undone) is what makes heal economical (8 frags → dozens of calls). Efficiency levers shipped: #1 selfheal, #2 summary-return, dashboard lazy-load (13MB→6.6KB); remaining #3 binary-split / #5 output-budget = gated+validated pass.

- **Dense-card splitter TIER LADDER (2026-07-01) — the permanent fix for the recurring "dense card fails retry-cap" issue.** `src/pilot/autosplit_requeue.py` (per-card, in-place, NO rootmap churn; `_pilot_gen_merged --root-split` can't be used post-hoc — it regenerates a root's whole rootmap and orphans its translated cards). BUILT: **T0** whole-retry (tiny variance fails) · **T1** sense-split (numbered) · **T1b** sub-sense split (lettered a/b/c, greek) · **T2** citation-split (a sub-sense >LS_BUDGET `<ls>` → batches, stitched back into one sense) · completeness guard (never emit a lossy partial) · post-stitch `<ls>` fidelity check. Proven: recovered gam `sec_1_0` (117 `<ls>`) that beat every retry round; gam RU now 123/127. **UNDONE tiers (future):** (1) atom-GROUPING into budget-sized fragments — 1-atom/frag over-splits (85 calls for a 9.6KB card); (2) **sparse/degenerate cards** — near-empty cross-refs (upa 237B "— upa vgl. X") fail because there's ~no gloss to translate → schema violation; needs pass-through (ru=de, no LLM) or zero-gloss schema; (3) multi-record/homonym split (reassembly assumes records[0]); (4) **auto-escalation INTO the harness** — self-heal on accept()-null/retry-cap instead of a manual requeue step (the real end state); (5) order-robust regroup reassembly; (6) fidelity auto-repair (warns, doesn't fix); (7) `{#..#}`-density budget (T2 only counts `<ls>`).

- **RU sub-90% catch-up sweep DONE (2026-07-01).** Ran the RU solo-requeue recipe over the 12 gate-flagged sub-90% roots (waves ≤3-wide; ⚠️ RU batched runs whole-batch-poison — use budget=1 solo, same EN lesson). Store **8592→10,107 rows**; RU roots <90% dropped **13→4** (As 80 / Ap 87 / ji 88 / laB 88 — stubborn dense subcards, head-split territory; d_a 0% = dA/DA case-collision artifact, ignore). gam **5%→94%** (119/127). Dashboard rebuilt+republished to gh-pages (RU-attach 70%). Promote each root via `--merge` (subcard-level, idempotent).

- **gam RU catch-up + dashboard PUBLISHED + RU coverage safety net (2026-07-01, Opus 4.8
  `claude-opus-4-8` orch; RU gen = `sonnet` alias, matching the existing RU corpus).**
  - **Why gam RU was 6/127:** never finished (staged RU run did 6 sub-cards, moved on); all
    backups confirm 6 — NOT a clobber, NOT a requested cut. `src/pilot/ru_coverage.py` (new,
    zero-LLM) checks per-root RU% vs EN `meta.selected_keys`; flagged gam + **12 other partial
    roots** (vas 83%, viS 85%, jan 87%, …).
  - **Catch-up:** RU harness for gam's 121 missing sub-cards (`gen_opt_harness2 --lang=ru`,
    13 batches) — ⚠️ RU harness needs **CRLF→LF strip before launch** (git autocrlf gives
    `gen_opt_harness2.py` CRLF → the JS *template literal* carries raw `\r` that the Workflow
    approval rejects; EN path was already LF). 88/121 succeeded (409 senses); 33 = the dense-head
    batches (retry-cap, same giants as EN — retry to recover). Store: gam **6→94 sub-cards, RU
    27→437 senses**; total store **8592→9001 rows, 46 roots preserved**.
  - **⚠️ promotion wipe hazard fixed:** `promote_final_cards.py` full-overwrites the store from
    on-disk wf_output files, and the original 45 roots' RU outputs are GONE — a naive re-run would
    WIPE them. Added **`--merge` (SUB-CARD level)**: replaces only the sub-cards in this run, keeps
    every other row (backup `.premerge.bak`). Root-level merge was wrong (would drop gam's 6 old
    disjoint sub-cards) — fixed to sub-card. Use `--merge` for every per-root catch-up.
  - **Published:** article site is now on the **`gh-pages`** branch (orphan, `.nojekyll` required —
    subcard files start with `_`), Pages source switched master→gh-pages, LIVE at
    https://gasyoun.github.io/SanskritLexicography/ . `article_site/` un-ignored (MG asked public).
    ⚠️ content is `ai_translated` (public draft). Rebuild+publish: `build_article_site.py` then copy
    `article_site/` onto gh-pages. `articles.js` ≈13 MB (slow first load; screenshot tools time out).

- **FU1 EN residual sweep COMPLETE + article dashboard shipped (2026-07-01, Opus 4.8
  `claude-opus-4-8` orchestration; generation = Sonnet 5 `claude-sonnet-5`).**
  - **Sweep:** ran the small-batch→**solo** (`--budget=1`, one card/batch) requeue recipe over
    every partial FU1 root. Helper [`src/pilot/en_residual_keys.py`](src/pilot/en_residual_keys.py)
    computes residual straight from the EN store (the RU `requeue_from_audit.py` needs an
    audit-emitted `requeue.keys.txt`; the EN gate is report-only). ≤3 roots wide.
    **EN now 2096/2121 = 98.8% across all 46 EN files** (was 94.9% on the FU1-30). Roots taken to
    100%: gam viS jan Sru vah han iz jIv vac(28/29) diS(48/49) vA + the 12 already-100% =. The
    25 residual cards remain null. **⚠️ CORRECTION (same day, later): they are NOT size-bound.**
    [`src/pilot/en_split_triage.py`](src/pilot/en_split_triage.py) labels every card pass/fail
    from the stores (null=fail, no LLM) and searches for a size threshold — **NO clean cut exists
    on any feature** (`<ls>`, `{#`, bytes, spans): the **5 largest cards by `<ls>` all PASSED**
    (hA 224, buj 223, vas 212, han 209, yaj 208 — several bigger than brU 198 which failed; dA
    passed at 128 `{#`). So failure is **stochastic retry-cap variance + content difficulty, not
    card size**. Therefore a spend-free "needs head-split" predictor is **impossible**;
    **RETRY-FIRST is correct** — most of the 25 recover on further independent solo tries (as
    vA/diS-h0/han-a_bi/jIv/jYA-h2 already did). A card is a genuine head-splitter candidate only
    after it persists across several independent attempts; the splitter
    (`_pilot_gen_merged.py --root-split`, lower `HEAD_CIT_BUDGET`) is a last resort for the few
    that never yield, NOT a batch remedy. `en_split_triage.py` is a risk-ranker + budget cap
    (best feature = spans, Youden J≈0.66), not an oracle.
    Anomaly: `jYA~~h1_00_pwg00` (436 B, trivial) failed 2× — LLM variance echoing 16 `{Tn}`;
    hand-author if needed. `dA` shows a false residual = the `wf_output.en.d_a.json` Windows
    filename-collision (genuinely 100%, verify separately). Non-FU1 EN roots (Bid Sam pA hi pat
    vad vraj yaj yat) have their own dense-head residual — out of FU1 scope.
  - **Article dashboard:** [`src/pilot/build_article_site.py`](src/pilot/build_article_site.py)
    compiles `src/pwg_ru_translated.jsonl` (RU spine, 46 roots / 8,592 senses) joined to the EN
    stores **by German source text** (`de`==`german`; 72% EN attach — tag-join was only 29%) into
    `article_site/` (gitignored, rebuildable): per-root `md/<root>.md` + per-sub-card
    `md/subcards/*.md`, plus a **self-contained `index.html` + `articles.js`** (open by
    double-click, no server — data embedded via `<script src>`). RU default, **EN tab**, Sanskrit
    `{#..#}`→**IAST** (reuses `corpus_gate._S2I`), `<ls>`/`<ab>`/`<lex>` styled, DCS-freq badges.
    Launch config `pwg-articles` (:8737) added to root `.claude/launch.json`.

- **⚠️ CORRECTION — gam residual did NOT need head-splitting (2026-07-01, Opus 4.8
  `claude-opus-4-8` orchestration; generation = Sonnet 5 `claude-sonnet-5`).** The Queue/handoff
  predicted gam's 23 residual cards were "truly-giant output-size-bound heads" needing
  `--root-split` / lower `HEAD_CIT_BUDGET`. **False for gam.** The main head `gam~~h0_00_pwg00`
  was already translated; the 23 residual were **prefix sub-cards** (a_bi, ud, ni, upa, pari, the
  h2 homonym set…) 0.5–5.6 KB each + one `zz_pw00` pw-supplement (10 KB). They capped on
  **citation/entity density inside a shared batch, not single-card output size**. Fix that worked:
  (1) small-batch requeue `--budget=6000` (6 batches) → 104→123; (2) **solo re-run** (`--budget=1`,
  one card/batch) of the last 4 → **127/127 = 100%, 0 null**. The batch-1 trio (a_bi/__a_0/pary_a)
  had gone null together = whole-thunk retry-cap poisoning small members, not size. **Lesson for
  viS/jan/Sru/vac/… : check whether the residual actually contains the `pwg00`/head sense-parts
  before reaching for the head-splitter; if it's prefix/supplement cards, solo re-run first.** Only
  the AB-LOSS hard flag `gam~~h0_24__a_1/scaus-4 (2/4)` remains (a step-2 item, unrelated to coverage).

- **Freq Max preflight refreshed (2026-06-26):** `python freq_route.py 8` rebuilt
  the DCS-frequency manifest at **43,968 / 106,082** DCS-attested PWG headwords
  (41%); top queue = `sTA`, `BU`, `gam`, `yuj`, `as`, `i`, `vid`, `han`.
  `_pilot_gen_merged.py --manifest freq --root-split --limit 3` found the top
  three already generated (`0 to generate`), and `verify_root_glue.py` passed all
  gates across 60 rootmaps. Durable runbook:
  [`src/pilot/RUN_FREQ_MAX.md`](src/pilot/RUN_FREQ_MAX.md).

- **Judge gate result (`b9e27fb`, 2026-06-26):** severity **{1:24, 2:13, 3:1} → 37/38
  publishable** (sev ≤ 2), disc "good" everywhere. Sev-3 = idam swapped NWS Geldner/Grassmann
  owner rows — a translator slip the production `nws_split.py` check catches, not a map error.
  Per-unit verdicts: [`src/pilot/FREQ_TEST_RUNBOOK.md`](src/pilot/FREQ_TEST_RUNBOOK.md). The
  `src/*_judge_out.txt`/`_qa_out.txt` files are untracked raw DE/RU dumps, **not** score files.
  The old note that launch needed a human-driven Max session is obsolete: optimized harnesses
  are run through the in-chat Workflow path.
- **sandhi-join prefix-portrait (`pwg_preverb1.txt`) is DROPPED as futile** (only 3/15 of
  man's prefixed surface forms corpus-attested). Don't revive it; HANDOFF still lists it — stale.
- **Head-splitter for large non-giant nouns** (kāla 530 / ka 522 / śrī 412 / para 401) is
  deferred — build only on a *confirmed* single-pass overflow during the scale run.
- **persense LLM assignment is reproducible, not a live call** — `LLM_ASSIGN` map in
  [`src/_build_persense_ru.py`](src/_build_persense_ru.py), keyed surface form → 0-based sense
  ordinal; ordinals stable only while sense tables are frozen.
- **anya is a pronoun** → long function-word tail; only confident routes taken, rest honestly
  in *other*. Don't force.
- **Renou = badge, never re-sort.** Preserve PWG printed sense order; `{%…%}` wraps BOTH German
  glosses (translate) AND Latin (leave); keep `{#..#}`/`<ls>` sigla verbatim; NWS owners
  verbatim, one row per source.
- **Out of scope (maintainer-owned):** SamudraManthanam `#sa` Cyrillic cleanup — tools shipped,
  human runs the server fix
  ([SamudraManthanam #16](https://github.com/gasyoun/SamudraManthanam/issues/16)).

## ✅ Completed (Recent only)

- **H404 — cross-reference count/resolve generalized to every gate source (H397
  generalization) — DONE 09-07-2026 (Sonnet 5 `claude-sonnet-5`).** Part A (RU
  family): measured `kna` (0.2%), `smirnov` (1.0%, corrects H397's unpersisted
  ~1.4% claim), `kow` (0.0%) — all below the ~2% materiality bar, not touched.
  `fri` clears the bar at 4.2% (340/8,151) via its own Latin-apparatus
  convention (`v.`/`cf.`/`q.v.`, not koch's `см.`). New `src/fri_xref.py`
  resolves 111/340 (32.6%) via `build_src.iast_to_slp1` + `corpus_gate.form_key`,
  wired into `annotate_evidence.py` alongside koch. A mojibake mis-resolution
  bug caught by the spot-check was fixed before shipping (truncated-token
  matches now refuse instead of guessing). Full typology of the 229 unresolved
  cases persisted (target-genuinely-absent 39.3%, root cross-forms 19.2%,
  phonological spelling lists 18.3%, grammatical-form-of-headword 8.3%,
  caus./desid./intens. derivative 4.4%, compound decomposition 2.2%, chained
  silence 1.3%, unextractable target 7.0%). Part B (English/German CDSL — MW,
  PWG, GRA, PWKVN, AP90): new read-only `src/part_b_xref_discovery.py`
  discovered each dictionary's actual redirect marker before counting.
  Headline: PWG itself carries 5,303/123,366 (4.3%) bare redirects — flagged
  as a fork for a human ruling on a future handoff (resolution there requires
  the csl-orig correction-workflow, out of scope here). `PIPELINE_CAPABILITY_AUDIT_2026-07-08.md`
  §W2b, CHANGELOG entry, `LANG_PARITY.md` sibling entry `fri_xref_resolution_h404`
  (INTENTIONAL-DIVERGENCE). `window_selftest.py` + `lang_parity_check.py` green.
  Built in an isolated worktree (`SanskritLexicography-h404`); rebased onto
  origin/master post-H405 merge, resolved additive conflicts in CHANGELOG.md/
  LANG_PARITY.md.

- **H397 — koch `см. X` cross-reference resolution — DONE 09-07-2026 (Sonnet 5
  `claude-sonnet-5`).** New `src/koch_xref.py`: resolves koch's bare `см. X`
  redirects (3,472 of koch's 4,048 no-meaning entries, H337) to the target
  headword's real gloss via a Devanagari→SLP1 crosswalk harvested from koch's own
  self-describing `<devanagari> /iast/` headword prefix (no external
  transliterator). 3,204/3,472 (92.3%) resolve dictionary-wide (target was
  ≥2,500), chain-safe up to 2 hops with a cycle guard, never fabricates. Wired
  into `annotate_evidence.py`'s koch lane before relation classification
  (`--no-resolve-xref` reproduces H337 exactly). Live store backfilled: koch
  `provides` 143→155, `supports` 560→578, `silent` 79→74 (145-lemma current
  store). 20-sample spot-check: zero fabricated meanings. `PIPELINE_CAPABILITY_AUDIT_2026-07-08.md`
  §W2 extended, CHANGELOG entry added, `LANG_PARITY.md` sibling entry
  `koch_xref_resolution_h397` (INTENTIONAL-DIVERGENCE). Built in an isolated
  worktree since the main tree had an unresolved merge conflict from a
  concurrent Codex session at the time; worktree removed after merge.
  [SanskritLexicography PR #271](https://github.com/gasyoun/SanskritLexicography/pull/271) merged.

- **FIX_PLAN_2026-07-09 closeout selftest portability — DONE 09-07-2026 (Codex/GPT-5).**
  `window_selftest.py` no longer fails in a checkout missing gitignored runtime
  rootmaps/inputs: perf-preflight tests now provision minimal temporary rootmap,
  nominal, matrix, and TM fixtures with restore-on-exit, and the canonical harness
  scope test skips only the rootmap-provenance comparison when the referenced
  rootmap is absent while still checking the generated JS tool guard. Refreshed
  the affected `LANG_PARITY.md` `window_selftest.py` hashes after re-checking the
  verdicts (fixture-only/test-portability change; RU/EN behavior unchanged).
  Verified closeout checks: `python src/pilot/window_selftest.py`,
  `python src/pilot/lang_parity_check.py`, and
  `python src/pilot/check_launch_ledger.py --since 2026-07-05`.

- **Coordinator orchestration hardening audit — DONE 09-07-2026 (Codex/GPT-5).**
  `src/pilot/coordinator.py` now enforces lease TTL only for abandoned pre-prepare
  `claimed` leases (freeing the global 3-lane cap) while preserving already
  `prepared` harness leases as durable operator artifacts; coordinator locks now
  create their parent dir in a fresh worktree; coordinator registry/daily JSONL
  appends now reuse the shared atomic `append_jsonl_line()` primitive. Added
  `test_coordinator_expired_leases_release_cap` to `window_selftest.py`, recorded
  parity as `coordinator_claim_expiry_and_atomic_jsonl`, and updated
  `RUN_FREQ_MAX.md` so 3-wide is an upper bound after a clean solo reference, not
  the default. Verified: `py_compile`, `check_launch_ledger.py --since 2026-07-05`,
  `lang_parity_check.py`, full `window_selftest.py`, and live coordinator status
  still shows the three H151 leases as `prepared`.

- **RussianTranslation performance audit + TM load optimization — ✅ DONE 08-07-2026 (Codex/GPT-5).**
  Audited the live H317/pilot performance surface and measured the canonical generator path:
  `gen_opt_harness2.py dah` pre-resolves 26/31 cards from `translation_memory.ru.json` and emits
  a 3-batch harness in ~1.4s wall time; cProfile showed `translation_memory.load_tm()` as the main
  local hot path. Optimized [`src/pilot/translation_memory.py`](src/pilot/translation_memory.py)
  by replacing sort-for-first with `max()` and adding an mtime/size-guarded in-process `load_tm`
  cache that invalidates on TM or denylist changes. Measured same-process repeat load:
  first parse 0.3443s; 20 cached calls 0.0105s total (~0.00053s each). Also fixed the H317 launch
  ledger schema drift by adding explicit `open-paused` residual status in
  [`src/pilot/check_launch_ledger.py`](src/pilot/check_launch_ledger.py) and normalizing
  [`LAUNCH_FUCKUPS.md`](LAUNCH_FUCKUPS.md). Refreshed the three affected
  [`LANG_PARITY.md`](LANG_PARITY.md) hashes after re-checking the SHARED verdicts. Verification:
  `translation_memory.py selftest`, `translation_memory.py validate --lang ru`,
  `check_launch_ledger.py --handoff H317`, `lang_parity_check.py`, and full
  `window_selftest.py` all pass.

- **H317 pwg-ru medium50 pipeline test window — CLOSED 08-07-2026, 0/50 promoted (finding filed,
  not a silent absorb).** Sonnet 5 (`claude-sonnet-5`), isolated worktree `SanskritLexicography-h317`
  (branch `h317-pwg-ru-medium50-test`). Froze a 50-key band-4 (DCS frequency), nominal,
  single-record worklist via new [`src/pilot/select_medium50.py`](src/pilot/select_medium50.py) →
  [`src/pilot/H317_medium50_worklist.08.07.26.json`](src/pilot/H317_medium50_worklist.08.07.26.json)
  ("senses" operationalized as total citation count per entry, 3-30, since real PWG nominal
  entries carry a single top-level sense — see script docstring). Generated 4 nominal harness
  sub-windows (h317_w1a/w1b/w2a/w2b, 13+12+13+12 keys; the naive 2x25-key split oversized the
  harness byte cap). Two launch attempts, both failed to net any clean cards:
  - **Attempt 1** — 3 windows concurrently (≤3-wide per RUN_FREQ_MAX.md): 0/38 ok, every batch +
    self-heal group hit kill-timeout cascading until each window's own MAX_AGENTS budget-kill-switch
    tripped (67/61/49 agents spent vs ~19/17/13 expected).
  - **Attempt 2** — solo (1-wide) retries of w1a and w1b: still 0 net clean (w1a: 1 raw return
    flagged `missing_senses`; w1b: 3 raw returns, 1 high-confidence defect + 2 more flagged in
    review queue), with 8 `Connection closed mid-response` agent errors on the w1a retry (0 on
    w1b) — consistent with a session-wide transient API instability layered on top of the
    concurrency issue, not fully explained by concurrency alone.
  - **Totals: 305 agent() calls, ~6.65M subagent tokens, 0/50 cards promoted.** h317_w2b never
    launched (paused after attempt 2's result).
  - Logged as `H317_MEDIUM50_3WIDE_KILL_CASCADE_2026-07-08` in
    [`LAUNCH_FUCKUPS.md`](LAUNCH_FUCKUPS.md); full write-up in
    [`MEASUREMENT_2026-07-08_H317.md`](MEASUREMENT_2026-07-08_H317.md).
  - **Next physical action (future session):** resume from the per-window requeue lists
    (`src/pilot/output/requeue.h317_w1a.keys.txt`, `requeue.h317_w1b.keys.txt`,
    `requeue.h317_w2a.keys.txt`; h317_w2b still needs its first launch) — but ONLY after a solo
    diagnostic Workflow launch comes back mostly/fully clean, confirming the transient instability
    has passed. If a clean-environment solo retry still shows this failure pattern, escalate as a
    genuine kill-gate miscalibration for nominal medium-band cards rather than assuming transient
    infra again.

- **H350 / E7 — LOD-graph upgrade (08-07-2026, Opus 4.8 `claude-opus-4-8`).** Turned the flat
  one-way OntoLex string export (`src/export_interop.py` → `release/ontolex.ttl`, `example.org`
  IRIs, no query surface) into a real LOD graph via new [`src/export_lod.py`](src/export_lod.py):
  configurable dereferenceable IRIs, `vartrans` sense-relations (from `pwg_ru_relationships.jsonl`),
  PROV-O evidence grades (SKOS scheme; citable = SPARQL filter), per-sense `<ls>` as first-class
  `pwglex:Citation`, Renou stratum (`pwg_sense_stratum.jsonl`) as dated `StratumAttestation`, and a
  LiLa-style lemma bank (one `lila:Lemma` per `key1`, the join spine). Separate DCS-frequency graph
  (`export_lod.py dcs-freq`) keys freq to the same lemma IRI. Acceptance gate
  [`src/lod_acceptance.py`](src/lod_acceptance.py) PASSES: federated SPARQL join (sense→citation+DCS
  freq, 25 rows), lossless round-trip (byte-identical regen + graph-isomorphism + source-coverage:
  372 citations / 2 strata / 40 relations / grades all match source), SHACL conformance. Committed
  fixture [`release/fixture/`](release/fixture/) (`rakz`+`a`/`aMSa`/`aMSaka`, 4157 triples). Design +
  before/after coverage table: [`LOD_GRAPH.md`](LOD_GRAPH.md). Worktree-isolated (H214). Open items:
  (1) IRI publication domain `@DECIDE` (placeholder w3id PURL); (2) `csl-standards` owns the published
  graph + SPARQL surface (routed, G2); (3) full data publication gated on G5 approvals (store is 100%
  `ai_translated`). `export_interop.py` untouched. Backlog #2 (BLI eval) + #3 (sense-genre) can start.

- **H316 code-hardening pass (07-07-2026, Fable 5 `claude-fable-5`).** Closed the FL6/CODE_REVIEW
  BOM class: `src/pwg_mask.py records()` now reads `utf-8-sig` (a BOM-bearing PWG source no longer
  silently drops the FIRST record) and warns on stderr for a truncated final record instead of
  dropping it silently. Windows cp1252 pitfall: `encoding='utf-8'` added to the capture-output
  `subprocess.run` calls in `src/make_edition_cut.py`, `src/preflight_remaining_gates.py`,
  `src/release_readiness.py`. Hang guards: timeouts on the shell-outs in `save_and_audit.py`
  (1800s), `src/pilot/audit_window.py` (1800s), `src/pilot/autosplit_requeue.py` (600s).
  Pinned by 3 new selftests (`test_pwg_mask_bom_source_keeps_first_record`,
  `test_pwg_mask_truncated_final_record_not_silent`, `test_subprocess_gate_calls_hardened`);
  LANG_PARITY ledger entry `subprocess_and_bom_hardening_h316` (SHARED), all hashes re-verified,
  no drift. Remaining 🔴 correctness backlog (positional card misassignment, TM poisoning,
  corpus-gate silent degradation) deliberately NOT touched — parked in H316's backlog section.

- **H136: corpus-lexicon RECALL gold measurement + consolidated A42 P/R memo (2026-07-07,
  Fable 5 `claude-fable-5`).** The precision half existed (gold/precision_report.md, 84.4%,
  2026-06-16); this session built the missing recall half. New committed sampler
  [`src/gold_recall_sample.py`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/gold_recall_sample.py)
  (`coverage` = deterministic full-corpus scan; `sample` = stratified seed=42 working sample,
  `CORPUS_LEXICON_PATH` override for worktrees). Results: **group-level coverage 98.9%**
  (58,271/58,897 eligible groups; weakest Classical 90.2%); **word-level recall 92.1%**
  (Wilson 95% CI 88.4–94.7; 258/280 translated content lemmata over 32 groups / 287 lemmata,
  Fable-adjudicated token-by-token). Per period: Vedic 97.3 / Epic 91.3 / Classical 96.3 /
  **Medieval 84.0** — dominant failure class = mūla-only alignment of verse+commentary groups
  (Gītārthasaṃgraha tails; 13/22 misses), runner-up = temporal/manner adverbs skipped by the
  noun/verb/adj prompt wording (5/22). Committed:
  [`gold/recall_set.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/gold/recall_set.jsonl)
  (per-group adjudication, auditable),
  [`gold/recall_report.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/gold/recall_report.md),
  [`gold/PR_MEMO_A42.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/gold/PR_MEMO_A42.md)
  (the single citation point for A42: P 84.4% / R 92.1% / coverage 98.9%, both LLM-estimated,
  human packets pending). Delivered via worktree branch `feat/corpus-lexicon-recall-gold` → PR.

- **H206: nominal-worklist PWG-miss gap fixed; render-path gap flagged as `@DECIDE` (2026-07-05,
  Sonnet 5 `claude-sonnet-5`).** `nominals_worklist.py`'s `build_worklist()` split every nominal
  candidate into PWG `hits` (queued) and `misses` (only aggregate-counted, never queued) —
  ~36% of the local headword union has zero PWG record (FINDINGS.md §61), so this was a real
  drop. **Damage-scope:** of 1,743 total PWG misses across the 3 already-run wordlists (Приложение
  5, Приложение 10, Сборное ядро), **416** (232 deduplicated) DO carry a real PW/SCH/PWKVN record —
  translatable, not non-words. **Fixed:** misses now split into `other_layer_hits` (tagged with
  layer(s)) vs `true_misses` (absent everywhere), surfaced in the payload and the 3 committed
  `.coverage.md` reports. **NOT forced:** `other_layer_hits` are deliberately NOT promoted into
  `runnable_remaining` — `_pilot_gen_merged.gen_card()` hard-requires a non-empty PWG buf and has
  no card-building path for a PW/SCH/PWKVN-only headword (the portrait/microstructure step is
  PWG-shape-specific); forcing promotion would just feed the runner keys it treats as `MISSING in
  PWG`. Flagged as a new `@DECIDE` (how to portrait/render a no-PWG card) rather than papered
  over. The 232 lemmas are queued in the new committed
  [`src/pilot/lexical_cores/pwg_miss_backfill_queue.md`](src/pilot/lexical_cores/pwg_miss_backfill_queue.md),
  ready to promote once the render-path decision lands. Full writeup in `PIPELINE_HISTORY.md`
  Phase 9. `verb_worklist.py` and `dict_merge.py` untouched (unaffected per the handoff).

- **Render-path `@DECIDE` ruled (2026-07-05, Sonnet 5 `claude-sonnet-5`) —
  [`PWG_MISS_RENDER_PATH_DECISIONS.md`](PWG_MISS_RENDER_PATH_DECISIONS.md).** Build the no-PWG
  render path (extend `microstructure.portrait()` to parse PW/SCH/PWKVN buf shapes, not a cheap
  stub); queue it as **backlog behind the active Track A H151/SLA-scaling effort** (not an
  interrupt); flag PWG-less cards distinctly downstream (translator prompt/judge/dashboard) once
  built. The 232-lemma `pwg_miss_backfill_queue.md` stays parked until this backlog item is
  picked up.

- **pwg_ru reuse-source wiring: SamudraManthanam name-glossaries → corpus_gate SPECIALIST tier
  (2026-07-05, Sonnet 5 `claude-sonnet-5`, H184).** Wrote
  [`src/build_glossaries.py`](src/build_glossaries.py) to extract 2 of the 6 candidate
  SamudraManthanam name-index glossaries (Гринцер Рамаяна I-II/III, 663 IAST-keyed entries;
  reuses `iast_to_slp1` from `build_src.py`), registered as `SPECIALIST` (`grin12`/`grin3`) in
  [`src/corpus_gate.py`](src/corpus_gate.py) — separate from `INDEP` on purpose, surfaced only
  in `build_card()`'s `specialist_glosses` field, rights unconfirmed → evidence-only. The other
  4 candidates were investigated and NOT wired: Потапова/Эрман-Темкин/Бада-Кадамбари have
  Cyrillic-only headwords with no deterministic Sanskrit transliteration (dental/retroflex
  collapse risk in a correctness-authority signal); Топоров is a page-index, not a gloss
  source. Wrote [`REUSE_MAP.md`](REUSE_MAP.md) as the canonical Russian Sa→Ru reuse inventory
  (a 05-07-2026 Opus 4.8 scout found ~98% of MG's brainstormed source list already
  wired/extracted). Registered in
  [`Uprava/PROJECT_INTERLINKS.md`](https://github.com/gasyoun/Uprava/blob/main/PROJECT_INTERLINKS.md)
  + [`SHARED_CODE.md`](https://github.com/gasyoun/github-spine/blob/main/SHARED_CODE.md) §14.

- **Full-tree code review + critical fixes + pilot measurement + ACL bibliography (2026-07-04,
  Opus 4.8 `claude-opus-4-8`, 5 parallel reviewers).** Reviewed all 34,668 LOC of
  `src/`; findings in [`CODE_REVIEW_2026-07-04.md`](CODE_REVIEW_2026-07-04.md). Landed the
  critical subset via [PR #138](https://github.com/gasyoun/SanskritLexicography/pull/138)
  (merged): atomic `.tmp`+`os.replace` store writes in `promote_en.py`/`promote_final_cards.py`
  (the "layer wiped" scar), `run_batch.cmd_collect` no longer crashes on a string-wrapped
  `result` (the "output lost" scar), and the `pwg_mask.classify_pct` Latin-cue leak (33
  Latin/Greek cognate glosses across PWG were being sent for German translation) — fixed +
  regression test `test_pwg_mask_latin_cue_behind_ab_tag` + SHARED parity entry
  `latin_cue_masking`; full suite 54/54. Measurement in
  [`MEASUREMENT_2026-07-04.md`](MEASUREMENT_2026-07-04.md): 4,392 cards across 108 root
  outputs, current RU/EN coverage = 0 null / 0 partial (137 legacy `sc/sd` nulls only);
  fidelity battery 98% ok / 2% sev≥3; Fable S7 100% sev0. ACL Anthology first annotated
  bibliography written to [`research/ACL_ANTHOLOGY_MONITOR.md`](research/ACL_ANTHOLOGY_MONITOR.md)
  (GEMBA-MQM, Chain-of-Dictionary, RA-NMT retrieval, JSONSchemaBench, ByT5-Sanskrit, ISCLS).
  **Backlog (not yet fixed):** QA-gate false-negatives (`audit_window_en` DUP never emitted;
  RU gate stdout-scrape), positional card-misassignment in `resolveGroup`/`healGroup`, TM
  reuse gaps — all ranked in CODE_REVIEW. **No fresh root drained** (see run note in
  MEASUREMENT); next drain = standing H151 queue head.

- **Publication-grade TM artifact lane + fuzzy-speed profile support implemented (2026-07-04,
  Codex/GPT-5).** Implemented D10-D14 follow-through in
  [`src/pilot/translation_memory.py`](src/pilot/translation_memory.py) and
  [`src/pilot/gen_opt_harness2.py`](src/pilot/gen_opt_harness2.py): new
  `export-publication`, `export-terminology`, and `speed-report` commands; strict
  publication records with `tm_record_id` / `record_type` / source hashes / provenance /
  evidence; deterministic fuzzy ranking profiles (`semantic`, `german`, `sanskrit`,
  `balanced`) with `semantic` default; harness metadata now records `suggest_profile` and
  `suggest_tm_top`; exact TM behavior and agent accounting remain unchanged. Generated tracked
  publication snapshot under [`release/translation_memory/`](release/translation_memory/)
  (**2,392 rows = 2,175 exact-card + 217 fragment; manifest SHA verified**) and separate
  DOI-ready terminology scaffold under
  [`release/sa_ru_terminology/`](release/sa_ru_terminology/) (0 terms until curated
  suggestions exist). Added [`research/ACL_ANTHOLOGY_MONITOR.md`](research/ACL_ANTHOLOGY_MONITOR.md)
  as the monthly monitor's tracked output home and documented the terminology DOI track in
  [`DOI_PLAN.md`](DOI_PLAN.md). Validation passed:
  `python src\pilot\translation_memory.py selftest`,
  `python src\pilot\translation_memory.py validate --lang ru --publication release\translation_memory\translation_memory.ru.publication.jsonl`,
  `python src\pilot\window_selftest.py`, schema JSON parse, py_compile, and manifest checksum
  checks.

- **TM publication/fuzzy/monitoring decisions locked (2026-07-04, Codex/GPT-5).**
  Appended D10-D14 to [`TRANSLATION_MEMORY_DECISIONS.md`](TRANSLATION_MEMORY_DECISIONS.md):
  publication-grade TM becomes a tracked artifact now; fuzzy scoring will test
  German-fragment, Sanskrit-headword, and semantic-tag/register channels (semantic/register
  is only the prior); fuzzy TM is evaluated for speed first; curated Sanskrit→Russian
  terminology gets a separate DOI path; and monthly ACL Anthology / NLP-for-DH monitoring
  is active via Codex automation `monthly-acl-anthology-sanskrit-nlp-monitor`.

- **Fragment-tag collision fix + SAN-LOSS hardening (2026-07-04, Sonnet 5
  `claude-sonnet-5`). [PR #127](https://github.com/gasyoun/SanskritLexicography/pull/127)
  (merged).** Traced the SENSE-DUPE false positive on `vid`'s giant
  single-sense heads (`h0_00_pwg00`/`h2_00_pwg0*`) to `gen_opt_harness2.py`
  discarding `split_plan()`'s `sense_ord` when building the selfHeal fragment
  fallback — citation-batch fragments of ONE source sense had no shared
  identity, so the model fabricated fresh incrementing tags per fragment that
  collided with a sibling rootmap part's real senses in
  `audit_sense_dupes.py`. Fixed: `sense_ord` now threads through as `si` in
  `FRAGS[k]`; JS `selfHeal()` normalizes every fragment's tag to its group's
  first-seen tag per `si` (`siTag`/`applyTag`). Also hardened
  `autosplit_requeue._cit_parts()` against a related SAN-LOSS hypothesis (a
  `{#...#}` Sanskrit span, unlike `<ls>...</ls>`, never counted toward the
  citation budget that decides cut points, so it could be torn mid-span,
  leaking unmasked Sanskrit into the model prompt) via a new `_span_open()`
  guard — defensive, NOT empirically reproduced against the actual failing
  data (the run's raw fragment files were no longer present in-tree). Both
  fixes covered by new `window_selftest.py` fixtures
  (`test_selfheal_fragment_si_threaded_and_tags_normalized`,
  `test_cit_split_never_tears_open_span`), full suite re-run green (only
  pre-existing `gam`-fixture-dependent failure unrelated to this change, and
  `translation_memory.py selftest` OK); generated JS harness syntax-checked
  with `node --check`. Written up as Phase 8 item 5 in
  [`PIPELINE_HISTORY.md`](PIPELINE_HISTORY.md). Same-shape risk flagged for
  BU/yuj/as/sTA/i (H151 queue) — worth a SENSE-DUPE re-check on already-
  promoted cards once this fix has run on a live drain.

- **Translation provenance audit/backfill shipped (2026-07-03, Codex/GPT-5).**
  Added [`src/audit_translation_provenance.py`](src/audit_translation_provenance.py)
  and hardened [`src/promote_final_cards.py`](src/promote_final_cards.py) so future
  RU promotions require `--gen-model-version <exact-model-id>` instead of relying on
  a stale implicit `sonnet` mapping. Baseline audit of the live local store:
  **10,856 rows; 10,446 RU rows missing exact `model_version`; 410 RU rows already
  exact-versioned (`claude-sonnet-5`); 8,574 EN provenance rows exact-versioned;
  15 rows missing input hashes; 80 partial-card rows.** Applied conservative
  backfill to local `src/pwg_ru_translated.jsonl`: legacy ambiguous RU rows now
  carry `model_version_unresolved` + `model_version_note`; no translation text,
  review status, hashes, or `wf_file` values changed. Next operator action: when
  promoting any new RU window, pass the exact generation model explicitly, then run
  `python src/audit_translation_provenance.py` and refresh TM with
  `python src/pilot/translation_memory.py build --lang ru`.

- **S7 quality follow-through — ✅ DONE 2026-07-02, Opus 4.8 (`claude-opus-4-8`); A/B
  generation Sonnet 5 (`claude-sonnet-5`). [PR #89](https://github.com/gasyoun/SanskritLexicography/pull/89)
  (merged).** The three [`FABLE_JUDGE_S7_2026-07-02.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/FABLE_JUDGE_S7_2026-07-02.md)
  recommendations delivered (handoff [`H056-Opus_SanskritLexicography_pwg_s7_quality_gate_02.07.26.md`](https://github.com/gasyoun/Uprava/blob/main/handoffs/H056-Opus_SanskritLexicography_pwg_s7_quality_gate_02.07.26.md)):
  (1) **"translate, don't annotate"** HARD RULE added to BOTH the RU template
  ([`run_pilot_wf.js`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/run_pilot_wf.js)
  `TR` rule 6) and the EN prompt ([`tr_en.txt`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/tr_en.txt)
  rule 4) — render exactly what the German says, never add glosses/qualifiers/attributions;
  (2) **MW-TM guard** — [`gen_opt_harness2.mw_tm_block`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/pilot/gen_opt_harness2.py)
  injected block + `tr_en.txt` MW REFERENCE now state MW is a terminology cross-check only
  (no phrase copied unless the German licenses it); (3) the **`{%..%}`-pair-count soft gate**
  the memo asked for **already shipped** in [PR #78](https://github.com/gasyoun/SanskritLexicography/pull/78)
  (RU `markup_wrapper_dropped` + EN `MARKUP-LOSS`, both soft/report-only) — this session
  pinned both with `window_selftest.py` fixtures (`test_markup_loss_soft_flag_ru/_en`; suite
  **26/26 green**). **A/B** (yaj, 7 cards incl. the 208-`<ls>` dense head, old vs new prompt,
  Sonnet 5, ≤3-wide, scratch-only — RU store untouched): on the 6 identical non-partial cards
  marker retention **2/13 → 13/13** (drop **85 % → 0 %**); addition-class high-conf risks
  **3 → 0**; coverage **7/7 ok** both arms (no regression). n small + stochastic = indicative
  not conclusive; direction unambiguous, nothing regressed. ⚠ concurrent session was editing
  `gen_opt_harness2.py`/`translation_memory.py`/`window_selftest.py` (sense-level-TM increment)
  in the working tree during this session — PR #89 is off `origin/master`, edits localized, any
  merge conflict is trivial (keep both).

- **S10 architecture audit + big-card failure closure — ✅ DONE 2026-07-02, Fable 5
  (`claude-fable-5`).** Full findings:
  [`ARCHITECTURE_AUDIT_2026-07-02.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/ARCHITECTURE_AUDIT_2026-07-02.md).
  Verdict: "fails too often" = ten distinct failure modes, PR #35/#37/#38/#40 cover one;
  systemic root defect = every failure collapsed to an undifferentiated `card:null` with
  the cause discarded. **PARTIALLY CLOSED** by 5 PRs (all mock/selftest-validated):
  [#63](https://github.com/gasyoun/SanskritLexicography/pull/63) harness total-accounting
  invariant + failure ledger + key1-matched acceptance + heal-side fidelity + partial
  credit in healGroup; [#64](https://github.com/gasyoun/SanskritLexicography/pull/64)
  autosplit accounted nulls + persisted fail-trail + per-root manifest (race);
  [#65](https://github.com/gasyoun/SanskritLexicography/pull/65) promote: EN-shadow fix +
  partial markers + model_version provenance;
  [#66](https://github.com/gasyoun/SanskritLexicography/pull/66) audit: partials +
  reason histogram + fidelity-nulls→defect lane;
  [#67](https://github.com/gasyoun/SanskritLexicography/pull/67) dead code + stale
  operator pointers + this doc. **Live validation PASSED 4/4**: the 4 store-missing `vas`
  keys re-run through the #63 harness via the Workflow tool (15 agent calls / ~988k tok;
  generation = Sonnet 5 (`claude-sonnet-5`) — ⚠ the `sonnet` alias now resolves to
  Sonnet 5, not 4.6); one junk `"test"` response rejected by key1-matching, one
  StructuredOutput-cap hard throw absorbed into selfheal, `zz_pw01` healed complete
  across 9 groups. Merge-saved (`wf_output.sc.vas.json` 7→11 non-null) + merge-promoted
  (store 10,614→10,771 rows, `--gen-model-version claude-sonnet-5`, backup
  `pwg_ru_translated.jsonl.SAFE_s10_20260702.keep`). Remaining open work is queued above
  (dense-residual re-run; audit-churn fix).


- **pwg selfheal/binary-split/output-budget/partial-credit fixes — 4 PRs merged, live-validated
  (2026-07-02, Sonnet 5 `claude-sonnet-5`).**
  [PR #35](https://github.com/gasyoun/SanskritLexicography/pull/35) fragment-grouping (citation-
  weighted, not bytes); [PR #37](https://github.com/gasyoun/SanskritLexicography/pull/37)
  `--binary-split`/`--output-budget` + `healGroup` bisection;
  [PR #38](https://github.com/gasyoun/SanskritLexicography/pull/38) root-caused+fixed the
  `brū`/`pwg00` header-budgeting bug + a crash-guard for hard `agent()` failures;
  [PR #40](https://github.com/gasyoun/SanskritLexicography/pull/40) fixed the crash-guard's
  scope bug + added per-group partial-credit healing for giant flat headwords. Live-validated:
  `brū` 23/23 (0 nulls); `kāla`/`ka`/`śrī` (the 3 biggest non-root nominal stems, zero prior RU
  coverage) all translated and promoted to the store (10,122→10,614 rows). Dashboard rebuilt +
  republished to `gh-pages` (51 roots/14,996 senses). Full writeup:
  [`H038-Sonnet_RussianTranslation_pwg_selfheal_fixes_02.07.26.md`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/H038-Sonnet_RussianTranslation_pwg_selfheal_fixes_02.07.26.md).
  Follow-up (M.G.: pipeline still fails "too often" over the whole week) queued for Fable 5 as
  a full architecture audit — see Next Steps above.

- **Sa→Ru glossary (surface · lemma · root) BUILT from `corpus_lexicon.jsonl` (2026-07-01).**
  Full inventory of how every Sanskrit word is rendered into Russian, 3 granularities, from
  the 1,091,528-token aligned corpus. Answers "how is √gam translated" → root rollup: 678
  forms / 44 lemmas / 7,116 occ, ranked (пришёл 196, отправился 177, ушёл 141…). Pipeline in
  [`glossary/README.md`](glossary/README.md): DCS `dcs_full.sqlite` form→lemma join (IAST→SLP1
  via sanscript; root = longest DCS-root suffix, sidesteps preverb sandhi) → **vidyut.kosha**
  fallback → morpheme-marker tier (`A+gam`=ā+√gam) → provenance-tagged rollups. Coverage
  **87.1% of tokens**; 190,838 forms → 40,370 lemmas → 2,021 roots; 78,842 unresolved with a
  documented failure typology. Scripts:
  [`src/build_dcs_maps.py`](src/build_dcs_maps.py),
  [`src/build_surface_glossary.py`](src/build_surface_glossary.py),
  [`src/build_vidyut_fallback.py`](src/build_vidyut_fallback.py),
  [`src/build_rollup_glossaries.py`](src/build_rollup_glossaries.py). Bulk data git-ignored
  (regenerable); committed = scripts + README + `SAMPLE_root_glossary.md`. PR #30.

- **PWG→EN follow-ups FU2 + FU3 SHIPPED (2026-06-30)** — from
  [`H033-Opus_RussianTranslation_pwg_en_followups_30.06.26.md`](H033-Opus_RussianTranslation_pwg_en_followups_30.06.26.md). FU1
  (the freq-ordered bulk EN run, real Max-quota spend) remains open, pending MG scope.
  - **FU2 — EN audit gate:** [`src/pilot/audit_window_en.py`](src/pilot/audit_window_en.py),
    report-only, operates directly on `wf_output.en.<root>.json` per-sense `german`/`english`
    (the RU `audit_window.py` is `.merged.md`-/RU-bound). Keeps the language-agnostic gates
    (LS/SAN markup fidelity reusing `audit_translation.py` thresholds, `<ab>`/`<lex>` sigla,
    coverage) and **delegates the canonical cross-card sense-dupe gate to the existing
    `audit_sense_dupes.py`** (it keys on tags, not gloss language → runs unchanged on EN).
    Swaps the RU semantic checks for EN ones: DE-residue (umlaut/eszett in lowercase tokens +
    German function words; capitalized names like Graßmann/Böhtlingk excluded), RU-residue
    (Cyrillic), circular gloss, within-card SAME-GLOSS, and a soft MW-divergence cross-check
    vs `src/mw_en_tm.json`. Wired into `save_and_audit.py` (tag `en` → EN gate). Pilot result
    over all 16 roots: **0 hard flags** (663 nn cards / 2906 senses); soft: 21 DE-residue
    (genuine `eig.`/`überh.`/`übertr.`), 27 MW-diverge, 2 SAME-GLOSS — all verified real.
  - **FU3 — tri-lingual store merge:** [`src/promote_en.py`](src/promote_en.py) attaches each
    EN sense's `english` as an `en` field onto the matching RU store row in
    `src/pwg_ru_translated.jsonl` (de+ru+en per sense). RU and EN are independent runs that
    DISAGREE on sense tags AND segmentation, so the join is on the **German source text**
    (normalized, then difflib fuzzy ≥0.92 unambiguous), NOT tag/position. Unmatched senses
    are left absent (never fabricated); `review_status='ai_translated'` (G5) untouched. Run
    result: 2417 / 2882 EN-eligible rows now carry `en`; 463 unmatched (different
    segmentation); 5710 rows are non-EN roots (FU1's job). `annotate_dcs_freq.py` re-run after
    — all 8592 rows keep `dcs_freq`, `en` preserved. `--selftest` + `--dry-run` both pass.
    (Store/outputs are gitignored regenerable artifacts; only the code is committed.)

- **Step 1 grammar-parity re-runs (2026-06-29):** sTA/BU/as/i/gam re-translated on
  `gen_opt_harness2.py` (3-layer batched+masked). 611 cards, 516 clean (84.5%), 95 requeue,
  all gates PASS, **$20.16 / ~62 min total**. ~8× cheaper per card than 2-layer baseline.
  Full findings: [`STEP1_GRAMMAR_PARITY_FINDINGS.md`](STEP1_GRAMMAR_PARITY_FINDINGS.md).

- **F-gate-nws-fp fix (2026-06-29):** eliminated the ~70 `suspicious_attested_without_text_signal`
  false positives that fired on `*_zz_pw*` PW cards and NWS cards. Two-pronged fix in
  [`src/pilot/prompt_rule_audit.py`](src/pilot/prompt_rule_audit.py): (1) `has_text_signal()`
  now scans all sense field values (not just `citation_blob()` which skips `equivalence_type`)
  for `NWS_OWNER_CITATION = re.compile(r'\[NWS:\s')` patterns; (2) `semantic_risks()` computes
  `card_has_text_signal` over all attested senses before looping, and suppresses the per-sense
  flag when the card already has a text signal elsewhere. Added `test_nws_fp_suppressed()` to
  [`src/pilot/window_selftest.py`](src/pilot/window_selftest.py) (PW card case + NWS card case).
  All 13 selftests pass. Also extended [`.gitignore`](.gitignore) to cover `run_pilot_wf.opt2.js`,
  `run_pilot_wf.batched.js`, `run_pilot_wf.sc.js`, `run_pilot_wf.tlonly.js`, and
  `wf_after*.json`/`wf_before*.json`/`wf_opt*.json`.

- **Fast print/DH acceleration review (2026-06-29):** added
  `PRINT_DH_ACCELERATION_REVIEW.md` and
  `roadmap/print_dh_acceleration_review.json`. The review answers the recurring
  "what to review next?" question with a low-human-action order: fresh `sTA`,
  deterministic audit, `prompt_semantic`/`judge_sample.keys.txt`, G5/G6/G7, then
  print/DH renderer and front-matter decisions. It preserves the status line:
  bulk can continue after fresh `sTA`; print remains blocked by G5/G6/G7/G10.
  Follow-up: `NEXT_REVIEW_PACKET.md` is the executable checklist for the next
  operator pass, with fresh `sTA` audit stop/go rules and print feasibility
  review from `agni`, `akzara`, and `ap`.
  Follow-up v2: Max should not translate 10 big dhātus in one push yet. The
  staged evidence run is now documented: fresh `sTA`, then clean-ready
  `BU`/`as`/`i`, then prune/recheck `gam`/`yuj`/`vid`/`han` before any Max spend.
  Follow-up v3: `H027-Sonnet_RussianTranslation_claude_code_max_29.06.26.md` gives Claude Code Max
  the current Stage A/B/C commands, root status, audit return artifacts, and
  token/time fields to preserve.
- **Real print-entry examples review (2026-06-28):** added
  `PRINT_ENTRY_EXAMPLES.md` and `roadmap/print_entry_examples_review.json`,
  using real local `src/pilot/output/agni.merged.md`,
  `src/pilot/output/akzara.merged.md`, and `src/pilot/output/ap.merged.md` as
  layout/QA prototypes. Linked the examples from `PRINT_ENTRY_SPEC.md` and
  `PRINT_DH_SPEED_REVIEW.md` while preserving the verdict: bulk can continue
  after fresh `sTA`; print remains blocked by G5/G6/G7/G10.
- **Human-review minimization + print-entry spec (2026-06-28):** added
  `HUMAN_REVIEW_MINIMIZATION.md`, `PRINT_ENTRY_SPEC.md`, and
  `roadmap/print_review_minimum.json`. The docs convert G5/G6/G7 into exact
  reviewer files, allowed columns, validation commands, and done criteria, and
  define the target printed bilingual entry shape without changing prompts,
  schemas, or gates. Verified with `json.tool`, `window_selftest.py`,
  `root_window_status.py sTA`, `preflight_remaining_gates.py`, and
  `release_readiness.py`.
- **Print/DH/speed readiness review pack (2026-06-28):** added
  `PRINT_DH_SPEED_REVIEW.md` and `roadmap/print_dh_speed_review.json`. The
  review separates "ready to continue bulk translation" from "ready for reviewed
  core tranche", "immutable digital edition", "printed bilingual dictionary",
  and "full PWG tail". Current verdict: production engine is ready
  window-by-window after a fresh `sTA` Max run; print/release remain blocked by
  G5/G6/G7/G10 human gates (0/217 review decisions, 0/320 gold labels, no real
  double-review report, no edition cut). Verified evidence with
  `root_window_status.py sTA`, `prompt_rule_audit.py --cards wf_output.json
  --review-limit 25`, `preflight_remaining_gates.py`, `release_readiness.py`,
  `window_selftest.py`, and JSON validation for the new roadmap companion.
- **Fast low-human audit hardening (2026-06-28):** integrated
  `prompt_rule_audit.py` into the canonical `audit_window.py` loop as the new
  `prompt_semantic` gate. Prompt/manual-rule drift now blocks as a crashed gate;
  semantic-risk scanning feeds a ranked review queue and only high-confidence
  mechanical risks (`empty_russian`, broken markup, translated sigla/grammar,
  German residue, and conservative `{%...%}` gloss leaks) become requeue
  candidates. `audit_window.report.*` and `window_status.*` now surface live
  manual coverage (Apresjan, Hartmann, Gonda/Vogel, Tubb, Baalbaki,
  Apte/Gillon/Inglese-Geupel, Mitrenina/Zaliznyak-Paducheva/Ruppel) and note
  Riemer/Klosa as methodology/design-only. Verified with `py_compile`,
  `window_selftest.py`, `prompt_rule_audit.py --cards wf_output.json`, stale
  audit preservation, and a one-card synthetic audit smoke.
- **Quick deterministic audit pack (2026-06-28):** added
  `src/pilot/prompt_rule_audit.py`, a no-token scanner for live prompt-rule
  coverage in the committed template and generated optimized harness. It now
  reports ignored `prompt_rule_audit.{json,md}` artifacts and exposes cheap
  semantic-risk checks for German residue, collapsed synonym strings, circular
  glosses, missing `equivalence_type`, and missing Apresjan differentiae. The
  live prompt now explicitly warns against non-circular/cryptographic glosses,
  `window_selftest.py` covers prompt-rule and semantic-risk fixtures, and
  docs state that LLM semantic judging remains only the later
  `judge_sample.keys.txt` spend queue.
  Follow-up v2: `prompt_rule_audit.py --cards wf_output.json` now scans
  translated workflow/card JSON directly, adds `card_risks` to the ignored
  JSON/Markdown reports, flags empty required fields, identical glosses,
  formula drift, suspicious `source_type` evidence, and keeps `--fail-on-risk`
  opt-in for fixture/CI use.
  Follow-up v3: card-risk reports now include a severity-scored `review_queue`
  for minimum-interaction human review, plus markup/sigla leakage checks,
  formula inventory counts, sense-compression signals, `--review-limit`, and
  opt-in `--fail-on-high-risk`.
- **Architecture hardening pass (2026-06-28):** split `audit_window.py`'s
  workflow parsing, root/input provenance checks, report/queue/status writing,
  and judge-sample/status helpers into importable `src/pilot/` modules while
  preserving the existing CLI. Added `window_selftest.py` covering nested
  workflow payloads, optimized-harness selected-key/tool guards, stale audit
  refusal preserving `requeue.keys.txt`, and release manifest hash validation.
  Marked `run_pilot_wf.js` as template-only and refreshed
  `PIPELINE_ARCHITECTURE.md` with the current production architecture.
- **Release validator diagnostic polish (2026-06-28):** `validate_release.py`
  now explains that a missing `release_manifest.json` usually means the operator
  passed staging `release/` instead of an immutable edition subdirectory such as
  `release/edition_v1`.
- **Validator CLI polish (2026-06-28):** `validate_assembled_export.py` and
  `validate_final_card_schema.py` now use `argparse`, so `-h/--help` works and
  their existing fixture/default validation semantics are preserved.
- **Audit follow-up validation docs (2026-06-28):** broad local gates pass
  (`compileall`, interop, schema selftests, assembled export, root glue, NWS,
  root-window status). The live `sTA` `wf_output.json` still correctly refuses
  as stale and G5/G6/G7/G10 remain human/Max gates. Added `gold_validate.py` to
  the reviewer handoff so completed human gold labels have an explicit G6
  validation command.
- **Audit follow-up tooling fix (2026-06-28):** `audit_window.py` now uses the
  optimized-harness provenance before choosing the stale-artifact next action,
  so a current `sTA` harness says to run Max instead of regenerating. The
  portrait-schema validator now has normal `argparse` help plus `--limit`, and
  release-validation docs clarify that `validate_release.py` expects an
  immutable edition subdirectory such as `release\edition_v1`, not staging
  `release/`.
- **Operator docs refreshed (2026-06-28):** added [`USE_CASES.md`](USE_CASES.md)
  with command-oriented flows for root preflight, fresh Max windows, stale-output
  recovery, requeue, sampled judging, dashboard monitoring, release checks, and
  corpus API retry. [`README.md`](README.md) now links those flows and summarizes
  the active guardrails; [`CHANGELOG.md`](CHANGELOG.md) records the audit
  guardrail hardening and docs update.
- **Audit guardrails hardened (2026-06-28):** `nws_split.py` now resolves
  root-split `~~` sub-card stems literally before safe-name fallbacks and returns
  distinct `NO-RAW`/`NO-CARD`/`NO-NWS` verdicts; `audit_window.py` requeues
  missing raw/card and quarantines only true NWS misattribution. `root_window_status.py`
  now requires optimized harness `selected_keys` to match the intended full or
  pending key scope before telling the operator to run Max. Verified with
  `nws_split.py selftest`, harness-scope assertions, `py_compile`,
  `root_window_status.py sTA`, stale `audit_window.py` refusal,
  `verify_root_glue.py`, and `node --check` on both workflow scripts.
- **Repo-local AGENTS.md compiled (2026-06-28):** added
  [`AGENTS.md`](AGENTS.md) with RussianTranslation-specific operating rules for
  the PWG→Russian Max workflow, deterministic audit/requeue policy, generated
  artifact boundaries, translation/markup invariants, and local session-state
  maintenance.
- **Token optimization for the Max bulk run — BALANCED tier (2026-06-27):** measured the quota
  driver (`cache_read ≈ context × turns`, driven by agents re-`Read`ing files 4–12×/card; one
  giant root ≈ 9–10 M tokens) and reshaped the run to "Python at max, LLM at minimum." A/B on
  tyaj: cache_read 2.74 M→0.85 M (**3.2×**), turns 138→47, Reads 60→19, 6.6→2.5 min, transient
  failures 3→0. Shipped: new free gate [`src/audit_coverage.py`](src/audit_coverage.py)
  (dropped/fabricated senses, 0 tokens, 100 % coverage); per-card LLM judge dropped → 3 free
  Python gates + judge only on flags + ~5–10 % sample; abs-diff false-positive guard in
  [`src/audit_translation.py`](src/audit_translation.py); sense-aware Python head split
  `sense_chunks()` in [`src/_pilot_gen_merged.py`](src/_pilot_gen_merged.py)
  (`HEAD_CIT_BUDGET=18`, Finding 5: heads fail by citation density, tyaj head 1 blob→8 parts);
  1 retry + missing-key re-queue (Finding 4). Full writeup
  [`TOKEN_OPTIMIZATION_2026-06-27.md`](TOKEN_OPTIMIZATION_2026-06-27.md); runbook
  [`src/pilot/RUN_FREQ_MAX.md`](src/pilot/RUN_FREQ_MAX.md) updated to the optimized loop + 3-gate
  QA stack; RT CHANGELOG dated entry. Local generated follow-up: `run_pilot_wf.opt.js`
  and the headtest harness now pass `tools: []` to translate agents, closing the
  residual Read-tool headroom for the optimized path.
  Code follow-up: `_pilot_gen_merged.py` now merges sense chunks after a numbering
  reset so secondary-conjugation sections keep their caus./desid./intens. marker
  instead of orphaning later sub-senses. Verification: `py_compile` and
  `_pilot_gen_merged.py --manifest freq --root-split --limit 1`.
- **Judge escalation policy implemented in the harness (2026-06-26):** flipped
  [`src/pilot/run_pilot_wf.js`](src/pilot/run_pilot_wf.js) from Opus-judges-every-card to the
  decided [`research/JUDGE_POLICY.md`](research/JUDGE_POLICY.md) policy — **Sonnet judges every
  card; Opus re-judges ONLY the hard cases Sonnet can't clear** (`isHard` = `ok=false ||
  severity>=3`), Opus verdict final (becomes `judge`; Sonnet's kept as `judge_sonnet`,
  `escalated:true`). Publishable cards (sev 1–2) spend zero Opus tokens → weekly-quota headroom.
  Justified by the κ=1.0/474-card A/B; `node --check` clean; `_pilot_collect.py` reads `judge`
  unchanged. Later production-loop follow-up superseded the old periodic-Opus TODO with
  `audit_window.py`'s deterministic `judge_sample.keys.txt` semantic review queue.
- **Corpus-gate deterministic boundary tightened (2026-06-26):** `build_corpus_lexicon.py`
  now lazy-loads `DEEPSEEK_API_KEY` only when a live DeepSeek call is made, so local
  `status`/inspection remains provider-free. `corpus_gate.py card` now emits a
  `deterministic_precheck` block (`pass` / `review` / `no-check`, score, threshold,
  route) for candidate PWG-RU text, while `run_batch.py` records an explicit
  `provider_gate` in batch input and the append-only store because pre-translation
  batches cannot honestly score Russian overlap yet. Verification:
  `py_compile`, `build_corpus_lexicon.py status`, `corpus_gate.py card agni "огонь"`,
  `run_batch.py build 3 covered`, and bounded assembled export validation
  (`assemble.py build 25` + `validate_assembled_export.py --min-cards 25`).
- **Bulk-run preflight audit + harvest ported into the Max harness (2026-06-26):** audited
  launch readiness — **Sonnet translator GREEN to start the first instrumented window**; all 4
  prompt nits already in [`src/pilot/run_pilot_wf.js`](src/pilot/run_pilot_wf.js) (HARD RULES
  3/4/5), all gate scripts wired. Key finding: the harness **inlines its own prompt** (does not
  read `pwg_ru_prompts/*.txt`), so ported the harvest refinements (samāsa, *yad…tad* correlatives,
  śāstric formulas, synonym cardinality, comma/semicolon, manner/position) + judge check 7 into it
  (`node --check` clean). Refreshed [`RUN_FREQ_MAX.md`](src/pilot/RUN_FREQ_MAX.md) (nits→checklist,
  SECTION warning, fidelity-gate step); rewrote the [`MANUALS_FIVE_DEEP_DIVE.md`](MANUALS_FIVE_DEEP_DIVE.md)
  closing as a per-finding pipeline-status table (Riemer battery + Klosa display = deliberately
  out-of-scope for bulk). Repo changelog `[0.0.29]`. **Open candidate:** Apresjan circular-gloss
  judge check is not yet explicit. *Obsolete caveat:* this used to need a human-driven Max
  session because `run_pilot_wf.js` read local files with `node:fs`; the optimized generated
  harness now inlines inputs and runs through the in-chat Workflow path.
- **Literature harvest folded into the live pipeline (2026-06-26):** harvest
  [`LITERATURE_FOR_PWG_RU.md`](LITERATURE_FOR_PWG_RU.md) → translator prompt
  [`pwg_ru_prompts/1_perevod.txt`](pwg_ru_prompts/1_perevod.txt) (samāsa rendering, case-absolute
  constructions, śāstric formulas, glossary pointer) + judge prompt
  [`pwg_ru_prompts/2_qa_sudya_opus.txt`](pwg_ru_prompts/2_qa_sudya_opus.txt) (matching defect
  classes) + new hand glossary [`glossaries/de_ru_translation_aids.md`](glossaries/de_ru_translation_aids.md).
  Committed + pushed with the two manuals docs, repo changelog `[0.0.28]`, RT CHANGELOG, README,
  and INDEX refresh.
- **Five-manual deep-dive (2026-06-26):** [`MANUALS_FIVE_DEEP_DIVE.md`](MANUALS_FIVE_DEEP_DIVE.md)
  — detailed, text-grounded theoretical input of the 5 load-bearing manuals (Apresjan ·
  Riemer · Hartmann & James · Gonda–Vogel · Klosa) for the Sa–Ru dictionary, with quotes +
  page/chapter anchors from the full-text extractions (these are NOT stubs — 312 KB–2.5 MB
  each). Each point carries a "→ Sa–Ru application" to glossary/translator/judge/corpus-gate/
  display, plus a "how the five compose" synthesis (Riemer→Hartmann→Apresjan decision chain;
  Apresjan⇄Klosa glossary/reverse-index loop; Gonda–Vogel as the only source-tradition guard)
  and a pre-run asks checklist mapped to the Track-A queue.
- **Per-manual pre-run audit (2026-06-26):** [`MANUALS_FOR_PWG_RU.md`](MANUALS_FOR_PWG_RU.md)
  re-files the [`LITERATURE_FOR_PWG_RU.md`](LITERATURE_FOR_PWG_RU.md) harvest one manual at a
  time across all 37 `literature/md/Lexicography-Manuals/` files: **19 ★ drive theory · 2 ◐
  marginal · 15 ○ serve other repos · 1 ⚠ blocked** (Rātānjanakar, Devanagari OCR noise — not
  a run blocker, covered by Apte/Gillon/Ruppel). Adds no new theory beyond the harvest; its
  value is the audit that the whole shelf was examined before the bulk run.
- **Freq Max local preflight + runbook refresh (2026-06-26):** refreshed the
  frequency manifest, verified the first root-split slice is present, ran the
  independent split/glue audit, added `src/pilot/RUN_FREQ_MAX.md`, and corrected
  the stale handoff text that still said the Opus judge pass was not run.
- **Opus 4.8 judge severity pass — DONE** (executed 2026-06-24, recorded commit **`b9e27fb`**
  2026-06-26): 37/38 publishable. Track A is cleared to scale, pending the four prompt nits +
  `nws_split.py` gate.
- **Renou two-axis tagging subsystem — COMPLETE & pushed.** On top of the five-state (I–V)
  axis, an orthogonal **20-register** axis (`renou_register`) built from DCS genre/name +
  `<ls>` sigla + detectors, multi-label, provenance-graded, materialized per-dict in
  `src/{code}.renou.jsonl`, audited (`renou_audit.py` + DCS min-support fix), surfaced as an
  editor badge (`renou_portrait.py`). Shipped: catalog
  [`RENOU_REGISTERS.md`](RENOU_REGISTERS.md); 8 glossaries [`glossaries/`](glossaries/) via
  `src/renou_glossary.py`; 6-slice cross-axis [`RENOU_CROSSAXIS.md`](RENOU_CROSSAXIS.md); 5
  findings [`RENOU_FINDINGS.md`](RENOU_FINDINGS.md) F1–F5; corpus verification of Renou's
  nominal-style thesis [`RENOU_NOMINAL_STYLE.md`](RENOU_NOMINAL_STYLE.md) +
  [`src/nominal_style.py`](src/nominal_style.py) (finite-verb density halves I 14.4 % →
  IV 6.7 %; bhāṣya < kāvya — the strongest single result); data note
  [`../papers/A34_renou_register_note.md`](../papers/A34_renou_register_note.md) (3/5). Plan
  [`RENOU_SUBSECTIONS_PLAN.md`](RENOU_SUBSECTIONS_PLAN.md) all phases done. Residuals: cite
  Tubb & Boose 2007 + Speijer 1886; human bhāṣya precision spot-check + inline-dict
  (ap/ben/bhs) `<ls>` register route; @WAITING badge-into-live-store join by key1.
- **Track B residual per-sense pass (commit `40108e5`, 2026-06-25):** coverage **agni 100 % /
  akṣara 99 % / anya 97 % / ananta 97 %**; Max-LLM (Opus 4.8) renderings marked **°**;
  reproducible `LLM_ASSIGN` map in `_build_persense_ru.py`.
- **agni 130-gloss agent draft review (commit `a544233` = tag `v0.0.10`):**
  [`../article-comparison/agni.gloss-review.md`](../article-comparison/agni.gloss-review.md),
  7 substantive + 4 optional + 6 FYI typos. Agent half done; **human sign-off still pending**.
- **akṣara gloss-review shipped (2026-06-26):**
  [`../article-comparison/aksara.gloss-review.md`](../article-comparison/aksara.gloss-review.md)
  — 55/55 senses, draft→adversarial-finalize; flagship слог↔Непреходящее/Brahman split verified
  correct, **0 H / 2 M / 4 optional**. Agent half done; **human sign-off pending** → flagship pair
  now has QA parity.
- **Track B table enrichments (after v0.0.7):** edition-years (v0.0.8), volume-names (v0.0.9),
  full untruncated tables + committed `src/_build_tables.py` (v0.0.11), chronological table
  sort (v0.0.12).
- **Versioning:** [`changelog.md`](../changelog.md) now at **[0.0.27]** (2026-06-26),
  `[Unreleased]` empty; tags contiguous `v0.0.1`..`v0.0.27`. (Supersedes the earlier "v0.0.7
  cut" note — that was the 2026-06-25 tail only.)
- **38-unit freq test** run + audited (38/38 fidelity-clean); giant-root split + glue solved;
  frequency-first queue built (41.4 % DCS-attested).
