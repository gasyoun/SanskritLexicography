# Project Objective: PWG (Petersburg Dictionary) → Russian edition + the CDSL article-comparison study

Two live tracks (see [`HANDOFF_2026-06-25_pwg_ru_max.md`](HANDOFF_2026-06-25_pwg_ru_max.md)
for the full cold-start brief):

- **Track A (PRIMARY):** scale PWG→Russian on the **Max** harness.
- **Track B (SECONDARY):** lock the article-comparison flagship pair (**agni + akṣara**)
  and finish its Russian to 100 %.

## ➡️ Next Steps (Queue)

- **Track A — gate before scaling:** run the **Opus 4.8 judge severity pass** over the
  38 freq-test outputs (Sonnet translate → Opus judge). *Not yet run* — this is the
  gate before the bulk run.
- **Track A — bulk run:** run the freq queue on Max window-by-window per
  [`src/pilot/RUN_ASECTION_MAX.md`](src/pilot/RUN_ASECTION_MAX.md) with `--root-split`
  (top of queue = sthā/bhū/gam); instrument tokens + wall-clock until the Max weekly
  cap fires ([`PILOT_COST.md`](PILOT_COST.md) §6) → answers "2-months-feasible?".
- **Track B — remaining tail item:** publication-quality review of the **hand-authored
  RU sense-glosses** for the flagship pair (`agni`'s 130 senses are hand-authored;
  `akṣara` came from `_build_skeletons_ru.py`). The residual per-sense assignment is
  now done (see Completed).
- **Versioning decision (needs M.G.):** the semver [`changelog.md`](../changelog.md)
  is desynced from tag `v0.0.6` (a parallel actor tagged judge-A/B + Renou against the
  project `CHANGELOG.md` only). Backfill `[0.0.6]` and decide whether the Track B tail
  becomes `[0.0.7]` before cutting the next tag. Surfaced in `changelog.md [Unreleased]`.

## 🚧 Current Work-In-Progress (WIP)

- None in flight. Track B residual pass landed; Track A judge pass is the next concrete
  action.

## 🧠 Dev Notes & Hypotheses

- **persense LLM assignment is reproducible, not a live call.** Decisions live as the
  `LLM_ASSIGN` map in [`src/_build_persense_ru.py`](src/_build_persense_ru.py), keyed by
  surface form → 0-based sense ordinal (row order in `<w>.pd-min.ru.md`). Ordinals are
  stable only while the sense tables are frozen — if a `pd-min.ru.md` sense row is
  inserted/removed, the ordinals shift and the map must be re-checked.
- **anya is a pronoun** → long function-word tail (никого/ничего/кто-то); only
  confident routes were taken, the rest left honestly in *other*. Don't force these.
- Builders `_build_corpus_ru.py` / `_build_skeletons_ru.py` are upstream of persense and
  were **not** re-run (their inputs are unchanged); re-running them only adds date churn.
- Policy reminders for the PWG translator (Track A): preserve PWG sense order (Renou =
  badge, never re-sort); `{%…%}` wraps BOTH German glosses (translate) AND Latin (leave);
  keep `{#..#}` / `<ls>` sigla verbatim; NWS owners verbatim, one row per source.
- **Out of scope (maintainer-owned):** the SamudraManthanam `#sa` Cyrillic cleanup —
  tools shipped, the human runs the server fix
  ([SamudraManthanam #16](https://github.com/gasyoun/SamudraManthanam/issues/16)).

## ✅ Completed (Recent only)

- **Track B residual per-sense pass (2026-06-25).** Opus-4.8 adjudication of every
  `### Не привязано к значению` rendering in the four `*.persense-ru.md`, routed to a PD
  sense or honest *other*. Coverage now **agni 100 % / akṣara 99 % / anya 97 % /
  ananta 97 %**; LLM-assigned renderings marked **°**; reproducible `LLM_ASSIGN` map in
  `_build_persense_ru.py`. README + both changelogs updated.
- Judge-model A/B settled (Sonnet bulk + Opus repass/audit); Renou register axis wired
  end-to-end. (Tag `v0.0.6`.)
- 38-unit freq test run + audited (38/38 fidelity-clean); giant-root split + glue
  solved; frequency-first queue built (41 % DCS-attested).
