
# docs_site/ research-conventions site (ZettelkastenWiki Wave-3 pilot #2)
zettelkastenwiki @ git+https://github.com/gasyoun/ZettelkastenWiki@481a3da

# ACC x NCC works-catalogue P1 fuzzy crosswalk (Tier D edit-distance)
rapidfuzz

# Shared review-sheet HTML emitter (H925) — RussianTranslation/src/build_renou_pilot_sheet.py
# Pinned to the SAME tag .github/workflows/ci.yml installs (H2889 / #1802): this said
# @main while CI pinned v0.7.0, so CI was green on a version no contributor had.
# H5282 (24-09-2026): moved to 0.25.0 because src/nkrya_client.py is now a shim
# over csl_pyutil.nkrya — an older pin makes that import fail closed.
csl-pyutil @ git+https://github.com/sanskrit-lexicon/csl-pyutil@b1606944fe8d9bbb1c3f5447404b6ecdb58ed993

# Unconditional top-level imports across tracked scripts
requests
indic-transliteration
rdflib
numpy
matplotlib
scikit-learn
vidyut
pytest

# Optional extras (guarded/lazy imports — install on demand):
# pyshacl
# pymorphy3
# torch
# transformers
# unbabel-comet
# xlrd
# openpyxl
